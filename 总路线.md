# OBF 总路线

> 最后更新：2026-09-10（Asia/Shanghai）
>
> 当前里程碑：公开 AST→IR→canonical OBF v2/ISA2 register bytecode 保持默认；生成脚本使用 private **ISA16**（K12：operand lane key 折上“按镜像顺序推进、吃已解记录 token”的链，`WIRE_ISA_VERSION=16`）。ISA12-A/B已加入无碰撞per-prototype operand/register ABI并删除统一`6+3*i`、actual-opcode与全像register-key桥；ISA12-C再用受控token结构分析把安全相邻primitive改为carried-value pair，consumer read经register-family `RF`直接接收首项结果（含nil/false），不安全/多写/control形态保持single，并把顶层entry改为六状态seed-shuffled graph；ISA13把record/dictionary/segment/metadata字段顺序逐seed重排（record/segment用per-id/slot factorial置换，dictionary/metadata/tuple用per-image置换）；ISA14再把capture/constant移入两个全局洗牌池（掩码owner/slot/index/tag token，per-record factorial profile，池顺序按image翻转），fetch/validator静态字段下标随seed变化，header仅保留24字节metadata。ISA15再把transport换成逐image混合base86（三段独立长度前缀、链式4/5/6字组、随机高位填充，求值语义不变）。K7再把目标端字运算按目标分叉为多态工具箱（Lua51纯算术、Luau隐藏名捕获的bit32+buffer；每操作3变体、hot双闭包，模数拼写与求和项序逐use-site抽取，求值语义逐bit不变，wire冻结仍为ISA15）。ISA11双ChaCha8/anti-hook、ISA10 runtime-witness/control、ISA9 global segment pool、capture/constant全局池和8 KiB bounded LZW全部保留。固定 seed golden 为 **99,090/108,766 B**（体积轨迹，按 git 内历史 golden 核过：K9a 93,044/102,734 → K7 94,553/103,728 → K8/K9 98,586/108,690 → 09-10 keyed dual-lane frame tag +303/+248 → 同日 probe transcript 去明文 +0/+111 = 98,889/109,049 → 同日 K12/ISA16 链式 lane +201/−283 = 99,090/108,766）；整脚本预算仍是 K10 钉的 **112,000 B/目标**（K12 后实测 10 种子最坏 Lua51 99,981 B、Luau 110,094 B，Luau 余量 1.7%；`OBF_BENCH_SCRIPT_CAP=off` 保留为施工窗口暂停开关）；压缩硬门仅比较完整LZW frame与压缩前semantic bytecode（当前golden seed：Lua51 14,073→13,475 B、Luau 17,763→17,527 B）；K9 per-lane payload-derived affine value key 已完成：Form 1–5 encoder/runtime inverse 对齐，Form 4 使用 16-bit inverse，Lua 5.1/Luau 双目标行为等价，K9 round-trip 与 fail-closed 验证通过；K8 per-prototype indirect route 同步落地并把私有字段扩到16项。当前进入 K10：继续收紧生成脚本体积与静态表面，先保持 OBF_BENCH_SCRIPT_CAP=off，不宣称 executor 实机验证。**门禁现状（2026-09-10，HEAD 7106908 + 同日红门处置、K10 与 K12/ISA16）：161 单元 + 116 集成 = 277 门，全绿**。frame-tag 批次（Adler→keyed dual-lane hash）曾留下 3 个红灯：`heuristic_audit`/`product_audit` 是 pin 未重录，`k7_bit_library_names_are_never_spelled` 是 Luau probe 把库名/反射名写成明文的**真实契约破口**——本批把 transcript 改回隐藏名池运行时拼装并收紧静态门，两套 pin 按观测重录。逐条见 `项目交接总结.md` 第 0.1 节；K10 批次后 `tools/test-matrix.sh` 不再导出 `OBF_BENCH_SCRIPT_CAP=off`，体积门以 112,000 B/目标参与 enforce；此处“275 门全绿”已包含体积门。不删步骤的体积 shrink 经三项实测判定无空间，进一步压缩需要 K9b/ISA16 级的分发形态改动。
>
> 支持范围：仅 Lua 5.1.5 与 Luau 0.735（Roblox 方向）

## 1. 不可破坏的工程约束

1. 混淆器使用 Rust 编写，只依赖 `std`，`Cargo.toml` 不允许加入第三方 crate。
2. Rust 最低兼容版本为 1.88.0；代码不得使用 1.88 之后才稳定的功能，并保持可由 rustc 1.96.0 构建。
3. 每次代码变更必须运行 `tools/test-matrix.sh`，同时通过 Lua 5.1.5 和 Luau 0.735 的源码检查、编译检查、运行结果对比及字节码检查。
4. 所有随机变换最终必须接受显式 seed；相同输入、配置和 seed 必须可复现。
5. 输出必须是单物理行。Lua 5.1 目标不得产生二进制字面量、下划线数字、类型标注或其他 Luau 独有语法。
6. 默认保守处理全局名称、字段名称、环境/API 名称和动态访问；没有证明可安全改名时绝不改名。
7. 所有输入解析必须有边界检查、计数上限、溢出检查和可定位的错误，不允许因畸形源码/字节码 panic。
8. `src/anti/` 继续留给通用 Anti；ISA11 引入、ISA12/13 保留的 decrypt anti-hook 因必须参与KDF而位于 `src/vm/custom/chacha.rs`，每次outer/inner decrypt前执行并纳入双目标fail-closed测试。不得把它宣称为不可绕过或客户端秘密。
9. 混淆器实现的任何 `src/**/*.rs` 单文件不得超过 **80 KiB（81,920 B）**；超过时必须拆分，保持逻辑，并用固定 seed golden 逐 byte 相同证明“拆分本身”没有改变生成代码。`tools/test-matrix.sh` 永久执行该门。

## 2. 固定验证环境

| 组件 | 固定版本 | 仓库位置 | 状态 |
|---|---:|---|---|
| Lua 解释器 | 5.1.5 | `toolchains/bin/lua5.1` | 已编译并纳入仓库 |
| luac | 5.1.5 | `toolchains/bin/luac5.1` | 已编译并纳入仓库 |
| Lua 源码 | 5.1.5 | `vendor/lua-5.1.5` | 已纳入仓库 |
| Luau 自定义解释器 | 0.735 | `toolchains/bin/luau` | 已编译并纳入仓库 |
| Luau 编译器 | 0.735 | `toolchains/bin/luau-compile` | 已编译并纳入仓库 |
| Luau 源码 | 0.735 | `vendor/luau-0.735` | 已纳入仓库（仅保留构建所需目录） |
| Rust/Cargo/rustfmt | 1.88.0 | `.toolchains/rust-1.88.0` | 由固定 `@rustbin` 包可复现安装 |
| gcc/g++ | Debian 12 / 12.2 | 系统 | 已验证 |

Luau 运行器入口为 `tools/luau_runner_main.cpp`。实际 CLI 环境由固定版本的 `Repl.cpp` 设置，包含 `loadstring`、文件 `require`、标准库和 `luaL_sandbox`；测试矩阵会实际调用 `loadstring` 与 `require`。

Rust 预编译工具链体积约 550 MiB，不直接提交进 Git；`tools/bootstrap-rust.sh` 会把版本固定的 npm 包安装到仓库内 `.toolchains/`。项目本身仍保持 std-only。

## 3. 里程碑

### M0：可复现工具链与测试门禁（已完成）

- [x] 仓库内固定 Lua 5.1.5 源码、解释器和 luac。
- [x] 仓库内固定 Luau 0.735 必需源码、解释器和编译器。
- [x] 自写 Luau `main`，复用固定 CLI 环境并增加稳定版本探针。
- [x] 添加 Rust 1.88.0 `@rustbin` 可复现安装脚本。
- [x] 添加 Lua 5.1/Luau 双目标测试矩阵。
- [x] 对原始/处理后脚本进行输出逐字节比较。
- [x] 对两个目标生成真实字节码，并交给内部解析器检查。
- [x] 对截断字节码执行拒绝测试。

### M1：源码前端与只改变词法的单行压缩（第一阶段已完成）

- [x] 带行列位置的 Lua 5.1/Luau 词法器。
- [x] 短字符串、长字符串、长注释、转义、数字和目标专属符号处理。
- [x] Lua 5.1 递归下降语法检查器。
- [x] 常用 Luau 类型标注、泛型、if expression、复合赋值和二进制数字解析。
- [x] 删除注释和冗余空白，避免 token 合并改变含义。
- [x] 公共 minify 在完整语句之间/非空块结尾分号优先，保留语法必需空格和字符串；解析器私有边界覆盖嵌套函数、类型、插值，改名后重新取偏移，不放宽空语句或调用语法。
- [x] 仅 crate-owned 最终 VM 可在块关键字或 `)`/`]`/`}`→identifier 的无歧义边界省略可选分号；歧义 `;(` 保留，最终重解析和绑定复核不变（ISA9 体积配套，用户 minify 不启用）。
- [x] 将多行长字符串转写为单行安全转义字符串。
- [x] 将语法检查器升级为 owned AST，为 chunk/block/statement/expression/function/table/name/type 节点保留半开 byte span。
- [x] 在内部递归解析 Luau 插值字符串表达式并恢复全局源码 span，不再把整段插值当作不透明 token。
- [x] 覆盖 Luau 0.735 已启用的完整 type/type-pack 节点、泛型默认值、type function、attribute、const/export 与上下文关键字。
- [x] 增加 Lua 5.1/Luau focused AST 语料、目标差分检查及畸形 token/过深嵌套回归集。
- [x] 原生 parser audit：contextual continue、调用基底/换行、类型断言优先级、显式方法类型、const/导出及 type-function 捕获；单独记录 parser/compiler 控制流边界。
- [x] 增加 64 MiB source、1,000,000 token/node 与 64 层嵌套门限，公开 token-array 入口会重新词法化核对。

### M2：字节码前端（原生 reader + 新 IR/v2 已交付）

- [x] Lua 5.1 二进制 chunk 头解析，包括大小、端序和格式验证。
- [x] Lua 5.1 prototype、常量、子函数和调试表递归解析。
- [x] 验证 Lua 5.1 全部 38 个 opcode 编号。
- [x] Luau 版本 3–14 及实验 classes 版本的容器解析。
- [x] Luau 字符串表、userdata 类型映射、prototype、常量、调试信息、反馈槽和 cost 字段解析。
- [x] 全部读取执行边界、整数溢出和计数上限检查。
- [x] VM lowering 已能解码 Lua 5.1 全部 38 条指令，并识别 `SETLIST C==0` trailing data word。
- [x] VM lowering 已能解码 Luau v14 的固定 91 条 opcode、静态 AUX word、常量描述、prototype 与 capture。
- [x] 新 `ir::Module` 直接从 AST 生成，含基本块/符号跳转、寄存器、常量、闭包；v2 校验 operand/index/jump/capture，不拼装原生 word。
- [x] v2 逐字节损坏/截断、恶意计数/索引/捕获图、修复 checksum 后拒绝、大跳转和非法 IR 回归；原生格式的更多 fuzz 继续扩展。

### M3：作用域分析与安全压缩（含生成后随机命名、跨域复用增量）

- [x] 建立 lexical scope、local、parameter、upvalue 和 shadowing 模型。
- [x] 对全部可证明安全的局部绑定随机分配 `[a-z]{1,2}`，引用频率高者优先单字母；原有一/两字母也换名。
- [x] 不改全局 API、表字段、方法名、动态 `_G`/`_ENV`/`getfenv` 名称。
- [x] 分别维护 Lua 5.1/Luau 保留字和 Roblox API 保留集合。
- [x] 正确处理声明初始化可见性、local function 递归、repeat 条件、循环绑定、隐式 self 与 Lua 5.1 兼容 arg；AST 保留无标注 vararg 标志。
- [x] Luau 插值表达式、typeof 值引用和限定类型模块前缀同步改名；export API 与 type function 名称保守保留。
- [x] 默认启用安全短名；检测到已知反射/动态环境访问时整份源码保留名称，提供 `--no-rename` 和 `MinifyOptions` 显式退回词法模式。
- [x] 最终单行输出重新解析并核对每个引用/写入、global、scope、声明生效顺序与 upvalue 身份；独立检查同域不出现新名称冲突，含 unused/shadowed locals；双目标原始/词法/改名运行差分。
- [x] 作用域遍历使用有工作量上限的显式栈；线性运算/后缀链增加 64 次限制，避免深 AST 析构栈溢出。
- [x] `minify` / VM 默认生成新 seed，CLI stderr 报告；显式十进制/十六进制 seed 可复现，命名流与布局流隔离。
- [x] 只释放将被同时替换的旧 local 拼写，避开 globals/types/protected bindings；名称池耗尽或无法全体换名时诊断拒绝，不静默使用三字母名/部分改名。
- [x] 全部 VM 代码组装完成后统一做一次私有 finalizer；仅审计固定环境捕获结构，最终每个显式绑定必须已变为不同的一/两字母名称。
- [x] 多 seed 原生语法/运行、并发新 seed、末位置换修复、650 短名与耗尽/CLI 文件保护回归。
- [x] 跨 function/if/while/do/for/repeat 复用短名，同域唯一、跨域稀疏干涉；参数/body 和 for 变量/body 共命名域但不改变 lexical scope ID。总绑定数不再受 702 限制。
- [x] 声明生效 reference ordinal、已遮蔽声明约束、同域迭代多步匹配修复、8M 工作/1M 跨域边门限；10,000 相邻块、小图穷举重解析和生成式运行差分。
- [ ] 常量折叠与无副作用死代码删除必须经过语义测试。
- [ ] 数字随机表示：Lua 5.1 仅十进制/十六进制；Luau 可增加二进制和下划线格式。

### M4：自定义 bytecode v2（本阶段完成）

- [x] 真正 AST→typed register IR→Bytecode，默认不调用原生 compiler、不 fallback。
- [x] 固定 32-byte Header；指令操作数按 Form 列以 7-bit varint 序列化（2~7 bytes/条），prototype header 带 instruction_byte_count；自定义稳定 49 条 ISA，Lua51 支持 46、Luau 支持 49。
- [x] 固定顺序 prototype/capture/constants/code；独立 reader/encoder/round-trip、binary64/i64 精确编码和双侧防御式验证。
- [x] CLI `dump-ir`、`compile`、`inspect-bytecode`、`wrap-bytecode`，可独立保存/检查/运行 bytecode。
- [x] 完整 Header/prototype/ISA/边界规范见 [`自定义字节码.md`](自定义字节码.md)。
- [x] 旧 OBF v1（13-byte Header / 6-byte native-derived record）只保留在显式 `--backend native`，仍单独回归。
- [ ] 更强数据流/类型验证和优化；不是当前格式完成的前置伪装。

### M5：可执行自定义 register VM（本阶段完成，继续巩固）

- [x] 新 handlers 放入 `src/vm/opcode/{lua51,luau}/`，每条一文件；按最新用户许可分目标目录，旧 129 文件不移动。
- [x] Rust reader 校验公开 varint；生成脚本再降为 ISA15 global-segment/pool-wrapped edge-token/recipe-token records，并使用 per-prototype 异构 operand/register/field-order ABI 与 capture/constant 全局池。目标端按 validation-equivalent descriptor 与当前 field profile 校验 dictionary/varint/random-label graph，frame setup与全部primitive handler经当前register mapper访问物理key；ISA12-C仅把结构证明安全的相邻项发为carried-value 2-op fragment，其余保持single，运行时按`code[label]` fetch并经`rid→sid`执行全局打乱fragment，不调用load/loadstring。
- [x] locals 使用 heap cells、闭包捕获、作用域释放、循环新 cell、pack.n 多返回值/nil、vararg、调用与 trampoline 尾调用。
- [x] 两端赋值/方法时机、numeric-for、构造器刷新、Lua51 arg，及 Luau 插值/i64/导出模块/可见 __iter/静态 userdata NAMECALL。
- [x] decoder/runtime/dispatcher/已用 handlers/执行尾部完整生成后，一次随机 local 短名、单行化、作用域/引用/同域唯一性验证。
- [x] 自定义 VM 的 **16 个**私有 prototype/control 字段（`k tags u parent m p flags nu nk nc shared self code cached control routes`，`routes` 由 K8 引入）在同一最终阶段按独立 seeded 双射缩为一/两字母（当前均为单字母），覆盖构造/读/写；保护公开字段、API/元方法、NAMECALL 与字面量，未标记 token 不变。
- [x] 默认后端整体输出为 `local x={};return setmetatable({...},x):m()` 的分函数载荷：5 个随机数字键 section 函数 + 随机字母键入口函数，section 间以参数/返回值传递状态，chunk 使用 `...` 时以 `:m(...)` 转发；环境捕获审计与根表检查同步适配，native 后端不变。
- [x] live recipe 执行插桩展开后的 primitive 覆盖 Lua51 46/46、Luau 49/49；回调/coroutine/GC、20k 尾调用与畸形 canonical/semantic image 双侧拒绝。
- [x] 默认源码/独立 bytecode wrap、缺失 compiler、debug/release、seed 复现及 golden 对照。
- [x] Luau 纯条件/只读标量/可达性、运行时捕获裁剪/重编号、常用闭包共享/递归身份与 raw `__iter`/callable iterator；ISA 修订 2 metadata 双侧校验，修订 1 继续 round-trip/运行。
- [x] 145 个生成式组合、应用语料、多 seed、模块/回调/大 pack、原生和 debug/release 对照；支持条件明确记录在 [`虚拟机兼容性.md`](虚拟机兼容性.md)。
- [ ] 完整 repeat/continue 数据流、全部优化相关函数身份、表布局/GC、隐藏元表和宿主边界；不声称已经通过 executor 实机验证。

### M6：数据隐藏与分阶段加载（当前授权项已实现）

- [x] VM 数据已切换为带长度和 checksum 的字节级容器。
- [x] 为每次构建派生加密密钥并对内嵌 payload 加密（seed 派生 Lehmer 流 XOR，熵 > 7.5 bits/byte；`.obf` 文件本身保持明文 canonical）。
- [x] 密钥拆分为三个份额分存于三个 payload 函数，各自先验证环境（Luau `debug.info(loadstring,"s")=="[C]"` / Lua 5.1 `getinfo(...).what=="C"`）再交回，入口按 seed 洗牌顺序调用并结合；无单一 `decrypt(blob)` 结构。份额由脚本结构（表键对+密文长度）运行时动态计算，密钥零字面量（2026-09-06 追加）。
- [x] 原常量池独立内层密钥流保留其结构键对、seed-specific 轮数与三族 arithmetic stream，但 ISA8 将覆盖范围扩大为完整 compressed LZW body；semantic parser 不再逐常量解密（2026-09-08 第五批）。
- [x] ISA8 bounded LZW：cipher 前按精确 8,192-byte 输出边界重置 Welch dictionary，连续 canonical prefix bitstream 放入 16-byte `original_len/bit_len/Adler32` frame；只有完整 frame 严格缩小时成功，全部截断/逐 frame byte corruption/畸形 bitstream/incompressible rejection 均有门（2026-09-08 第五批）。
- [x] 历史 block transport v1 已在 ISA7–ISA10 完成验证；ISA11 起、ISA12/13 保留的当前链已删除 generalized Feistel和旧custom streams，以双domain标准ChaCha8 + strict frame v2替代。
- [x] base86 传输层 + 三段打乱：payload 编码为三段可打印 base86 分存三个探针门控的 `[数字]=function`，段序由验证侧全 Adler 判定，PASS157（2026-09-06 追加）。
- [x] 固定传输水印 XXS: + 双函数隐藏检测（打包器/不透明比较器拆分，脚本零水印字样，篡改即静默），PASS159（2026-09-06 追加）。
- [x] decoder拆为ChaCha word ops、quarter round、block、KDF/stream、anti-hook五个crypto sibling fields，另有二/三字段随机LZW inverse、semantic reader簇与parser core；全部位置独立洗牌。
- [x] 明文函数/API 名由 seed-shuffled 单字符函数池拼装；除审计允许的外层捕获外不直接出现。
- [x] 非递归阶段活局部改为 per-seed `g[key]` 槽并按生命周期清理；递归 frame F/R/va 明确保留 local，避免嵌套覆写。
- [ ] Roblox 专用 bit32 数据表示作为可选后端，不进入 Lua 5.1 输出。
- [x] canonical Program→private ISA15 camouflaged-recipe/edge-token/fragment/pool graph 与 reachable neutral split 已落地；semantic lowering 本身不是压缩/加密，也不归入 Anti。
- [x] ISA9 global prototype code-segment pool：每 prototype 两个 owner/context-bound 非空 segment，metadata masked unique root，pool record 显式 masked `id/owner/next` 并跨 prototype 全记录洗牌；目标端严格验证 count/total/unique root/完整覆盖、duplicate/missing/越界/wrong-owner/cycle/truncation/trailing（2026-09-08 第六批，ISA14 保留）。
- [x] ISA10 runtime-witness key schedule：前三个 probe 折叠实际 debug source transcript 后混入各 share；Rust 只按目标规范 transcript 加密，错误 witness 由 outer ChaCha8/frame v2 fail closed（2026-09-08 第七批）。
- [x] ISA10 data-dependent control：以 runtime shares 的和模 65,520 建立 mask，`mask%2` 选择两套互异 fetch/dispatch 物理 state，初始化、短路条件、比较与转移再按模 65,521 表示；不是可删除入口门或统一可约平移（2026-09-08 第七批，ISA14保留）。
- [x] ISA11双ChaCha8：inner保护完整compressed body，outer保护frame v2；标准8-round block通过published vector，domain/context/salt分离，最终256-bit key/96-bit nonce/counter由runtime shares、`pv`与attestation派生且不作Lua literal。
- [x] ISA11每次decrypt anti-hook：outer/inner各执行一次source/helper/primitive/KAT检查，attestation进入KDF；hook、错误attestation或ciphertext损坏在用户代码前fail closed（ISA14保留）。
- [x] ISA12-A per-prototype operand ABI：模4/3/2,731的seed-derived可逆仿射分量给完整32,767 prototype id域分配互异profile；四种physical family、三种rotation与四种fragment binding替换统一`6+3*i`读取，并删除实际opcode赋值。parser/getter共享实现，不复制per-function handler。
- [x] ISA12-B per-prototype register ABI：模4/127/257的seed-derived可逆仿射profile周期130,556，完整private id域不重复；四个互斥257-key bank把逻辑0..255单射为物理key。`SETUP`与全部双目标primitive模板经同一frame-local mapper，使用lexer token span lowering而非全局正则，不输出256项映射表、不复制per-prototype handler。
- [x] ISA12-C carried-value fusion + entry graph：首项只接受单语句`R[a]` producer，次项只接受单赋值或零direct-slot-write且至少含一个read的形态；最长4项recipe枚举最大非重叠pair，每pair保留首write并把consumer read改为per-family `RF(index,target,value)`，显式保留nil/false。顶层prelude→probe→segment→decode/parse→bind→execute接线改为六状态乱序graph，wire header仍为ISA12。
- [x] ISA13 parser field-order：record 4槽按`(prototype_id×M+A)%24`、segment 3 token按`(physical_slot×M+A)%6`的factorial置换重排，乘子固定取自与阶乘互素候选集；Rust与Lua凭同一id/slot实时重算，不输出逐项对照表。dictionary entry header、metadata宽度组与内存record tuple用per-image置换baked进parser；wire header升为ISA13，固定slot宽度与opcode/edge/recipe语义不变，错序由既有门fail closed。
- [x] ISA14 capture/constant pools：capture/constant移入两个全局洗牌池，每记录3匿名u16 token携带掩码owner/slot/index/tag并按per-record factorial profile重排，池顺序按image翻转；header仅保留24字节metadata，parser先验证两池再按owner切片。wire header升为ISA14。

### M7：高级结构随机化（semantic virtualization 主批次已完成）

- [x] canonical `.obf` 与生成脚本私有格式分层：公开 encoding 0/ISA1|2 不变，解压后的 embedded encoding 1/ISA15 seed-specific。
- [x] CFG-safe 1~4 primitive recipe；随机非零 u16 recipe ID；use-site 无 opcode。live dictionary descriptor 与真实 execute sequence 分离，只在 Form/validator/control 严格等价类中 camouflage（专项门 ≥45% live op）。
- [x] use-site recipe ID 已替换为绑定 label/next/skip/prototype/交叉项的五阶段 token；验证与每次 fetch 共用 11-state 乱序解码器，五个 live stage 各嵌 4~5 层动态谓词，并带五个高密度算术/repeat 死 state。
- [x] 随机 label + Jump label 后继、record 物理洗牌；next/skip 改为绑定 source/prototype/edge kind 的三阶段 token，validator 与每次 fetch 经独立 3-live/3-dead ED 动态恢复，不把明文后继缓存回 `code[label]`，也不存 ordinal/linear PC。
- [x] sibling prototype 重排并同步 parent/Closure Bx；2~4 节点 synthetic unreachable subtree 改变 count/topology；四个 unreferenced mismatched poison recipe 强制分析先做 reachability。
- [x] 每个真实 prototype 入口加入 2~4 个 reachable neutral bundle，按函数规模抽样至多 5 条 next/test 边且各加入 1 个；scratch-register `Nil/Not/Move/Clear`（满 256 时 self-move）实际 fetch/执行，不能仅靠 graph reachability 删除。
- [x] 所有多-op recipe规划为1~2 primitive fragment；2-op只在ISA12-C结构分析能生成真实carry/read转发时出现，其他邻接项拆为single。planner选最大非重叠安全pair；`rid`仅选择随机入口`sid`，所有fragment arms跨recipe全局打乱，final fragment才恢复fetch。
- [x] F3 `o`、semantic entry `rid` 与 fragment `sid` 分别形成 2~4 grouped dispatch，四比较拼写、整数边界、元方法分支和入口真假包装继续保留。
- [x] 性能与大小分层验收：`tools/bench-vm.sh`仅维护整脚本 **112,000/112,000 B** 独立膨胀预算和VM 1500ms灾难阈值（K10 由 98,000 B 按实测重钉并恢复 enforce：`src/vm/custom/tests/semantic.rs` 同数、`tools/test-matrix.sh` 不再导出 `OBF_BENCH_SCRIPT_CAP=off`；两份 golden 99,090/108,766 B 在门内，K12 后实测最坏 10 种子 99,981/110,094 B（Luau 余量 1.7%，K13 需连带处理）；bench 相对 K7 的 262/91 ms 退化（K12 提交树为 323/103 ms）仍未归因，留待专门批次回看（best-of-15，跨批次抖动可达 ±15%，native 侧 2–4 ms 抖动使比值只作量级参考））；compression单元门逐seed要求**完整LZW frame（含16-byte header）严格小于压缩前private semantic bytecode**。Lua decoder、ChaCha8、anti-hook和历史ISA脚本均不进入压缩比。
- [x] 第二批：可达中性 decoy、successor/state token 与更强 CFG 拆分（private ISA5）。
- [x] 第三批：live descriptor camouflage + semantic fusion/splitting + 全局 fragment state machine（private ISA6）。
- [x] 第四批：动态双 key、自定义 32-bit 分组 transport、strict framing/integrity/padding（private ISA7 / block transport v1）。
- [x] 第五批：8 KiB bounded LZW、whole compressed-body inner stream与二/三字段随机Lua inverse（private ISA8）；当时附加的整脚本strict-smaller比较已按用户后续澄清移除。
- [x] 第六批：global prototype code-segment pool、context-masked id/owner/next/root 两节点图、完整拓扑/双目标 corruption 门（private ISA9）。
- [x] 第七批（本批目标 1/2）：runtime source-transcript key schedule + share-masked fetch/dispatch control flow（private ISA10）。
- [x] 第八批：双domain标准ChaCha8、五字段组合decoder、runtime material、每次decrypt anti-hook与frame v2（private ISA11）。
- [x] 第九批首个结构子阶段（ISA12-A operand ABI）：每prototype异构4-family/3-rotation/2,731-lane profile，完整private id域无碰撞；四种fragment binding；删除fixed operand slot bridge与canonical actual-op marker。
- [x] 第九批第二个结构子阶段（ISA12-B register ABI）：每prototype异构4-family/127-stride/257-shift profile；frame setup与全部handler register access统一lowering，覆盖连续范围、动态capture、cells/upvalues/callbacks/tailcalls及legacy vararg，不嵌完整映射表。
- [x] 第九批第三个结构子阶段（ISA12-C）：真正cross-primitive carried-value fusion、四register-family forwarding与六状态entry stage graph；完整fixture多seed证明live fusion，不把旧handler文本相邻拼接称作fusion。
- [x] 第九批第四个结构子阶段（ISA13 field-order）：record/segment按per-id/slot factorial置换重排，dictionary/metadata/tuple用per-image置换；parser静态字段下标随seed变化，四类错序在双目标用户代码前fail closed。
- [x] 第九批第五个结构子阶段（ISA14 capture/constant pools）：capture/constant移入两个全局洗牌池，每记录3匿名u16 token携带掩码owner/slot/index/tag并按per-record factorial profile重排，池顺序按image翻转；header仅保留24字节metadata，parser先验证两池再按owner切片；池错序在双目标用户代码前fail closed。
- [ ] 后续独立评估：受证明安全的函数inlining/outlining/reorder；通用anti-debug与多组合理假状态/假记录另行评估。ISA11 transport与ISA12/13/14 operand/register/dataflow/field-order/pool机制均不能冒充完成这些目标。
- [x] **2026-09-10 红门清账**：Luau key probe 的 transcript 不再拼字面量，改为隐藏名池运行时拼装成 prelude 单个 local 并以额外参数下发给三个 probe（恢复上面“明文函数/API 名由 seed-shuffled 单字符函数池拼装”的不变量，见第 137 行）；K7 静态门词表 14→18 且直接审计两份 golden，另钉“不得出现 `A=A..` 紧跟字面量”与“Luau 恰有 5 个池名连接点”；`heuristic_audit` 与 `product_audit` pin 按观测重录，注释写明每项归因（`256` +18 来自 K8/K9 批次，非 frame tag）。Lua51 golden 逐字节零 diff，Luau +111 B。
- [x] **K12（09-10，= private ISA16）**：operand lane key 折上按镜像顺序推进的链（`st` 吃本条已解 token），“字流→指令/常量”在私有镜像层不再是纯函数；新增 2 条门，image/golden/审计 pin 重录，公开 `.obf` 不变。
- [ ] **K13（T4，未开工）**：段级 unlock/re-lock（复用 K12 链做可重放回锁，须缓存 mask；实测反推 key 48/64 失败），受 112,000 B 余量 1.7% 限制，需与 K9b 同批或先重钉预算。
- [x] **K10（09-10）**：
`src/vm/custom/emit.rs` 按发射阶段拆出 `emit_decode.rs`/`emit_prelude.rs`（固定 seed golden 逐 byte 零差异，`emit.rs` 81,861→64,639 B），整脚本预算重钉为 112,000 B/目标并恢复 enforce；“不删步骤再 shrink”经三项实测判定无空间（空白 0 B 可省、top 重复片段是设计内噪声（lane 轮换窗口重读 Lua51 405/Luau 414 次）、嵌入字符串已在 base86 编码下界同量级）。
- [ ] **K 系列剩余待办**：K9b（token-layer pool 审计漂亮值归零）、K6（打散）、K2、K3-FULL（全去池）、K4（≈E1+B6）尚未开工；F1、Luau-buffer 直用、D4-D5 key-from-payload 继续冻结。
- [ ] 可选最外层 table/function 包装变体（**用户明确排除，不做**）。

### Anti：decrypt gate 已完成，通用模块仍待独立设计

- [x] ISA11引入、ISA14保留的 decrypt anti-hook位于`src/vm/custom/chacha.rs`：outer/inner各执行一次，attestation为KDF必需输入；双目标hook/helper/wrong-attestation门已落地。
- [ ] `src/anti/`当前只有空`.gitkeep`，留给面向用户源码或宿主的通用anti-debug；不得重复实现与ChaCha material无关的装饰性入口判断。
- [ ] 后续Anti必须按目标隔离、fail closed并通过双环境测试；不得假设标准Lua 5.1必有Roblox executor API，也不得宣称客户端gate不可绕过。

## 4. 每次更新的固定验收顺序

```bash
# 首次环境准备
./tools/bootstrap-rust.sh
./tools/build-reference-tools.sh

# 每次代码修改后
./tools/test-matrix.sh
```

矩阵必须完成：

1. `cargo test --all-targets`、debug build 和 release build；
2. 内部 AST 解析器检查 Lua 5.1 basic fixture 与 focused AST corpus；
3. `luac5.1 -p` 检查两套语料的原始与单行输出源码；
4. Lua 5.1 两套语料执行输出逐字节对比；
5. Lua 5.1 字节码生成、内部解析和截断拒绝；
6. 内部 AST 解析器检查 Luau basic fixture 与 focused AST corpus；
7. `luau-compile` 检查两套语料的原始与单行输出源码；
8. Luau 两套语料执行输出逐字节对比；
9. Luau 字节码生成、内部解析和截断拒绝；
10. 自定义 Luau runner 的 `loadstring`/`require` 环境探针；
11. Lua 5.1 VM 同 seed 逐字节可复现、测试的异 seed 输出不同，所有变体均通过 `luac5.1 -p`；
12. Lua 5.1 VM 与原 fixture 运行输出逐字节一致；
13. Luau VM 同 seed 逐字节可复现、测试的异 seed 输出不同，所有变体均通过 `luau-compile`；
14. Luau VM 与原 fixture 运行输出逐字节一致；
15. 确认所有压缩及 VM 输出不存在 CR/LF、生成器错误消息，且 VM 不委托 `loadstring`；新 ISA executed coverage 必须是 46/46 和 49/49；旧 native 后端单独保留 38/38 和至少 60 条核心覆盖；
16. 两套 scope corpus 的原始、`--no-rename` 与 seed 735/736 安全改名输出均通过目标编译且运行一致；相同 seed 可复现，异 seed 换名，这些 fixture 比词法模式更短；
17. 两套 reflection corpus 的自动保留输出与 `--no-rename` 逐字节一致，local/upvalue/function 可观察名称不变；
18. 所有显式 VM 绑定为不同于各自原名的 `[a-z]{1,2}`，普通源码不能使用 VM 环境例外；bytecode/string 字节不变，AST 检查不依赖旧 B/P 名称；
19. 核对根目录生成物（Lua 5.1 seed 7001、Luau seed 7351），多 seed `0/1/0x735/u64::MAX` 的原生执行、名称池/末位冲突与 CLI 失败不覆盖输出回归通过；
20. parser audit、小图全部三色分配的重解析对照、10,000 相邻块、生成式遮蔽/初始化程序、同域唯一性和跨域闭包/类型/写入约束通过；
21. v2 固定 Header/varint 指令流/reader round-trip/修复 checksum 后拒绝/大 jump、CLI 文件保护；
22. 无 compiler 生成、compile/inspect/wrap、debug/release binary 和脚本一致；`--backend native` 必须显式且旧回归不删除；
23. 公共 minify 的分号完整边界，以及 crate-owned VM 仅省略无歧义可选分号（歧义 `;(` 保留），连同必需空格、调用后缀、类型/嵌套插值、字符串字节、已有分号/空块、非法输入、幂等性、改名偏移及全部压缩入口通过双目标检查；
24. ChaCha8必须通过published block vector、Rust/Lua arithmetic parity、双domain/context/material差分、stream round-trip、最终key/nonce/counter零字面量、wrong seed/attestation、frame-v2严格长度/descriptor/cookie/tag/padding及逐ciphertext byte corruption；
25. bounded LZW必须逐byte无损，拒绝全部截断、逐frame byte损坏、非法forward/canonical code、越界phrase、非零padding、尾bit、checksum、畸形与不可压缩输入；Lua51/Luau解压字段须按seed为二/三段并全局洗牌；每个受测seed的完整LZW frame（含16-byte header）必须严格小于对应压缩前semantic bytecode，禁止以整份Lua或ISA7 predecessor替代该比较；
26. ISA9 global segment pool 必须在跨 seed 下证明每 prototype 恰有两个非空节点、物理记录跨 owner 洗牌、ID 域完整唯一、显式 owner/root/next 与重组字节精确；duplicate、missing、越界、错误 owner/root、cycle、截断和尾随须在 Lua51/Luau 用户输出前 fail closed；
27. ISA10–13必须逐probe证明实际source transcript参与对应share，witness加一只改变对应share、改变control mask/parity且无法打开原frame v2；解释器fetch/dispatch继续保留share-derived mask；
28. ISA11/12/13 outer/inner每次decrypt都必须调用direct anti-hook；source metadata、行为等价native wrapper、helper同源、primitive behavior、ChaCha KAT或attestation任一篡改，Lua51/Luau均在用户输出前fail closed；scope审计严格为12-or-0 direct probes；
29. ISA12 operand ABI必须在完整32,767 prototype id域无profile碰撞，覆盖四种layout family/三种rotation/四种binding；生成fragment不得恢复`I[6+3*i..]`统一读取或实际opcode数字赋值。双目标、多seed语法、运行、payload round-trip与旧semantic corruption门必须通过；
30. ISA12 register ABI必须在完整32,767 prototype id域无profile碰撞，每个profile对逻辑0..255单射且物理key有界，不输出完整register map；`SETUP`与所有双目标primitive handler必须使用同一mapper，静态门禁止旧`R[a]/R[b]/R[c]/R[d[2]]`访问面，运行门覆盖连续区间、动态capture、cells/upvalues/callbacks/tailcalls及Lua51 legacy vararg；
31. ISA12-C每个2-op fragment必须有且只有一个producer carry并至少一个consumer forwarded read，满足`fragment_count+fusion_count=operation_count`；full fixture fusion数必须超过四个poison recipe的最大承载量。control/多写/未证明形态保持single，四family `RF`及nil/false/alias/元方法/error差分通过；entry总状态机数为9且旧`local FMt,PT`/`local P,np,entry`线性导出锚点消失；
32. ISA13 record/segment factorial置换必须全范围合法且Rust/Lua双端按同id/slot重算一致，parser字段序须有固定seed期望并跨seed变化；record-slot/segment-token/dictionary-header/metadata-slot/pool五类错序（checksum修复后）在Lua51/Luau双目标均须用户代码前无stdout失败；ISA14池掩码/token/profile合法、池洗牌coverage与池序翻转一致须由新增专项门覆盖；
33. 在任何编译前扫描全部 `src/**/*.rs`，单文件必须 `<=81,920 B`。拆分必须先用旧 fixed-seed golden 证明零生成差异，再进入功能变更。

任何一步失败都不能把对应里程碑标记为完成。

## 5. 更新日志
### 2026-09-10 — K13（T4 第一步）：per-prototype 惰性解码 + 校验后释放

- `DC(id)` 成为 validator 的第 4 个导出 sibling；装载期逐 prototype 解码+校验后**退回链态串**，解释器首次执行才物化词表（`if not code[0] then code=DC(fid) end`，`DC` 内幂等短路）。
- 语义与契约不变：校验仍在用户代码之前，image 字节不变（`k7_wire_image_bytes_are_pinned` 原样通过），不升 ISA。
- 面：`emit.rs` 71,146→71,282 B，golden 各 +136 B（99,226/108,902）；仅 `heuristic_audit` M4 的 `0/2` 残差位移，其余 pin 原样通过；`cargo test --lib` 162 全绿，全量集成绿。
- 待办：K13b 空闲回锁（帧引用计数归零 → 退回链态串）、K13c 常量/捕获池惰性化；本批不宣称抗 dump 完成。

### 2026-09-10 — K12（= private ISA16）：operand lane key 折上按镜像顺序推进的链（T1 第一步）

- **改法**：`src/vm/custom/semantic.rs` 增加 `K9_CHAIN_MOD=2147483647` 与 `k9_chain_init`/`k9_chain_next`；每 prototype 起链 `st0=(id*7919+nr*4099+start*131+mask_salt)%2^31-1`，逐条记录用**本条已解出的 recipe token** 推进 `st=(st*213+token*1031+at*7517+mask_salt)%2^31-1`；`k9_add` 与运行时 `AK` 的加性项同步加 `st%m`（m=256/65536 逐形）。`emit.rs` 渲染的两句直接从同一组 Rust 常数 format 出来，杜绝两侧漂移。
- **安全含义**：第 r 条记录的 operand 不再是 (密文, 位置, 静态常数) 的纯函数——必须先按镜像顺序重放前 r-1 条，不可跳读、不可并行、不可逐条独立试解。K9 时每条记录自洽，K12 把“顺序”本身变成密钥的一部分；外部分析 T1 的“停在字流→指令这一步之后”从此在私有镜像层成立。
- **可执行性**：链不掺时钟/随机/宿主计数；中间量 < 2^53（`operand_lane_chain_is_order_dependent_and_double_exact` 断言）保证 Lua 5.1/Luau 双精度精确重放；前缀错解被 `vld()` operand 边界门接住，仍是“用户代码前无 stdout 失败”。踩过的坑：Rust 侧用 u32 乘会回绕而 Lua double 不回绕，变异测试当场炸出，两侧统一 u64 中间量。
- **证据与 pin 归因**：image 1,159→1,141 / 882→870 B（FNV `0xd96c049c46c02c21` / `0xb4e955d8ec85a75c`）；golden 98,889/109,049 → **99,090/108,766 B**；LZW 门 Lua51 `14,071→13,479`、Luau `17,713→17,482 B`；公开 `.obf` 5,525/6,575 B 与两份 SHA 不变。`stream_audit` 987→975 / 784→778 字（重复对、gcd 仍 0/0）；`heuristic_audit` 位移只在 M2/M3b/M5/M7（Luau 另有 `23` 残差 −1），M1=86、KAT 词表、M6=(0,0)、Lua51 的 M4 全表不变 ⇒ 无新值类、无新 API 面；`product_audit` 里 `2147483647` +2（正是链的两处）、check8 配对 +2（gcd 仍 1）、check9 流长 +15/+7，check2/3/4/5/6/7 不变。`k7_wire_image_bytes_are_frozen` 更名 `…_are_pinned`：跨 ISA 的逐字节冻结语义到 ISA16 为止，之后是回归指纹。
- **门数与预算**：新增 2 条 lane 门 → **161 单元 + 116 集成 = 277 全绿**（不带 `off`，体积门 enforce）；112,000 B 预算不动，K12 后实测最坏 10 种子 99,981/110,094 B。提交树 `tools/test-matrix.sh` → `[matrix] PASS`，bench 为 Lua51 323 ms / 161.5x、Luau 103 ms / 34.3x。
- **附带**：`tests/semantic.rs` 被新门顶到 83,419 B（超 81,920 上限），按 `include!` 惯例拆出 `src/vm/custom/tests/lane.rs`；环境坑记录：被 `include!` 的文件不能写 `//!` 头（E0753），只能普通注释。
- **下一步（K13=T4）**：段级 unlock/re-lock 让明文只在被执行到的窗口内存在，直接复用这条链做可重放回锁（外部分析已实测：靠 `inv` 反推 key 会 48/64 失败，必须缓存 mask 或让链只依赖 `ikey+index`）；因余量只剩 1.7%，K13 需与 K9b 同批或先按实测重钉预算。
### 2026-09-10 — K10：emit.rs 按发射阶段拆分 + 整脚本体积预算重钉 112,000 B（275 门全绿，含体积门）

- **拆分**：`emit.rs` 从 81,861 B 降到 64,639 B。5 个只返回 Lua 文本的自由函数（`pool_loops_lua`/`k9a_take`/`k9a_emit`/`compression_decoder_sections`/`transport_frame_decoder`）→ `src/vm/custom/emit_decode.rs`（8,923 B）；宿主捕获 prelude 阶段（环境捕获、隐藏名字符池与逐名 local 拼装、打乱后的单元语句、K7 工具箱渲染、prelude 返回表，9,364 B）→ `src/vm/custom/emit_prelude.rs`（10,915 B），返回 `PreludeStage { text, header_end, ret_names, probe_transcript }`。两模块由 `src/vm/custom/mod.rs` 以 `pub(crate) use` 重导出，模块内 `use super::*` 继续提供上下文；`structure`/`bitops_rng` 按原顺序以 `&mut` 贯穿。`validation()` 有意留在 `emit.rs`（`tests/semantic.rs` 走 `super::emit::validation`）。
- **零 diff 证据**：release 构建后按矩阵规范命令重生成（`obf virtualize --target lua51 --seed 7001 tests/fixtures/vm_lua51.lua`、`--target luau --seed 7351 tests/fixtures/vm_luau.lua`），两份输出与根目录 golden `cmp` 逐字节相同；`cargo test --lib` 159 门绿。生成物、公开 `.obf`、wire ISA15、审计 pin 全部不变。
- **预算门**：`tools/bench-vm.sh` 默认 98,000 → **112,000 B**，`src/vm/custom/tests/semantic.rs` 双目标预算同数，`tools/test-matrix.sh` 删除 `export OBF_BENCH_SCRIPT_CAP=off`；`OBF_BENCH_SCRIPT_CAP=off` 保留为施工窗口暂停开关。定标依据：实测 8 种子最坏 Lua51 100,049 B、Luau 109,825 B；单元门 5 种子（`0/735/7001/7351/u64::MAX`）最坏 99,784/109,810 B → 约 2% 余量。不带 `off` 的 `cargo test --all-targets --no-fail-fast` → **275 门全绿**。
- **shrink 判定（不做）**：两份 golden 的 5,737/5,801 个空格 100% 是必需 token 分隔符（可省 0 B）；最长重复片段全部落在设计内噪声（寄存器窗口按 lane 轮换重读：`{5 名}=k(h,q,g,l)` 一类拼写 Luau 共 414 次、每种 103–104 次，Lua51 对偶形式共约 405 次、每种 101–102 次；数字三形态/谓词四拼写；控制流扁平化状态跳转）；两份 golden 的嵌入字符串段合计 21,323 / 27,225 B，承载的是 LZW frame（13,475 / 17,527 B）加 ISA14 池/元数据；按 base86 每字符至多 log2(86)≈6.43 bit 计，frame 的编码下界就是 16,775 / 21,819 B，余下 4.5 / 5.4 KB 是池数据本身加每段 4 字符长度前缀与变宽组，不是可挤掉的空白。再压必须删混淆步骤或改分发形态（K9b/ISA16 范畴），不为凑数字而做。
- **遗留**：本批复测 bench 为 326–329 / 103–106 ms（Lua51/Luau，两轮 best-of-15），相对 K7 的 262/91 ms 退化未归因（候选：keyed tag 每字节双 lane 滚动、运行时 transcript 拼装）；`emit.rs` 余量恢复到约 17 KB，最紧文件回到 `seed.rs` 80,646 B。

### 2026-09-10 — 红门清账：Luau probe transcript 去明文 + 两套审计 pin 重录（275 门全绿）

- **真实破口**：上一条批次为了区分 target，把 `"buffer|bit32|table.freeze|debug.info"` 作为 Lua 字符串字面量拼进 Luau key probe 的 transcript（Rust 侧 `runtime_probe_witness` 折叠同串）。这直接违反“库名/反射名不得出现在产物明文里”的既有不变量（本文第 137 行、README 安全边界、`虚拟机兼容性.md` §5），并且 3 个 probe 各存一份拷贝，等于送出一个 36 字节的稳定 grep 锚点。
- **改法**：`src/vm/custom/cipher.rs` 新增 `PROBE_TRANSCRIPT_NAMES`/`PROBE_TRANSCRIPT_UNIT`/`probe_transcript_unit`，由隐藏名池的 6 个名字 local 拼出 `local PTS=v_a.."|"..v_b..` 放进 prelude；`emit.rs` 把 `PTS` 追加进 unit 返回表并作为**额外参数**下发给三个 probe（它们是 VMS 兄弟条目，看不见 prelude local，只能与 `SB/DB/GI/LS` 同通道传参），probe 体变为 `A=A..PTS`。Lua 表达式与 Rust fold 都由同一份常量列表推导，**结构上不可能漂移**；transcript 的池槽位排布逐 seed 变化，产物中出现一次而非三次。
- **门侧**：`k7_bit_library_names_are_never_spelled` 词表 14→18（加 `table.freeze`/`debug.info`/`getinfo`/`loadstring`），审计对象从“只有探针程序的 emit”改为**同时审计两份根目录 golden**，再加两条结构断言：不得出现 `A=A..` 紧跟字符串字面量、Luau 恰有 5 个池名连接点而 Lua51 为 0。未放宽任何既有断言。
- **pin 重录（含归因，不靠猜）**：`heuristic_surface_pins_hold_on_both_audit_configs` 与 `product_text_audit_pins_hold_on_both_goldens` 的 pin 全部按观测重录，注释写明哪些项为什么动。用 git 里的历史 golden 以同一 `count_decimal` 算法对数后确认：`256` 的 +18 发生在 `89dcc91` 之前（K8/K9 批次把脚本从 93,044 长到 98,586 B 时新增 base-256 打包拼写），frame tag 只移动 `65536`（102→99 / 94→91）与 `4294967296`（18→20 / 16→15）；本批对 check1/check5 数值零影响。check9 的 `rem5` 由 0 变 1/4，是流长离开“全为 5 的倍数”病态，方向安全，`fail` 仍 false。M1=86、KAT 词计数、M6=(0,0)、check2/3/4 全部不变，说明没有新增可疑 API 面。
- **实测**：wire image 与求值语义不变；Lua51 golden 用 seed 7001 重生成后与仓库文件 `cmp` 逐字节相同，Luau 由 108,938 → **109,049 B**（SHA-256 `9cc4d6d42df5fef723b6d8825725fed59cd5d8df83701351c70b0fb593cc430f`），fixture/golden stdout 逐字节一致，`luac5.1 -p`、`luau-compile`、`obf check` 全过。`OBF_BENCH_SCRIPT_CAP=off cargo test --all-targets --no-fail-fast` → **159 单元 + 116 集成 = 275 门全绿**。`src/vm/custom/emit.rs` 81,567 → 81,861 B（上限 81,920 B，**余量 59 B**，K10 需先腾地方——K10 已拆）。体积门当时仍处暂停，本批不宣称体积达标；K10 之后预算按实测重钉为 112,000 B/目标并恢复 enforce。

### 2026-09-10 — transport frame tag：Adler-32 → keyed dual-lane integrity hash（该批遗留的 3 个红灯已在上面的同日子条目清掉）

- `src/vm/custom/cipher.rs::frame_tag` 不再用位置无关的 Adler 折叠：两条 lane 以运行时派生的两个 frame key state + `tag_salt`/`cookie_salt` 起始（各自模 65,521），逐字节推进为 `left = (left*257 + b) % 65521`、`right = (right*263 + b + left) % 65521`，最后混入 cookie（×17）与 descriptor（×31）压成 32-bit tag；`open_transport_frame` 端逐字节重算，descriptor/cookie/tag/padding 全过后才交 inner。
- 动机：Adler 的加法结构使相邻字节可交换、长度不变即可保 tag，是纯静态复刻骨干里最容易当成“锚点”使用的字段。新 tag 依赖运行时 key state，位置敏感，但**仍是 fail-closed 损坏门，不是 AEAD/签名**；公开 `.obf` 的 Adler-32 payload checksum 与 LZW header 的 semantic Adler 门按设计保留，三层不要混称。
- Luau 侧 key probe 的 transcript 同步从 `"[C]"` 扩为折叠 `[C]buffer|bit32|table.freeze|debug.info`（`runtime_probe_witness`），脚本端以 `A=A.."buffer|bit32|table.freeze|debug.info"` 拼接——**这一处是本次的失误**：库名与反射名重新成为明文锚点（**已在同日子条目修复**），直接撞坏 K7/T7 的 `bit32`/`buffer`/`bxor`/… 零拼写契约（`k7_bit_library_names_are_never_spelled` 红）。修法是把 transcript 改回运行时拼装（隐藏名池 / `string.char` 分片 / `\` 转义），Rust 与 Lua 折叠同一字符串来源，禁止放宽断言。
- 本批未重录的 pin：`heuristic_audit`（lua51 seed 7001 观测 `(1384,1,0,4)`/`(5,1878)`/`([567,526,517,134,134],383)`，pin 仍是 `(1383,0,3,3)`/`(5,1864)`/`([539,532,525,134,134],391)`）与 `product_audit`（check1 `256`：181→199、`65536`：102→99、`4294967296`：18→20；check9 流 `(18445,0)`→`(18411,1)`）。重 pin 前必须先解释 `256` 命中增加的来源（新 lane 常数与 0..3 padding 计算），否则等于把退化固化成基线。
- 复核结果（rustc/cargo 1.88.0，`toolchains/bin` 参考工具）：两份 golden 与 `--seed 7001/7351` 重新生成**逐字节一致**，并在各自解释器下与 fixture 输出一致；`.obf` 仍是 5,525 / 6,575 B，SHA-256 未变；golden 为 **98,889 / 108,938 B**（脚本 SHA-256 `c01c17d4…c999b0` / `daa1e765…c38f6e`）；LZW 门 Lua51 `14,073→13,475 B`、Luau `17,763→17,527 B`；15-run bench **383 ms / 191.5x**、**103 ms / 34.3x**（相对 K7 的 262/91 ms 退化，列入 K10 回看项）。
- 门禁：**275 门（159 单元 + 116 集成），272 绿 / 3 红**。因此本批**不能**标记为完成；K10 的第一步是清红门（含补一条“probe transcript 不得拼出反射/库名”的双目标静态门，防同类回退），再谈 shrink 与新体积预算。（后续记录：这 3 个红灯与那条静态门已在同日子条目完成，当前 275 门全绿。）

### 2026-09-10 — K9：per-lane payload-derived affine value key 完成，进入 K10

- encoder 按 record recipe token、prototype id 和 lane 对 Form 1–5 operand wire value 做 affine 编码；Lua runtime 在 semantic validator 中通过 `FM[op]` 恢复对应 Form 后执行 `AK` 逆变换，保留 `PT[op]` 作为 opcode permutation validator 输入。
- 修复了此前把 `PT[op]` 误当作 Form 表导致的 VM 失败；补齐 `routes` 私有字段 schema，保持 K8 indirect route 与 K9 operand key 同时 fail-closed。
- Form 4 的 `bx` 使用模 65536 affine inverse；Form 1 的 packed Ax 按三个 byte lane 处理；Lua 5.1 与 Luau 基础执行、corruption、watermark、parity 均通过。
- 新增 K9 affine 全 token/prototype/lane/boundary round-trip 测试；更新 K9 后 wire image、heuristic、stream 与 product audit pins/goldens。
- 验收：`cargo test --test custom_vm` 为 20/20；`tools/test-matrix.sh` 为 159/159 单元和全部集成门禁 PASS。K10 指针切换到生成脚本体积 shrink 与静态表面进一步收紧；体积门仍按既定施工期策略保持 `OBF_BENCH_SCRIPT_CAP=off`。


### 2026-09-09 — K系列第2批（K7：按目标分叉的多态字运算工具箱，用户指令提前，K9b/K6 顺延）

- 发射侧：新增 `src/vm/custom/bitops.rs`——Lua 5.1 纯算术工具箱（X8/X32/rotate/L32 各 3 种结构变体），Luau 经隐藏名池捕获 `bit32`/`buffer`（直接调用/buffer表组合/De Morgan 重组各 3 变体，库名方法名永不拼写）；hot xor/rotate 每 image 各抽两种组成双闭包，quarter round 逐调用点交替；KDF/stream/fold/attestation/ctx/frame/`fin` 全部模 `2^32` 站点逐 use-site 抽拼写（字面/减法三明治 `((a+b)-c)`/双模字面外层）并洗求和项序；三明治任两数之和永不等于 `2^32`，除法只除 2 的幂，中间值恒低于 `2^53`。独立 `bitops_rng` 流，wire image 逐字节冻结（单测 pin）。
- 验证/门禁：新增 15 个 K7 门（wire冻结保持门、RED 签名门、发射器单测、真解释器 1300 断言向量 harness 跨 6 变体×双目标、`LS` 碰撞回归二门、10 seed×双目标执行门）；`heuristic_audit` M 面重 pin；`semantic`/`transport` 签名断言跟进；`custom_vm` attestation 篡改改打 KAT 比较（fold 已洗牌）；`product_audit` 重 pin（双 golden check3=0，check6 为 `;` 分片漂移误报）；golden 重生成并逐字节对跑一致。途中抓到真 bug：prelude/entry 双 `LS` 遮蔽方向随 unit 洗牌翻转致 `bnot(nil)`（7/14 seed），改名 `SHL` 根治。
- fixed-seed golden 为 **94,553/103,728 B**（体积门暂停中不设预算），SHA-256 为 `765885e9aba3a6edd301a27f682191295e4cd222dac335df100503c38a245e63` / `3ce919f4a90d66b466ba8941ca10c9013316789a3563b8f13c236041bb4b257b`；Lua 语义 image LZW 不变（wire 冻结）；完整矩阵 **PASS273（157单元 + 116集成）**，15-run bench 为 Lua51 262 ms / 131.0x、Luau 91 ms / 30.3x。K9b/K6 与 K2/K3-FULL/K8/K1/K4 待后续批次。

### 2026-09-09 — K系列第1批（K9a：混合 base86 段传输，ISA15）

- 发射侧：`base86_encode_mixed`——每段 4-char 长度前缀 + 字节驱动链式 4/5/6-char 组（width 取上一组 raw 值 % 3）+ 2/3-char 尾组；全部组值叠加 `86^width` 冗余空间内随机高位填充（解码按字节宽截断）；字母表逐 image 从 28..=126 选 86（强制含 28/29/125/126、跨度恒 99，逐 seed 丢 13 个并重排），以 8 个 sub-12 片段 baked，Lua 端 `VAL` 表解码；基数 86/`MM`/`M24` 以随机加法对隐藏，slot 键避开 85/86、状态号避开 256；每段独立 4 状态机（per-seed 状态号、分支洗牌、精确 end-of-string），`~` 标记字节改 `\127`，forms/诱饵串永不混入段收集。单 image 一条 pad 流贯穿三段。
- 验证/门禁：`product_audit` [2][9] 重 pin（段长规则/整除在混合编码下天然消失，门改为按发射参数重算期望并核对六种段序唯一解码），[1] 漂亮常数重 pin（35/92/121 消失，新增均为发射侧随机对）；`stream_audit` 六段序容忍 + `seg_golden` 帧上界；`custom_vm` 腐蚀两门按 lexer+字母表 span 重写（水印翻 decoded 第 4 字、chacha 跳短段改明文邻位）；`private_fields` ISA pin 15；新增 9 个单测（字母表/填充/往返/六段序/状态号/slot/转义/污染/长度上界）。
- fixed-seed golden 为 **93,044/102,734 B**（transport 改写，体积门暂停中不设预算），SHA-256 为 `7cf2a36d9e21dc4233f8f210fb3e0c82af501cef7ba574436792871fa42ef64e` / `8717cd1784887a14873a4643770957c41ffcd8ab1f2e4b79b7766db857372d32`；Lua 语义 image LZW 为 Lua51 12,332→11,329 B、Luau 15,559→15,104 B；完整矩阵 **PASS258（142单元 + 116集成）**，15-run bench 为 Lua51 123 ms / 61.5x、Luau 136 ms / 45.3x。K9b（token-layer pool nice 值）与 K6/K7/K2/K3-FULL/K8/K1/K4 待后续批次。

### 2026-09-09 — K系列第0批（门禁先行）：§3审计落库 + K5验证 + 体积门暂停开关

- 审计harness：`tests/product_audit.rs`（文档§3九项检查的OBF适配版，对双golden文本跑，基线pin：[1]漂亮常数8/7项、[2]字母表连续FAIL、[9]流长整除FAIL、[5]同型模板4/5形；[3][4][6][7][8]干净）+ `tests/stream_audit.rs`（真实字流check 8）。命名：K1–K9对应文档T1–T9（避开imitation T1/T6撞号）；本批解冻H区中与9项重叠部分（K4≈E1+B6、K1/K9的transport改动、K6打散），F1/Luau-buffer/D4-D5-key-from-payload继续冻结；K3定为lite版（transport保池、消费侧逐记录化），全去池需另批。
- K5验证结论：已满足，无需改造——外层T1-M6早pin(0,0)，内层新门pin(848,0,0)/(677,0,0)，零重复；census确认载荷路径只剩counter-ChaCha8双层+frame tag（旧custom stream ISA11已删），无位置无关XOR残留。
- `tools/bench-vm.sh`新增`OBF_BENCH_SCRIPT_CAP`（默认98000行为不变，`off`暂停体积门但照常报告尺寸，双向验证过）；本批零发射改动，golden/SHA不动，体积门仍在执行。
- 完整矩阵 **PASS249（133单元 + 116集成）**，15-run bench为Lua51 120 ms / 60.0x、Luau 133 ms / 44.3x。

### 2026-09-09 — T-zone imitation第一批：T1启发式审计 + T6 G4括号噪声

- T6：模板数字拼写器新增`(dec)`括号形态（`p1_number_form_full`，模板专用；`routine_lua`沿用无括号`p1_number_form`，routine文本与腐蚀手术锚点逐字节不动），资格与旧表一致（3+位、`2^53`下、燃料预算豁免），括号恰+2B（growth上限128→256）；测试侧`p4_canon_numbers`镜像同规则（字面边界+3+位+排除链式调用，`TY(0)`/`seedfail(14)`等调用原样通过），`)(ddd+)`链式形状断言不存在。门禁：64种子双目标括号出现+变体相异+值合法（≥100、<2^53、非预算）+ routine零括号 + minify双链acceptance。
- T1：新增`tests/heuristic_audit.rs`启发式表面审计（2固定配置Lua51/7001、Luau/7351，7指标：M1流去重86字母、M2流长+余数、M3 KAT词命中+≥1e6字面量统计、M4 top-12值星座、M5长串单例、M6 outer密文u32重复gcd、M7 top-5串长+3/4位gap），数字指标全部按值计数（拼写免疫，T6前后pin天然成立）；量测得KAT仅`getfenv`命中2（compat垫片）、Luau 40个浮点字面量跳过、outer密文零u32重复、三段与余串gap 231/161。
- fixed-seed golden 为 **85,217/94,145 B**（+0/+1，lua51恰抽中`(255)`、luau本seed零括号），SHA-256 为 `dd7bd5bf972c03722517ace00e13c73a125dc3663bd7b0e744cb495a65bd6336` / `82aa70d2d168b8bc12438be7bd125efbcd7d87926bfd10a709a35612345e16a7`；预算维持 **98,000/98,000 B**（8种子最坏 lua51 86,553 B、luau 94,992 B，headroom 约 11.4KB/3.0KB）；完整矩阵 **PASS247（132单元 + 115集成）**，15-run bench 为 Lua51 121 ms / 60.5x、Luau 136 ms / 45.3x。

### 2026-09-09 — P1 seed-ISA逐seed等价变形第六批：routine lane重映射

- B6a发射侧（`P6Mapper`单choke）：image-wide置换——tmp 0..15无约束（16!，主力）、site按模板约束（lane 1/4 pin、`{2,3}`闭、5..w自由，逐op宽度限制1/2/3/4/7/8/9）、STAB/SEEDH全置换；raw位skip（RET action、CALL nargs、变长BR target），kind 2/3/6、kind>8与越界词直通；`routine_lua`加`site_w`参、arm site与prelude pools联动排放（content-follows-lane），模板零改动。
- B6b驱动侧：双differential driver进image空间——共享Lua mapper移植（与Rust逐规则转录）、SITE/STAB/SEEDH原地重排、向量逐dseed映射（v1用9/3/4、85-diff用7/3/5几何）；40/40与85/85标记在映射空间全绿，pin结构与keep规则得行为证明。
- 门禁先行：双RED（逐seed词方差、parse→逆映射round-trip）转绿；untouched/conservation/pool-binding/port-proof（200+样本×2几何×2 runner）全程锁死；skip表经模板重推纠正（初版1-based/0-based混淆），pool gather/scatter方向bug由21个行为失败捕获。
- fixed-seed golden 为 **85,217/94,144 B**（+438/+456，重拼写拉长），SHA-256 为 `691d9eace02bcb35192c4796923b15347ee645f6a6d1d44fe568d23eeac58a77` / `eeb983132c6025d4a98999d554ec6e8f9ec4a49c2b0ce4d1d4dec6de80eaac23`；预算维持 **98,000/98,000 B**（8种子最坏 lua51 86,147 B、luau 94,990 B，headroom 约 11.6KB/2.9KB）；完整矩阵 **PASS245（130单元 + 115集成）**，15-run bench 为 Lua51 121 ms / 60.5x、Luau 137 ms / 45.7x；另修矩阵fmt硬门（Batch-5遗留7处dirty，纯空白单提交）。

### 2026-09-09 — P1 seed-ISA逐seed等价变形第五批：11臂臂内变形

- B5a比较对偶（`seed ^ DOMAIN_TMPL ^ 9`，与split同表同流）：36站点——12臂size检查（`#q`/`nargs`界）、10处`ip=ip+1`、9处guard单比较（TY单例×3、`nargs~=1`、`m==nil`/`m>8`、expect双比较）+ BR分支取反交换；B4b链内比较全部排除（已置换），`expect or 0`无Lua对偶除外。
- B5b行内temp-split（同上）：16站点key-side hoist（`local w1..w15` + BR多重赋值语句拆分），安全性由双目标求值序探针保证（store键先值后、多重赋值/索引左到右，双端一致，与模板求值序逐位相同）；行中性，`seed.rs`零改动。
- 门禁先行：双RED（52站点穷举witness落点，共享span用组合稳定锚定位到行；共享span守恒/单有span XOR/`wN`单定义锁）+ 实现内双fail-closed（针`count==1`、变体禁含canon）；28项炸穿修分号融合与B4b witness定位子后转绿；register-ABI测试STORE拼写改为XOR双拼写（RX包裹守恒）。
- fixed-seed golden 为 **84,779/93,688 B**（+38/+50），SHA-256 为 `42bfd52e315cecee7d7515a9257cc93aab30515b7fd39504686eff7843d15e34` / `dd4c27bcf93ce8fa2d045d73841c82181100f7dd5a244ae41a017c4f9b87e019`；预算维持 **98,000/98,000 B**（8种子最坏 lua51 85,711 B、luau 94,660 B，headroom 约 12.3KB/3.3KB）；完整矩阵 **PASS239（124单元 + 115集成）**，15-run bench 为 Lua51 121 ms / 40.3x、Luau 136 ms / 45.3x。

### 2026-09-09 — P1 seed-ISA逐seed等价变形第四批：模板内ALU/操作数链/死临时量

- B4a ALU双形态（`seed ^ DOMAIN_TMPL ^ 6`）：10站点显式表，比较对偶拼写 + `local uN=x;` temp-split；仅`<`/`<=`两路求值序互换（total布尔臂）。
- B4b操作数链置换（`^ 7`）：20行`P4Chain{open,inner,close,sep,pinned,witness}`表，pin规则只许total操作数/已证数值输入换序；`tgtc`/`nargs`类型检查置首，refd-head/stix零自由度排除；组合约2^12×6^6×24。
- B4c死临时量（`^ 8`）：6处`local qN=<digit>;`（纯字面量、永不读取，个位数不触发重拼）；`seed_loop_lua`行数断言放宽为base..=base+6，deformer内保留`len == base + inserted`精确记账。
- 门禁先行：五RED（ALU双形态/链变序/分裂形态/死临时量，64种子）+ 插入锁（`P4_CHAINS`单源枚举，数字拼写→值归一化，respell免疫）；40向量双目标差分曾RED（未pin `or` 破坏类型收窄：37/40，s6/f1/f2原生算术错），pin规则修复后全绿。
- fixed-seed golden 为 **84,741/93,638 B**（+40/+100），SHA-256 为 `946df83823ff70b49c6bb9d8ee3b68b18bc466c3a4cf2634a45e21ccb9a7df84` / `92b91c3b2b8f515b6d3de9e6db3792ea55e664997a06b12279af198eb9a23aab`；预算维持 **98,000/98,000 B**（8种子最坏 lua51 85,052 B、luau 94,531 B，headroom 约 12.9KB/3.5KB）；完整矩阵 **PASS237（122单元 + 115集成）**，15-run bench 为 Lua51 122 ms / 61.0x、Luau 134 ms / 44.7x。

### 2026-09-09 — P1 seed-ISA逐seed等价变形第三批：helper/reader发射层

- `p3_cv_lua`/`p3_sv_lua`（`seed ^ DOMAIN_HELPER ^ 1/2`）：`if cell[2]`与`if not cell[2]`双形态，条件精确取反、读序列逐路径一致、无求值重排。
- `p3_lookup_order(n, seed)`（`^ 3`）：Luau方法链seed置换（`Prng::shuffle`，n<2自动恒等）；emit按发射位（非方法下标）选`if`/`elseif`。
- `p3_reader_group_lua(seed)`（`seed ^ DOMAIN_POOLS ^ 1`）：六reader定义Kahn-seeded拓扑序重排（b8<b16/b32，b32+take<str，`local bp=1;`恒首，导出绑定序不动）。
- 门禁先行：四RED（双形态×2/变序×2，64种子）+ raw接线锁 + Lookup链序精确比对锁（含ud_check形态无关提取）；现有夹具仅1方法（`add`），wiring门内建双方法夹具（`fixture lost its methods`曾RED，修夹具后绿）。
- fixed-seed golden 为 **84,701/93,538 B**（+4/+4），SHA-256 为 `bf8efbe921d782b119880fdd3807dcb0ab167bbe5437b206122f9b4a83c382c1` / `b742e99d46e27b0fd368c87e9728ad8306f06dd19bdaca0ef0d22ab079a07b40`；预算维持 **98,000/98,000 B**（8种子最坏 lua51 85,276 B、luau 94,565 B，headroom 约 12.7KB/3.4KB）；完整矩阵 **PASS232（117单元 + 115集成）**，15-run bench 为 Lua51 120 ms / 60.0x、Luau 135 ms / 45.0x。
- P1 item 1 收官：模板/routine/arm/helper/reader 五层逐seed等价变形完成；TN/TS/MT/RG/TY/NX（loader注入）、G/W（别名/构造）、Make（循环下标风险）、P/SEEDT/STAB/SEEDH（位置语义）经论证排除。

### 2026-09-09 — P1 seed-ISA逐seed等价变形第二批：routine/arm发射层

- `routine_lua(prog, seed, slot)`：每槽位独立子流（`seed ^ DOMAIN_ROUTINE ^ slot`）逐字重拼，复用模板拼写器（含round-trip断言）；小字保持十进制，全部腐蚀手术锚点（`{7,`、`{1,0}`等）零漂移。
- `seed_arm_lua_for(target, op, seed)`：Test臂双形态（`if-elseif`/`if act~=1 then if…`嵌套，三路径逐一等价）、TailCall臂双序（两动作检查互斥，顺序纯装饰）；Jump/Return/默认臂无变体，管线内`seed`贯通。
- 门禁先行：三RED（routine相异/Test双形态/TailCall双序，64种子扫描）+ 独立解析器round-trip锁（双目标×全op×四种子）；bad-kind-tag手术因锚点跨入重拼字改为直接构造`{{7,1,9004000}}`（fail-closed定位到它：`surgery hit nothing`）。
- fixed-seed golden 为 **84,697/93,534 B**（-34/+27），SHA-256 为 `77ff05a15997f0dcdad866037652fcba55f5b4de3c26a24e671a54d7b5dc504a` / `ad5f3c5660ed952044b384c007f3cc697d1d3bbac1c749b5427fd61fe3b1cbb6`；预算维持 **98,000/98,000 B**（8种子最坏 lua51 85,272 B、luau 94,561 B，headroom 约 12.7KB/3.4KB）；完整矩阵 **PASS226（111单元 + 115集成）**，15-run bench 为 Lua51 119 ms / 59.5x、Luau 134 ms / 44.7x。

### 2026-09-09 — P1 seed-ISA逐seed等价变形第一批：模板层

- `seed_loop_lua(target, seed)` 经新模块 `src/vm/custom/seed_deform.rs` 后处理：12臂op链/13路ALU/8路取值/3路返回/2路跳转置换 + expect三形态 + 数字重拼（dec/0x/科学计数，+2B容差），五域独立子流，同seed字节确定；跨样本统一模板指纹打破。
- 审计面保持十进制：仅3+位数字重拼，seedfail码/操作数/小界全部豁免，燃料预算`100000`按值豁免（既有`contains("100000")`门与文档引用零改动）。
- 门禁先行：distinctness+确定性RED门与数字拼写双目标acceptance先行提交（Batch-0），实现后转绿；every×4/control×4/85×8/40×4/Jump×2/sweep×2/tailcall×4行为套件全绿。
- fixed-seed golden 为 **84,731/93,507 B**（+4/+5 B），SHA-256 为 `4a414d6acf2a6f9a16e36d3d4b046990a243f1c398fb44d3d30a1462bae07482` / `d29d2dbde6a4d80b91184ccca2e7b32effe03afe1d24055bae90b4a3dd2b2864`；整脚本预算调整为 **98,000/98,000 B**（8种子实测最坏 lua51 85,274 B、luau 94,531 B，headroom 约 12.7KB/3.4KB）；完整矩阵 **PASS222（107单元 + 115集成）**，15-run bench 为 Lua51 120 ms / 60.0x、Luau 135 ms / 45.0x。
- `seed.rs` 触 80 KiB 源文件上限，P1 后处理拆入 `seed_deform.rs`（拆分前后 golden 逐字节一致，矩阵 ceiling 门通过）。

### 2026-09-09 — P0 seed-ISA 完成：47 形态迁移 + classic 明文删除

- wire 保持 private ISA14；opcode 0–48 全量 seed 化（routine + 统一臂），`src/vm/opcode/lua51/` 与 `luau/` 共 95 个 classic 明文 handler 整体删除，`tools/test-matrix.sh` 对应计数门改为缺席 pin（复现即红）。
- 调用约定收敛为 `SEED(prog,site,expect)`：SEED 内移到 H 以闭包捕获 R/RX/K，site 按每 op 实际 lane 引用裁成前缀（lane-use ⊆ 测试逐 (target,op) 锁死），expect 编码为 nil=0（默认臂零参数）/1/2/9（Test 与 TailCall 免检），返回改为 value-first（Jump/Return 臂省掉 locals，TailCall 短返回用 `act or v2` 恢复 action）。
- 模板 CALL 改直调：callee 报错不再转 `seedfail:28`，按 Lua 语义原样向上传播（classic `Call` helper 一致）；85 号向量以 PROP marker 锁死错误身份，47 号 spread 空包向量同步改为 LUAERR，52 号 helper-OOB 下标后移一位。
- 门禁增至 85 向量直接差分 + 40 向量 v1 差分 + lane 子集 + 活性探测 sweep（腐坏后输出必须为空或是 pristine 前缀，任一 corpus 覆盖即计入 live）+ tailcall 臂文本/执行 pin + 控制流/全 op 执行双目标绿。
- fixed-seed golden 为 **84,727/93,502 B**，SHA-256 为 `d503c6981bc11536444cc007585a375bb99b74bd7f978bb27f8c9527c37c873f` / `50a501471daee0efbaf2f59b5948f864a841e83f1f83605c4ee6aec6a293a77e`；整脚本预算维持 **95,000/95,000 B**（headroom 约 10.3KB/1.5KB）；完整矩阵 **PASS220（105单元 + 115集成）**，15-run bench 为 Lua51 120 ms / 60.0x、Luau 135 ms / 45.0x。
- 边界：luau headroom 较薄（~1.5KB），后续增长优先从 SEEDT 打包或模板 arity 表挤压；`seedfail:28` 退役（callee 错误传播取代）；site 形状粗粒度可区分 closure/varargs/test/jump（预算硬约束下的取舍，已记录）。


### 2026-09-09 — 体积预算调整为双目标统一 95,000 B

- lua51 整脚本预算 87,000 → 95,000 B（`src/vm/custom/tests/semantic.rs` compression 单元门与 `tools/bench-vm.sh` 同步）；luau 保持 95,000 B。golden 内容与 SHA 不变（84,626/92,850 B）。
- 按 slice-1 实测最坏 seed（lua51 seed-735：85,522 B）计，新 headroom 为 9,478 B；全 handler 迁移按约 80B/形态估算约 3.8KB，可直接容纳——字节打包从 slice 2 阻塞项降为 hygiene 项，下条“预算支撑约 2 个切片”结论作废。

### 2026-09-09 — P0 seed-ISA 第一切片：seed loop + jump/test/return 迁移

- wire 保持 private ISA14（`WIRE_ISA_VERSION=14`，canonical `.obf` 与 SHA 不变）；本批只改发射端：新增 `src/vm/custom/seed.rs`（7-op 汇编/验证/模板），`emit.rs` 对 jump/test/return 启用 dual-form seed 臂，其余形态保持 classic 体，矩阵在迁移中保持全绿。
- seed 调用约定 `SEED(prog,site,R,RX,K,STAB,SEEDH)`，site 七槽 `{a,b,c,k,j,skip1,pc}` 全臂统一；routine 数据 `SEEDJ={{7,1,8004000}}` / `SEEDT={{2,0,1000000},{6,0,4},{7,1,8005000},{7,0}}` / `SEEDR={{1,0,1000000},{7,2,0}}`，构建期 assemble+validate 双保险，PSEUDO(0/1) pc/fid 引用构造期即拒。
- hidden 全局池新增 `pcall→PC`（`local PC=G[vN]` 经 prelude units → ret_order → 解释器模块参数），模板只用模块内 `E/TY/MF/PC/U/Z`，除唯一 `seedfail:` 字面量外无裸全局、无类型名字符串；hidden 全局门扩展覆盖 `pcall`。
- 门禁新增 14 项：seed 单元 9（汇编/验证/模板双目标洁净/12KB 帽）、发射 2（seed 臂存在、经典 `pc=j;`/`pc=skip1`/`return R[RX(a)];` 零残留）、运行 3（79 向量双目标直接差分、8 类 routine 腐蚀双目标拒绝、控制流行为守卫）；`while true do` 仍为 9（seed loop 用有界 `for`）。
- fixed-seed golden 为 **84,626/92,850 B**，SHA-256 为 `7469993baba87a364ef60086e65f0c4729f04ce5bfa1c357bb3af32f7105e695` / `0d87bd8afbca99e44c39a199bbdd587e49029e002375741d8bea807e5a4e2481`；整脚本预算按实测 seed 固定成本 +2% headroom 调整为 **87,000/95,000 B**；完整矩阵 **PASS214（99单元 + 115集成）**，15-run bench 为 Lua51 101 ms / 50.5x、Luau 120 ms / 40.0x。
- 边界：classic 路径保留作 dual-form 对照（P0 末删除）；按约 80B/形态增长，预算支撑约 2 个切片，字节打包（routine 引用字节化）与 arm 瘦身必须在 slice 4 前落地；`T` 与 `MOV` 语义分野（布尔归一 vs 透传）已用 falsy 向量锁死。

### 2026-09-08 — 第九阶段第五个结构子阶段：ISA14 capture/constant pools

- private header ISA13→ISA14；公开OBF v2/ISA1|2、canonical `.obf`、record/segment factorial、dictionary/metadata/tuple置换、operand/register profile、carried-value fragment、六状态entry、segment/LZW/ChaCha8/frame-v2均保持。变化只在capture/constant从header后连续区移入两个全局洗牌池，header仅保留24字节metadata。
- `semantic.rs`新增池布局/洗牌/翻转，`lowering.rs`池draws与`pool_decode_lua`，`emit.rs`新增`pool_loops_lua`与`CU/CK`切片器：每记录3匿名u16 token携带掩码owner/slot/index/tag，per-record factorial profile重排，两池全局洗牌且池顺序按image翻转；parser先验证两池再按owner切片。
- 新增池翻转行为差分与双目标池corruption门：翻转seed一致输出`1`，池错序（checksum修复后）在Lua51/Luau双目标均用户代码前无stdout失败；旧walker迁移到`semantic_pool_layouts`。
- fixed-seed golden为 **81,515/89,777 B**，SHA-256为`d6392170a448f3e400ea5786920f5517e00dd80cbd96a030149f3bed93154eee` / `ca21e582f5d3cbcb24eb0646728ed200f2b2fb163bb0421bbc2f73852dfacde1`；完整LZW frame为`12,332→11,327 B` / `15,559→15,104 B`。完整矩阵 **PASS200（85单元 + 115集成）**，15-run bench为Lua51 102 ms / 51.0x、Luau 122 ms / 40.7x。
- 边界：本批削弱per-prototype capture/constant邻接，但池掩码/置换规则、primitive语义、semantic parser字段和全部inverse均随脚本交付。未做函数inlining/outlining/reorder或全部静态恢复，不得宣称不可逆。

### 2026-09-08 — 第九阶段第四个结构子阶段：ISA13 parser field-order

- private header ISA12→ISA13；公开OBF v2/ISA1|2、canonical `.obf`、operand/register profile、carried-value fragment、六状态entry、segment/LZW/ChaCha8/frame-v2均保持。变化只在record/dictionary/segment/metadata/tuple字段顺序，不改opcode/edge/recipe语义与固定slot宽度。
- `lowering.rs`新增`FieldLayout`/`field_perm`/`emit_field_perm_lua`：`field_seed`从独立salt派生`(mult,add)`，乘子固定于与阶乘互素候选集；record用prototype id、segment用物理slot做`(id×M+A)%n!` factorial置换，Rust编码/`decode`与生成Lua parser/validator/fetch实时重算同一映射。dictionary/metadata/tuple用per-image置换直接baked进parser文本，Lua新增`sont` keyed查找helper。
- 新增`field_order_permutations_decouple_parser_from_canonical_layout`与`target_decoder_rejects_field_order_confusion_on_both_targets`：factorial全范围合法、双端重算一致、固定seed parser字段序期望、跨seed变化，以及双目标record-slot/segment-token/dictionary-header/metadata-slot四类错序（checksum修复后）无输出拒绝；旧ISA12 version pin测试同步升到13。双目标syntax/runtime、46/49 coverage、corruption和compression门保留。完整矩阵 **PASS192（77单元 + 115集成）**。
- fixed-seed golden为 **79,426/87,873 B**，SHA-256为`f6ece30c4bbdea852a8fd365d8902a1285d03618c7e0d1636469443f6904bd92` / `d5980b569759fead1099e88802890373328528d2bb281c21162fdb89c2ea49c8`；完整LZW frame为`11,872→10,703 B` / `15,203→14,653 B`。15-run bench为Lua51 98 ms / 49.0x、Luau 116 ms / 38.7x，仍在85,000/94,000 B与1500 ms独立预算。
- 边界：本批削弱统一record/segment/dictionary/metadata字段顺序与`I[2]/I[3]/I[1]`静态fetch锚点，但factorial置换规则、primitive语义、semantic parser字段和全部inverse均随脚本交付。未完成captures/constants全局池、函数inlining/outlining/reorder或全部静态恢复防护。

### 2026-09-08 — 第九阶段第三个结构子阶段：ISA12-C carried-value fusion + entry graph

- private wire/header继续为ISA12；公开OBF v2/ISA1|2、semantic bytes、operand/register profile、segment/LZW/ChaCha8/frame-v2均保持。变化只在生成handler fragment与entry wiring。
- `lowering.rs`对crate-owned primitive source做token-span结构分析：producer必须是单语句`R[a]=expr`；consumer必须至少有一个register read，且direct register write只能是开头单赋值。control、多写、重叠`R[R[..]]`及其他不确定形态返回不可融合，而非猜测改写。
- fragment planner在最长4项recipe上穷举最大非重叠匹配。每个pair保存首项逻辑目标、提前计算物理key、一次求值结果并保持原write；第二项operand随后解码，所有安全read调用`RF(index,target,value)`。`RK(fid,R)`同时返回`RX/RF`，四family使用逻辑相等、反向不等、物理key相等与shifted-mod-257相等；显式value参数保留nil/false，不复制per-prototype handler。
- entry把prelude、三share probes、三segment+watermark、decrypt/LZW/parser、validator/runtime bind、execute变为六状态machine；状态数、arm文本顺序和四种比较拼写随seed变化。依赖仍严格可恢复，所有inverse仍在客户端。
- 新增`carried_value_fusion_rewrites_both_primitive_boundaries`、`fused_dataflow_preserves_nil_false_aliasing_metamethods_and_errors`并增强fragment/entry结构门。sampled full fixture每份50~60个fusion、65~85个forwarded reads，超过四个poison recipe最多8 pair，证明live recipe实际使用；双目标syntax/runtime、46/49 coverage、corruption和compression门保留。完整矩阵 **PASS190（75单元 + 115集成）**。
- fixed-seed golden为 **78,706/87,167 B**，SHA-256为`a9ef0ee9782615c9ca2eaa5c7b5a36aaa83b8e0900fc37ec0f47b1b9db26477b` / `d43482096f2bb4f9f89e69f0607f4c508a4d82b5498c5d65a1ff8b689967c68a`；15-run bench为Lua51 96 ms / 48.0x、Luau 117 ms / 39.0x，仍在85,000/94,000 B与1500 ms独立预算。
- 边界：本批削弱逐primitive“operand bind+原handler”扫描桥和顶层稳定入口链，但`RF`、eligibility规则、primitive语义、semantic parser字段和全部inverse均随脚本交付。未完成constants/captures全局池、函数inlining/outlining/reorder或全部静态恢复防护。

### 2026-09-08 — 第九阶段第二个结构子阶段：ISA12-B per-prototype register ABI

- private header继续为ISA12；公开OBF v2/ISA1|2、canonical `.obf`、semantic/segment schema、LZW、双ChaCha8/frame-v2 transport及ISA12-A operand ABI均保持。`src/vm/custom/lowering.rs`新增隔离的register planner，避免复制/批量修改两目标约95个primitive模板。
- family/stride/shift分别为模4/127/257的seed-derived可逆仿射分量，组合周期130,556，完整32,767 private prototype id域profile互异。family选择互斥257-key bank，四种域内公式均把逻辑寄存器0..255单射为物理key；目标每frame只计算一次mapper，不存256项映射表。
- 生成器通过lexer token span把crate-owned handler中的每个`R[index]`改为当前mapper访问，字符串/注释不做文本正则替换；天然覆盖`Clear`连续区间、numeric-for的`a+1/a+2`、`Closure`动态`d[2]`capture。`SETUP`用同一mapper初始化参数和Lua51 legacy args/varargs cell；cells/upvalues/callbacks/tailcalls保持同一ABI且不复制per-prototype handler。
- 新增`per_prototype_register_abi_lowers_every_primitive_access`和`register_abi_preserves_cells_captures_callbacks_loops_and_tailcalls`：完整id域唯一性、256槽单射/物理界限、seed差异、无映射表、双目标全部handler token lowering、旧规范访问面消失与目标runtime差分。完整矩阵 **PASS188（74单元 + 114集成）**。
- fixed-seed golden为 **74,540/83,040 B**，SHA-256为`927b84824299ab9a45df3a5aad3db67d016ed11a7ab31bc2155ea1029e4e7dd1` / `135514905e4e101cc282b0d58b098426fb2dc2bee5a9e4424bb46113685f758c`；完整LZW frame仍为`11,872→10,678 B` / `15,203→14,644 B`。15-run bench为Lua51 96 ms / 48.0x、Luau 115 ms / 38.3x，仍低于85,000/94,000 B与1500 ms独立预算。
- 边界：mapper、primitive算术/表操作和共享`OG`仍随客户端交付并可静态模拟；本批没有完成真正cross-primitive dataflow fusion、parser/入口/recipe/CFG去自文档化、captures/constants全局池或函数结构变换，不得把register ABI宣称为全部静态恢复问题已解决。

### 2026-09-08 — 第九阶段首个结构子阶段：ISA12-A per-prototype operand ABI

- private header ISA11→ISA12；公开 OBF v2/ISA1|2、canonical `.obf`、recipe/edge/segment schema、bounded LZW与双ChaCha8/frame-v2 transport均保持。新增`src/vm/custom/lowering.rs`，使用与其他布局隔离的seed stream派生family/rotation/lane仿射参数。
- profile模数为4、3、2,731，乘数分别从各模环可逆元素抽取；三者两两互素、周期32,772，故覆盖private image最多32,767个prototype id且无碰撞。lane使用`6+13×lane`稀疏基址；四family为旋转packed-u24、反向AC+B、旋转转置三列、反向嵌套tuple，均避开record固定头。
- semantic parser在原operand varint/validator之后按profile写record；graph validator与runtime fragment通过共享`OG`恢复`a/b/c/k/j`。fragment四种匹配返回/LHS顺序按operation轮换，每四项后重洗牌；旧`I[6+3*i..8+3*i]`统一读取、`base=3+qi*3`及`o=<actual opcode>`赋值删除。没有为每函数复制handler，尺寸增长有界。
- 新增`per_prototype_operand_abi_breaks_the_static_slot_handler_bridge`：双目标、8 seed、完整32,767-id profile唯一性、family/rotation/binding覆盖、旧静态桥消失、semantic payload逐byte round-trip；既有syntax/runtime/coverage/corruption/compression门全部保留。作用域修复明确不把通用`v`加入slot rewrite，`OG`中会被重写的locals均显式初始化。
- fixed-seed golden为 **72,198/80,636 B**，SHA-256为`5defa5829298f395b1f91c64b5926ed510cc7a87c48cc1272ad98e627b30dc6c` / `7fd96ee94bb83fe7b27470e74927162c9e6f4828e0eb9ef0f30a3265cecfeefc`；完整LZW frame为`11,872→10,678 B` / `15,203→14,644 B`。完整矩阵 **PASS186（73单元 + 113集成）**；15-run bench为Lua51 95 ms / 47.5x、Luau 114 ms / 38.0x。
- 边界：该批切断的是统一operand slot→handler与actual-op marker，不隐藏共享getter公式、handler语义、register ABI或完整dataflow。register ABI与handler register-access lowering现已由上列ISA12-B完成；真正cross-primitive fusion仍未完成，不得把任一子阶段宣称为全部静态恢复问题已解决。

### 2026-09-08 — 压缩大小验收口径纠正

- 用户明确：压缩成功只看压缩后bytecode是否小于压缩前bytecode，不看整份生成Lua。比较边界固定为`private semantic image bytes → 完整LZW frame（含16-byte header）`。
- `compression_reduces_bytecode_while_script_budget_is_independent`对Lua51/Luau多seed逐项执行该比较并验证逐byte解压；ISA13 golden seed实测Lua51 `11,872→10,703 B`、Luau `15,203→14,653 B`（均含16-byte frame header）；不可压缩输入继续fail closed。
- `tools/bench-vm.sh`删除ISA7 predecessor参数和整脚本strict-smaller拒绝，仅保留独立85,000/94,000 B膨胀预算与1500ms性能灾难门。历史golden无需重建，其大小关系只作信息记录。

### 2026-09-08 — 第八阶段：ISA11 ChaCha8 + runtime material + decrypt anti-hook

- private header ISA10→ISA11；公开OBF v2/ISA1|2、canonical `.obf`、semantic/segment graph、bounded LZW、水印和base86不变。旧inner/outer arithmetic streams与generalized Feistel从当前生成链删除。
- Rust与Lua共用标准ChaCha8定义：256-bit key、96-bit nonce、32-bit counter、8 rounds及16/12/8/7 quarter-round rotate；published zero vector锁定算法。inner/outer由domain/context/salt分离，最终material经runtime shares、`pv`、context与attestation执行ChaCha block后得到，不写入Lua数字字面量。
- 目标decoder拆为word ops、quarter round、block、KDF/stream、anti-hook五个随机sibling fields。outer/inner调用前各执行anti-hook，验证source metadata、helper同源、primitive behavior与ChaCha KAT；strict frame v2验证descriptor/version、length、cookie、tag及0..3 byte padding。
- 测试新增wrong attestation、frame逐ciphertext byte corruption、行为等价`string.byte` wrapper、loadstring/source hook、helper/KAT和attestation常量tamper。scope direct probes从6扩为严格12-or-0。固定seed golden为 **82,201/90,866 B**，SHA-256为`8192a5df629f8db9730bf6536dcdfe1012e408a268216ad057545289eb4403be` / `e40507fff0de952a29d7a66fed6fac4c5e249a7c0b7fd97ba6ca1745f0186f24`；完整矩阵 **PASS185（72单元 + 113集成）**，bench为94/113 ms。
- 安全边界：ciphertext与decoder仍同时交付；该批迫使恢复器执行或精确模拟runtime环境/KDF/attestation，但不创造不可提取客户端秘密。下一批应回到每函数异构lowering与parser去自文档化。

### 2026-09-08 — 分阶段复杂化第七批：ISA10 runtime witness + data-dependent control

- private header ISA9→ISA10；公开 OBF v2/ISA1|2、canonical `.obf`、ISA9 segment graph、ISA8 LZW 和 block/outer/base86 wire 全部保持。前三个 key probe 逐 byte 折叠目标 debug source transcript（257 进制模 `2^31-1`），并在各自 seeded rounds 后混入 share；Rust 仅按 Lua51 `=[C]` / Luau `[C]` 规范 transcript 加密。
- 入口在 `c1/c2/c3` 都重建后写入 `(c1+c2+c3)%65520` 私有 control 字段。解释器为 fetch/dispatch 各生成两套互异 state，`mask%2` 通过短路条件选择实际物理对，初始化、四种等价比较拼写和转移再使用 `(selected_state+mask)%65521`；这避免只做统一可约平移。final fragments 的公共回 fetch 从每臂重复赋值改为统一出口。
- 新增 `runtime_probe_witness_is_required_by_every_share_and_control_mask` 与 `emitted_probe_transcript_masks_interpreter_control_states`；现有环境集成门增加仅篡改前三次 source、后三个 segment probe 正常的路径。witness 加一必须只改变对应 share、改变 mask/parity、切换物理 state 对、无法打开旧 block frame并且双目标无 stdout。
- 永久加入 80 KiB Rust 源文件门。原 `src/vm/custom/tests.rs` 124,624 B 拆为 `tests/runtime.rs`、`tests/transport.rs`、`tests/semantic.rs`；拆分后先核对 ISA9 两份 golden SHA/bytes 完全不变，再实施本批功能。当前全部 `src/**/*.rs <=81,920 B`。
- fixed-seed golden 为 **82,514/91,344 B**，SHA-256 为 `d987f38e79b2c1d4b47748367f67a67ba804cb47e3d06ed6489bdb8077741e47` / `d6321eb2cb6906c33b7e37ebcdbd8549f516d53e4744c85bf3a2baa8db40592f`；比 ISA7 少 **1,126/772 B**，仍在 **85,000/94,000 B** 门内。完整矩阵 **PASS184（71 单元 + 113 集成）**；15-run bench 为 Lua51 67 ms / 33.5x、Luau 88 ms / 29.3x。
- 安全边界：纯客户端没有秘密。该批迫使恢复器执行目标环境或精确模拟 transcript/share/control dataflow，但规范 transcript 与 inverse 均可从客户端建模，不能宣称不可静态恢复。下一批按顺序只做目标 3/4（每函数异构模式、parser 去自文档化）。

### 2026-09-08 — 分阶段复杂化第六批：ISA9 global prototype code-segment graph

- private header ISA8→ISA9；公开 OBF v2/ISA1|2、canonical `.obf` bytes/reader/serializer 保持不变。ISA8 descriptor camouflage、edge/recipe token、reachable neutral CFG、synthetic subtree、global semantic fragments、bounded LZW、block/outer/base86、水印与六个环境探针全部叠加保留。
- 每个 prototype 先生成完整 semantic dictionary/record code，再由 full-width token-layer `add/multiplier` 与 owner 推导非空切点，形成 root/terminal 两节点。prototype header 的原 reserved u16 改存 context-masked root；所有 metadata/capture/constant 完成后，全部节点作为一个 pool 做 seed-specific 全记录 shuffle，打破 metadata 后连续函数 code body。
- 每条 pool record 显式携带分别绑定 image/物理槽/id/owner 上下文的 masked `id/owner/next`；terminal next=0。目标 parser 固定读取 `2×np` 节点并检查 ID 范围/唯一/完整、owner、唯一 root、两节点 next、无环、全局 used、每函数总长和 pool 尾位置，之后才运行既有 recipe/CFG validator。
- 新增结构门 `global_function_segment_pool_is_decoder_coupled_interleaved_and_exact`，以及双目标 target corruption 门；duplicate/missing/out-of-range/wrong-owner-token/wrong-root/cycle/truncation/trailing 均在用户代码前无 stdout 拒绝。既有 semantic corruption 与 private-field/transport 门同步升级到 ISA9。
- 为控制整脚本体积，只有 crate-owned VM finalizer 在语法无歧义的块关键字边界和 `)`/`]`/`}`→identifier 边界省略可选分号；公共 minify 仍逐语句保留 `;`，`f();(g)()` 的分号不得删除，最终重解析/绑定复核继续 fail closed。
- 固定 seed golden 为 **82,729/91,722 B**，SHA-256 为 `be0757fd570a13cb2262a315e96eceeaac2ed78b64463de11f9b8e5dc9e9ff2f` / `b7963ed55553718179c8e4a22376adc1638fc6a45dc801bec0ce27360060bf14`；保持独立 **85,000/94,000 B** 整脚本预算。与ISA7 **83,640/92,116 B** 的关系仅作历史体积信息。完整矩阵 **PASS182（69 单元 + 113 集成）**；默认 15-run bench 为 Lua51 68 ms / 34.0x、Luau 88 ms / 29.3x。
- 本批只全局化 code body segments；captures/constants 未全局化，安全 inlining/outlining/reorder、跨 primitive dataflow fusion 和不可逆静态防护仍是后续独立批次，不能把可逆 graph/transport 宣称为已解决全部静态恢复。

### 2026-09-08 — 分阶段复杂化第五批：ISA8 bounded LZW

- private header ISA7→ISA8；公开 OBF v2/ISA1|2、canonical `.obf` bytes、semantic recipe/edge/fragment schema 和既有环境探针行为不变。
- 选择 std-only bounded LZW 而非 ZSTD：semantic image 在 cipher 前按 8 KiB 输出 reset，code 以 canonical literal/reference prefix 连续 LSB-first packing。16-byte frame 只含 magic/version、原长、有效 bit 数和全像 Adler-32；完整 frame 不严格缩小时拒绝生成。
- 原 constant stream 的 key pair/轮数/三族 arithmetic family 保留并扩大到整个 compressed body；clear header 全字段参与 state 派生，并继续由 block tag 覆盖。block transport、outer stream、watermark 和 base86 三段顺序不变。
- Lua bit reader/LZW/frame inverse 按 seed 组成二或三个 sibling payload functions，并与其他字段全局洗牌。Rust/Lua 均限制原长 16 MiB、reset 2,048、单段 dictionary/phrase 8 KiB，拒绝截断、越界/forward code、首 code reference、非 canonical literal、输出越界、非零 padding、未完整消费和 checksum 不符，不返回部分结果。
- codec 门覆盖全部截断、逐 frame byte bit corruption、256 个结构合理随机畸形 frame、incompressible rejection、wrong inner key、双目标语法/真实运行；另重算合法上游 transport，将 checksum/body 畸形 frame 直接注入 Lua51/Luau inverse 并验证用户代码前无输出拒绝。该历史批次曾附加85,000/94,000 B和ISA7整脚本比较；后者现已取消，正确compression门只比较完整LZW frame与原semantic image。
- fixed-seed golden 为 **82,572/92,022 B**，SHA-256 为 `50917035eeaf57c78f44f4efd5b2f3073b1c72e3360d499574da4a901b03b34c` / `fb3a850f5087685d0ffd3563d374312def42fdda503c2158f32bfc752a3ff7ea`。完整矩阵 **PASS179（66 单元 + 113 集成）**；bench 为 66/86 ms。压缩与所有 transport 均可逆，不解决静态语义恢复；全局 prototype/function segment pool 与 inlining/outlining/reorder 继续暂缓。

### 2026-09-08 — 分阶段复杂化第四批：ISA7 dynamic block transport v1

- private header ISA6→ISA7；公开 OBF v2/ISA1|2、canonical `.obf` 字节及其 SHA 不变。ISA6 的 validation-equivalent descriptor、`rid→sid` global fragments、edge/recipe token、reachable neutral CFG 和 prototype camouflage 原样保留。
- pipeline 固定为 `semantic→constant cipher→block transport→outer stream→watermark/base86`。block 层以 16-byte frame 记录 transport descriptor/version/7~10 round count/4-byte width、原长、dynamic cookie 与 keyed integrity tag，并补 0~3 byte key-dependent padding。
- 新增 32-bit generalized Feistel（两个 16-bit half）：每 schedule 必含 quadratic、byte-bilinear、two-stage quadratic 三 family；round 顺序/数量/参数、subkey 系数、position 项及 IV/chaining salt 全部由独立 seed stream 变化。两个 31-bit final key state 由三个 runtime probe share、forms 重建的 `pv` 和 padded ciphertext length 非对称计算，不写入脚本字面量；probe 调用仍洗牌，但返回值按所属 probe 绑定 canonical `c1/c2/c3`。
- 目标端 reverse rounds 展开进 outer-decrypt 状态机完成态；只有 ciphertext 长度/对齐、descriptor、精确 framed length、cookie、tag 和每个 padding byte 都通过才向 semantic parser 返回。Rust `extract_embedded` 的六种 segment-order 候选也必须先过 block frame；错误 seed/顺序/损坏不再仅依赖 inner magic/Adler。
- 新增 `block_transport_roundtrips_rejects_every_byte_and_varies_by_seed`：padding residue、三 family、7~10 rounds、schedule/ciphertext diversity、wrong share/`pv`/seed、截断/扩展、overflow 及每个 ciphertext byte 翻转；key non-leak 门加入两个 final block state。新增双目标集成门保持 base86 与水印有效，修改三 segment 首/中/尾密文组，均须无 stdout 失败。
- 固定 seed golden 为 **83,640/92,116 B**，SHA-256 为 `499ebc32d10ba9543f233380aa20b894a4b921740bac4fc7e77e4e31e2f87afa` / `35c80e5e890c42a7b8f029aacab790d781cc8fcb672eeaffdb55b637c62e8607`，仍在 **85,000/94,000 B** 门内。完整门禁 **PASS178（65 单元 + 113 集成）**；bench 为 Lua51 58 ms / 29.0x、Luau 81 ms / 27.0x。该层是可逆 transport/corruption gate，不是经证明的密码学认证；不能替代后续全局 segment pool 与 inlining/outlining/reorder。

### 2026-09-08 — 分阶段复杂化第三批：ISA6 live descriptor camouflage + global semantic fragments

- `SemanticRecipe` 将 wire `descriptor_ops` 与真实 `execute_ops` 分离。live dictionary 只在 encoding Form、具体目标 validator 与 control/terminal graph class 完全相同的六类 family 内随机替换；singleton 安全类不为增加比例而合并。固定双目标/多 seed 门要求超过三分之一 live recipe、至少 45% live primitive 的 descriptor 说谎，同时保持 operand 和图验证 fail-closed。
- 旧连续 superhandler 被拆成随机 continuation graph：2-op=`1+1`，3-op=`1+2|2+1`，4-op=`2+2|1+1+2|2+1+1`。因此多-op recipe 必定分片，3/4-op 仍至少融合一对 primitive。每个 fragment 获得唯一三位 `sid`；`rid` grouped chain 只设置入口，独立 `sid` grouped chain 收集并跨 recipe 全局洗牌全部 fragment arms，只有 final fragment 回到 fetch。
- 解释器现在具有 `o/rid/sid` 三维 dispatch topology；Jump/Test label rewrite、Return/TailCall 词法终止和 final-fetch state 继续保持。private header ISA5→ISA6，公开 OBF v2/ISA1|2 与 canonical `.obf` SHA 不变。
- 新增 dedicated live camouflage class/密度/seed 变化测试，并扩展 semantic wire、fragment split/fusion、三维 dispatch、真实 primitive coverage、corruption 与双目标语法/runtime 门。固定 seed golden 为 **80,369/89,149 B**，SHA-256 为 `1be51fd30c946c4803d311b9c9cb3cbb7fbfe05b7d09826a5afc316209e06f78` / `3009575f7aef69b4ecfa331c09cfdc68fb0e9c4791efa3c8b88e0abf08508cfa`。
- 体积门有意识调整为 **85,000/94,000 B**；bench 为 Lua51 47 ms / 23.5x、Luau 64 ms / 21.3x。完整门禁 **PASS176（64 单元 + 112 集成）**。
- dictionary 已不再直接自描述 live semantics，recipe handler 也不再保持连续；但 generated stage arms 与 prototype 元数据仍可静态分析。当时动态密钥/自定义分组加密、全局 segment pool 与函数 inlining/outlining/reorder 仍未完成，不能把第三批宣称为全部目标结束。

### 2026-09-08 — 分阶段复杂化第二批：ISA5 successor-token + reachable neutral CFG

- private record 的四个 u16 位置保持紧凑，但 next/skip 两槽改存三阶段 edge token；每层以奇乘数/逆元做模 2^16 仿射变换，混入 source label、prototype、edge kind、seed 参数和交叉项。recipe token 继续绑定 ED 解出的真实后继。
- 新增 seed-shuffled `ED`：3 live inverse + 1 return + 3 dead state，每个 live stage 还有 2~3 层动态 opaque predicate 和 arithmetic/repeat 死侧。target validator 与每次 fetch 都调用 ED；persistent code record 不缓存明文 successor。
- 每个真实 prototype 的入口插入 2~4 个中性 bundle，并按函数规模抽样至多 5 条原 next/test edge、各插入 1 个。正常 frame 使用新增 scratch register 上的 `Nil/Not/Move/Clear`，满 256 slots 时退化为 `Move r0,r0`；插入后重新洗牌 physical records。运行插桩证明 neutral recipe graph-referenced 且真实执行，而原四个 poison recipe 仍不执行。
- private header 从 ISA4 升为 ISA5；公开 OBF v2/ISA1|2 不变。新增 edge token 三层逆元/64-context 差异、neutral execution、未知 successor token fail-closed、双状态机深度与双目标语法/runtime 回归。
- 固定 seed golden 为 **71,084/78,081 B**，保持在 **72,000/80,000 B** 预算内；bench 为 46/62 ms；完整门禁 **PASS175（63 单元 + 112 集成）**。
- 仍未完成 semantic fusion/splitting、动态密钥/自定义分组加密、全局 segment pool 或函数 inlining/outlining/reorder；不能把本批描述为全部反恢复目标完成。

### 2026-09-08 — 分阶段复杂化第一批：ISA4 contextual recipe-token machine

- record 第四字段由稳定 recipe ID 改为五阶段 u16 context token；每层使用奇乘数可逆仿射变换，并混入 label/next/skip/prototype、seed 系数及交叉项。同一 recipe 的不同 use-site 不再共享 wire 值。
- 目标 validator 和解释器共用一个 RD：验证时恢复 operand schema，运行时每次 fetch 再恢复，`code[label]` 不缓存明文 recipe ID。RD 使用 5 live inverse + 1 return + 5 dead 的乱序状态机；每个 live state 内嵌 4~5 层上下文不透明谓词，每层死侧和每个 dead state 都注入算术与单次 repeat。
- private header 从 ISA3 升至 ISA4；公开 canonical OBF v2/ISA1|2 和既有 `.obf` SHA 不变。新增五层逆元/往返、64 上下文差分、深度/死代码密度、损坏 token fail-closed 及双目标语法/运行回归。
- golden 更新为 **68,344/75,615 B**，体积预算调整为 **72,000/80,000 B**；bench 40/57 ms，完整门禁 **PASS174（62 单元 + 112 集成）**。本批只完成深层状态机、多层谓词、高密度死代码和动态指令 token；可达 CFG decoy、successor 编码、分组密码以及 function segment/inlining/outlining 按风险留给后续批次。

### 2026-09-07 — 针对静态破解报告的 ISA3 semantic virtualization

- 报告证明旧生成脚本可在不执行样本的情况下恢复双层载荷、识别 45 条 custom opcode、逐 handler 一对一翻译并按近同构 prototype/linear PC 还原全部指令。结论是继续只换 opcode 编号或密钥流不能解决根因。
- 新增 `src/vm/custom/semantic.rs`：从已验证 `Program` 构造 seed-specific image；CFG-safe bundle 最多 4 primitive，全程序 recipe 去重/随机 u16 ID，operand-only use-site，随机 label/显式后继、record 洗牌、兄弟 prototype 重排、Closure remap、2~4 节点 synthetic unreachable prototype subtree 及四个 mismatched decoy recipe。
- `emit.rs` 目标 parser/validator 改为只接受 encoding 1/ISA3，解析 masked dictionary、按 recipe Form 解 operand，严格检查 graph 和 prototype；interpreter 以 `code[label]` fetch 并将 recipe 展开为 unrolled superhandler。Jump/Test 改 label 语义，不再 `pc+=4`。
- `structure.rs` 新增 u16 recipe-ID grouped dispatch；`random.rs` 新增 `dispatch_condition_for`，修复硬编码 `o` 导致 semantic chain 首次比较 nil 的问题。另修复目标 varint reader 对“中间 7-bit group 为 0、后续仍 continuation”的合法最小编码误拒绝。
- 测试契约改为解密结果等于 deterministic ISA3 re-encode，而非 canonical `.obf`；coverage 记录 live recipe 后展开 primitive；新增/替换 superoperator 比例、4-word recipe、seed wire、record/prototype shuffle、decoy reachability，以及 recipe/label/prototype metadata 破坏性拒绝门。公开 `.obf` ISA1/2 round-trip 与 SHA 保持不变。
- 所有结构仍可逆：primitive Lua semantics 与大部分 prototype metadata 仍在产物中；静态恢复现在需要 recipe 甄别、CFG 重建与重新线性化，不能宣称不可破解。golden 更新为 **64,599/71,598 B**；体积门因 program-specific superhandler 提高至 68,000/74,000 B；完整门禁维持 **PASS173（61 单元 + 112 集成）**。

### 2026-09-06 — M7 不透明真假分支（实际指令 + 不运行的真指令）

- 按用户指定的两种形式实现：① 入口方法体整体包装 `if <恒真式> then 实际链 else 不运行的真指令 end`（seed 翻转为 `if <恒假式> then 诱饵 else 实际链`）；② F3/F5 dispatch 链尾各 2 条死 elseif 臂，条件是比较 200..254 的不可能 opcode 号（四比较拼写 × 三数字字面量形态），臂内为逼真 handler/parser 语句。
- 死路由构造保证：F3 的 `FM[o]` 门先于 dispatch 拒绝一切未知 opcode（合法 opcode ≤63）；入口守卫为常数恒真/恒假整数谓词（`opaque_pair` 四对同形谓词，无 NaN/元方法/浮点）。诱饵从不执行由全部原生/VM 逐字节差分证明。
- 新增单元测试 `opaque_true_false_branches_carry_real_but_unreachable_instructions`：token 级检测死臂（须覆盖全部四种比较拼写与十/十六/二进制字面量——`integer_literal` 变体会输出 0x/0b/下划线分组）、入口恰含一个池内谓词、两种形态跨 seed 都出现、同 seed 复现。性能 bench 不变（10-12ms，死臂仅在链尾多两次比较）。
- 完整门禁 **PASS173**（61 单元 + 112 集成）；矩阵（含 bench）PASS、debug/release 一致。golden **28,360 / 30,777 B**（门 29,500/31,800 B 内）；`.obf` 与其 SHA-256 不变。
- 密钥流原语族+跨阶段密钥派生（A2+B1）：破解报告 §3/§9 用十行 Python 复刻了单一 Lehmer 流并"以校验和为反馈"。A2——payload 层与常量层各自独立抽取三族算术流原语：族0 Lehmer（乘子 16807/48271/65539）、族1 双 Lehmer 和（两状态 1+(st*7+31)%2^31-2 初始化，输出 (u+v)%2^31-1 再 %256）、族2 mod-2^32 LCG（奇乘子 40503/69069/1664525 + 奇加数，输出取 (s-s%2^24)/2^24 高位字节——mod-2^32 LCG 低 8 位周期仅 256，必须取高位）；家族+参数逐 seed 抽取（CipherParams 扩展 outer_stream/constant_stream），发射端 xor_step 与 KA 函数按族生成，全部乘积 <2^53 保证 Lua 双精度精确。B1——payload 种子加 `pv=1+(PT[i1]*31+PT[i2]*7+PT[i3])%2^31-2`：PT 是 forms 字段从打包串重建的重编号表（i1..i3 专用盐流抽三个互异槽位），forms 字段调用因此前移到解密之前；常量层种子加 `ka2=SB(B,33)*31+SB(B,#B)`：首 proto 头第 1 字节与文件末尾代码字节都在加密常量范围之外，故 Rust 加密端（明文镜像）与 Lua 运行端（解密镜像）取值恒同，无环。效果：两级密钥都不再是纯静态量，逐段黑盒复刻必须按依赖序（forms→pv→外层解密→ka2→常量层）模拟整条管线。探针份额派生保持 Lehmer；假锚点（C1）不动。新增单元测试 `keystream_families_and_cross_stage_terms_couple_the_pipeline`；零字面量测试的 pv/state/常量态秘密集同步更新。
- 像内诱饵语义臂+假锚点（针对静态破解报告的反制）：破解报告方法论=把 handler 当黑盒用 Python 复刻 I/O、以 Adler-32/魔数为格式锚点、重建打包重编号表后用 45 条 ISA 语义一次性写通用反编译器。**A1**——F5 分派链为程序未用 opcode 槽位（每 seed 随机弃 0..2）发射假臂：键=perm[未用槽]（在置换像内，重建 PT 过滤不掉）、体=另一 opcode 的真实 handler 文本（毒化语义表，程序自身 assert 无法证伪）、不可达性=形态表把未用槽编码为非法形态（decode 在分派前 E()）。旧像外诱饵改用完整 64 槽置换像（原先只避开已用操作数像，会与假臂撞键）。**C1**——entry 死侧新增三个假锚点模式（与真串同形的 129 字节假打包 base86 串+重建循环恒为死代码；真表键参与的假 LCG 份额派生 `(k*31+k)%2^31`+48271/65539 迭代；假 Adler 双累加校验门对假串求和比对随机 u32）——"从校验和下手"与"找密钥派生"的方法论先撞假货。体积门 27,500/29,800→29,500/31,800 B（假臂+假锚约 +1.0~1.5KB）；bench 21-22/28ms。新增单元测试 `decoy_arms_and_fake_anchors_poison_static_recovery`：四拼写（十/十六/二进制字面量）扫描 F5 全部臂键、断言像内诱饵覆盖未用槽≥n-2、未用 opcode 自身 handler 文本绝不出现（毒化）、像外诱饵≥2、恰 2 条 129 字节 tilde 串且恰 1 条解出真置换、跨 seed 诱饵集变化、差分运行等价。
- g 槽位表：全部非递归阶段的活局部改写为 `local g={}` 槽位读写——payload 解密（st/XB/i/w/x/y/r/p/q）、三个 base86 分段（S/o/i/L/st/v/m/b/r2 等）、forms 形态表重建（t/p/S/U/b/x/y）、varint decode（w/v/o/f/a/b/c/j/k2）、validate 边界臂（ok）、解析 core 驱动（P/work/id/w/np/entry/isa/check/sa/sb 等）、F3 校验循环（F/CD/p/XB/o/a/b/c/p2/k/j）以及解释器 SETUP 的中间量（nn/va2/vv）全部进入槽位：每变量绑定 per-seed 随机数字 key（1..99 去重），local 声明消失、值持续流经同一槽位；decrypt/三段/forms/core 在作用域尾 `g=nil` 清空（decode/validate/F3/SETUP 因闭包或循环存续期不清）。重写器引号感知，跳过字符串字面量（base86 文本内的孤立单词不受影响）。解释器帧寄存器 F/R/va 为实测例外：pcall/闭包经 SETUP 递归时嵌套帧会覆写单槽（症状=拼接/index nil），保留普通 local；`local F,R,va=SETUP(fid,args)` 把 varargs 表交回 handler（`R[a]=va` 依赖它）。槽化不可处理的三类 authoring 形态已改写：局部函数形参表（PH/PU/PK 无参化）、`{k=v}` 构造器简写（显式赋值）、裸 `local X;` 声明（删除）。`g[key]` 比短名 local 长，体积门 luau 28,500→29,800（Lua51 27,500 不变）；bench 17-18/22-23ms（槽位表访问慢于 local，灾难阈值 1500ms 内）。新增单元测试 `core_logic_flows_through_scratch_table_slots`。复核修补：validate 字段的槽化调用曾整体丢失（`ok` 裸 local、宽松 8..10 计数掩盖），已补回并把计数断言收紧为恰 10 张表；vld 结尾锚点改用 `E()end;return true` 形态。
- 全阶段控制流扁平化：base86 三分段、外层解密、解析 core、解释器 fetch/dispatch 四类逻辑全部改为 `while true do if <状态> then … elseif … else E()end end` 状态机——状态数三位随机（structure 流 100..999 去重）、分支文本序洗牌、条件四拼写（`w==K`/`K==w`/`not(w~=K)`/`w-K==0`，新助手 state_values/state_condition/state_machine）；解析 core 拆出局部函数 PH（头读+校验）/PU（upvalue 接线）/PK（常量池，定义序洗牌）+ 五态驱动机（next/upvalues/constants/take-code/finish，finish 经 break 走字段 return）；解释器帧 setup 拆出 SETUP(fid,args) 函数，fetch/dispatch 两相位机（o,a,b,c,k,j 提升为循环前声明，fetch 行文本原样保留供覆盖探针改写）；分段字段三态机（主步自循环/尾处理/收尾）。同 seed 复现、差分不变；bench 9/10ms 无损。
- 随机 section 拆分：原 F2 单体（解密+十来个 helper 闭包+解析循环）扁平化为多个平级 `[数字]=function` 字段——解密字段（keys[1]）、6 个依赖序 helper 组（bp/b8..str+pos、fin、ku/ks/KA、DX、db8/db32/dstr、num/dnum）按 seed 随机切点分成 2..4 簇、解析 core（keys[20]，签名含全部导出）；每簇参数=所需基名∪前序导出（精确推导，ca/cb 仅传含密钥流组的簇），entry 内按依赖序接线（replacen 替换原单体调用行）；bp 是读簇的 upvalue，core 终检用导出的 `pos()` 访问器（数值拷贝会过期）；全部字段参与布局洗牌，数字字段总数 19..21、含入口 20..22 逐 seed 变化。
- 全局名隐藏：prelude 捕获改为 `G[拼名]`——26 个左右全局/成员名（select/string/math/error/tonumber/type/debug/loadstring/getinfo/…）由单字符函数池（每 seed 洗牌槽位）+ 每名字随机格式（直接 `s[i]()..s[j]()` 或表驱动 helper `C(s,{i,j})`）运行时拼装；环境取 `getfenv(1)`（块级 fenv，与裸名解析语义一致，setfenv 沙箱篡改仍可被探针检出，审计接受 0|1 两级以兼容 legacy 后端）；探针/分段门改收入口转发的 DB/GI/LS 参数，产物中 debug/loadstring/getinfo 明文出现 0 次，setmetatable 恰 1 次（外层包装）。scope 审计改为结构化探针识别（`X and Y(Z,TAG)`），barrier 记账=恰好 3（捕获行内）。体积门按测试注释规定路径有意识上调至 25,500/27,000 B。
- 全字段解除锚定+微变体：入口字段与解释器字段并入洗牌池（17 字段位置完全随机，文件尾仅 `},x):m()` 收口；解释器字段锚点在不透明分支注入后用唯一文本 rfind 定位）；每字段分隔符 `,`/`;` 由 structure 流独立抽取（表构造器尾随分隔符合法，语法经内部审计+luac5.1 -p+luau 三方验证）；prelude 捕获语句（9-10 个独立单元，唯一依赖 Z 读 SC，shuffle 后按需对调保证 SC 先于 Z）与返回序、entry 解构序（同一洗牌的两种呈现）全部逐 seed 变化。
- 分派链二级拆分：F3/F5 两条 dispatch 长链各按 seed 取 G∈2..4 条子链，选择器 `o%G==g`（四等价拼写），每臂按其重编号值的模数指派子链，链内臂序照旧洗牌；子链各带 `else E()end`，空残基组（无任何臂落此模数）恒不可达、直接 `E()`。跨 seed 拓扑（G3,G5）与链长分布全变（12 seed ≥3 种拓扑），同 seed 复现；求值次数不多于单链，vm-best 8/10ms 不变。
- 随机 opcode 重编号：`opcode_permutation(seed)`（盐 `0x6f70_636f_6465_7333`）把 64 个 canonical 槽位拒绝采样单射到 0..255；生成端以此重编号 F3 边界臂、F5 解释器分派臂与 Jump/Return/TailCall 终止检查，死臂从重编号像之外取值；目标端由形态字段的第二打包串（2 base86 字符/槽、`~` 前缀标记，不参与传输分段审计）重建同一置换表，验证展开时 `PT[o]` 重写每条操作码字节。跨 seed 编号全变（8 seed 8/8 distinct），同 seed 双端一致；`.obf`、varint 流与差分不变。
- 特征拆分隐藏：原 F3 验证器内三处静态特征（顺序键形态表字面量、`if f==1 elseif f==2 …` 形状链、逐 opcode 边界臂）各自独立为 `[数字]=function` 字段并参与布局洗牌；形态表不再以字面量出现，改由独立字段从 per-seed 旋转的 base86 打包串（每 opcode 一字符、空位编码非法形态）重建，跨 seed 串字节变化；验证语义与拒绝行为不变（差分/篡改回归全过）。
- 全代码随机化落地：13 个 `[数字]=function` payload 字段（含全部解密/探针/分段/水印函数）按 `structure` 盐 seed 洗牌发射，头部仅留 `local x={};return setmetatable({`；加密参数 `CipherParams`（外层/常量层 Lehmer 乘子取自 {16807,48271,65539} 原根集、混合常数取自 {31,33,37,41}、探针 3+0..6 轮、常量层 9+0..4 轮）由 `cipher_params(seed)` 派生并注入 Rust 加密与 Lua 解密两侧模板，乘积均 <2^53 保证 double 精确；同 seed 双端字节复现、跨 seed 布局/乘子/混合全变化（12 seed 布局 12/12 distinct）。

### 2026-09-06 — M7 高级结构随机化（不含外层包装变体）

- 按用户要求启动 M7，明确排除最外层 table/function 包装变体。三项落地：① dispatch 比较四形态（复用 `dispatch_condition`，F3 校验链与 F5 解释器链逐 opcode 随机）；② 整数边界检查三形态（新 `gt()` 辅助：`x>K`/`K<x`/`not(x<=K)`，操作数恒为 varint 整数，严格等价）；③ 元方法 dispatch 分支变体（Call 包装器缓存分支纯反转、userdata 守卫 `==` 序翻转——字符串 raw 比较可交换）。独立 structure 随机流，同 seed 复现。
- 测试：新增 `m7_structural_variants_vary_across_seeds_and_stay_reproducible`（token/子串级检测变体族跨 seed 并集全覆盖 + 同 seed 复现；两个实现教训：检测必须在 finalizer 改名后仍成立、聚合必须跨 seed 取并集而非单脚本 max）与 `generation_respects_the_documented_size_budget`；全部既有差分（原生/VM 逐字节一致、篡改静默、破坏性载荷拒绝）继续通过，证明变体等价。
- 性能与体积：新 `tools/bench-vm.sh`（best-of-15 计时、golden 体积硬上限 24,000/26,000 B、VM 单次 1500ms 灾难阈值）接入矩阵末尾；实测 VM best 10-12ms ≈ 原生 4-5×。
- 完整门禁 **PASS161**（48 单元 + 113 集成）；矩阵（含 bench）PASS、debug/release 一致。golden **21,191 / 23,395 B**（变体 +253/+445 B，预算内）；`.obf` 与其 SHA-256 不变。

### 2026-09-06 — 固定传输水印与隐藏式双函数检测

- 按用户要求：base86 流解码后的前 4 字节固定为 `XXS:`；运行时检测，不匹配静默中止；检测函数拆成两个 function 隐藏。实现：`W1` = 首 4 字节大端打包器（无水印常数），`W2` = 与不透明 u32 1482183482（Rust 侧 `u32::from_be_bytes(*b"XXS:")` 派生）比较。脚本全文不含字符串 XXS:（单元断言）。入口先 W1/Y1→W2 校验，再 `SS(流,5)` 剥离水印传 F2——外层密钥 `31*#B` 公式不受影响。
- `extract_embedded` 要求水印前缀并剥离；水印把流首段钉死，与全镜像 Adler 共同把有效段序约束为唯一。新增单元测试（水印存在于唯一流首段、脚本无 XXS: 字样、不透明常数在场、roundtrip 保持）与集成测试（外科手术式翻转流首段首字符：base86 组独立 → 仅水印 4 字节变化、密文其余逐字节不变 → 必须无输出中止；对照组正常输出）。
- 途中修复 W1 未接收 SB 参数的 bug（调用全局 SB → nil）。完整门禁 **PASS159**（47 单元 + 112 集成）；矩阵、debug/release、golden 运行对照通过。golden **20,938 / 22,950 B**（+222/+220 B：水印组与两个检测函数）；`.obf` 与其 SHA-256 不变。

### 2026-09-06 — base86 传输层与三段打乱

- 按用户要求给整个字节码 payload 叠加 base86 编码并打乱：双重加密（常量层+外层）后的镜像整体编码为 base86（86 个可打印字符，避开引号/反斜杠；4 字节→5 字符，尾部 1..3 字节→2..4 字符，全部运算 < 2^53，Lua 端纯 double 解码），切成三段按 seed 洗牌分放到三个新的 `[数字]=function`。
- 按用户要求，每个分段解密函数都先做 loadstring 原生检测（Luau `debug.info(loadstring,"s")=="[C]"` / Lua 5.1 `getinfo(...).what=="C"`）再解码自己的分段（字符域/尾长/32 位组值/尾宽四重校验）；连同三个份额探针，脚本共有 6 个探针门控函数，`scope::generated_vm_plan` 审计计数同步 3→6。入口按流序取段拼接为外层密文，F2 改为接收 B 参数。
- 新增 base86 codec 单元回归（0..39 长度逐长度 roundtrip + 5000 字节缓冲 + 越界字符/悬空尾/32 位溢出/尾宽溢出拒绝）；熵值测试改用分段重组后的外层密文。实现中修复提取器缺陷：段序判定最初只用 magic+目标字节，共享首段的两个置换都能通过，改为全镜像 Adler-32 判定唯一有效序。
- 环境篡改 fail-closed 双目标复验通过。完整门禁 **PASS157**（46 单元 + 111 集成）；矩阵、debug/release、golden 运行对照通过。嵌入成本从约 2.9 转义字符/字节降到 1.25 可打印字符/字节，golden **20,716 / 22,730 B**（较上一批次缩小 24%/26%）；`.obf` 与其 SHA-256 不变。base86 是编码不是加密。

### 2026-09-06 — 常量池独立第二层加密

- 按用户要求为字节码常量加独立加密层：`generate` 在外层 blob 密码之前，对嵌入镜像中每个常量记录的 payload（布尔值字节、数字/整数 8 字节、字符串内容）用独立 Lehmer 密钥流 XOR；字符串长度 u32 保留明文框架（否则密文镜像不可再扫描，对称解密无法定位——这是实现中发现并修正的设计点）。密钥来自另一对 wrapper 键 `(keys[4],keys[7])`（31 混合 + 11 轮步进 + `31*#B` 长度混合），零字面量；嵌入镜像 Adler-32 相应重算。
- 目标端 F2 增加 `fin/db8/db32/dstr/dnum` 读取器：常量在解析时逐条解密；密钥错误得到乱码常量，被框架/语义门拒绝，用户代码不执行。入口向 F2 传入两个常量层结构键。`.obf` 磁盘文件与 `compile` 输出保持明文 canonical、逐字节与 SHA-256 不变。
- 新增 2 项单元测试：第二层独立性（外层解开后 SECRET/数字常量不可见、不同 seed 密文不同、双层解密精确还原 canonical）与 `constant_ranges` 扫描器防御性（空/坏 magic/np=0/未知 tag/截断/尾随字节全部诊断错误）。零字面量测试加入常量层初态；破坏性测试更新为“破坏 framing 由生成器拒绝（kind 4），其余由目标端拒绝”。修掉一个轮数失配 bug（Lua 10 轮 vs Rust 11 轮 → 静默中止）。
- 完整门禁 **PASS156**（45 单元 + 111 集成）；矩阵、debug/release 一致、golden 运行对照通过。golden 为 **27,223 / 30,888 B**。如实声明：字符串长度与常量种类仍可见（框架元数据），混淆而非加密。

### 2026-09-06 — 密钥动态生成（零字面量）

- 按用户要求，三个密钥份额不再以十进制字面量出现在脚本中：每个探针函数在环境校验通过后，对入口传入的 payload 表数字键对运行时计算 `x=(a*31+b)%2147483647` 并叠 3/5/7 轮 `x=48271*x%2147483647` 步进得出份额；decoder 结合公式改为 `1+(s1+s2+s3+31*#B)%2147483646`（混入密文长度）。Rust 端用完全相同的公式从 wrapper keys 推导初态并加密，双端逐位一致。
- 新增单元测试 `cipher_key_is_derived_dynamically_and_never_appears_in_plaintext`：多 seed/双目标断言份额与密钥流初态的十进制串不出现在脚本文本或任何 Number token 中，同时保持同 seed 复现与解密 roundtrip。环境篡改 fail-closed 集成（上一条目）不变并重新验证通过。
- 完整门禁 **PASS154**（43 单元 + 111 集成）；矩阵、debug/release 一致、golden 运行对照均通过。golden 因探针携带公式变为 **26,322 / 29,999 B**；`.obf` 与其 SHA-256 不变。

### 2026-09-06 — 嵌入 payload 字节级加密与三探针密钥拆分

- 按用户授权实现 M6 前两项：生成阶段用 seed 派生 Lehmer 密钥流（48271 mod 2147483647，初态 1+(s1+s2+s3)%2147483646）对内嵌 blob 逐字节 XOR，密文熵 > 7.5 bits/byte 且高于明文（单元测试断言）；全部中间量 < 2^53，Rust/Lua 双端逐位一致。`.obf` 文件与 `compile` 输出保持明文 canonical、与 seed 无关。
- 密钥拆成三个份额分存于三个 payload 函数；每个函数先执行固定形状环境探针（Luau `debug.info(loadstring,"s")=="[C]"`，Lua 5.1 `debug.getinfo(loadstring,"S").what=="C"`）再返回份额；入口按 seed 洗牌顺序调用三者，decoder 结合解密后走原有 magic/Adler/结构校验。环境篡改（替换 loadstring / setfenv 注入）时无输出中止，双目标集成回归固化。
- `scope::generated_vm_plan` 审计扩展：允许恰好 3 个固定形状探针（或 0 个，旧 native 后端）；屏障计数 Luau 每探针 3、Lua 5.1 每探针 4（`getinfo` 字段名本身在屏障名单）；其余 `debug/loadstring` 拼写拒绝。新增公开验证助手 `vm::custom::decrypt_embedded`。
- 矩阵适配：loadstring 检查改为只匹配调用语法、custom VM 的 blob 检查改为包装形状、Luau 二进制字面量检查收窄到 legacy（密文字符串可能偶含 `0bX` 可打印序列，语法合法性由 `luac5.1 -p` 全量保证）。fields 白名单加入 `.info/.getinfo/.what`。
- 新增 1 单元（密文熵值/解密等价/seed 相关性）+ 1 集成（环境篡改 fail-closed），完整门禁 **PASS153**（42 单元 + 111 集成）；46/49 执行覆盖、debug/release、golden 运行对照不变。golden：**26,080 / 29,569 B**（Lua51 较加密前缩小 3.3 KB：密文可打印比例高于原 `\000` 密集明文）；`.obf` 与其 SHA-256 不变。仍无压缩容器、随机 section/opcode 或 native fallback；这是混淆层，不是密码学加密。

### 2026-09-06 — 指令序列化改为 7-bit varint

- 按用户要求，`.obf` 内每条指令从定长 4-byte 改为 `[opcode][各字段 7-bit varint]`：按 ISA 表 Form 列 A/AB/ABC/ABx/Ax 决定操作数个数，小数值 1 byte、较大值 2~N bytes，每条 2~7 bytes；流程为 IR → 逻辑指令 → 7-bit Encoder → `.obf`，读取时 7-bit Decoder 恢复 VM Instruction（定长 word）后执行。
- prototype header 追加 `instruction_byte_count`（20→24 bytes，区间 `2n..7n`）；Header 宽度码改为 `0`。Rust 与目标 decoder 双侧验证 varint 最小编码（末组非零、≤4 组）、字段值域（A/B/C≤255、Bx≤65535、Ax≤0xFFFFFF）、指令数/字节数精确匹配与无尾随字节；非最简或越界编码拒绝。
- 逻辑层完全不变：`Word(opcode,A,B,C)`、49 条 ISA 编号、operand 校验、fetch-loop、全部 handler、`Jump.Ax` 指令序号语义与 IR lowering 均保持原状；`src/vm/opcode` 与 `src/ir` 零改动。
- 测试适配：Header/布局/宽跳转断言按 varint 重写；checksum 修复绕过测试补充 varint 帧检查与逐 Word 语义拒绝；新增 1 项 codec 回归（非最简 varint、寄存器/常量索引越界、悬空继续位、尾随字节、`return 7` 的 13 字节指令流）。完整门禁 **PASS151**（41 单元 + 110 集成），46/49 实际执行覆盖、debug/release、golden 对照与 ISA 修订 1/2 兼容不变。
- 两个 golden 重新生成：bytecode **6,879→5,525 / 8,257→6,575 B**（−19.7%/−20.4%），脚本 **29,356 / 28,729 B**；新 `.obf` SHA-256 见交接文档。仍无加密、压缩容器、随机 section/opcode 或 native fallback。

### 2026-09-06 — 输出整体改为 setmetatable 分函数载荷包装

- 按用户要求，最终脚本严格为 `local x={}` 与 `return setmetatable({...},x):m()` 两条 chunk 语句；全部代码以多个函数形式存放在载荷表内，不再是一个大 function：5 个随机数字键 section 函数（宿主捕获预导、bytecode decoder、操作数校验、运行时辅助、解释器簇）+ 1 个随机单字母字符串键入口函数。
- section 之间用参数/返回值传递状态（预导 `return SC,Z,...`、decoder `return P,np,entry`、辅助 `return CV,SV,Lookup`、解释器 `return H`）；互相递归的 Call/Make/H 保持同一函数作用域。`:m()` 直接命中载荷表自有键；入口 prototype 含 Varargs 指令时写 `:m(...)` 转发。方法名与数字键走独立 seeded 随机流，不影响 bytecode、最终 local 或私有字段名。
- `scope::generated_vm_plan` 下降到载荷表各函数体内定位唯一环境捕获（屏障计数/范围/引用检查不变）；`vm::tests::no_inline_metadata` 允许数字/字符串键的函数字段。旧 `--backend native` 输出与审计路径保持原状。
- 形状/差分单元测试改为断言新结构（两条语句、载荷表字段形态、键与方法名一致、varargs 条件转发、双目标原生执行一致）；完整门禁 **PASS150**（41 单元 + 109 集成），保留此前全部回归、46/49 实际执行覆盖、debug/release 与 golden 对照。
- 两个 golden 重新生成：**33,316 / 34,066 B**（seed 7001/7351，相对私有字段批次各 +484 B，方法名 `p`/`e`）；内嵌与独立 `.obf` bytecode 及 SHA-256 逐字节不变。不涉及加密、压缩、随机 section/opcode 或 vendor/参考工具改动。

### 2026-09-06 — 输出整体包装为单一 setmetatable 方法调用（已被上一条取代）

- 按用户要求，默认 AST 后端与 `wrap-bytecode` 的最终脚本改为两条 chunk 语句：`local x={__index={["m"]=function(s,k,...) ... end}}` 与 `return setmetatable({},x):m()`。decoder、校验、runtime、全部 handler 与执行尾部都在唯一入口函数内，挂在 `__index` 分发表的随机单字母字符串键上；方法调用先经元表解析出该函数，再以 `(self, 方法名, ...)` 进入 VM，`(s,k,...)` 吸收前两个参数，chunk varargs 语义不变。
- 入口 prototype 实际包含 Varargs 指令（chunk 读取 `...`）时调用写为 `:m(...)` 转发，否则为 `:m()`；方法名单字母、非关键字，来自独立 seeded 随机流，不影响 bytecode、最终 local 或私有字段名。
- `scope::generated_vm_plan` 下降到包装函数体内定位唯一环境捕获（屏障计数/范围/引用检查不变）；`vm::tests::no_inline_metadata` 允许根层 `__index` 分发表。旧 `--backend native` 输出与审计路径保持原状。
- 新增 1 项单元测试：AST 形状（两条语句、分发表键=方法名、参数/单字母断言、varargs 条件转发）+ 双目标原生执行差分；完整门禁 **PASS150**（41 单元 + 109 集成），保留原 149 项全部回归、46/49 实际执行覆盖、debug/release 与 golden 对照。
- 两个 golden 重新生成：**32,911 / 33,661 B**（seed 7001/7351，各 +79 B，方法名 `p`/`e`）；内嵌与独立 `.obf` bytecode 及 SHA-256 逐字节不变。不涉及加密、压缩、随机 section/opcode 或 vendor/参考工具改动。

### 2026-09-06 — VM 私有字段一/两字母压缩

- 在 `8efa9b6`（PASS140）基础上新增 `src/vm/fields.rs`；模板显式标记闭合 prototype schema，完整生成后用 token/span 应用双射，再执行既有 local finalizer。不做普通字段猜测、字符串替换或 runtime 映射表。
- `code/tags/parent/flags/shared/self/cached` 等 14 个字段全为随机单字母；池可扩展到双字母，排除关键字/重复/原名，超限拒绝。标准库、元方法、用户 key/导出及同名/marker-like 方法不动。
- 新增 4 单元 + 5 集成，完整门禁 **PASS149**（40 单元 + 109 集成），保留原 140 项、双目标、debug/release、两后端及 46/49 执行覆盖。
- golden 为 **32,832 / 33,582 B**（seed 7001/7351），减少 164/159 B；170/176 个私有字段 token 改名，其他所有 token、原 local 名称、字面量与内嵌/独立 bytecode 完全不变。ISA/Header/指令、IR、checksum 和 payload 均未改。

### 2026-09-06 — 自定义 VM 运行语义兼容性

- 基线 `0d2d40e` PASS124；用原生对照复现并修复 Luau `__iter` 原始查找/false、callable table/userdata 迭代器、闭包身份与死 continue 误拒绝。
- 新增有界标量分析、CFG 可达性、实际运行时 upvalue 裁剪及传递重编号；分离类型解析/安全命名与执行捕获，不执行用户代码来推断常量。固定目标的 integer/只读常量/递归共享分别回归。
- OBF v2 仍为 32-byte Header/20-byte prototype/4-byte 指令，46/49 编号不变；ISA 修订 2 增加共享 flag/self capture，旧修订 1 保留读取/运行/原样序列化，CLI 显示实际版本。
- 完整门禁 **PASS140**（36 单元 + 104 集成），新增 13 项 parity、2 项 codec、1 项独立 decoder；145 生成式组合、应用式语料、两端原生输出及 debug/release 产物对照通过。
- 新 golden：**32,996 / 33,741 B**；bytecode：**6,879 / 8,257 B**，两者均 14 prototypes，1436/1816 条指令。bytecode 仍不受 seed 影响。
- 不新增加密/随机 section/native fallback。GC、含洞表长度/无序遍历、反射、隐藏宿主与全部原生优化相关的函数身份仍有边界，不能声称任意源码等价；详见 [`虚拟机兼容性.md`](虚拟机兼容性.md)。

### 2026-09-06 — 源码分号优先分隔

- 在 `6c54026` 基础上调整共享 minifier：完整语句以 `;` 分隔，包括块末语句；保留必要空格、字面值和调用后缀，不生成 `;;`、空语句或额外 EOF 分号。
- parser 的私有、有界、升序 statement-end sidecar 覆盖所有嵌套函数、类型和插值，公开 AST/语法不变；改名后重新取边界，子解析器共享移动同一 Vec，最终仍复核绑定。
- 新增 3 项内部 + 7 项双目标原生差分/入口测试，完整门禁 **PASS124**（35 单元 + 89 集成）。保留原 114 项全部回归、debug/release、两套 VM 和 46/49 实际执行覆盖。
- 新 golden 为 **32,568 / 34,033 B**（seed 7001/7351）；除分号与间隔外所有 token 不变。内嵌和独立 bytecode 仍为 **6,879 / 8,489 B**，与修改前逐字节一致。scope fixture seed 735 为 **1,979 / 2,283 B**。
- 这是源码分隔规则，不是新加密/bytecode 压缩；因也在原可直接相连的语句间补分号，脚本大小允许小幅增加。

### 2026-09-06 — AST → IR → OBF v2 默认自定义寄存器 VM

- 核心提交 `e5447e8`：独立 typed IR、32-byte Header / 4-byte ISA、46/49 target handlers、双侧校验、真实执行及格式/边界测试；完整门禁 PASS110 后先推送。
- 接入批次：默认 virtualize 改用 AST，不调用原生 compiler；新增 dump-ir/compile/wrap-bytecode，inspect 自动识别 v2，native 为显式兼容后端。
- 最终完整门禁 **PASS114**（32 单元 + 82 集成）；新 30 项测试含真正 fetch-loop 的 **46/46、49/49**，同时保留全部先前 parser/minify/reuse/native 回归。
- 新 golden 为 **32,524 / 33,980 B**；私有 bytecode 为 **6,879 / 8,489 B**；debug/release、独立 wrap、无 compiler 及多 seed 校验均通过。
- 没有进行复杂加密、压缩、随机 section/opcode；源码及标准库/宿主边界、保守 repeat/continue 检查、精确数据格式详见新协议文档。


### 2026-09-06 — function / if / while 等作用域的安全短名复用

- 先以 `6bc94ce` 单独提交并推送解析器审计，72 项完整门禁通过后才开始本增量。
- 新增 `src/scope/rename.rs`：按 seeded 引用频率分配一/两字母，跨域可以同色，同域所有可改名绑定（含 unused/原先 shadowed）必须不同。`Scope.name_scope` 让参数与直接函数体、for 变量与直接循环体共命名域，保留原 lexical scope IDs/parent/可见性。
- 用 resolver 语义 reference ordinal 记录声明生效时点，为能截获外层引用的 active inner binding 建立稀疏边，包括闭包/传递 upvalue、写入、typeof、方法类型实参和插值。globals、types、API 和 protected 名称仍从全局候选池排除。
- 不建立同域全互斥的平方边集；同域颜色占用+迭代增广路径修复，外部颜色固定。不是全局最优求解器，安全方案找不到则拒绝；8M 工作、1M 跨域边门限，仍不扩展到三字母、不部分输出。
- 最终单行输出再次解析、核对绑定图和声明生效顺序，并独立核对同域唯一性，防止无引用的 locals 撞名漏过图比较。完整 VM 仍只有组装完毕后的一次私有 finalizer，不改变环境审计策略。
- 新增 7 项 scope-reuse 集成测试及 5 项内部约束/修复/预算/穷举验证；10,000 相邻块+累计变量共 10,001 个绑定仅用两个单字母，双目标多 seed 原生执行通过。96 种生成式遮蔽/初始化程序同样经过双目标差分；更新旧全局唯一/703 siblings 耗尽的测试假设，CLI 改用原生可编译的 global 池耗尽案例。
- 完整矩阵 **84/84 PASS**（29 单元 + 55 集成），debug/release 构建、原生语法/运行、反射退回、固定 seed 复现、blob 和 golden 检查通过。release 生成输出与 debug/golden 一致。
- 7001/7351 示例已更新：Lua 5.1 **16,212 B**（原 16,491），Luau **23,986 B**（原 24,491）；私有 blob 分别 3,520/4,694 B，与复用前逐字节相同。覆盖仍 **38/38** 和 **63/91**；scope fixture seed 735 缩至 **1,938/2,241 B**。
- 不改 vendor、参考工具、依赖、Anti、bytecode 格式；提交并推送当前会话分支 `arena/01a074dc-obf`。

以下日志保留各历史批次的行为和当时测试数量；当前策略以本条及 M3 说明为准。

### 2026-09-06 — 名称复用前的解析器审计

- 按要求先审计 parser，再开展复用。旧 61 项基线没有覆盖的语法差异已修复：contextual continue、禁止裸 literal/function/vararg suffix、跨行括号调用、Luau assertion 次数/优先级、模块 return/export、const 初始化及绑定写入、type function 外层捕获、泛型方法调用和插值 shorthand 限制。
- 裸 CR 对两个目标含义不同；调用换行检查从 callee 结束位置起判断，并保留 string/table shorthand 的合法跨行行为。
- `Call.type_arguments`、`LocalBinding.is_const`、`Reference.is_write` 纳入 AST/绑定分析及最终验证；绑定相关语法复用有界 resolver，不形成 parser 循环。
- 新增 `tests/parser_audit.rs` 11 项持久化回归（原生接受/拒绝、生成式运算符组合、lexical/seed 0/seed MAX 编译、运行及结构断言），抽取共享 `tests/support`。完整门禁 **72/72 PASS**，debug/release、双目标语法/运行、私有 blob、固定 seed golden 均通过且示例未改变。
- 明确 `check` 不负责原生编译器控制流/资源限制；repeat/continue 跨初始化案例是 CompileError，不是 SyntaxError。有限差分测试不是绝对无 bug 的证明。
- 该批次不实现短名复用；后续独立提交干涉约束和同域唯一性证明。未改变 vendor、参考工具、依赖、Anti 或 bytecode 格式。

### 2026-09-06 — 完整生成后随机一/两字母命名

- 按新要求，将原确定性“只缩短”策略改为对全部安全绑定重新随机分配一/两字母；已是短名的 local/parameter/loop/local-function 也必须换名。保持绑定图、反射保留、API/字段/方法/类型/字符串保护，不使用全文替换。
- 新增 `MinifyOptions::seeded(seed)` / `::lexical()` 和 `seed` 字段；`minify` / VM 默认 fresh seed，CLI 仅 stderr 报告，支持显式十进制/十六进制复现，拒绝无效命令和 `--seed` + `--no-rename`。
- 将已有 PRNG 移至 `src/random.rs`，保持原 VM layout 算法；命名使用独立目标域随机流。std-only 新 seed 使用进程随机状态与原子计数；不是密码学随机/加密实现。
- 全局唯一候选池最多 702 个，排除关键词/API/global/type/保留 local；允许重用将同时改写的旧拼写。处理末位自名冲突交换；池耗尽或无完整分配时安全诊断，不输出部分结果，也不覆盖已有输出文件。
- 双目标生成器先返回完整 decoder/runtime/dispatcher/全部已用 handler/尾部，再由唯一私有 `finalize_vm` 处理。仅允许固定 `local G=(getfenv and getfenv(0))or _G` 的三个全局引用；额外反射、绑定遮蔽或受保护显式 local 拒绝。用户源码没有绕过反射保护的开关。
- 最终输出重新解析并核对绑定图；VM 还验证每个显式名字均已改变且为 `[a-z]{1,2}`。不改 bytecode/string/字段/方法；隐式 Lua 5.1 `arg` 保留。
- 矩阵不再依赖旧 `B` / `P` 局部拼写：按 blob 字节/AST 检查，区分 NAMECALL 函数包装表与旧 inline prototype 数据。所有重复/异 seed VM 变体均目标编译并执行，根目录输出与固定 seed 逐字节核对。

**验收结果**

- Rust **61/61**（24 单元 + 37 集成），fmt、debug/release 构建及完整双目标矩阵 **PASS**。
- 原有 scope/reflection 差分扩展到多个 seed；VM `0/1/0x735/u64::MAX`、默认 CLI 新 seed 与可复现、650 个原短名、耗尽、末位交换、保护名称和私有环境审计回归通过。
- 7001/7351 示例已重新生成：Lua 5.1 **16,491 B**，Luau **24,491 B**；覆盖仍为 **38/38**、**63/91**，输出仍为 `vm:lua51:ok` / `vm:luau:ok`。名称全局唯一可使原单字母变成双字母，因此不承诺体积总是减少。
- scope fixture 在 seed 735 下仍为 4,131 → 3,543 → 1,978 B 与 4,194 → 3,643 → 2,271 B；两种反射模式保持一致。
- 不修改 bytecode 格式、vendor、reference tools、依赖和 Anti；每个完整测试通过的批次只提交、推送 `arena/01a074dc-obf`。

以下为更早批次的历史行为；当前默认命名策略以最新“跨域安全复用”日志及 M3 说明为准。

### 2026-09-06 — 固定环境部署与 M3 安全压缩第一阶段

- 在本会话分支 `arena/01a074dc-obf` 安装固定 Rust/Cargo/rustfmt 1.88.0；沿用仓库 Lua 5.1.5 / Luau 0.735 二进制，原始 20 测试基线矩阵通过。工具链位于被忽略的 `.toolchains/`，不提交 Git。
- 新增公开 `scope::analyze`，建立 scope、binding、reference、global 和传递 upvalue 模型；以显式工作栈替代递归遍历，保留声明及引用的原始 byte span。
- 默认 `minify` 按引用频率分配确定性短名，只改 proven local/parameter/loop/local-function 绑定。保留原有名字集合并使用全局唯一新名字，不做名字复用、表达式重排、折叠或 DCE。
- 区分 local 初始化与递归 local function；处理 shadowing、闭包写入、repeat/for、方法隐式 self、重复参数及 Lua 5.1 隐式 arg。`FunctionBody.has_vararg` 修复无标注变参信息丢失。
- Luau 类型名字/泛型/字段/方法/value export 名不变，typeof 和模块类型前缀中的值引用按绑定改写；签名遵循固定 Parser.cpp 的外层作用域规则。type function 本体和涉及的外层绑定保守保留。
- 新增 `--no-rename` / `MinifyOptions`；已知 debug、动态环境、加载入口及静态可识别的反射字段触发整 chunk 保留名称。未知外部宿主反射必须显式关闭改名；两种模式均不保证源代码行号、dump 字节或错误位置文本。
- 最终输出重新解析并核对绑定图，防止捕获或 global 变化。插值内部多行表达式/注释现在可单行输出，保留文字转义并避免 table 表达式形成非法 `{{`。
- 修正词法压缩对参考解释器允许的未知字符串转义的处理；公开 token-array emitter 先验证输入，畸形 spans 返回 Diagnostic。
- 压力测试发现旧 parser 可迭代构造十万项深 AST 并在析构时栈溢出；为运算、调用/字段/type-instantiation 与可选类型后缀链增加每层 64 次限制，超长输入安全拒绝。
- 新增 4 份 scope/reflection fixture 与 27 项 Rust 集成回归，覆盖原生运行差分、闭包/元方法/多返回、类型导入、CLI、关键字避让、嵌套插值和十万项链拒绝。

**验收结果**

- Rust 单元/集成测试 47/47，fmt、debug/release 构建及完整双目标矩阵 PASS。
- Lua 5.1 scope corpus：4,131 bytes 原始 → 3,543 bytes 词法 → **1,978 bytes 安全压缩**（比词法再减 44.2%）。
- Luau scope corpus：4,194 bytes 原始 → 3,643 bytes 词法 → **2,271 bytes 安全压缩**（比词法再减 37.7%）。
- 两套反射语料自动保留与显式 `--no-rename` 相同；VM 仍为 Lua 5.1 38/38、Luau 63/91 覆盖，生成结果保持 16,570 / 24,455 bytes。VM wrapper 的环境访问触发保守保留，未改变 dispatcher/opcode/bytecode 格式；Anti 仍为空。
- 每个通过完整门禁的修改批次只提交、推送到 `arena/01a074dc-obf`，不更新旧分支或旧 PR #2。

### 2026-09-05 — 项目交接总结

- 新增 [`项目交接总结.md`](项目交接总结.md)，集中记录项目目标、源码/AST/bytecode/VM 架构、固定环境、强制测试、不可破坏约束、常见陷阱、关键文件和当前里程碑。
- 明确 AST 完成后的推荐顺序：先做 M3 lexical scope 与安全 local 改名，再推进统一 bytecode IR、私有 reader/round-trip、分阶段数据隐藏及 handler 多模板。
- README 增加交接文档入口；后续架构、状态或优先级发生实质变化时，应同步更新该交接文档。

### 2026-09-05 — owned AST、完整 Luau 类型节点与解析安全门限

- 新增公开 `src/ast.rs` 与 `obf::parse`：chunk、block、statement、expression、function、binding、table field、operator、attribute 及 type/type-pack 结构全部拥有数据并保留半开 UTF-8 byte span；原 `check` 兼容接口不变。
- 将原来的递归语法验证器重写为 AST 构造器，分别执行 Lua 5.1 与 Luau 0.735 目标规则；补齐 type function、generic default/type pack、table read/write type、attribute、const/export、if-expression elseif 与显式 `f<<T>>` 实例化。
- Luau 插值字符串不再是 opaque token：lexer 识别嵌套 brace/string/comment/interpolation，parser 为每个内嵌表达式构造 AST 并恢复相对于完整源码的 span。
- 数字扫描与固定参考实现对齐，包括 Lua 5.1 的 number-like token 行为、Luau 任意位置分隔符和 integer suffix；同时修正 BOM、semicolon、escape、singleton type、union/intersection 混用等目标差异。
- 新增 64 MiB source、1,000,000 token、1,000,000 AST node、64 层 parser/interpolation nesting 上限；计数使用 checked arithmetic。公开 token-array 入口验证 UTF-8 span、顺序、EOF/位置并与重新词法化结果逐 token 比较。
- 新增 `ast_lua51.lua` 与 `ast_luau.lua` focused corpus；强制矩阵会对原始/单行版本分别执行内部 AST 解析、目标编译和运行输出比较。Rust 单元测试现为 20 个，包含 span 结构断言、嵌套插值、错误上下文、畸形外部 token stream 与 stack-limit 回归。

### 2026-09-05 — 输出字符压缩与 opcode 语义覆盖

- 删除生成输出中所有内部 `error` 文本，统一绑定为短 `E()`；保留边界、长度、checksum 和非法 opcode 检查本身。
- 私有 instruction 从重复的 26-byte 字段记录压缩为 6-byte 的 `u16 opcode + u32 word`，运行时按 Lua 5.1/Luau 官方布局恢复 A/B/C/Bx/sBx/D/E。
- dispatcher 仅生成当前 chunk 实际使用的 handler，未使用指令不再进入输出。
- 扩展 Lua 5.1 fixture，使固定 `luac5.1` 实际产生并执行全部 38/38 opcode。
- 扩展 Luau fixture，覆盖 register/K 算术、逻辑选择、比较跳转、动态 table、SETLIST、generic/numeric loops、闭包和 FASTCALL fallback；当前实际覆盖 63/91 opcode，其余主要为优化提示、运行时伪指令和实验 class/userdata 路径。
- 完整矩阵通过：Lua 5.1 VM 为 16,570 bytes，Luau VM 为 24,455 bytes；此前分别为 40,907 与 56,117 bytes。

### 2026-09-05 — 私有字节码实体化与 opcode 目录重构

**按用户纠正重做**

- 将验证通过的 `vm_lua51.out.lua` 与 `vm_luau.out.lua` 作为生成物提交到 GitHub，方便直接查看实际混淆输出。
- 删除把 prototype 与 instruction 直接写成 Lua table literal 的旧输出方式。
- 新增 `src/vm/binary.rs`，将两目标 VM 数据序列化为真正连续的私有 bytecode container；header 包含 `OBF` magic、格式版本、目标标记、payload 长度和 Adler-32 checksum。
- 输出仅嵌入 binary byte string；目标端通过有边界检查的 `u8/u16/u32/i32/bytes/number` decoder 恢复常量、prototype、child、capture 及 instruction stream，解析完成后清理原 blob。
- 删除 `src/vm/handlers/{lua51,luau}`。全部 129 条指令现在平铺在唯一的 `src/vm/opcode/`：每条指令一个 Rust 文件，并由固定 `code() -> &'static str` 返回解释器代码。
- 测试矩阵新增真实 blob、禁止旧内联 instruction table，以及 opcode 文件数量检查。

**验证状态**

- Rust 1.88.0 单元测试增至 16 个；私有容器 header/长度/checksum writer 测试通过。
- 二进制私有字节码版本的 Lua 5.1 与 Luau coverage fixture 均已通过目标编译和运行，输出仍分别为 `vm:lua51:ok` 与 `vm:luau:ok`。

### 2026-09-05 — Lua 5.1 / Luau 私有寄存器 VM 第一阶段

**新增**

- 新增 `virtualize --target <lua51|luau> [--seed N]` CLI 与公开 Rust API；seed 支持十进制和十六进制。
- 通过仓库固定的 `luac5.1` / `luau-compile` 编译源码，严格解析原生 chunk 后降低到独立私有 opcode、常量、prototype、闭包/capture 与 instruction table。
- Lua 5.1 完成 38 条 opcode handler；Luau 0.735 完成固定 91 条 opcode handler及 AUX word 保留，FASTCALL/FASTPCALL 优化提示回退到其普通 fallback 指令路径。
- 129 个 handler 分别拆入 `src/vm/handlers/lua51/` 与 `src/vm/handlers/luau/`，由编译期 `include_str!` 注册，Anti 目录保持空白。
- 相同 seed 可复现；不同 seed 会改变私有 opcode、dispatcher 顺序、四种等价 opcode 比较形式及目标安全的数字字面量写法。
- 新增双目标 VM 覆盖 fixture，并把原生执行、三 seed 生成、单行检查、禁止 `loadstring`、目标编译检查和 VM 输出对比加入强制矩阵。
- 修正 Lua 5.1 字节码检查器对 `SETLIST` trailing data word 的识别；修正 Luau `type`/`export` 上下文关键字处理，使内建 `type(...)` 可正常解析。

**测试结果**

- Rust/Cargo 1.88.0：`cargo test --all-targets` 15 个单元测试全部通过，debug/release 构建通过。
- Lua 5.1.5 VM coverage fixture：原生与 VM 都输出 `vm:lua51:ok`；VM 输出通过 `luac5.1 -p`。
- Luau 0.735 VM coverage fixture：原生与 VM 都输出 `vm:luau:ok`；VM 输出通过 `luau-compile`。
- 同 seed 输出由 `cmp` 验证完全一致，异 seed 输出确实不同；两个 VM 输出均为单物理行且不包含 `loadstring`。
- 基础 fixture 继续输出 Lua 5.1 `OBF|matrix:13|` 与 Luau `OBF:luau:165|`。

**下一步**

1. 为每个 handler 增加独立语义向量与多个等价模板，扩大错误及元方法求值顺序差分测试。
2. 把内联私有 table 升级为有版本、长度和完整性字段的字节级私有格式。
3. 实现分阶段数据恢复、拆分隐藏密钥与执行后临时槽清理。
4. 完成 AST/scope/upvalue 模型以及仅针对可证明安全 local 的短名改写。
5. 增加 Roblox executor 实机矩阵和可选 bit32 存储后端；Anti 仍等待用户提供具体实现。

### 2026-09-05 — 初始化实现

**新增**

- 创建 Rust std-only 工程和 `obf` CLI。
- 新增 `check`、`minify`、`inspect-bytecode` 三个命令。
- 新增 Lua 5.1/Luau 词法器和递归下降语法检查器。
- 新增保语义单行词法压缩器。
- 新增 Lua 5.1 与 Luau 字节码安全解析器。
- 固定并编译 Lua 5.1.5 与 Luau 0.735 环境。
- 新增自定义 Luau 入口、工具链构建脚本、Rust bootstrap 和双环境矩阵。
- 按要求保留空的 `src/anti/`。

**测试结果**

- Rust 1.88.0：13 个单元测试全部通过。
- Lua 5.1.5：原始/单行输出均通过 luac，运行输出一致。
- Luau 0.735：原始/单行输出均通过编译器，运行输出一致。
- Lua 5.1 fixture：592 B → 372 B。
- Luau fixture：414 B → 287 B。
- Lua 5.1 字节码：3 prototypes、58 instruction words、17 constants。
- Luau 字节码 v14/type v3：2 prototypes、40 instruction words、18 constants。
- 两种截断字节码均被拒绝。
- 自定义 Luau runner 的 `loadstring`、`require` 与只读 sandbox 探针通过。

**下一步**

1. 把 parser 从语法检查升级为完整 AST。
2. 完成 scope/upvalue 分析。
3. 开始安全局部变量改名和完整差分语料。
4. 解码并验证 Lua 5.1/Luau 的每条字节码指令。


## 2026-09-06：AST → IR → OBF v2 核心批次

新增独立 typed register IR、32-byte Header / 4-byte 指令的 OBF v2 codec 与可执行双目标 VM。新路径不调用外部 compiler；49 个稳定 opcode 中 Lua51 支持 46 个、Luau 支持全部 49 个，fetch-loop 实际执行覆盖已建立。旧原生路径暂留 CLI 默认，下一批接入默认命令和示例。新增目录只涉及 `src/ir/` 与现有 opcode 下两个目标目录，不移动旧实现。

完整格式、资源门限、源码/反射/受保护 `__iter` 等边界和 API 见 [`自定义字节码.md`](自定义字节码.md)。按最新要求，暂不做复杂加密、压缩、随机 section 或随机 opcode；此前路线中的相关随机化项只描述旧 backend，不能当作新 v2 的实现要求。

核心批次最终门禁：`./tools/test-matrix.sh` **PASS 110**（32 单元 + 78 集成）；其中新增 26 项 AST/IR/v2 VM 回归，含两端逐 opcode 实际执行覆盖。debug/release 与原有门禁均通过。
