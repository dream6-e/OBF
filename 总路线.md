# OBF 总路线

> 最后更新：2026-09-05（Asia/Shanghai）
>
> 当前里程碑：M5 双目标私有寄存器 VM 第一阶段（可执行）
>
> 支持范围：仅 Lua 5.1.5 与 Luau 0.735（Roblox 方向）

## 1. 不可破坏的工程约束

1. 混淆器使用 Rust 编写，只依赖 `std`，`Cargo.toml` 不允许加入第三方 crate。
2. Rust 最低兼容版本为 1.88.0；代码不得使用 1.88 之后才稳定的功能，并保持可由 rustc 1.96.0 构建。
3. 每次代码变更必须运行 `tools/test-matrix.sh`，同时通过 Lua 5.1.5 和 Luau 0.735 的源码检查、编译检查、运行结果对比及字节码检查。
4. 所有随机变换最终必须接受显式 seed；相同输入、配置和 seed 必须可复现。
5. 输出必须是单物理行。Lua 5.1 目标不得产生二进制字面量、下划线数字、类型标注或其他 Luau 独有语法。
6. 默认保守处理全局名称、字段名称、环境/API 名称和动态访问；没有证明可安全改名时绝不改名。
7. 所有输入解析必须有边界检查、计数上限、溢出检查和可定位的错误，不允许因畸形源码/字节码 panic。
8. `src/anti/` 按用户要求暂时保持空白；在收到具体实现前，不加入任何反调试或 antidump 逻辑。

## 2. 固定验证环境

| 组件 | 固定版本 | 仓库位置 | 状态 |
|---|---:|---|---|
| Lua 解释器 | 5.1.5 | `toolchains/bin/lua5.1` | 已编译并纳入仓库 |
| luac | 5.1.5 | `toolchains/bin/luac5.1` | 已编译并纳入仓库 |
| Lua 源码 | 5.1.5 | `vendor/lua-5.1.5` | 已纳入仓库 |
| Luau 自定义解释器 | 0.735 | `toolchains/bin/luau` | 已编译并纳入仓库 |
| Luau 编译器 | 0.735 | `toolchains/bin/luau-compile` | 已编译并纳入仓库 |
| Luau 源码 | 0.735 | `vendor/luau-0.735` | 已纳入仓库（仅保留构建所需目录） |
| Rust/Cargo/rustfmt | 1.88.0 | `.toolchains/rust-1.88.0` | 由固定 `@rustbin` 包可复现安装 |
| gcc/g++ | Debian 12 / 12.2 | 系统 | 已验证 |

Luau 运行器入口为 `tools/luau_runner_main.cpp`。实际 CLI 环境由固定版本的 `Repl.cpp` 设置，包含 `loadstring`、文件 `require`、标准库和 `luaL_sandbox`；测试矩阵会实际调用 `loadstring` 与 `require`。

Rust 预编译工具链体积约 550 MiB，不直接提交进 Git；`tools/bootstrap-rust.sh` 会把版本固定的 npm 包安装到仓库内 `.toolchains/`。项目本身仍保持 std-only。

## 3. 里程碑

### M0：可复现工具链与测试门禁（已完成）

- [x] 仓库内固定 Lua 5.1.5 源码、解释器和 luac。
- [x] 仓库内固定 Luau 0.735 必需源码、解释器和编译器。
- [x] 自写 Luau `main`，复用固定 CLI 环境并增加稳定版本探针。
- [x] 添加 Rust 1.88.0 `@rustbin` 可复现安装脚本。
- [x] 添加 Lua 5.1/Luau 双目标测试矩阵。
- [x] 对原始/处理后脚本进行输出逐字节比较。
- [x] 对两个目标生成真实字节码，并交给内部解析器检查。
- [x] 对截断字节码执行拒绝测试。

### M1：源码前端与只改变词法的单行压缩（进行中）

- [x] 带行列位置的 Lua 5.1/Luau 词法器。
- [x] 短字符串、长字符串、长注释、转义、数字和目标专属符号处理。
- [x] Lua 5.1 递归下降语法检查器。
- [x] 常用 Luau 类型标注、泛型、if expression、复合赋值和二进制数字解析。
- [x] 删除注释和冗余空白，避免 token 合并改变含义。
- [x] 将多行长字符串转写为单行安全转义字符串。
- [ ] 将语法检查器升级为保留完整语义节点和 span 的 AST。
- [ ] 在内部解析 Luau 插值字符串的表达式；当前整体识别后由官方 Luau 编译器二次验证。
- [ ] 覆盖 Luau 完整类型语法、type pack、泛型默认值及全部上下文关键字。
- [ ] 增加差分语法语料与畸形输入回归集。

### M2：字节码前端（进行中）

- [x] Lua 5.1 二进制 chunk 头解析，包括大小、端序和格式验证。
- [x] Lua 5.1 prototype、常量、子函数和调试表递归解析。
- [x] 验证 Lua 5.1 全部 38 个 opcode 编号。
- [x] Luau 版本 3–14 及实验 classes 版本的容器解析。
- [x] Luau 字符串表、userdata 类型映射、prototype、常量、调试信息、反馈槽和 cost 字段解析。
- [x] 全部读取执行边界、整数溢出和计数上限检查。
- [x] VM lowering 已能解码 Lua 5.1 全部 38 条指令，并识别 `SETLIST C==0` trailing data word。
- [x] VM lowering 已能解码 Luau v14 的固定 91 条 opcode、静态 AUX word、常量描述、prototype 与 capture。
- [ ] 将两套目标专属结构进一步收敛为统一 IR，并完整验证寄存器、常量索引和跳转目标。
- [ ] 增加字节翻转、截断、超大 varint 和递归深度测试。

### M3：作用域分析与安全压缩（未开始）

- [ ] 建立 lexical scope、local、parameter、upvalue 和 shadowing 模型。
- [ ] 仅对可证明安全的局部变量改名为 `a,b,c,...`，按出现频率优先分配短名。
- [ ] 不改全局 API、表字段、方法名、动态 `_G`/`_ENV`/`getfenv` 名称。
- [ ] 分别维护 Lua 5.1/Luau 保留字和 Roblox API 保留集合。
- [ ] 常量折叠与无副作用死代码删除必须经过语义测试。
- [ ] 数字随机表示：Lua 5.1 仅十进制/十六进制；Luau 可增加二进制和下划线格式。

### M4：私有字节码（第一阶段进行中）

- [x] 原生 Lua/Luau opcode 已降低到按 seed 唯一分配的私有数值编号；输出不嵌入原生 chunk。
- [x] 当前私有结构保留独立常量描述、prototype、闭包/capture 与 data/AUX word。
- [ ] 把当前内联 table 结构升级为带显式版本的二进制私有格式。
- [ ] 编译 AST/统一 IR 到私有 opcode、常量池、prototype 和闭包描述，减少对外部目标编译器的依赖。
- [ ] 序列化器与解析器双向 round-trip 测试。
- [ ] 每段带长度、版本、上限和完整性字段；拒绝截断、重叠和越界引用。
- [ ] 在不暴露完整明文的前提下分阶段恢复执行所需片段。

### M5：虚拟机混淆（核心，第一阶段已可执行）

- [x] Lua 5.1 的 38 个指令处理器拆分为 `src/vm/handlers/lua51/` 独立模板文件。
- [x] Luau 0.735 的 91 个指令处理器拆分为 `src/vm/handlers/luau/` 独立模板文件。
- [ ] 每个 handler 至少提供多个等价模板，模板需单独做语义测试；当前先完成每 opcode 一份固定语义模板。
- [x] 使用 seed 随机分配私有 opcode，并随机重排 dispatch 顺序。
- [x] dispatcher 比较在 `o==id`、`id==o`、`not(o~=id)` 与 `o-id==0` 间按 seed 变化。
- [x] opcode 数字表示按目标随机化：Lua 5.1 仅十进制/十六进制，Luau 可增加二进制及分隔符。
- [x] 主循环采用 `while true do`，输出不调用 `load`/`loadstring`，而是直接解释私有 register instruction。
- [x] 真实分支模板保持运行时求值顺序，不提前计算可能触发元方法或错误的操作数。
- [x] 双目标 fixture 已验证闭包/upvalue、vararg、尾调用/调用、多返回值、循环、table、元表/方法和异常捕获路径。
- [ ] 补充每条 handler 的独立语义向量、Roblox executor 实机差分和更复杂的错误/元方法顺序语料。

### M6：数据隐藏与分阶段加载（未开始）

- [ ] 字节级数据编码与每次构建派生密钥。
- [ ] 密钥拆分存储，不生成明显的单一 `decrypt(blob)` 固定结构。
- [ ] 私有字节码恢复分为至少三个有边界的阶段。
- [ ] 明文函数/API 名通过局部片段组合或临时存储取得。
- [ ] 临时表槽位复用后清理，保证不会破坏 GC 和闭包捕获语义。
- [ ] Roblox 专用 bit32 数据表示作为可选后端，不进入 Lua 5.1 输出。

### M7：高级结构随机化（低优先级）

- [x] dispatcher opcode 比较已有四种等价写法并由 seed 选择。
- [ ] 等价真假分支和 handler 内部分支继续扩展。
- [ ] 安全条件下的元方法 dispatch 变体。
- [ ] 可选最外层 table/function 包装变体。
- [ ] 性能基准和代码体积预算，避免复杂操作造成不可接受退化。

### Anti：按要求暂缓

- [ ] `src/anti/` 当前只有空 `.gitkeep`。
- [ ] 等用户给出具体实现后，再设计目标能力检测、随机组合和失败策略。
- [ ] Anti 功能必须可配置、按目标隔离，并通过双环境测试；不得假设标准 Lua 5.1 一定存在 Roblox executor API。

## 4. 每次更新的固定验收顺序

```bash
# 首次环境准备
./tools/bootstrap-rust.sh
./tools/build-reference-tools.sh

# 每次代码修改后
./tools/test-matrix.sh
```

矩阵必须完成：

1. `cargo test --all-targets`、debug build 和 release build；
2. 内部解析器检查 Lua 5.1 fixture；
3. `luac5.1 -p` 检查原始与输出源码；
4. Lua 5.1 执行输出逐字节对比；
5. Lua 5.1 字节码生成、内部解析和截断拒绝；
6. 内部解析器检查 Luau fixture；
7. `luau-compile` 检查原始与输出源码；
8. Luau 执行输出逐字节对比；
9. Luau 字节码生成、内部解析和截断拒绝；
10. 自定义 Luau runner 的 `loadstring`/`require` 环境探针；
11. Lua 5.1 VM 同 seed 逐字节可复现、异 seed 输出不同，且通过 `luac5.1 -p`；
12. Lua 5.1 VM 与原 fixture 运行输出逐字节一致；
13. Luau VM 同 seed 逐字节可复现、异 seed 输出不同，且通过 `luau-compile`；
14. Luau VM 与原 fixture 运行输出逐字节一致；
15. 确认所有压缩及 VM 输出不存在 CR/LF，且 VM 不委托 `loadstring`。

任何一步失败都不能把对应里程碑标记为完成。

## 5. 更新日志

### 2026-09-05 — Lua 5.1 / Luau 私有寄存器 VM 第一阶段

**新增**

- 新增 `virtualize --target <lua51|luau> [--seed N]` CLI 与公开 Rust API；seed 支持十进制和十六进制。
- 通过仓库固定的 `luac5.1` / `luau-compile` 编译源码，严格解析原生 chunk 后降低到独立私有 opcode、常量、prototype、闭包/capture 与 instruction table。
- Lua 5.1 完成 38 条 opcode handler；Luau 0.735 完成固定 91 条 opcode handler及 AUX word 保留，FASTCALL/FASTPCALL 优化提示回退到其普通 fallback 指令路径。
- 129 个 handler 分别拆入 `src/vm/handlers/lua51/` 与 `src/vm/handlers/luau/`，由编译期 `include_str!` 注册，Anti 目录保持空白。
- 相同 seed 可复现；不同 seed 会改变私有 opcode、dispatcher 顺序、四种等价 opcode 比较形式及目标安全的数字字面量写法。
- 新增双目标 VM 覆盖 fixture，并把原生执行、三 seed 生成、单行检查、禁止 `loadstring`、目标编译检查和 VM 输出对比加入强制矩阵。
- 修正 Lua 5.1 字节码检查器对 `SETLIST` trailing data word 的识别；修正 Luau `type`/`export` 上下文关键字处理，使内建 `type(...)` 可正常解析。

**测试结果**

- Rust/Cargo 1.88.0：`cargo test --all-targets` 15 个单元测试全部通过，debug/release 构建通过。
- Lua 5.1.5 VM coverage fixture：原生与 VM 都输出 `vm:lua51:ok`；VM 输出通过 `luac5.1 -p`。
- Luau 0.735 VM coverage fixture：原生与 VM 都输出 `vm:luau:ok`；VM 输出通过 `luau-compile`。
- 同 seed 输出由 `cmp` 验证完全一致，异 seed 输出确实不同；两个 VM 输出均为单物理行且不包含 `loadstring`。
- 基础 fixture 继续输出 Lua 5.1 `OBF|matrix:13|` 与 Luau `OBF:luau:165|`。

**下一步**

1. 为每个 handler 增加独立语义向量与多个等价模板，扩大错误及元方法求值顺序差分测试。
2. 把内联私有 table 升级为有版本、长度和完整性字段的字节级私有格式。
3. 实现分阶段数据恢复、拆分隐藏密钥与执行后临时槽清理。
4. 完成 AST/scope/upvalue 模型以及仅针对可证明安全 local 的短名改写。
5. 增加 Roblox executor 实机矩阵和可选 bit32 存储后端；Anti 仍等待用户提供具体实现。

### 2026-09-05 — 初始化实现

**新增**

- 创建 Rust std-only 工程和 `obf` CLI。
- 新增 `check`、`minify`、`inspect-bytecode` 三个命令。
- 新增 Lua 5.1/Luau 词法器和递归下降语法检查器。
- 新增保语义单行词法压缩器。
- 新增 Lua 5.1 与 Luau 字节码安全解析器。
- 固定并编译 Lua 5.1.5 与 Luau 0.735 环境。
- 新增自定义 Luau 入口、工具链构建脚本、Rust bootstrap 和双环境矩阵。
- 按要求保留空的 `src/anti/`。

**测试结果**

- Rust 1.88.0：13 个单元测试全部通过。
- Lua 5.1.5：原始/单行输出均通过 luac，运行输出一致。
- Luau 0.735：原始/单行输出均通过编译器，运行输出一致。
- Lua 5.1 fixture：592 B → 372 B。
- Luau fixture：414 B → 287 B。
- Lua 5.1 字节码：3 prototypes、58 instruction words、17 constants。
- Luau 字节码 v14/type v3：2 prototypes、40 instruction words、18 constants。
- 两种截断字节码均被拒绝。
- 自定义 Luau runner 的 `loadstring`、`require` 与只读 sandbox 探针通过。

**下一步**

1. 把 parser 从语法检查升级为完整 AST。
2. 完成 scope/upvalue 分析。
3. 开始安全局部变量改名和完整差分语料。
4. 解码并验证 Lua 5.1/Luau 的每条字节码指令。
