# OBF `out.lua` 破解报告

> 目标:`/home/user/OBF/out.lua`(83,640 字节,Lua 5.1 自定义虚拟机 + 嵌入式字节码程序)
> 结果:完整语义重建 → `decoded_source.lua`,运行行为与原程序**逐字节一致**
> 日期:2026-09-08

---

## 1. 摘要(TL;DR)

`out.lua` 是一个**自解释的 Lua 5.1 虚拟机**:一个 83KB 的单行文件,内含

1. 一个明文写成的 VM 解释器(278 个状态体 / 约 50 种 opcode),
2. 一段 base-86 编码的嵌入式"字节码"程序(483 条记录、278 个状态),
3. CRC 完整性校验、常量混洗器、反篡改检查。

**嵌入程序的真实语义**:这是一套**虚拟机自检测试程序**——依次验证闭包/上值、蹦床调用、
算术与比较、字符串拼接、表构造/拷贝/长度、`ipairs` 迭代、元表 `__add`/`__tostring`、
全局环境读写,每项断言通过,最后打印 `vm:lua51:ok` 并退出 0。它还临时写了一个
全局探针 `_G.__obf_vm_probe = 41` 再清掉。

破解方法:**静态解剖结构 + 动态插桩取真值 + 逐记录对齐 + 语义重建 + 运行验证**。
全程以"真实解释器"为 ground truth,不做任何猜测。

---

## 2. 样本概览

| 项 | 值 |
|---|---|
| 文件大小 | 83,640 字节,**1 行**(0 个换行)|
| 顶层形状 | `local j={}; return setmetatable({ [7244]=fn, [8948]=fn, [5571]=fn, [9025]=fn, [4861]=fn, [1832]=fn, [400]=fn, … }, j):p()` |
| 结构 | 一张以**数字为键**的函数表,最后调用方法 `p()`(编排入口)|
| 载荷 | 两段 base-86 大字符串(≈4.6KB + ≈2.4KB),5 字符 → 4 字节,解码后 `loadstring` |
| 运行输出 | 恰好 `vm:lua51:ok\n`,exit 0 |
| 全局副作用 | 恰好一次临时写入 `__obf_vm_probe = 41`,运行结束前清为 `nil` |

---

## 3. 明显特征(如何一眼判断这是 VM 混淆)

以下特征全部来自对原始文件的直接观察,是判断"这是虚拟机混淆、且值得用动态手段"的依据:

### 3.1 单行 + 数字键函数表

```lua
local j={};return setmetatable({[7244]=function(e,j,s,l,t,y,f,b,o) … end,
  [8948]=function(r,j) … end, [5571]=function(q,r,u,s) … end,
  [9025]=function() … end, [4861]=function(q,r,u,s) … end,
  [1832]=function(q,r,l,c,k) … end, [400]=function(j,r,q,s,l,c,k) … end,
  … },j):p()
```

- 函数不以名字而以**随机数字**为键 → 典型混淆器手法(防字符串搜索、防重命名追踪)。
- 结尾 `:p()` 说明真正入口是表里的方法 `p`,其余全是"库"。
- 单行 83KB:放弃可读性,但**没放弃明文**——这是最大的特征:它不是二进制,
  每个"状态"都是可读的 Lua 表达式。

### 3.2 两大段 base-86 字符串

文件里有两段巨型字符串,字符集为可打印 ASCII 35..121(不含 92 反斜杠),共 86 个字符:

```
9v&J>]&/9VA2NlaRL9K]77PoUEVjKmn0`,wYuDVU8En_Cqnf^g?YIm.=fexAQW^hK=52;a1LEq…
&sl<rFu+ErC@+R:3B.P)g8XW^Whp:DkI&#q=8nt^JD_*qYxc<AOfW7_0pk-Q@ZqXgWfd-(e?…
```

解码器是同一个 `while true do if x[21]==778 then …` 状态机:每次读 **5 个字符**,
按 `value = Σ char·86^i`(≤ 2^32−1)还原 **4 字节**,最后 `s(bytes)` 即
`loadstring` —— 也就是说,**核心代码本身就是运行时解码+编译出来的**。
base-86 只是字符替换,不是加密:字母表就在文件里(见 3.3),**无密钥**。

### 3.3 环境名解码器([9025] = "罗塞塔石碑")

```lua
local x={"d","y","l","s","x","t","k","i","m","w","c","e","g","n","q","b","f",
        "h","o","a","u","r","p"}
local k = q(x,{4,12,3,12,11,6})  -- → "select"
-- 同理解码: error unpack string byte sub format table concat char math floor
--           tonumber type tostring next getmetatable setmetatable rawget
--           rawequal debug loadstring getinfo
```

23 个字母 + 索引数组,立刻暴露 VM 用到的**全部标准库函数**——其中赫然有
`debug`、`loadstring`、`getinfo`:说明程序做动态编译与运行时自省
(这对后面"直接 hook 解释器帧"是决定性提示)。

### 3.4 混淆控制流

所有算术函数都是同一种模式:

```lua
local r=338; while true do
  if not(r~=338) then … r=295 …
  elseif 460==r then return x;
  elseif r==905 then …
  else l(); end
end
```

- 数值状态 + `while true` 代替 if/else 树;`repeat … until false; break` 代替分支;
- 填充条件:`(x-j)~=(x-j)`(NaN 哨兵,恒 false)、`x<=x and j<=j`(恒 true);
- 运算本身是 `x=(x*31+j+q+u+(206*256+185))%65536` 这类 **mod 65536 的 LCG 哈希**。

目的:让 CRC/混洗函数看起来像"不可读的数学",实际是**线性可逆的模运算**。

### 3.5 反篡改检查

```lua
local x = s and l(c,"S")      -- getinfo(c, "S")
if not (x and x.what=="C") then q() end    -- 必须是 C 函数,否则 error
```

两处([4861]、[400]),外加 `65539*x%2147483647` ×13 之类的 LCG 常量指纹。
这类检查只挡"改一个字节就跑"的粗篡改,**对理解程序毫无帮助**——
它们是可计算的,而且我们根本不需要通过它:直接在 Lua 里运行原文件即可。

### 3.6 记录格式"自带说明书"

[7244] 函数是**记录解析器**,它把整个字节码格式写成了代码(见 4.3),
包括 CRC 派生、操作数读取、跳转目标合法性校验、流必须恰好读完等。
**解析器本身就是格式规格**——读解析器比读数据容易得多,这是本次破解的关键切入点。

---

## 4. 切入路径(我实际从哪里切入)

按实际执行顺序:

1. **确认它是明文 Lua** → 不需要逆向二进制,`debug` 库、`loadstring`、
   `getfenv` 全部可用 → 决定采用"**动态插桩为主、静态解剖为辅**"的路线。
2. **先跑起来** → 拿到 ground truth 行为基准:`vm:lua51:ok`,exit 0。
   验收标准直接由程序自己打印出来(这是自检测试程序的特征)。
3. **解剖顶层结构** → 识别各数字键函数的角色(7244=记录解析器、8948=字母表、
   5571=PRNG/密钥派生、9025=环境名、4861/400=反篡改、1832=二进制流读取器、
   400 里还嵌第二段 base-86 载荷)。
4. **读 [9025]** → 得到 VM 的标准库依赖表 → 确认它自省能力强 →
   推断"可以直接 hook 它的调用栈"。
5. **读 [7244] 解析器** → 得到完整的记录格式(见 5.4)→ 静态还原
   483 条记录的结构(操作码表 + 4 个 u16 + 每 op 3 个 varint)。
6. **解码两段 base-86 载荷** → 得到 VM 解释器核心(278 个状态体)→
   每个 opcode 的语义从此可读(85=move、79=push、135=call、169=闭包工厂、
   201/66/246/213/5/227 = `+ - * / // % ^`、150=concat、62=jump、245=return …)。
7. **在解释器分发循环上打 hook**(debug.getlocal,函数帧 level 3,
   局部变量 15..20 = `x q r u c f`)→ 记录**每一次真实分发**的 opcode 与操作数
   → 得到 599 条执行记录 / 415 条不同记录的 ground-truth 操作日志。
8. **逐记录对齐** → 静态操作数(flat_ops2)+ 动态状态链(oplog_flat)+
   运行时值快照(vals2)三方合并 → 得到每条记录"输入什么值、执行哪个 op、
   输出什么值"的完整表格。
9. **常量混洗器对齐** → 发现同一原始常量在不同记录里解析成不同值
   (如 q=7 在一条记录里是 4,在另一条里是 `"abcd"`)→ 定位到源码里的
   `(s-(q*61+r*23+11)%64)%64` 混洗公式 → 全部 81 个常量落位。
10. **语义重建 + 运行验证**(见第 7 节)。

**最关键的单点切入**是第 6 步:**VM 解释器是明文 Lua**,278 个状态体的源码
直接可读 → 每个 opcode 的语义是"读出来的"而不是"猜出来的"。

---

## 5. 破解过程(八个阶段)

### 5.1 环境搭建

沙箱无法访问 lua.org,改走 GitHub API tarball 下载官方 Lua 5.1.1
(`https://api.github.com/repos/lua/lua/tarball/refs/tags/v5.1.1`),
编译出 `/tmp/lua-lua-98194db/lua`,先确认原程序可运行、输出基准正确。

### 5.2 静态解剖

把单行文件按 `,[数字]=function` 切分,识别出各函数角色(见第 3 节)。
确认顶层是 `setmetatable(数字键函数表, {}):p()` 的编排模式,
入口 `p()` 负责:解码字母表 → 反篡改检查 → 解码两段 base-86 载荷
(`loadstring`)→ 构造环境表 → 解析记录 → 启动分发。

### 5.3 环境解析器(罗塞塔石碑)

[9025] 的 23 字母表 + 索引数组,还原出 VM 使用的全部标准库符号
(第 3.3 节列表)。这一步的价值:
(a) 确认 VM 的"系统调用面";(b) 发现 `debug`/`loadstring`/`getinfo`
在环境中可用 → 动态插桩可行。

### 5.4 记录格式逆向([7244] 解析器即规格)

从解析器代码直接读出格式:

```
proto := { z = <packed bytes>, l = <record count> }
每个记录头 := 4 × u16 (c, r, b, o)
    h = U(c, r, seed, 0)          -- mod-65536 LCG 哈希
    m = U(b, r, seed, 1)
    w = W(o, r, h, m, seed)       -- 第二个 LCG
    选择 4 张 op 表之一 k[w]
每条 op := 3 个 varint 操作数 (a, b, c)
    lo   = b + c*256
    addr = a + lo*256             -- 24-bit 跳转/目标地址
结构校验:
    · 记录索引必须唯一、op 表必须存在
    · op ∈ 1..48,且 44..47 只允许出现在 op 列表末位
    · op=62(jump):s=q=0 且目标记录必须已定义
    · op=245(return)/55(trampoline):s=q=0
    · op=83(条件跳转):两个目标都必须存在
    · 流必须恰好读完,否则 error
分发:  Y(op_body, a, b, c, addr, lo, rec_idx, proto, env, seed)
```

静态还原出 **483 条记录**,其中 415 条在实际执行路径上(总执行 599 次)。

### 5.5 常量混洗器

proto 头部有一段"常量字母表"解析:

```lua
local x = (s - (q*61 + r*23 + 11) % 64) % 64
if x > 48 or b[x]==nil … then l() end
```

即每个原始常量按 **(记录号, 位置)** 做 mod 64 线性混洗。
这解释了"同一个原始值在不同记录里含义不同"的现象。
用公式 + 运行时值快照双向验证,81 个常量全部落位,例如:

| 索引 | 值 | 用途 |
|---|---|---|
| 0 | 10 | box1 断言值 |
| 1 | `"assert"` | 断言 |
| 3/4/5 | 12 / −5 / 7 | 算术测试 |
| 13/14 | 2.6667 / 512 | 数学常量套件 |
| 20..24 | `"abcd" "a" "b" "ab3"` | 字符串测试 |
| 31 | 36 | ipairs 累加终值 |
| 72/73 | `"__add" "__tostring"` | 元表 |
| 76 | `"box:10"` | 元表测试期望值 |
| 77/78 | `"_G" "__obf_vm_probe"` | 全局探针 |
| 80 | `"vm:lua51:ok"` | 最终输出 |

### 5.6 动态插桩(ground truth)

在解释器分发循环(函数帧 level 3)上用 `debug.getlocal` 读取
局部变量 `x, q, r, u, c, f`,对每次 `f==780`(状态体)事件记录
opcode 与操作数 → `/tmp/vm_oplog.txt`。
599 次分发、415 条不同记录,得到每条记录的**真实执行顺序**与
**每条记录内多个 op 的状态跳转链**(每个记录最多 4 个 op)。

### 5.7 值快照 + 逐记录对齐

在分发点同时抓取局部值表 `l`(VM 的"寄存器文件")→
每条记录"执行前/执行后"的值快照(1709 行)。
三方合并:静态操作数 + 动态状态链 + 运行时值 →
**每条记录:哪个 op、操作数是什么、读写了哪些槽、值从什么变成什么**。
这是语义重建的地基,消除了所有猜测。

### 5.8 语义重建

按执行顺序把 599 条记录归约为 10 个语义段,写成可读 Lua
(`decoded_source.lua`),并保留每段与原始记录 ID 的对应关系:

1. **闭包/单元格/蹦床自检**:`trampoline(double,9)==18`、捕获单元格、
   `counter(2)==12`、`pack(1,2,3).n==3`
2. **常量与运算**:`12-5==7`、`1<2`、`1<=2`、`-16==-(16)`、`not false`…
3. **字符串**:`#"abcd"==4`、`'a'..'b'..3=='ab3'`
4. **表**:`{1..50}` 构造 + k40 拷贝 + 追加 51..55 → `#t==55`
5. **循环与累加器**:1..5 求和=15 → `tonumber` 三重守卫块 `5-(-2)==7` →
   +3×3=24 → `{2,4,6}` 打包/拷贝 → `ipairs` 累加=36(最终 assert)
6. **Box 元表**:`box.add(box,6)==10`;`mt={__add,__tostring}` 下
   `tostring(b1+b2)=='box:10'`(注意:`__add` 返回**新 box** 而非数字)
7. **全局探针**:临时写 `_G.__obf_vm_probe=41` → 回读校验 → 清为 `nil`
8. **结尾**:`print('vm:lua51:ok')` → return

---

## 6. 弱点分析(为什么这个混淆挡不住分析)

按影响大小排序:

1. **解释器是明文 Lua,不是原生 VM。**
   这是致命的:每个状态体、每个 opcode 语义、每次分发的操作数都是
   Lua 运行时里可读的对象。换成 C 写 VM 就得走二进制逆向,难度完全不同。

2. **`debug` 库敞开。**
   程序自己都在用 `debug`/`getinfo`;分析者因此可以
   `debug.getlocal` 直接读解释器帧里的 opcode 与操作数——
   "混淆数据"在运行时就是一排普通数字。

3. **base-86 不是加密,是字符替换。**
   字母表(86 个可打印字符)明文写在文件里,解码是无密钥的 5→4 字节映射;
   两段载荷离线即可还原,`loadstring` 出来的核心代码全文可读。

4. **CRC / 混洗器是线性模运算。**
   `x=(x*31+j+q+u+K)%65536`、`(s-(q*61+r*23+11)%64)%64`——
   可计算、可逆、可穷举;它只防"手改一个字节",不防理解。

5. **反篡改检查与理解路径正交。**
   `getinfo(what=="C")` 检查只防运行期篡改,对静态分析零阻力;
   而且我们不需要通过它——直接在 Lua 里运行原文件就是最忠实的"验证环境"。

6. **解析器自带格式说明书。**
   [7244] 把记录布局、校验规则、分发约定全写成了可读代码,
   读它 = 拿到字节码的完整 spec,比读 483 条数据高效得多。

7. **程序是自检 harness,"payload"没有秘密。**
   嵌入程序就是一堆 assert + 最后打印 `vm:lua51:ok`;
   它自己把验收标准(输出字符串)泄露出来,还临时写了一个
   名字里就带 "probe" 的全局变量——行为边界一目了然。

8. **完全确定性、无环境依赖。**
   无随机输入、无时钟、无网络 → 动态 trace 一次就是完整 ground truth,
   不存在"跑两次结果不同"的问题。

**一句话总结**:这套混淆的全部强度都押在"看起来不可读"上
(单行、数字键、状态机化控制流、base-86),而**可读性在运行时完整保留**,
所以"以真实解释器为 oracle 的动态对齐"可以在不破解任何密码学的前提下
100% 还原语义。

---

## 7. 行为验证(验收)

重建版 `decoded_source.lua` 与原程序 `out.lua` 在
`/tmp/lua-lua-98194db/lua`(Lua 5.1.1)下:

| 检查项 | 原 `out.lua` | 重建 `decoded_source.lua` | 结果 |
|---|---|---|---|
| stdout | `vm:lua51:ok\n` | `vm:lua51:ok\n` | **逐字节一致**(diff 为空)|
| 退出码 | 0 | 0 | ✅ |
| 全局写入(`__newindex` 陷阱)| 恰好 1 次:`__obf_vm_probe = 41` | 恰好 1 次:`__obf_vm_probe = 41` | ✅ 完全一致 |
| 运行结束后的全局状态 | `__obf_vm_probe = nil` | `__obf_vm_probe = nil` | ✅ |
| 自检断言 | 全部通过(程序内部)| 30/30 通过 | ✅ |

验证手段说明:全局副作用用 `setmetatable(_G,{__newindex=…})` 陷阱捕获——
早期的"前后 diff 全局表"方法漏掉了**运行中临时写入再清除**的写操作,
必须用写入陷阱才能抓到 `= 41` 这一步(这也是重建版同样复现的)。

---

## 8. 走过的弯路(方法论备忘)

| 弯路 | 教训 |
|---|---|
| 前后 diff 全局表判断"无副作用" | 漏掉临时写;必须用 `__newindex` 陷阱 |
| 手工解析 oplog 里的常量索引 | 常量是**按记录混洗**的,同一原始值在不同记录里含义不同;必须以运行时快照为准 |
| 用 oplog 里每个 op 的 x/q/r | 多 op 记录里只有**最后一个 op** 的操作数是新的,前面的会复用旧值;非末位 op 要用静态操作数表 |
| 机械 flat 反编译器 | 278 状态 + 跳转图太碎,写不出可靠的通用反编译器 → 放弃,改"逐记录语义归约" |
| 自动化的配对/执行路径 pickle | 若干条记录配对错误或缺失(r9990、r60644、r33538、r40481…)→ 全部以动态 trace 重新对齐 |
| 下载 Lua 源码走 lua.org / codeload | 不可达;改走 GitHub API tarball |
| `rawlen`、`_G.loadstring` 包装 | 5.1 无 `rawlen`;包装 loadstring 会破坏 VM 的 C 函数检查 |

---

## 9. 产物清单

| 文件 | 说明 |
|---|---|
| `decoded_source.lua` | **最终交付**:语义重建后的可读源码(236 行,含逐段注释)|
| `out.lua` | 原始混淆样本(未改动)|
| 工作目录 `/home/user/obf_work/` | 静态/动态中间产物:`states5.json`(278 状态体)、`flat_ops2.txt`(逐 op 操作数)、`vals2.txt`(运行时值快照)、`oplog_flat.txt`(状态跳转链)、`p0_final.txt`(合并解码,599 记录)、`consts.pkl`/`protos.pkl`(常量与 proto)|
| `/tmp/vm_oplog.txt`、`/tmp/trace_ops2.lua`、`/tmp/track_g.lua` | ground-truth 操作日志、插桩 hook、全局写入陷阱 |

---

## 10. 结论

`out.lua` 的混淆层次是:**字符替换(base-86)+ 控制流扁平化(数值状态机)+
线性模哈希(CRC/常量混洗)+ 反篡改哨兵**。每一层单独看都是"提高阅读成本",
没有一层提供真正的计算强度;而 VM 用明文 Lua 实现这一点,使得
**运行时自省成为最强工具**。

破解的关键顺序是:
**先跑(拿基准)→ 读解析器(拿格式)→ 读状态体(拿语义)→
插桩(拿真值)→ 对齐(消歧)→ 重建(可读化)→ 运行验证(闭环)**。
其中"以真实解释器为 ground truth"贯穿始终,使重建结果不含任何猜测成分。

对同类混淆的通用启示:

1. 遇到"Lua 里的 Lua VM",第一优先是**在解释器分发循环上打 hook**,
   而不是逐行读懂混淆控制流;
2. **解析器/解码器永远是格式的说明书**,先读它;
3. 判断"是否有副作用"要用**写入陷阱**,不能用前后状态 diff;
4. 验收标准要**从程序自身行为中提取**(本例:程序自己打印了
   `vm:lua51:ok`,直接成为测试用例)。
