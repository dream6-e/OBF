# 主库 luraph14.8.lua 反虚拟化实测

日期：2026-09-27。

**最新交付：已按用户要求重新尝试，并保存 328 条可读操作、376 条字符串和一段 2444 字节的 loadstring 输入源码。完整静态还原仍未完成，但已不再只报告失败。见 [全部可读逻辑与输出](artifacts/main-v148/README.md)。以下保留首次验证记录。**

## 结论

**已测试用户主库的 `luraph14.8.lua`，未完整反虚拟化，未生成 restored.lua。** 不是上游工具的 14.8 演示样本。

模拟执行能够返回，并记录到业务界面的构造与回调行为；默认静态提升和关闭长驻运行后的复测均失败。没有放宽完整性检查、没有修改输入，也没有将运行轨迹作为还原源码。

## 输入与环境

当前工作分支的旧快照没有该文件，从 GitHub main 的固定提交下载副本到外部验证目录，没有切换工作分支或合并 main。

- 仓库：`dream6-e/OBF`
- 实际文件名：`luraph14.8.lua`（小写 l）
- 提交：`d2c5de766162d0ce174e61a6c394f02de899948f`
- 大小：164,188 字节
- Git blob：`1398c9bb846777a29f4cdd32ee59d813562d2d9a`，下载后用 git hash-object 验证一致。
- SHA-256：`d55a76cbe6273d530aa406de045030fcd071e0df903f4943bb97a4f818f774b3`
- 文件头明确声明 Luraph Obfuscator v14.8。

当前环境中先前外部工具目录已不存在，因此从主库 Environment.zip 恢复原版工具，并使用其中 Luau 0.735 源码及同版本 AST 编码器重建分析工具。反混淆器恢复到固定提交 `d6158608bb512e919afe4e6485f6146598738274`，应用已保存的累计补丁。

输入通过 Luau 编译和 AST 解析。原有 **48 项回归测试通过（25.104 秒）**，包括上游四版本小样本；这不代表本文件被支持。本轮未修改生产反虚拟化算法或 upstream.patch。

## 第一轮：默认静态模式

```bash
python3 -B /home/user/luraph-v15-v14.x-deobfuscator/Deobfuscator/deobf/cli.py \
  /home/user/obf-v148-validation/input.lua \
  --engine 14.8 --timeout 60 --debug \
  --raw /home/user/obf-v148-validation/runtime.raw.txt \
  -o /home/user/obf-v148-validation/restored.lua
```

- 返回码 **1**，约 **3.16 秒**。
- 模拟主运行状态为 `finished`，记录 328 条行为语句；不等于所有异步路径或回调完整执行。轨迹中有循环截断标记。
- 捕获 **69 个原型、1073 个表对象**；有 **2 次执行前快照失败**，实时捕获保留。
- 初始入口从运行链推断为原型 35；这是诊断编号，没有编号特判。
- 第一轮请求 5 个常量；第二轮日志显示提升 4 个函数、0 未提升块、0 非结构化跳转，但仍被完整性门槛拒绝。
- 对第二轮保存的 dump 独立分析，确认仍有 **2 个未恢复运行时 helper**，所以不能用上述零计数宣布成功。

### 常量补全阶段的入口问题

检查发现：默认长驻 harness 的常量回复不包含原运行的调用链，刷新后的 dump 丢失了最初推断的入口及候选信息。此后提升的目标与初始选择不一致。这是本轮新发现的工具问题，**尚未在生产代码修复**。

研究中依据已记录的调用链恢复入口提示后，严格提升转而暴露下面的符号循环边界阻断。该操作只用于诊断，不作为生产入口覆盖或成功结果。

## 第二轮：关闭长驻模式复测

使用独立输入副本，设置 `DEOB_NO_SERVE=1`，其余仍为默认完整静态模式；没有启用 trace-fallback 或 partial。

- 返回码 **1**，约 **2.44 秒**。
- 每次常量补全重新运行脚本，保留完整运行链，第二轮仍选择原型 35。
- 5 个新常量请求后，不再产生新请求，但仍有 1 个未解析状态。
- 独立严格提升同样拒绝：

```text
WalkIncomplete: refusing partial devirtualization:
1 unresolved VM state(s): 0:324: numeric for with symbolic bounds
```

定位到 VM 内部结果搬运循环：

```lua
for k = O, A do
    s += 1
    u[k] = T[s]
end
```

此处起点可以确定为 14，但上界依赖尚未确定的函数返回值；符号解释器不能将其当作确定数字执行。没有猜测返回值数量、截断循环或删除这条操作。因此即使绕开常量刷新导致的入口丢失，本文件仍不能完整还原。

## 观察到的行为及限制

模拟轨迹出现 UI 库加载调用、窗口与渐变边框，以及“随机伪装”“重置回本体”“选择玩家”“刷新玩家列表”“伪装选中玩家”等界面项和相关回调记录。

这些只是模拟环境中的观测，不是完整源码或真实游戏运行验证。未启用真实网络访问；未获取或执行所请求 URL 的真实远程脚本。模拟器对回调的试探执行、循环截断和代理值也限制了该轨迹的语义完整性，不能把它直接作为可用脚本交付。

## 证据目录

`/home/user/obf-v148-validation/`：

- `input.lua`、`summary.json`：固定版本输入与校验信息、结果摘要。
- `run.log`、`runtime.raw.txt`：默认模式日志和原始运行记录。
- `.input.luraph_v14_8_work/input.protos.json`：默认模式的刷新后 dump。
- `stored.stats.json`、`trace-root.stats.json`：入口差异与严格提升诊断。
- `*.diagnostic.txt`：被拒绝的静态诊断片段，不是恢复源码，未作为 Lua 输出交付。
- `fresh-runs/run.log`、`fresh-runs/summary.json` 及其隐藏工作目录：关闭长驻模式的复测。
- `diagnose.log`：循环边界定位和严格提升拒绝原因。
- `regression.log`：48 项回归结果。

两次测试均没有 restored.lua。下一步需要处理长驻常量回复的入口保留，以及这种动态结果搬运的符号语义；修复其中一项也不能直接保证全部恢复。


## 按新交付约定再次测试

本次 main 提交为 `a1fb37bd0e772c22f7e9224a24009ab4e7bd724d`，14.8 文件大小、blob 与 SHA-256 均未改变。恢复 Environment.zip 工具及固定源码补丁后，默认静态和 `DEOB_NO_SERVE=1` 分别返回 1；前者仍丢失常驻入口，后者保留入口 35 并停在 `0:324` 的动态循环边界。未降低检查门槛。

随后执行无 VM 插桩的行为采集、字符串采集与具体 loadstring 参数记录：获得 328 条操作、434 行基线代码式文本（附中文说明后 437 行）、376 条字符串、一段 2444 字节的函数/环境包装源码。窗口、彩虹描边、随机/选定玩家伪装、重置、刷新列表及角色/摄像机相关调用已有可读记录。远程 UI 库未真实获取，模拟器的变量绑定缺失和未覆盖路径均明确标注。

交付文件保存在 [artifacts/main-v148/](artifacts/main-v148/README.md)，不再只依赖临时目录。本轮累计 **66 项测试通过（23.979 秒）**；三个交付 Luau 文件通过语法检查，没有将轨迹重执行为真实脚本。未修改用户输入或累计 upstream.patch，未提交或推送。
