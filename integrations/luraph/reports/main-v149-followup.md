# 主库 v14.9 继续分析：闭包阻断与布局假设实验

日期：2026-09-27。对象仍是主库 `luraph14.9.lua`，不是上游测试样本。

**结果：未成功还原，没有 restored.lua。原文件未修改，正式补丁未加入猜测布局值或样本特判。**

## 静态路径

继续检查先前 GUI 相关研究原型的阻断状态。当前静态模型在闭包创建指令中读取到空的原型操作数，继而得到 `Len(NilIndex(slot=4))` 作为捕获列表遍历上界。沿前驱回溯，错误节点位于一条含符号条件的路径上；不能据此认定真实执行必然经过该错误节点，也不能直接删除该分支。

研究证据在 `/home/user/obf-v149-closure/`：

- `details.log`：阻断点局部值与捕获表定位。
- `step-details.log`：相关 VM 状态中，符号解释器实际处理的语句。
- `graph-path.log`：前驱路径与中间表示；包含大量嵌入数据，属于诊断，不是恢复源码。

当前入口的 3 个未还原运行时 helper 也仍未解决。没有修改生产入口选择、循环边界或完整性检查。

## 布局实验

为了检验离线推进的可能性，在单独生成的 harness 中注入研究模型，不改动输入或生产 `envlog.luau`。模型仅为假设：未挂载 ScreenGui 的零矩形、比例/偏移与 AnchorPoint、UIScale、UISizeConstraint，以及不同的像素舍入/祖先旋转处理。**模型不完整，尤其未建模 Padding；没有真实引擎校准，不能用于正式结果。**

共尝试五种组合：浮点布局、整数舍入、祖先旋转，以及约束在缩放后应用的两个变体。全部最终被纯计算循环监测终止。布局属性读取次数有所不同（8 或 13 次），但这不能证明某种模型符合 Roblox，也不能当作还原覆盖率提升。

证据：`layout-probe.log`、`layout-clamp.log`、`layout-summary-initial.json`、`layout-summary.json`。模型及各次原始记录均留在外部研究目录，没有加入可重放生产补丁。

## 后续所需证据

建议使用已有的 `integrations/luraph/probes/gui_layout_probe.luau` 在真实 Roblox Studio 中取得校准数据。该脚本只创建未挂载的测试 GUI、读取布局并清理对象，不执行混淆输入、不联网；当前只通过语法检查，尚无真实引擎执行结果。它是校准起点，不保证拿到结果后就能完成主库文件的全部还原。

原文件 SHA-256：`2b8e2e5087c0a093ba65019e17abd6caba4634c6a6406897f2f6082faeba78bf`。

## 用户已选择真实引擎校准

用户确认可以在 Roblox Studio 执行独立脚本。已提供 `probes/README.md` 中的空白项目操作步骤，并将探针输出改为每段最多 2000 字符的 META/PART/DONE 格式，减少长 JSON 被截断的风险。

修改后的探针通过语法检查；环境相关的 8 项测试通过（2.678 秒）。**仍待用户回传真实 Studio 输出；尚未收到校准结果。** 本轮没有更新生产 `upstream.patch`，因为新增实验没有得到可验证的生产修复。
