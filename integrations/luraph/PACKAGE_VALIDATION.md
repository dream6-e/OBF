# 本次整理与上传验证

日期：2026-09-27。目标仓库 `dream6-e/OBF`，分支 `arena/01a0de62-obf`；不覆盖 main。

## 实际交付

- `tools/luraph/`：101 个修复后上游有效源码、资源、样本和文档文件，另附来源清单与说明。
- `integrations/luraph/`：累计补丁、66 项测试、Linux 离线部署/验证脚本、原始字节源文本观察器、Studio 探针、报告及最终逻辑输出。
- 主库 14.7/14.8 的最终可读记录、业务摘录、14.8 字符串及截获的 loadstring 源文本保留，哈希核对通过。
- 原始 OBF 样本及 `Environment.zip` 未修改；根 README 只追加项目入口。

## 清理

从上游包中排除 239 个缓存/重复源码/二进制/归档/历史生成文件，逐项清单见 SOURCE_MANIFEST.json。没有用占位文件替代运行模块。移除了输出目录中 7 份重复 raw/debug 日志，摘要和最终逻辑保留；清理列表见各输出目录 metadata.json。

已删除部署副本中的一次性编译对象，再次验证仍全部通过。部署工具在 `.tools/` 保留并受 Git 忽略；原版工具没有被分析版覆盖。上传不包含 `.tools/`、编译对象、临时工作目录、Windows EXE、旧 RAR 或嵌套 Git 目录。

## 验证结果

| 检查 | 本次实测 |
|---|---|
| Linux 首次离线部署 | 成功；使用仓库 Environment.zip 和固定 AST 补充源码 |
| 重复部署 | 成功；make 增量执行，原版工具保持不变 |
| 修复后源文件 | 101 个源文件哈希通过；23 个累计补丁目标与打包前实测源码逐字节相同 |
| Python 语法 | **85 / 85 通过** |
| Lua/Luau 语法 | **23 / 23 通过**，包含根目录用户样本、上游回归输入与最终逻辑文件 |
| 回归 | **66 / 66 通过，21.753 秒** |
| 源码/补丁/最终逻辑哈希 | 全部通过 |
| 凭据模式初筛 | 打包文本未命中常见 GitHub token、AWS key 或私钥头模式；这不等同于全面安全审计 |

统一复现：

```bash
python3 -B integrations/luraph/setup_tools.py --jobs 2
python3 -B integrations/luraph/validate.py
```

上游历史 `.SON.luraph_v14_8_work/SON.trace.luau` 曾有未注释说明导致的语法错误。本次作为旧生成物排除，没有通过屏蔽有效源文件的方式“通过”检查。其余字节级上游/截获内容原样保留，可能保留上游空白排版，不为追求格式整洁擅改样本或原始捕获源码。

## 能力边界未改变

四个上游小样本可静态还原并执行；用户主库文件仍不能据此宣称完整还原。14.7/14.8 输出是已捕获的可读逻辑，含未恢复变量关系、代理值、试探回调和截断；语法通过不等于可以直接运行或语义等价。完整源码打包不代表全部输入已经支持。详细限制见各输出目录 README。

上游未附独立 LICENSE，来源与授权不确定性已注明；不擅自给上游项目重新授予许可证。Luau AST 补充文件保留原 MIT 许可证。
