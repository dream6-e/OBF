# 环境部署

> 目标：**任何一台干净机器（或刚被清空的沙箱）上，一条命令回到可构建、可测试、可出产物的状态。**
> 首次实测环境：Debian 12 / x86_64 / 2 核，gcc 12.2.0、make、python3、npm、git 可用。
> 本文所有命令都实测过，括号里是实测耗时。

---

## 0. 一键复原

```bash
bash tools/setup-env.sh              # 完整复原 + 构建 + 自检（冷启动实测 87 s）
bash tools/setup-env.sh --no-build   # 只复原环境，不构建
```

跑完之后每条新命令都要先：

```bash
export PATH=/usr/local/obf-rust-1.88.0/bin:$PATH
```

脚本是幂等的，重复跑只会跳过已就绪的部分。

> **为什么需要这个脚本**：沙箱/容器重启时，仓库以外的东西会被整个清空 ——
> 实测 `/usr/local/obf-rust-1.88.0`（Rust 工具链）、`/usr/local/obf-crates-vendor`
> （离线依赖）、`~/.cargo/config.toml` 三样在一次对话之间全部消失。
> 仓库内的 `toolchains/bin` 参考运行时不受影响。所以"固定"落在两处：
> **仓库内的二进制 + 版本号钉死、可一条命令重建的脚本**。

---

## 1. 环境由三层构成

| 层 | 位置 | 是否随仓库固定 | 重建方式 |
|---|---|---|---|
| Rust 工具链 1.88.0 | `/usr/local/obf-rust-1.88.0` | ❌ 体积约 600 MB，不入库 | `tools/bootstrap-rust.sh`（走 npm） |
| 参考运行时 lua5.1 / luac5.1 / luau / luau-compile | `toolchains/bin/` | ✅ 4.4 MB 已入库 | 直接 `chmod +x`；必要时 `tools/build-reference-tools.sh` 从 `vendor/` 重编 |
| 第三方 crate 依赖（51 个包） | `/usr/local/obf-crates-vendor` | ❌ 按 Cargo.lock 现拉 | `tools/offline-crates/*.py` |

想让 Rust 工具链也跟着仓库留存，把它装进仓库里即可（代价是约 600 MB 的工作区体积）：

```bash
OBF_RUST_PREFIX=$PWD/.toolchains/rust-1.88.0 bash tools/setup-env.sh
```

---

## 2. Rust 工具链

### 2.1 先判断走哪条路

```bash
curl -sS --max-time 8 -o /dev/null https://index.crates.io/config.json && echo 官方源可达 || echo 被墙
```

- **可达** → 常规 rustup 安装即可，本文 §4 整段跳过。
- **被墙**（本项目沙箱就是这种）→ `sh.rustup.rs` 和 `static.rust-lang.org` 同样被拦，
  只能用 `tools/bootstrap-rust.sh` 走的 **npm `@rustbin`** 通道。

被墙时的原始报错长这样，认准它：

```
curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to static.rust-lang.org:443
```

`SSL_ERROR_SYSCALL` = TCP 连上了但 TLS 握手被掐断，属于出口按域名放行，不是 DNS 问题
（`getent hosts static.crates.io` 能正常解析出 Fastly 的 IP）。

### 2.2 安装

```bash
OBF_RUST_PREFIX=/usr/local/obf-rust-1.88.0 bash tools/bootstrap-rust.sh   # 17 s
```

脚本内部就是四个 npm 包，`@rustbin` 把 rustc 发行布局原样打进了 npm：

```
@rustbin/rustc-1.88.0-x86_64-unknown-linux-gnu
@rustbin/rust-std-1.88.0-x86_64-unknown-linux-gnu
@rustbin/cargo-1.88.0-x86_64-unknown-linux-gnu
@rustbin/rustfmt-1.88.0-x86_64-unknown-linux-gnu
```

注意这些包**平台写死**（`x86_64-unknown-linux-gnu`），换架构要换包名。

### 2.3 验收（三条都要过，只过第一条不算装好）

```bash
export PATH=/usr/local/obf-rust-1.88.0/bin:$PATH
command -v rustc cargo && rustc --version && cargo --version
# rustc 1.88.0 (6b00bc388 2025-06-23)
# cargo 1.88.0 (873a06493 2025-05-10)

printf 'fn main(){println!("direct-ok");}\n' > /tmp/d.rs && rustc /tmp/d.rs -o /tmp/d && /tmp/d
# direct-ok                                     ← 验证 rustc 本体 + 系统链接器

cd /tmp && cargo new hello && cd hello && cargo build --offline && ./target/debug/hello
# Hello, world!                                 ← 验证 cargo 能真正产出并执行二进制
```

链接器是前置条件，缺了会在最后一步报 `error: linker 'cc' not found`：

```bash
command -v cc gcc g++ make      # Debian: apt-get install -y build-essential
```

---

## 3. 参考运行时

仓库里已经带了四个编译好的 ELF，**不需要联网重建**：

| 文件 | 版本自报 |
|---|---|
| `toolchains/bin/lua5.1` | `Lua 5.1.5  Copyright (C) 1994-2012 Lua.org, PUC-Rio` |
| `toolchains/bin/luac5.1` | Lua 5.1.5 编译器 |
| `toolchains/bin/luau` | `Luau 0.735 (OBF CLI-compatible runner)` |
| `toolchains/bin/luau-compile` | Luau 0.735 编译器 |

> ⚠️ **必踩的坑**：zip 打包会丢掉可执行位。解包后四个文件都是 `-rw-r--r--`，
> 直接执行报 `Permission denied`。`setup-env.sh` 第 ① 步就是补这个位；
> git 里已经按 `100755` 记录，正常 clone 不受影响。

需要在新架构上重建时（要 gcc/g++，2 核大约十几分钟）：

```bash
bash tools/build-reference-tools.sh    # 用 vendor/lua-5.1.5 + vendor/luau-0.735 重编
```

---

## 4. 第三方依赖：crates.io 被墙时的离线源

### 4.1 症状

```
error: failed to get `crossterm` as a dependency of package `kryvex_cli v0.1.0`
Caused by: failed to download from `https://index.crates.io/config.json`
Caused by: [35] SSL connect error (TLS connect error: error:00000000:lib(0)::reason(0))
```

本项目沙箱实测的可达性（换环境请重新测一遍）：

| 目标 | 结果 |
|---|---|
| `index.crates.io` / `static.crates.io` / `crates.io` | ❌ `SSL_ERROR_SYSCALL` |
| `crates-io.s3.amazonaws.com`（S3 源桶） | ❌ |
| `rsproxy.cn` / `mirrors.ustc.edu.cn` / `tuna` / `ustclug` / 腾讯云镜像 | ❌ 全部 |
| `registry.npmjs.org` | ✅ |
| `github.com` / `api.github.com` / `codeload.github.com` | ✅ |
| `pypi.org` | ✅ |

### 4.2 解法：按 Cargo.lock 的精确版本从 GitHub 取源码，拼成 cargo 的 directory 源

```bash
python3 tools/offline-crates/vendor_crates.py     # 真源码，约 30 s（有 tarball 缓存时更快）
python3 tools/offline-crates/stub_crates.py       # windows 专用包占位，<1 s
cat > ~/.cargo/config.toml <<'EOF'
[source.crates-io]
replace-with = "obf-vendored"
[source.obf-vendored]
directory = "/usr/local/obf-crates-vendor"
[net]
offline = true
EOF
```

（`setup-env.sh` 会自动做完这三步，只在 crates.io 不可达时才做。）

结果：`/usr/local/obf-crates-vendor` 共 **51 个包** = `Cargo.lock` 的 53 个减去 2 个 workspace 成员。
其中 36 个是 GitHub 真源码，15 个是占位包。**`Cargo.lock` 一个字节都不改。**

### 4.3 为什么不能只用真源码：cargo 的解析图包含所有平台

哪怕只在 linux 上构建，`crossterm_winapi`、`winapi`、`windows-sys` 的 manifest 也必须在源里，
否则连 `cargo metadata` 都过不去：

```
error: no matching package named `crossterm_winapi` found
    ... required by package `crossterm v0.27.0`
```

而这几个包在 GitHub 上**没有对应 tag**（`crossterm-rs/crossterm-winapi` 仓库最高只到 0.6.1，
`microsoft/windows-rs` 没有 0.61.2 和 windows-link 0.2.1 的 tag），所以用占位包：
manifest 的 name/version/依赖照 `Cargo.lock` 抄，`lib.rs` 里放 `compile_error!` ——
它们在 linux 上永远不会被编译，一旦被误编译就立刻失败，不会静默出错。

### 4.4 五个必须知道的坑（换机器一定还会遇到，脚本里都处理了）

1. **`BurntSushi/regex` 是 2021 年就停更的 fork**（根 manifest 还写着 regex 0.2.0），
   真仓库是 `rust-lang/regex`。tag：`1.12.4`、`regex-automata-0.4.14`、`regex-syntax-0.8.11`。
2. **`rand_core 0.9.5` 没有任何 tag**，只在 `rust-random/rand_core` 的 `backports/v0.9`
   分支提交 `a34dab266970` 上。脚本里用 `PINNED_REF` 钉住。
3. **tag 命名五花八门**：`1.1.4` / `v0.3.4` / `lock_api-v0.4.14` / `mio-v0.2.5` /
   `ppv-lite86-0.2.21`，还要忽略 `+wasi-snapshot-preview1` 这类构建元数据。
   monorepo 的子目录不能靠猜，要在 tarball 里按 `name` + `version` 双匹配定位
   （`zerocopy-derive`、`regex-automata`、`rand_chacha`、`ppv-lite86`、`wit-bindgen` 都是）。
4. **manifest 里的 workspace 继承必须内联**，否则 cargo 报
   `error inheriting 'include' from workspace root manifest's 'workspace.package.include'`。
   三种写法都要认：`version = "0.8.11"`、`version       = "0.8.11"`（对齐空格）、
   `version = { workspace = true }`。依赖走 `[workspace.dependencies]`，其余走
   `[workspace.package]`；`[lints] workspace = true` 整段丢掉即可，不影响产物。
5. **`.cargo-checksum.json` 的 `package` 必须填 `Cargo.lock` 里的校验和**，
   填 `null` 会报：
   ```
   error: checksum for `signal-hook-mio v0.2.5` could not be calculated,
          but a checksum is listed in the existing lock file
   unable to verify that `signal-hook-mio v0.2.5` is the same as when the lockfile was generated
   ```

另外占位包还要补 feature，否则：

```
package `crossterm` depends on `winapi` with feature `winerror` but `winapi` does not have that feature
```

`stub_crates.py` 会扫真实依赖方（crossterm / getrandom 等）的 manifest，
把它们请求过的 feature 和 `winapi/winerror` 这种转发写法收集起来补上。

---

## 5. 部署成功的验收清单

```bash
export PATH=/usr/local/obf-rust-1.88.0/bin:$PATH
cd <仓库根>

cargo metadata --offline --format-version 1 >/dev/null && echo "① 依赖图离线解析 OK"
cargo build                     # ② debug   约 30 s
cargo build --release           # ③ release 约 40 s（lto=fat, codegen-units=1）
cargo build --workspace         # ④ 连 CLI/crossterm 约 17 s
cargo test                      # ⑤ 83 passed / 0 failed

mkdir -p /tmp/smoke && cd /tmp/smoke && cp <仓库根>/test/print.lua .
<仓库根>/target/debug/kryvex-simple print.lua        # ⑥ 出 obfuscated.lua + process/
<仓库根>/toolchains/bin/lua5.1 print.lua    > a.out
<仓库根>/toolchains/bin/lua5.1 obfuscated.lua > b.out
cmp a.out b.out && echo "⑦ 产物与原文件输出一致"
```

七条全过才算部署成功。⑦ 是唯一的端到端判据：混淆器的正确性只能靠"跑出来的输出和原文件一样"来证明。

---

## 6. 常见故障速查

| 现象 | 原因 | 处置 |
|---|---|---|
| `toolchains/bin/lua5.1: Permission denied` | zip 丢执行位 | `chmod +x toolchains/bin/*` |
| `curl: (35) SSL_ERROR_SYSCALL` | 出口按域名放行 | 走 §2.2 的 npm 通道 + §4 的离线源 |
| `error: linker 'cc' not found` | 缺 build-essential | `apt-get install -y build-essential` |
| `no matching package named X found` | 离线源缺包 | 重跑 `vendor_crates.py` / `stub_crates.py` |
| `unable to verify that X is the same as when the lockfile was generated` | `.cargo-checksum.json` 的 package 是 null | 填 `Cargo.lock` 里的校验和 |
| `error inheriting ... from workspace root manifest` | manifest 有 `workspace = true` 没内联 | 见 §4.4 第 4 条 |
| `package crossterm depends on winapi with feature ...` | 占位包缺 feature | 重跑 `stub_crates.py` |
| 产物运行报 `attempt to call field '?' (a nil value)` | 混淆器缺陷，不是环境问题 | 见 `项目交接总结.md` §5.2 |

---

## 7. 附：临时诊断入口（默认不入库）

定位压缩器缺陷时用过，只跑流水线第 ④ 步，能把"混淆器坏了"和"压缩器坏了"分开：

```rust
// examples/compress_only.rs
use kryvex_ob::compressor::Compressor;
fn main() {
    let src = std::fs::read_to_string(&std::env::args().nth(1).unwrap()).unwrap();
    match Compressor::compress(&src) {
        Ok(s) => print!("{}", s),
        Err(e) => { eprintln!("{}", e); std::process::exit(1); }
    }
}
```

```bash
cargo build --example compress_only
./target/debug/examples/compress_only process/process1.lua > out.lua
```

好处是**输入固定**：同一份 `process1.lua` 反复压 N 次，就能把随机缺陷的复现率量出来，
并且拿到"同输入的好/坏两份产物"做对比。
