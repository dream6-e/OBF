# Kryvex 项目交接总结

> 接手日期：2026-09-23　分支：`arena/01a0ce97-obf`（从 `main` 的 `9dfd4c0` 切出）
> 本文记录：项目是什么、怎么用、这次验证到什么程度、还有哪些坑。
> 环境的部署与复原步骤单独放在 **`环境部署.md`**。

---

## 1. 项目描述

Kryvex 是一个用 **Rust（edition 2024）** 写的 Lua 虚拟机化（VM-based）混淆器，作者 `ssssss85`。
它不是简单的改名/压行，而是把 Lua 源码编译成私有字节码，再生成一个自解释的 Lua 虚拟机脚本把
字节码跑起来，所以产物里看不到原始的变量名和语句结构。

### 1.1 处理流水线

`src/main.rs` 就是这条流水线，一共 5 步：

| 步骤 | 代码位置 | 中间产物（写在 `./process/`） |
|---|---|---|
| ① Lua 源码 → Lua 5.1 Proto（词法/语法/代码生成） | `src/compiler/` | `process/luac out.bin` |
| ② Proto 反序列化成内部 chunk | `src/BytecodeCompiler/virtualizer/deserializer.rs` | — |
| ③ 私有字节码序列化 + VM 脚本生成 | `src/VM/VM_Backend/{Serializer,Generator}.rs` | `process/process1.lua` |
| ④ 压缩（改名 + 压行） | `src/compressor/` | `process/process2.lua` |
| ⑤ RadixSieve 数字进制转换 | `src/VM/RadixSieve/` | `process/process3.lua` |

最终成品写到 **`./obfuscated.lua`**。加命令行参数 `MB` 会再走一遍 `src/packer/`（加壳 + 二次压缩）。

### 1.2 目录结构

```
Cargo.toml / Cargo.lock     workspace：根包 kryvex_ob（库 + kryvex-simple 可执行）+ CLI 成员
src/lib.rs                  库入口，导出 compiler / BytecodeCompiler / VM / compressor / packer
src/main.rs                 kryvex-simple 主程序（流水线）
src/compiler/               Lua 5.1 词法、语法、AST、字节码生成、dump
src/BytecodeCompiler/       字节码 chunk / 指令 / 反序列化
src/VM/                     VM_Backend（生成器、序列化、反篡改、沙箱）、Opcodes、Control_Flow、RadixSieve、packer
src/compressor/             独立的压缩前端（自己的 lexer/parser/codegen/renamer）
src/packer/                 MB 模式的加壳与加密
CLI/                        kryvex_cli，crossterm 写的 TUI
test/                       测试语料（print / hash / roblox / benchmark / Comptesting / Katchi / Prometheus/*）
tools/                      部署与测试脚本（bootstrap-rust.sh、build-reference-tools.sh、test-matrix.sh、bench-vm.sh）
toolchains/bin/             已固定的参考运行时二进制（见 §3.2）
vendor/                     lua-5.1.5 与 luau-0.735 源码，只用于重建 toolchains/bin
```

规模：`src/` + `CLI/` 共 **63 个 `.rs`，557,283 字节（544 KB）**（删掉 8 个重复副本、`codegen.rs` 拆成 6 个之后的数字）。

### 1.3 产物形态

成品是**单个物理行**的 Lua 脚本，内嵌加密后的私有字节码 + 解释器。参考量级（同一份 `print.lua`，557 B 输入）：

- 普通模式：`obfuscated.lua` 约 **64.9 KB**
- `MB` 模式：约 **38.4 KB**（加壳 + 二次压缩后反而更小）

> 上面是接手第一天的量级。后续几次加固 / 重写把产物做大了一些，
> 现在（§5.9–§5.11 之后）同一份 `print.lua` 是 **105.1 KB / 53.8 KB**，
> 最新数字只维护在 `samples/README.md` 与 §5.11。
### 1.4 README 里的长期约束（每次改动都要对照）

> README 在 2026-09-24 由用户更新过两次：第一次新增第 1 条，第二次把第 4 条扩成三项。
> 原文是一整段，这里拆成条目。

1. **不要添加新的外部库**，现有的保留；以后的更新**全部由纯 Rust 实现**。
2. 压缩包里自带的工具（rust / lua5.1 / luau）部署后要固定下来，不要清除。
3. 每次更新都要跑所有语法测试，**全部通过才能上传 GitHub**。
4. 每次更新都要上传三项（现在都放在 `samples/`）：
   - **混淆文件**（`test/print.lua` 的普通模式产物）；
   - **MB 压缩后的混淆文件**（同一输入的加壳产物）；
   - **混淆后的伪装.lua**（源文件是仓库根目录的 `#U4f2a#U88c5.lua`，即“伪装.lua”，Roblox 专用；**用户自己测**，因为只有 Roblox 环境跑得起来）。
5. 保持项目的可读性和可维护性。
6. 用户没要求的不要擅自添加；已有的步骤不要自己改，要改先问用户。
7. 单个 `.rs` 不超过 80 KB，超了必须拆成多个文件，且**输出和功能不变**。
8. 这是长期项目，接手并部署成功后要写 `项目交接总结.md`。
9. 库里的 `luraph14.9.lua` 只作结构参考，不要照抄。

第 1 条对当前代码的具体含义：`Cargo.toml` 里已有的 `rand`、`crossterm` 等保留，
但不能新增；`tools/offline-crates/` 只是离线镜像现有依赖，不是新依赖。


---

## 2. 使用方法

### 2.1 构建

```bash
export PATH=/usr/local/obf-rust-1.88.0/bin:$PATH   # 本机 Rust 位置，见 §3.1
cargo build              # debug，约 30 s
cargo build --release    # release（lto=fat, codegen-units=1），约 40 s
cargo build --workspace  # 连 CLI（crossterm TUI）一起编，约 17 s
cargo test               # 83 个单元测试
```

### 2.2 运行

```bash
# 指定文件（扩展名可省略，会自动补 .lua）
./target/debug/kryvex-simple test/print.lua
./target/release/kryvex-simple test/print.lua

# 加壳模式（多走一遍 src/packer）
./target/release/kryvex-simple test/print.lua MB

# 不带参数 = 交互模式，提示「请输入目标文件」
./target/release/kryvex-simple

# TUI
cargo run -p kryvex_cli
```

输出全部落在**当前工作目录**：

```
./obfuscated.lua          成品
./process/luac out.bin    ① Lua 5.1 字节码
./process/process1.lua    ③ VM 脚本
./process/process2.lua    ④ 压缩后
./process/process3.lua    ⑤ 进制转换后
```

> 注意：`main.rs` 每次运行会**清空并重建 `./process/`**，所以别在里面放要留的东西。
> 这两类产物已写进 `.gitignore`。

### 2.3 验证产物正确性

用仓库里已固定的参考运行时跑原文件和成品，比对输出：

```bash
toolchains/bin/lua5.1   test/print.lua          # Lua 5.1.5
toolchains/bin/lua5.1   obfuscated.lua
toolchains/bin/luac5.1 -p obfuscated.lua        # 只做语法/字节码校验
toolchains/bin/luau     obfuscated.lua          # Luau 0.735
toolchains/bin/luau-compile obfuscated.lua
```

---

## 3. 环境部署

完整步骤、踩坑记录和故障速查表都在 **`环境部署.md`**，这里只留结论。

| 层 | 位置 | 状态 |
|---|---|---|
| Rust 1.88.0 | `/usr/local/obf-rust-1.88.0` | `tools/bootstrap-rust.sh` 经 npm `@rustbin` 装成，17 s |
| 参考运行时 | `toolchains/bin/`（已入库） | Lua 5.1.5 / Luau 0.735，**需要 `chmod +x`**（zip 丢了执行位） |
| 51 个依赖包 | `/usr/local/obf-crates-vendor` | crates.io 被墙，改从 GitHub tag 取源码拼 directory 离线源，`Cargo.lock` 未改 |

一条命令复原：

```bash
bash tools/setup-env.sh      # 冷启动实测 87 s，含构建与 83 项自检
export PATH=/usr/local/obf-rust-1.88.0/bin:$PATH
```

> 沙箱重启会把仓库外的东西清空（实测 `/usr/local/obf-rust-1.88.0`、
> `/usr/local/obf-crates-vendor`、`~/.cargo/config.toml` 在一次对话之间全没了），
> 所以"固定"= 仓库内的参考运行时 + 版本号钉死 + 可一条命令重建的脚本。

## 4. 本次验证结果

### 4.1 构建

| 命令 | 结果 |
|---|---|
| `cargo metadata --offline` | exit 0（整个依赖图离线解析通过） |
| `cargo build` | ✅ 30.4 s |
| `cargo build --release` | ✅ 40.1 s |
| `cargo build --workspace`（含 CLI/crossterm） | ✅ 17.0 s |
| `cargo test` | ✅ **83 passed / 0 failed** |
| `cargo fmt --all -- --check` | ❌ 失败，diff 8499 行（见 §5.1） |

### 4.2 混淆 → 运行 → 输出比对

判据：用 `toolchains/bin/lua5.1` 跑原文件和 `obfuscated.lua`，stdout 逐字节比对
（`hash.lua` / `benchmark.lua` 含计时行，比对时剔除）。

修复后全量跑 **22 个语料 × 10 次 = 220 次：正确 219 / 坏 1**（那 1 次是 §5.2 的残留缺陷，
`closures.lua`，签名 `attempt to compare number with table` 的同类 `with boolean`）。

单个语料的一次性结果：

| 语料 | 输入大小 | 产物大小 | 输出 |
|---|---|---|---|
| `test.lua` | 9 B | 63,007 B | ✅ 一致（`67`） |
| `test/print.lua` | 557 B | 64,905 B | ✅ 一致（19 行） |
| `test/print.lua` + `MB` | 557 B | 38,381 B | ✅ 一致 |
| `test/Comptesting.lua` | 8,343 B | 81,880 B | ✅ 一致（40 行） |
| `test/roblox.lua` | 14,580 B | 91,531 B | ✅ 一致 |
| `test/hash.lua` | 21,935 B | 99,663 B | ✅ 一致（仅 `CPU seconds:` 计时行不同，VM 慢约 140 倍） |
| `test/benchmark.lua` | 891 B | 68,851 B | ✅ 一致（仅计时行不同，VM 慢约 10 倍） |
| `test/Prometheus/*.lua`（16 个） | 0.3–1 KB | — | 各 10 次，仅 `closures.lua` 坏 1 次 |

`process/` 四个中间产物每次都正常生成。

## 5. 已知问题（本次发现；标了「已修」的经用户同意后已改，其余未动）

### 5.1 `cargo fmt --check` 不通过

`cargo fmt --all -- --check` 报 8499 行 diff，整个代码库没有按 rustfmt 格式化过。
`tools/test-matrix.sh` 里紧跟在源文件体积门之后的就是 `"$CARGO" fmt --all -- --check`，按现状必挂。
**没有动**——全库格式化是一次性大改，需要用户点头。

### 5.2 混淆产物随机损坏 —— 主因已修，仍有约 0.7% 残留

#### 主因（已修）：CipherKeys 下标撞车

`CipherKeys` 的 8 个下标原先各自独立随机（`rng.range(0x10, 0x7F)`），而生成的辅助表是按

```lua
P[g1]={}; P[g1][bx]=xor; P[g1][add]=add; P[g1][ba]=and; P[g2]={}; P[g2][ba2]=..; P[g2][bs2]=..
```

建的。下标一撞：后写的赋值覆盖前一个（取到错的函数 ⇒ 算出错的常量）；
`g1==g2` 时 `P[g2]={}` 更会**把整个 `P[g1]` 清空** ⇒ 运行时取到 nil。
三处建表代码都有这个毛病：`Generator.rs` 两处 + `Control_Flow/Control_flow.rs` 一处。

修法：给 `GenRng` 加 `distinct(n, min, max)`，8 个下标一次性取互不相同的值。

实测数据（同一批语料，跑够样本量再下结论）：

| | 修前 | 修后 |
|---|---|---|
| 样本 | `closures`/`ambiguous-call` 各 40 次 + 各 8 次 = 112 次 | 4 个 Prometheus 语料各 50 次 = 200 次；再全量 22 语料各 10 次 = 220 次 |
| 坏产物 | **11 / 112（9.8%）** | 2 / 200 + 1 / 220 = **3 / 420（0.7%）** |
| 失败签名 | `attempt to call field '?' (a nil value)`、`attempt to get length of field 'data'`、挂死 | `attempt to compare number with table/boolean` |

**旧签名在修复后的 420 次里一次都没再出现** —— 这就是主因被修掉的证据。

#### 残留（未修）：压缩器还有一个独立缺陷，约 1%

修完主因后仍约 1% 概率产出坏脚本，签名换了（`attempt to compare number with table`、
`attempt to concatenate local 'gM' (a nil value)`）。**阶段已定位**：坏样本的
`process/process1.lua` 单独跑是正确的，`process2.lua`（压缩器产物）就已经错了
⇒ 缺陷在 `src/compressor/`，与 VM 生成、RadixSieve、packer 无关。

用固定输入量出来的复现率：拿一份 `process1.lua` 反复压 250 次，坏 3 次（1.2%）。
复现与定位手法见 `环境部署.md` §7（临时加一个只跑压缩器的 example，输入固定 ⇒
可以量复现率、可以拿到"同输入的好/坏两份产物"做对比）。

#### 顺手修掉的一个真实缺陷（但不是上面残留的原因）

压缩器原先让「声明时活跃变量数相同的变量共用一个名字」，那个启发式不成立 ——
两个可以同时存活的变量会拿到同一个名字，后声明的把前一个遮蔽掉。
已改成**每个被用到的变量独占一个名字**（仍按使用次数降序，用得多的拿短名），
排序用 `VarId` 兜底（原来相等项的先后取决于 HashMap 的随机遍历序，等于又掺一层随机）。

改完后 `attempt to perform arithmetic on field 'p1'` 那类错误消失，
但总失败率没降（250 次坏 3 → 300 次坏 4），所以残留是另一个独立缺陷，没敢声称修好。

### 5.3 `src/compiler/codegen.rs` 超过 80 KB 上限（已拆分）

README 规定单个 `.rs` 不超过 80 KB（81,920 B），原 `src/compiler/codegen.rs` 是 91,043 B，超限 9,123 B。
经用户同意后已拆成 `src/compiler/codegen/` 目录：

| 文件 | 大小 | 内容 |
|---|---|---|
| `mod.rs`   | 1,071 B | 子模块声明 + 对外导出（`compile` / `compile_with_lexer` / `Compiler`） |
| `types.rs` | 2,856 B | `ExprKind` / `ExprContext` / `UpvalDesc` / `BlockContext` / `ConstantKey` / `FuncState` |
| `emit.rs`  | 20,098 B | `Compiler` 结构体 + 指令发射与回填、常量池、寄存器、局部变量/upvalue/块作用域 |
| `expr.rs`  | 30,320 B | `impl Compiler` 的表达式半边 + `compile_expr` / 实参 / 表构造器 |
| `stat.rs`  | 22,968 B | 入口 `compile` / `compile_with_lexer` + 各类语句编译 |
| `tests.rs` | 16,559 B | 原单元测试 |

拆分只做三件事：整段原样搬移、把原先「同文件内私有」的项放宽成 `pub(super)`、
把 `super::ast` 之类相对路径改成 `crate::compiler::ast` 绝对路径。
另外去掉了 `tests.rs` 里冗余的内层 `mod tests {}`（`mod.rs` 已经 `#[cfg(test)] mod tests;`）。

**验证方式**（`codegen` 不碰 RNG，所以可以做到严格比对）：

1. 逐字节：拆分前后各跑一遍 25 个输入，把 `codegen::compile()` 产出的整棵 `Proto` 树
   `{:#?}` 渲染出来比对 —— 50 个文件 `cmp` 全部相同，内容指纹都是
   `0dc4114b65f063cf43989408506a58a0`。（`Katchi.lua` 本来就编译不过，错误信息也一致）
2. 逐字符：把拆分后的代码正文还原（去掉 `pub(super)`、路径换回相对写法、去掉为拆
   `impl` 插入的边界）再与原文件正文比对 —— 46,912 字符完全相同。
3. `cargo test` 83 passed / 0 failed。
4. 端到端：22 个语料 × 10 次 = **220 次混淆 → 运行 → stdout 比对，220 正确 / 0 坏**。
   （§5.2 的残留缺陷是随机的，这次 220 次里没触发；它并没有被这次拆分修好，也没有被它弄坏。）

拆分后全仓库已无超限文件，最大的是 `src/VM/VM_Backend/Generator.rs`（62,672 B）。

### 5.4 `tools/test-matrix.sh` 与当前源码布局不匹配

`tools/` + `vendor/` + `toolchains/` 三个目录是 2026-09-21 的，而 `src/` 是 2026-07 的，
两者不是一套代码。`test-matrix.sh` 里期待的东西当前都不存在：

| 脚本期待 | 仓库现状 |
|---|---|
| `target/debug/obf`（子命令 `check`/`minify`/`virtualize`/`inspect-bytecode`/`shell`） | 只有 `target/debug/kryvex-simple`，无子命令 |
| `tests/fixtures/*.lua` | 只有 `test/*.lua`，无 `tests/` 目录 |
| `src/vm/opcode/lua51_*.rs` ×38、`luau_*.rs` ×91 | 无 `src/vm/`，是 `src/VM/` 且没有按 opcode 分文件 |
| `src/vm/custom/tests/semantic.rs`、`src/shell.rs` | 不存在 |
| `vm_lua51.out.lua` / `vm_luau.out.lua` 等 golden | 不存在 |

**所以 `tools/test-matrix.sh` 和 `tools/bench-vm.sh` 现在跑不起来**，本次没有执行。
`tools/bootstrap-rust.sh`（已用）和 `tools/build-reference-tools.sh`（未跑）是与源码无关的，可以正常用。

### 5.5 仓库里的冗余文件（已清理）

按用户指示删掉了 `src/` 下 8 个 `* (1).rs`（`chunk (1).rs`、`mod (1).rs`、`instruction (1).rs` …
zip/下载产生的重复副本，文件名带空格括号，Rust 不会当模块编进去）和 `src/main.rs.bak`。
删除后 `cargo build` 与 `cargo test`（83/83）复验通过。

`#U4f2a#U88c5.lua` 是 zip 里中文文件名被转义的结果（原名 `伪装.lua`），按用户选择保留原样。

---

### 5.6 反编译加固：指令布局逐产物随机化 + 指令流加密（已做）

经用户同意后做的两项加固，都验证过。

**① 指令布局种子不再硬编码**

原来 `instructions.rs` 里 `get_global_seed()` 直接 `return 264765718`，
意味着**所有产物共用同一套指令字段布局、`xor_key` 和 90 个操作码的排列** ——
逆出一个样本就永久通吃。现在：

- 保留 `DEFAULT_LAYOUT_SEED = 264765718` 作为默认值，**没有显式设置种子的调用方
  （单元测试、库的直接使用者）得到与旧版逐位相同的编码**，所以 83 项断言不受影响；
- 新增 `set_global_seed()` / `randomize_global_seed()`，两个入口（`src/main.rs`、
  `CLI/src/main.rs`）在 `codegen::compile` 之前各调用一次；
- 容器头部从 16 字节扩到 **24 字节**，偏移 16..24 存本产物的布局种子（小端 u64），
  `Deserializer::decode_file` 从头部读回，不再用全局常量。

实测同一输入两次运行，头部种子分别是 `14306875147335191533` 和 `217218890171531370`，
`\n` 分隔符正确落在偏移 24。

**② 指令流不再明文**

原来 `Generator::build` 只对常量池做滚动字节变换，`rewritten_chunks`（指令流）是
**原样追加**的，而 `Serializer` 每条指令固定 10 字节（`1B op + 1B a + 4B b + 4B c`）——
剥掉外层 base86 之后可以直接按 10 字节切片还原。现在滚动变换覆盖 4 字节密钥之后的
**全部内容**（常量池 + 指令流共用一条连续 keystream），Lua 侧 chunk 解码器里 9 处
裸字节读取（`fn_a5` 的 4 字节循环、`fn_read_string` 的字节循环，以及
`nups`/`numparams`/`is_vararg`/`maxstack`/`a_arr`/常量 tag/bool 值 7 处单字节读）
全部改成走解密读取器 `fn_read_dec`。

代价与实测：

| | 改前 | 改后 |
|---|---|---|
| `hash.lua` 产物 | 100,260 B | 128,053 B |
| `roblox.lua` 产物 | 95,289 B | 111,233 B |
| 小脚本一次性启动 | 78–107 ms（中位） | 92–140 ms（中位） |

产物变大是因为滚动变换破坏了指令流的可压缩性，`Packer::encode_stream` 压不动了；
启动变慢是因为 payload 解码从裸 `string.byte` 变成逐字节算术 —— 都是**一次性**开销，
不进 VM 主循环（`benchmark.lua` 2.3 s、`hash.lua` 32 s 的总时长基本没变）。

回归：`cargo test` 83/83；端到端 22 语料 × 10 次 = 218 正确 / 2 坏，
坏的两条签名（`index field 'bc'`、`call global 'io'`）属于 §5.2 那个既有缺陷。
归因对照（同一输入各 120 次）：改前 2/240 坏、改后 2/240 坏，失败率无变化。

### 5.7 反编译加固 ③：SuperOperator 指令融合（已做，第一组）

经用户同意后做的第三项加固。`VOpcode::SuperOperator` 此前只有定义、从未被构造，
字节码和 handler 是严格 1:1 的 —— 攻击者给 90 个 handler 做一次指纹就能还原整张映射表。
现在接上了第一组融合：

```text
GetGlobal/GetGlobalStr A, Bx=name   （已被特化成 builtin-load）
LoadK                  A+1, Bx=arg
Call                   A, B=2, C=1  （1 个实参、0 个返回值）
        ↓  压成 1 条
FUSED_CALL_BUILTIN_K[slot]  A, C=arg
```

- 新操作码区间 `builtins::FUSED_OP_BASE = TOTAL_OPCODES`，共 `BUILTIN_NAMES.len()` 个，
  紧跟在 builtin-load 区间之后，复用同一套 `OpcodeBuilder` / 别名机制。
- **两个被压掉的槽位仍然占位**，handler 里 `pc += 2` 跳过它们。
  这是关键设计：因为不删除槽位，**所有跳转偏移一个都不用重算**。
- 安全前提（`rewrite_chunk` 里逐条检查）：
  1. 两个死槽不是任何跳转目标 —— 相对跳转目标是 `i + 1 + sBx`（`Jmp` 的 sBx 在 `b` 字段，
     由 `deserializer.rs` 的 `decode_instructions` 决定），条件跳下一条的
     `Eq/Lt/Le/Test/TestSet/TForLoop` 目标是 `i + 1`；
  2. 前一条指令一定顺序落入本条（不在跳转/条件跳/无后继集合里）；`i == 0` 是入口，天然安全；
  3. `LoadK` 引用的常量没被 `omit_const` 抹掉。

**踩到的两个坑**（都修了）：

1. handler 模板里 `{{BUILTINREG}}[{s1};` 少了一个右中括号，生成的 Lua 语法就错了。
2. 索引不一致：builtin-load 的指令侧用 `slot_perm[slot]` 选别名，
   而 handler 是按 `fused_map[perm[名字下标]]` 注册的。第一版指令侧直接用了
   `fused_map[slot]`，于是发出的是**另一个内建名**的融合指令 —— 表现为产物能跑但输出不对
   （`test.lua` 不打印 `67`）。改成 `fused_map[slot_perm[slot]]` 后一致。

**性能**（这条很重要，差点做成负优化）：

第一版给全部 50 个 slot 都生成 handler，每个 3~6 个别名，等于给分发树白加约 200 个条目。
而分发是**带不透明谓词的二叉搜索**，每条指令都要付这个代价。实测 `primes.lua` /
`strings.lua` 一组都没融合，`benchmark.lua` 只融合 1 组，却慢了约 7%（2340 → 2475 ms）。

改成**只为本程序真正融合过的 slot 生成 handler**（`fused_used` 集合在 `rewrite_chunk`
里收集，再传给 `generate_fused`）后：`benchmark.lua` 回到 2287 ms，比不做融合的
②-only 版本（2340 ms）还快。0 组融合时分发树大小与 ②-only 完全相同。

实测融合量：`test.lua` 1 组、`print.lua` 11 组、`Comptesting.lua` 4+1+1 组（含嵌套函数）、
`hash.lua` 2 组、`benchmark.lua` 1 组、`primes.lua` / `strings.lua` 0 组。
调试时设 `KRYVEX_DEBUG_FUSION=1` 会把融合组数打到 stderr。

回归：`cargo test` 83/83；端到端 22 语料 × 10 次 = 218 正确 / 2 坏，
与 ② 之后的水平一致，失败签名仍是 §5.2 那个既有缺陷家族
（`compare number with table`、`index field 'bc'`、`call global 'io'`）。

**还没做的**：`VOpcode::Mutated` / `FakeBranchTrap` 仍是空定义；
其他融合规则（`Move+SetTable`、算术+比较+跳转等）也还没接。

### 5.8 明文变量隐藏：状态表 / Proto 字段名逐产物随机化（已做）

**现象**：产物里能直接读到可辨识的字段名，一眼就能看出 VM 的结构：

```lua
local PS=function()X.data=TK[2]X.len=#X.data X.idx=1 X.kidx=0x00000 X.buf={}X.res={}X.pc=3
X.bc=0x0 X.f=0x0 X.p1=0 X.p2=0 X.w=0 X.u=0 X.ptr=0 ... end;
return Rp()end)({data=PE,pc=3,...,map={},idx=0x01,len=#PE,k={},kidx=0x00,buf={},memo={},
unpack=RU,char=RL,byte=RX,floor=RO,insert=Pb,concat=Rh,remove=RD,reverse=RK,bc=0,res={},...})
```

**根因在压缩器，但修法不能在压缩器**：

`src/compressor/codegen.rs` 的成员改名条件是 `member.len() > 4 && !is_reserved_member(member)`
（共 5 处：443 / 452 / 590 / 671 / 699 行）。于是：

- 长度 ≤ 4 的字段名**一个都不改** —— `pc` / `data` / `len` / `idx` / `kidx` / `buf` /
  `map` / `memo` / `k` / `bc` / `res` / `f` / `p1` / `p2` / `w` / `u` / `ptr` / `n` /
  `ld` / `lld` / `nups` 全部明文留到产物里；
- 长度 > 4 的（`insts` / `tamper` / `handlers` / `r_flg` / `r_vals` / `r_len` / `tail_flg` /
  `numparams` / `consts` / `protos` / `opcodes` / `a_arr` / `b_arr` / `c_arr` / `open_ups` …）
  被改掉 —— 所以之前看起来"部分隐藏、部分没隐藏"，很怪。

**不能只把 `len() > 4` 门槛删掉**：`is_reserved_member` 是一份库成员名保护名单
（`codegen.rs:11` 起，含 `char/byte/floor/concat/remove/reverse/unpack/insert/len/sub/
bxor/band/rshift/...`）。`s.floor`（状态表字段）和 `math.floor`（库调用）在语法上无法区分，
删掉门槛后 `s.floor` 被改名、`math.floor` 被保护名单挡住 —— 但 `s.floor` 依然明文，
因为它也在保护名单里。反过来缩小保护名单会直接把 `math.floor` 改坏。
**唯一安全的做法是在生成端就不输出明文名。**

**做法**（三处，都不改算法，只换字段名）：

1. `src/VM/packer.rs`（内嵌解码器状态表，32 个字段）
   - `build_decoder()` 里为 32 个字段各取一个 `rng.name()`；
   - 状态表字面量的键改用 `{p_xxx}` 占位符（值仍是原来的 `data` / `unpack` / `char` 等参数，不动）；
   - 脚本生成后调 `rename_state_fields()` 做一次定点替换：只认字面量 `s.` 前缀 + 整词匹配，
     所以既不会误伤 `s.k` / `s.kidx` 的前缀关系，也不会碰 `math.floor` / `table.concat`。
     `router_code` / `init_insts_loop` / `init_handlers_loop` 都已嵌进 `script`，
     放在最后一步替换即可全覆盖。
2. `src/VM/VM_Backend/Generator.rs`（载荷 Proto 字段 + 变参表计数字段 + `open_ups`）
   - 新增 15 个 `pf_*` 名：`n / ld / lld / nups / numparams / is_vararg / maxstack /
     opcodes / a_arr / b_arr / c_arr / consts / protos / open_ups / vn`；
   - 载荷解析器写入（`{fn_c}.xxx=`）、`chunk.xxx` 读取、`OpcodeConfig` 的
     `consts` / `protos` 字符串、`zm(...)` 里的 `{n=...}` 全部同步。
3. `src/VM/Opcodes/{mod,control_flow,environment}.rs`
   - `OpcodeConfig` 增加 `vararg_count` / `proto_nups` / `open_ups` 三个字段，
     `build()` 里加 `{VARARG_COUNT}` / `{PROTO_NUPS}` / `{OPEN_UPS}` 三个 replace；
   - `control_flow.rs`：`{{STK}}.open_ups`×6、`{STK}.open_ups`×3、`res.n`×3；
     `environment.rs`：`{{STK}}.open_ups`×8、`p.nups`×1。
   - **坑**：同一个 `open_ups` 在 `control_flow.rs` 里有两种写法 —— 在 `format!` 模板里是
     `{{STK}}.open_ups`（要写成 `{{STK}}.{{OPEN_UPS}}`），在第 85 行的**裸字符串**
     `close_ups_stmt` 里是 `{STK}.open_ups`（要写成 `{STK}.{OPEN_UPS}`，单花括号）。
     改错风格就会留下字面占位名。

**验证**：产物里 29 个旧明文字段名逐个 `grep` 全部为 0。只剩两类**必须保留**的可读名：

- `.what` —— `AntiTamper` 里 `debug.getinfo(...).what`，是真实 Lua 字段；
- `math.floor` / `string.char` / `table.concat` / `bit32.rshift` 等库成员。

尺寸略降：`print.lua` 产物 67,329 → 67,294 B；MB 变体 43,259 → 43,245 B。

回归：`cargo test` 83/0；端到端 22 语料 × 10 次跑了两轮 ——
第一轮 **220 正确 / 0 坏**，第二轮 **219 正确 / 1 坏**，失败签名是
`attempt to call global 'io' (a table value)`，与改动前的 218/2 完全同族
（`compare number with table` / `index field 'bc'` / `call global 'io'`）。
两轮合计 1/480，仍在原有水平内：**§5.2 的残留缺陷没有被本轮改动修掉，也没有被加重**。

性能：`benchmark.lua` 背靠背 6 产物 × 4 次，均值 2193.7 → 2188.7 ms；
`primes.lua` / `strings.lua` 同样无回归（字段名只影响常量表/哈希大小，不进主循环）。

### 5.9 Luraph 风格重写：线性逻辑 / 数据流打乱 / 随机块分配 / function + `:` 调用（已做）

**用户四项要求**：① 线性逻辑；② 数据流逻辑打乱；③ 随机代码块分配；④ 多用 function 与 `:` 调用。
附加约束：**不使用任何二进制字面量**、**有限性能预算**（热路径要保住）、
不需要照搬 `luraph14.9.lua` 的完整复杂度、**先把 69 KB 的 `Generator.rs` 拆成两个 ~30 KB 模块再重写**、
总要求"数据流，逻辑打乱，第一眼看上去根本不知道在运行什么"。

#### 第一步：拆分（`Generator.rs` 69 KB → 36 KB + 34 KB）

- 新增 `src/VM/VM_Backend/Generator_util.rs`（33,987 B）：工具层 ——
  `chacha8_*`（Lua 侧 ChaCha8 唯一实现）、`CipherKeys`、`ControlFlowBuilder`
  （不透明谓词 / `build_fast_router` / `build_opcode_tree`）、`rewrite_chunk`、
  `uses_ident` / `rename_ident`（按 token 匹配，避免 `s.k` 与 `s.kidx` 互相误伤）、
  `GenRng`（`name()` 随机标识符、`format_num` 数字混淆、`slot()` 不重复槽位号、
  `obfuscate_num`、`shuffle`、`distinct`）、`PayloadReader` / `write_string`。
- `Generator.rs` 只留 `Generator` 构造 + `build()`（发射顺序：VM 核心 → 解码器 →
  分发表 → 驱动循环 → 收尾）。
- `mod.rs` 加 `pub mod Generator_util;`。

#### 第二步：重写 `build()`（四项要求怎么落地）

| 要求 | 落地方式 |
| --- | --- |
| 随机代码块分配 | 每个 opcode 块 → 一个随机名方法；**注册顺序洗牌**；每块分一个随机大整数**状态号**；分发树按状态号路由，条件是 `P[a][b](v<=v and v or K,key) OP val` 形式的不透明谓词 |
| 多用 function + `:` | 方法写成 `.随机名=function(self,op,inst_A,inst_B,inst_C)` 注册进 VM 方法表；方法表挂在 `__index` 原型上（所有 VM 调用共享，**不能每个调用重建**）；调用点一律 `self:随机名(...)`。收尾是 `return ({...}):随机名()(...)` 骨架 |
| 线性逻辑 | 单层 `while true do` 驱动 + 每块"自校验 → 取槽位 → 干活 → 写回"的线性序列 |
| 数据流打乱 | 状态不放局部变量，放 VM 对象 `self[随机大整数]` 槽位；热块把用到的槽位取进**随机名局部变量**，冷块进出各同步一次槽位 |

**热/冷分治（性能预算的关键）**：

- 热块 = 算术 / 比较 / 跳转 / 栈与表存取（判定：不用 builtin 寄存器、env、protos、
  varargs、`open_ups`、`zm(`，也不递归调用 `execute`）→ **完全内联**进分发树，
  `pc`/`top` 直接以 `self[k]` 形式就地读写。
- 冷块 = 调用 / 返回 / 闭包 / 全局 / 上值 / 内建 → **提成方法**，入口自校验
  `if self[k_state]~=st then return end`，调用点前后各同步一次 `pc`/`top`。
- 全量方法化实测慢 5.6 倍（benchmark 16,561 ms、hash 215,908 ms），所以必须分治。

#### 踩过的两个坑（都已修，别再走回头路）

1. **出口写回不合法**：原先想在每块尾部追加 `{STOREBACK}` 把 `pc`/`top` 写回槽位，
   结果语句落在 `return` 之后 → `lua5.1` 报 `'end' expected`
   （`luau-compile --binary` 能精确定位到 `(3,7216)`）。
   Lua 规定 `return` 必须是块的最后一条语句 → 改成**就地替换** `self[k_pc]`/`self[k_top]`，
   `{STOREBACK}` 整段删除。
2. **方法注册不能用 `V["名字"]=function`**：压缩器改名器只统一改 `.名字` / `:名字` 成员访问，
   **字符串键不改** → 注册名与调用名对不上 → 运行期 `attempt to call method '?' (a nil value)`。
   4 处注册点（主 handler + ret0/1/2 返回钩子）全部改成 `.名字=function`。
   另外：块体里引用的 `env` 必须走 `self[k_env]`（共享方法表不能捕获某一次调用的 env）。

#### 顺带修掉的一个 MB 偶发损坏（`src/packer/mod.rs::pack_lua`）

`base122_encode` 把输入补到 4 字节倍数时**补 0**，但解码端会对**所有**字节（含补位）
再做一遍 XOR → 补位变成 `keys[i%16]`，偶尔被 LZ 解码器当成合法记号，
多解出一段垃圾拼在源码尾部 → MB 产物偶发 `attempt to call a nil value`
（实测 3~5 轮复现一次）。改成**补 `keys[i%16]`**（解密后为 0，解码器读到 0 头字节后
字节流已耗尽即正常收尾）。内层 `src/VM/packer.rs` 用 base86 + 显式余数分支，没有这个问题。

#### 验证

- `cargo test`（debug 与 `--release` 各一次）均 83/0；
- 端到端 22 语料 × 10 次 = **220 正确 / 0 坏**（§5.2 的 `index field 'bc'` 家族在重写后未再出现）；
- MB 稳定性：print 20 轮 + roblox 10 轮 + matrix 10 轮 = **40 轮 0 失败**
  （这就是上面那条补位修复修掉的偶发崩溃）；
- 产物形态：`lua5.1 -p` 与 `luau-compile --binary` 双语法通过；
  `0b`/`0B`/数字下划线 0 处；`:` 调用 260+；`.名=function` 注册 60+；随机大整数状态号 1000+。

#### 性能（同机中位数；base = `9cbf65b` 产物，new = 重写后；各语料 base/new 交替跑 7 次取中位）

| 语料 | 基线 | 重写后 | 说明 |
| --- | --- | --- | --- |
| print | 161 ms | 90 ms | 0.56× |
| roblox | 1,041 ms | 457 ms | 0.44×，重写后位运算回退更快 |
| closures | 146 ms | 96 ms | 0.66× |
| mb_table | 3,106 ms | 2,868 ms | 0.92× |
| mb_arith | 8,451 ms | 8,848 ms | 1.05× |
| mb_call | 1,747 ms | 2,001 ms | 1.15×，调用密集 |
| benchmark | 2,837 ms | 3,737 ms | 1.32×，调用密集 |

产物体积约 1.4~1.5×（例：print 68 KB → 101 KB），因为每个指令块都成了一个带
随机名的方法，外加随机大整数状态号和分发树。

调用密集的语料变慢的原因是：**每次调用都要走一次方法查表**（`self:随机名(...)`），
而基线是直接内联的 if-else 链。做过一轮实验，把 CALL 从"冷块判据"里去掉（即让调用
也内联进热路径），结果 mb_call 反而更慢，已回退 —— **`execute` 递归调用必须留在冷块**。

顺带把生成的位运算回退实现里的 `math.floor(a/2)` 换成 `(a-ra)*0.5`
（`Generator.rs` / `Generator_util.rs` / `Control_Flow.rs` 三处生成点），
纯 Lua 位运算快约 2.5 倍（roblox 从 ~1,000 ms 降到 ~460 ms）。

> 注：按用户要求，VM 内部的哨兵常量（`mode`/`state` 用的 0/1/2）一律走
> `obfuscate_num` 的位运算形式 `V[a][b](x,y)`，不为了热路径换成简单表达式；
> 代价就是上面 mb_call / benchmark 那一档的额外开销。

### 5.10 AntiTamper 爆栈函数换形（已做，按用户指定的写法）

`AntiTamper.rs` 里随产物生成的「递归爆栈」函数，原来写的是：

```lua
local function f() pcall(f) pcall(f) end;
pcall(f);
```

按用户要求改成「把要递归的函数当参数传进去」的形式，函数名与参数名都逐产物随机：

```lua
local function rp(Vc) Vc(rp,Vc) Vc(rp,Vc) end rp(pcall)
```

语义完全等价（一样是无限递归直到爆栈 / 卡死，只有被篡改时才调用），
但产物里不再出现 `pcall(f)` 这种一眼可辨的图案。

**做法**：`src/VM/VM_Backend/AntiTamper.rs` 里新增两个 `rand_var()` 名字
（`fn_crash_rec` / `v_crash_arg`）填进模板，共 10 个占位参数 ——
顺序是 `v_crash, fn, arg, arg, fn, arg, arg, fn, arg, fn`，
**改这行时数错占位符个数会直接生成语法错误**，改完必须跑一次产物语法检查。

验证：三个样本产物 `pcall(f)` 出现 0 次、新图案各出现 1 次；
`lua5.1 -p` 与 `luau-compile --binary` 双语法通过；print 普通 / MB 产物 stdout 与原文一致；
`cargo test --release` 83/0；抽查 4 语料 ×（普通 + MB）× 2 次 = 16/16 通过。

### 5.11 守卫 7 / 池解码器打乱 + 原生 `loadstring` 探测（已做，按用户点名的两处）

用户点名两处「一眼能看出在干什么」的代码，要求打乱它们的线性逻辑与数据流，
并在**每一个调用 `loadstring` 的地方**插入他给的原生 C 函数探测。两件事都做了。

#### ① 池解码器 `v_dec`（`src/VM/VM_Backend/AntiTamper.rs`）

原来是教科书式的 `for i = 1, #t do out = out .. chr((b - k) % 256) end`。现在：

```lua
local function D(xS) local YZ=xk[xS] if YZ==nil then return nil;end;
  local zk,xH=0x0,#YZ; local xR,Yy=string.char,''; local Ak=49;
  while zk<xH do zk=zk+0x0001; local xb=YZ[zk] Ak=Ak+1;
    if(Ak+Ak)-Ak>0x0000 then Yy=Yy..xR((xb-0x3B+Ak-Ak)%256)end;end; return Yy;end
```

打乱手法：`for` 换成 `while` + 显式自增下标（循环变量不再是 Lua 托管的下标）、
引入影子计数器 `Ak`（`+1` 再 `-1` 净零）、减法写成 `(b-k+j-j)`、
把拼接包进恒真谓词 `(Ak+Ak)-Ak>0x0000`、全部局部名逐产物随机。

#### ② 守卫 7（毒表守卫 `A[7]`）

原来是 `if type(env) ~= 'table' then <bad> else <mk metatable> ... end`，元方法按固定顺序挂。
现在：`type` 的结果先落到 flag 变量、判断条件写成恒真的复合式
（`if not flag and t~='table'`）、元方法表按**随机顺序**挂载并掺入随机诱饵
（`__eq` / `__concat` 之类，无副作用）、再走影子计数器 `j` 与
`pcall` 结果恒假的收尾判断（`if j~=0 or ok==j then <bad> else <good> end`）。
语义与原来完全一致：探测被 hook 篡改，才走爆栈分支。

#### ③ 原生 `loadstring` 探测（用户给定的代码）

新版 `src/VM/VM_Backend/Generator_util.rs::loadstring_probe_lua()` 按用户给的思路生成：

```lua
local function <nat>(f) local d=debug; if type(d)~='table' then return true end;
  local gi=d['getinfo']; ... return inf['what']=='C' and inf['source']=='=[C]' end;
local function <getf>() local g=(getfenv and getfenv())or _G; local gg=g['getgenv']; ... end;
local <pl>=<getf>();
```

两个接入点（即全部 `loadstring` 调用点）：

| 文件 | 位置 | 改动 |
|---|---|---|
| `src/VM/packer.rs` | `build_decoder` 生成的脚本头 | 插入 `{probe}` 段 |
| `src/packer/stub_generator.rs` | `f_load` 之前 | 插入 `{probe}`；`f_load` 改成 `({v_pload} or loadstring or load)` |

局部名全部来自新增的 3 个名池条目（`f_isnat` / `f_getls` / `v_pload`），共 17 个随机名。

#### ④ 探测里的字符串全部 XOR 加密（用户追加要求）

字符串键能躲开改名器，但**明文本身**还是特征：产物里出现 `['getinfo']`、`'=[C]'`
这种字面量，等于直接告诉逆向者「这里是原生函数探测」。按用户要求把这段里的
**9 个字符串**全部 XOR 加密，产物里不再出现明文：

`getinfo` / `what` / `C` / `source` / `=[C]` / `getgenv` / `getrenv` / `loadstring` / `load`
（前 5 个是用户点名的键与比较值；后 4 个是同一段里同族的字符串键与比较值，
不一起加密的话明文照样暴露这段代码的用途，所以一并处理了。）

落地方式（都在 `Generator_util.rs::loadstring_probe_lua`）：

- 密文以 **`"\ddd\ddd…"` 十进制转义字面量**写出（全三位定宽，后面跟数字也不会读错）；
- 解密器是**纯算术异或**，不依赖 `bit32` / `bit`，标准 Lua 5.1 与 Roblox 都能跑：

```lua
local function ZG(Gm,bq) local me,XA='',0x000; local bD=#Gm;
  while XA<bD do XA=XA+0x01; local mr,mP=bq,string.byte(Gm,XA);
    local mf,mP=0,1; ...（8 位逐位比较，%2 与 /2 取值、*2 进位）...
    me=me..string.char(mf) end; return me end;
local bO=ZG("\122\120\105\116\115\123\114",0x1D)   -- "getinfo"
local ba=ZG("\218\197\204\217",0xAD)               -- "what"
...共 9 行
```

- 每个串一个**独立随机密钥**，且密钥避开该串里出现过的字节 —— 保证密文里
  不会写出 `\000`（Lua 能装 NUL，但没必要给压缩器的 lexer 找麻烦）；
- 解密只在 VM 启动时跑 9 次短串，实测产物只增大 ~130 B（105,135 → 105,264），对性能无影响。
  对性能无影响。

#### 踩过的两个坑（都已修，别再走回头路）

1. **压缩器的成员改名器会改点访问**。第一版探测写成 `debug.getinfo(...)`、
   `info.what`、`info.source`、裸 `getrenv`，产物一出来这些名字全被改名器改掉，
   探测永远走兜底、形同虚设。**凡是要过压缩器的新生成代码，成员访问一律用字符串键**：
   `['getinfo']` / `['what']` / `['source']` / `['getgenv']` / `['getrenv']`；
   环境获取用**保留名** `getfenv` + `_G`（`debug` / `load` / `loadstring` / `pcall` / `type` 也在保留名名单里）。
2. `GenRng::new` 收 `u64` 而 `seed` 是 `u32`，直接传会 E0308，要 `seed as u64`。

#### 验证

- `cargo build --release` rc=0；`cargo test --release` **83 passed / 0 failed**；
- 三个样本产物（`samples/`）`lua5.1` 与 `luau-compile --binary` 双语法通过，
  print 的普通 / MB 产物 stdout 与原文件逐字节一致；
- 抽查 4 语料（print / Comptesting / closures / multi-return）×（普通 + MB）× 2 次 = **16/16 通过**；
- 产物中二进制字面量（`0b`/`0B`）出现 **0** 次；非关键字标识符泄漏检查：print 两产物 0 命中，
  伪装产物仅命中 `FFFFFF`（十六进制常量，原文件本来就有）；
- **字符串明文检查**：三个产物里 `getinfo`/`getgenv`/`getrenv`/`['what']`/`['source']`/`=[C]`
  等模式全部 **0 命中**；9 个密文调用点在三个产物里都在；
- **解密正确性实测**：把产物里那段解密器与 9 行密文调用**原文抠出来**丢给 `lua5.1` 跑
  （即经过压缩器改名 + 数字转换之后的真实代码），解出的集合与期望
  `=[C] | C | getgenv | getinfo | getrenv | load | loadstring | source | what` 逐项一致
  —— 说明压缩器没有破坏 `\ddd` 转义，纯算术异或实现也正确；
- 样本尺寸随之刷新：`print.obfuscated.lua` **105,264 B**（原 102,171）、
  `print.obfuscated.MB.lua` **54,496 B**（原 52,134）、
  `U4f2aU88c5.obfuscated.lua` **134,831 B**（原 133,607）。
- 顺带确认：`test/Katchi.lua` 生成时报 `function or expression too complex`
  （`in.lua:4907`）——这是解析器对超大函数的既有上限，发生在读取输入阶段，
  与本轮改动（只动了 Lua 生成侧 4 个文件）无关。

### 5.12 新要求第一批：十六进制全大写 + ChaCha 常量隐藏（已做）

用户 2026-09-24 在 GitHub 上又给 README 加了一段（远端提交 `f79201f`，本地随之同步），
原文是一句话 6 项要求：

> 要求:数字使用十六进制的话ABX都用大写，所有代码不能有清晰的运行逻辑，整体每个代码块紧密耦合，
> 互相嵌合起来防止被一步一步破解，能动态生成的就动态生成，比如常量，还有chacha加密或者其他加密
> 不要有明显的特征，比如chacha的常量，解密逻辑也要打乱

拆成 6 项，本次先做第 1、5 两项（用户指定），**剩下 4 项还没做**：

| # | 要求 | 状态 |
|---|---|---|
| 1 | 十六进制「ABX 都用大写」 | ✅ 本次已做 |
| 2 | 所有代码不能有清晰的运行逻辑 | ⏳ 未做 |
| 3 | 每个代码块紧密耦合、互相嵌合（防逐段破解） | ⏳ 未做 |
| 4 | 能动态生成的就动态生成（比如常量） | ⏳ 未做 |
| 5 | 加密不要有明显特征（如 ChaCha 的常量） | ✅ 本次已做 |
| 6 | 解密逻辑也要打乱 | ⏳ 未做 |

用户同时明确了两条边界：**Rust 源码里的字面量不用改**（只管最终产物长什么样）、
**`KRYVEX` / `KryvexObf_` 水印保留**。

#### ① 十六进制全大写（产物侧）

产物里的十六进制数字统一成 `0X1F3A`（前缀 `X` 大写 + `A-F` 大写）。生成点共 14 处：

| 文件 | 位置 |
|---|---|
| `src/VM/RadixSieve/convert.rs` | 14 / 17 / 21 行（`0X{}{:X}`、`0X{:X}`）—— 产物里绝大多数十六进制的来源 |
| `src/VM/Control_Flow/Control_flow.rs` | 22 / 24 行（`format_num`） |
| `src/VM/VM_Backend/Generator_util.rs` | 74 / 76 / 686 行（`format_num` / `obfuscate_num`）、826 行（探测密钥） |
| `src/VM/VM_Backend/Generator.rs` | 150 行 `hex_select_idx` |
| `src/VM/VM_Backend/AntiTamper.rs` | 132 行模板里的 `0X0` |
| `src/packer/stub_generator.rs` | 286 / 288 行模板里的 `0X0` / `0X1` / `0X14` |

**坑（记一下）**：Rust 的 `format!("{:#X}", n)` 输出的前缀**永远是小写 `0x`**（`#` 只管加前缀，
`X` 只管数字大写），所以必须手写成 `format!("0X{:X}", n)`。第一版就是漏了这个，
产物里还剩 4 处 `0x44` 之类的残留（`hex_select_idx`），改完才干净。

实测：`test/print.lua` 普通产物 3088 处 `0X` / 0 处 `0x`；MB 产物 12 / 0；伪装产物 3238 / 0
（伪装产物里剩的那 1 处「`0x`」是 base86 载荷随机字符碰巧撞上的，不是数字）。

#### ② ChaCha 常量隐藏

ChaCha 的 4 个 sigma 常量（`0x61707865 0x3320646e 0x79622d32 0x6b206574`，
拼起来是 "expand 32-byte k"）原来在产物里明文各 1 处，出自
`src/VM/VM_Backend/Generator.rs` 的 chacha 块模板。现在改成**逐产物派生**：

```lua
-- 产物实测（变量名/表达式形状逐产物随机）
local my={(((0X1DDF34DB-0X14527536)+s[3])%4294967296),
          ((y[0X4F][0X3D](0X93AF0A3A,0X3F9F6B2C)+s[7])%4294967296),
          (((0X1284EA488-0X000054A85275)+s[2])%4294967296),
          (((0X000AAED587E-0X4EA74DE2)+s[0X6])%0X100000000),
          s[1],s[0X0002],...}
```

做法：对每个 i 取 `d_i = sigma_i - K[idx_i]`（`wrapping_sub`），产物里写
`(d_i_混淆表达式 + K[idx_i]) % 4294967296`。其中：

- `K` 就是产物里那份**逐产物随机的 ChaCha key**（8 个 u32），所以 `d_i` 每次都不一样，
  静态看不到标准常量；
- `d_i` 本身再走一遍 `obfuscate_num`（和 key 同样的混淆），所以连 `d_i` 也不是裸字面量；
- 下标故意**打乱**成 `[3,7,2,6]`，恢复式与 K 的排列不对应，避免「按顺序减一遍就还原」；
- 语义完全等价（`%4294967296` 保证 s[1..4] 精确落在 u32 范围内，原始值快照 `o[]` 才不会失真）。

Rust 侧的标准常量（`src/VM/VM_Backend/Generator_util.rs` 的 `chacha8_block`）**没动**，
按用户要求源码不改；加密/解密仍然匹配。

实测：三个样本产物里 4 个常量各 **0 处**（改前是各 1 处）。

#### 验证

- `cargo build --release` rc=0；`cargo test --release` **83 passed / 0 failed**；
- 三个样本产物 `lua5.1` + `luau-compile --binary` 双语法通过；print 普通 / MB stdout 与原文件一致；
- 抽查 4 语料 ×（普通 + MB）× 2 次 = **16/16 通过**；
- samples 刷新：`print.obfuscated.lua` **104,444 B**、`print.obfuscated.MB.lua` **54,805 B**、
  `U4f2aU88c5.obfuscated.lua` **132,519 B**。


### 5.13 新要求第二批：解密逻辑打乱（第 6 项）+ 解码侧「没有清晰逻辑」（第 2 项的一部分）

用户指定做 6 项里的第 6（解密逻辑也要打乱）和第 2（所有代码不能有清晰的运行逻辑）。
本次落在**载荷解码链**上 —— 这一整条链只在产物加载时跑一次（冷路径），
所以可以放心换形，不用替性能保留原样。

#### 打了哪些地方（全在 `src/VM/VM_Backend/Generator.rs`）

| 函数 | 原来（一眼能读） | 现在 |
|---|---|---|
| `bxor(a,b)` | 逐位 `if ra~=rb then c=c+p` | 位值改写成 `((a%2)+(b%2))%2` 再乘权重；`math_floor(a/2)` 换成 `(a-(a%2))/2`；`while a>0 or b>0` 换成影子守卫 `while g<(r-r)+1` |
| `rotr(x,n)` | `n==0` 早返回 + `2^(8-n)` | 去掉早返回，用 `256/2^n` 代替，`floor(x/2^n)` 换成 `(x-(x%2^n))/2^n` |
| `read_dec()`（滚动密钥解密） | `dec` 被反复赋值 + key 顺序演化 | 拆成 5 个互不相同的中间变量、`-66` 写成 `+190`、`+27` 写成 `-229`、`floor(v/128)` 写成 `(v-(v%128))/128`、`k2*3` 写成 `k2*2+k2`，整体包进单次循环 + 影子返回 |
| `u32_dec` / `a5` | `for i=1,4` + 固定顺序累加 | `while` + 显式自增下标；累加项顺序打乱（整数加法精确，顺序无关）；权重换成 `2^8/2^16/2^24`；初值写成 `(t[3]-t[3])` |
| `read_string` / `a10` | `for _=1,l` / `2^31`、`-2^32` | `while` + 显式下标；`2^31`、`v-h-h` 换形 |
| `dec_num`（IEEE754 拆 f64） | `v8>=128 and -1 or 1` + `floor(v7/16)` | 符号位 `1-2*((v8-(v8%128))/128)`、指数位 `(v8%128)*8*2+(v7-(v7%16))/16`、三种情况分支顺序打乱 |
| `dec_str`（chacha 解字符串） | `for j=1,len` | `while` + 显式下标，连参数名都换成随机的 |
| **`dec_chunk`（状态机）** | `if state==A then <体>; state=B elseif …` 固定顺序、推进总在体尾 | 六个状态体**先各自构造再 shuffle**（条件是互斥的 `==`，顺序无关）、**推进提到体首**、所有 `for` 换 `while`、结尾统一 `return` 而不是分支内 return、带恒假谓词 |
| 两个池初始化 | `for i=1,n do` 套 `for j=1,len` | `while` + 显式下标 + 独立局部计数 |

**不能动的地方**（改了就是坏产物）：体内**读字节流的顺序**一个字都不能换（那些字节是按顺序读进来的）；
`2^(-1074)` / `2^(exp-1075)` 这类浮点语义不能"化简"；`"KryvexObf_"` 水印按要求保留。

#### 踩过的三个坑（都修了，别再走）

1. **Rust `format!` 的隐式捕获**：模板里写 `{k1}` 本意是"产物里那个变量名 k1"，
   但 Rust 2021 会把 `{k1}` 当作**作用域里的变量 k1** 抓走 —— 而 `Generator.rs` 里
   恰好有一个 payload 加密用的 `k1`，于是产物里出现了 `189=(189+...)` 这种给数字赋值的代码，
   直接语法错。**产物里的固定局部名（k1..k4）只能写裸标识符**。
2. **裸 `{}` 是位置参数占位符**：Lua 的空表必须写成 `{{}}`。修的时候正则还误伤了
   `f64_parts` 里**合法的**位置参数 `{}`（7 处），一并回退了。
3. **等价变形算错**：把 `(v8%128)*16` 误写成 `(v8-v8%128)/8`（那是 bit7×16，不是低 7 位×16），
   print 产物输出直接变成 `3.89e-308` 这种次正规数；改成 `(v8%128)*8*2` 才对。

#### 验证

- `cargo build --release` rc=0；`cargo test --release` **83 passed / 0 failed**；
- 三产物 `lua5.1` + `luau-compile --binary` 双语法通过；print 普通 / MB stdout 一致；
- 抽查 4 语料 ×（普通 + MB）× 2 = **16/16**；扩测 8 语料 ×（普通 + MB）= **16/16**
  （`hash` / `benchmark` / `loops` / `strings` / `metatables` / `fibonacci` / `upvalues` / `state-machine`，
  其中 benchmark 的两条只是计时行不同）；
- 加载耗时（解码链是冷路径）：旧产物中位 123.0 ms → 新产物 117.6 ms（**0.96x**，无退化）；
- 十六进制仍全大写（普通 0 处小写）、ChaCha 常量仍 0 处；
- samples 刷新：`print.obfuscated.lua` **104,051 B**、`print.obfuscated.MB.lua` **55,075 B**、
  `U4f2aU88c5.obfuscated.lua` **139,172 B**。

#### 6 项要求的最新进度

| # | 要求 | 状态 |
|---|---|---|
| 1 | 十六进制全大写 | ✅ §5.12 |
| 2 | 不能有清晰的运行逻辑 | 🟡 **解码链已做（本节）**；VM 执行侧在 §5.9 已做（方法化 + 随机状态号 + 分派树），其余零散处随时可继续 |
| 3 | 代码块紧密耦合、互相嵌合 | ⏳ 未做 |
| 4 | 能动态生成的就动态生成（常量） | ⏳ 未做（ChaCha sigma 已在 §5.12 做掉） |
| 5 | 加密不要有明显特征 | ✅ §5.12 |
| 6 | 解密逻辑也要打乱 | ✅ **本节** |


### 5.14 新要求第三批：反美化守卫（行号校验）+ 第 2 项续做（`m_main` 打乱）

#### 反美化守卫（guard 8）—— `src/VM/VM_Backend/AntiTamper.rs`

用户原话：**「添加反美化，比如行号校验」**，并给了参考做法（`pcall` 抓错误消息里的
行号 → 与另一路取值互验 → 不一致就 `while true do end`）。落地方式：

- 守卫列表加入编号 **8**（仍在既有的「洗牌 + 串链」结构里，good 才走到下一个守卫）。
- 原理：**产物整包只落在 2 个物理行里**（第 1 行是头部注释，第 2 行是全部代码），
  所以「在同一物理行上定义的函数」它们的 `debug.getinfo(f, 'S').linedefined` 必然相等。
  守卫在同一行上定义三个函数：`f1`（故意出错，访问 nil 的下标）、`f2`、`f3`，然后

  1. `pcall(f1)` 拿错误消息 → `string.find(msg, ':(%d+)[:\r\n ]')` 抓出行号；
  2. 抓到的行号用 `string.sub` 与 `string.char(string.byte(...))` **两路取值互验**
     （防止错误消息被伪造/替换成别的串）；
  3. 校验 `(l1 - l1) + l1 == l2`（即 `f1` 与 `f2` 的 `linedefined` 相等）——
     写成这种等价式是为了不留下「`a == b`」这种一眼看懂的判据；
  4. 校验抓出的行号与 `debug.getinfo(f1, 'l').currentline` 一致；

  以上任一不成立 → 走**既有的 bad 链**（爆栈/死循环），产物不再产出正确结果。
- **fail-open**：debug 不是表、`getinfo` 不是函数、`tonumber` 不存在、pattern 没匹配、
  返回不是 number/string …… 任何一步取不到信息都**放行**（当 good），避免在 Roblox
  之类环境里误杀。成员访问一律字符串键（`['linedefined']`、`['getinfo']`），
  这样成员改名器把点访问改掉后依然能用。
- 判据**刻意不比较「错误消息里的行号」与 `linedefined`**：两者来源不同（错误消息 vs
  debug 接口），某些环境可能差 1；只比较**同一口径**的「错误行号 vs `currentline`」。

验证：

- 原始产物 `rc=0`、stdout 与源文件逐字节一致（**不误杀**）；
- 定向实验：只在守卫自己那三个函数之间插入**一个换行**（token 完全不变）→
  产物立刻失效（`timeout 25s` → `rc=124`，卡死）；说明拆行确实会打中守卫；
- 在文件**末尾**追加一个换行 → 仍 `rc=0`、输出一致（无关空白不误杀）；
- 真实「美化」：按 Lua 词法安全的方式在语句边界把产物整包拆行（995 行、语法合法的
  Lua 5.1）→ 产物失效（不再产出正确输出）。

#### 第 2 项续做：`m_main` / `m_init_map` 打乱 —— `src/VM/packer.rs`

- `m_init_map` 里 4 个 `while true do ... if i > n then break end ... end` 全部改写成
  `i = 0; while i < n do i = i + 1; ... end`，去掉「先判断后累加」的教科书骨架。
- `m_main`：
  - `for part in gmatch(...) do ... end` 改成显式迭代器
    （`local it = gmatch(...)` → `while true do local t = it(); if t == nil then break end; ... end`），
    带一个恒不成立的影子守卫；
  - `stage2` 里 16 条状态初始化赋值**打乱顺序**（`data` / `len` 两行有依赖，保持原序）；
  - 状态表的 32 个键值对**打乱顺序**（每对之间用 `, ` 连接）；
  - 相关局部名全部随机化。

**坑（已写进代码注释）**：`rename_state_fields` 是按 `s.<源字段名>` 字面做替换的，
新写的片段里**必须写源字段名**（`s.idx`、`s.buf` …），直接写最终随机名会让该处漏改，
运行时字段对不上（第一次编译后产物直接报
`bad argument #1 to 'JW' (string expected, got nil)`，改成源字段名后正常）。

#### 本轮验证

- `cargo test --release`：**83 passed / 0 failed**（基线不变）；
- spot **16/16**（`print` / `Comptesting` / `closures` / `multiret` × 普通+MB × 2 轮，
  语法 + 运行输出逐字节一致）；
- 宽测 **16/16**（`hash` / `benchmark` / `loops` / `strings` / `metatables` / `fibonacci` /
  `upvalues` / `state-machine` × 2 模式；benchmark、hash 只过滤计时行）；
- 三产物（普通 / MB / 伪装）lua5.1 + luau 双语法通过；前两个输出与源文件一致；
  伪装产物仍不含明文字段名（`loadstring` / `getgenv` 命中是因为**原文件自己也用了**
  这两个全局，不是泄漏）；
- samples 刷新：`print.obfuscated.lua` **106,807 B**、`print.obfuscated.MB.lua` **55,309 B**、
  `U4f2aU88c5.obfuscated.lua` **137,854 B**。

#### 要求进度（最新）

| # | 要求 | 状态 |
|---|---|---|
| 1 | 十六进制全大写 | ✅ §5.12 |
| 2 | 不能有清晰的运行逻辑 | 🟡 解码链（§5.13）+ 本节的 `m_main`/`m_init_map`；VM 执行侧 §5.9；其余零散处继续 |
| 3 | 代码块紧密耦合、互相嵌合 | ⏳ 未做 |
| 4 | 能动态生成的就动态生成（常量） | ⏳ 未做（ChaCha sigma §5.12） |
| 5 | 加密不要有明显特征 | ✅ §5.12 |
| 6 | 解密逻辑也要打乱 | ✅ §5.13 |
| + | **反美化（行号校验）** | ✅ **本节 guard 8** |


### 5.15 压缩算法替换：MB 模式改用新自解压外壳（已采用）

用户上传了 `压缩.rs`（仓库根目录，768 行）并要求：

> 不需要他的水印和报错，只用它这个算法替换现在的，看输出是变小还是变大；变小就替换，
> 变大就不改动。解压速度也测一下，比现在快而且体积还小就采用这个新的。

#### 实验（先量，再改）

- 新算法（`压缩.rs`）与上一代 MB 管线里的 `encode_stream` 是同一族 DP/LZ，但强在：
  4/3 字节 hash 链（最多 96 候选，上一代是暴力扫全部历史位置）、`MAX_SPAN` 4096（上一代 258）、
  link cost 17 bit（上一代 25 bit）；输出同为「每 8 个 token 一个 header 字节」，
  但 span 用 255 进位的变长编码。
- 三种组合的产物体积（同一份 `processed_vm`，106,786 B）：
  | 组合 | 产物 |
  |---|---|
  | (a) 新外壳直接包 | 51,836 B |
  | (b) 先过源码级 `Compressor` 再包 | 51,836 B（源码级压缩对这份输入 **零收益**：106,786 → 106,787 B） |
  | (c) 包完再让源码级 `Compressor` 压 | **不可行**：它的解析器不认 `for i = a, b, step do` 这类带步长的数值 for |
  | 上一代 MB 管线 | 55,309 B |
- 因此**新外壳直接包**即最优，`(c)` 那条老形状必须去掉（否则产物直接生成失败）。

#### 实装

- 新增 `src/packer/shell.rs`（从 `压缩.rs` 适配，30 KB）：
  - 自带 sfc64 随机源，不再依赖原项目里才有的 `crate::random`；
  - `wrap(source, seed) -> Option<Shell>`：压不出收益时返回 `None`，调用方回退旧管线；
  - 交付前 `finalize()` 做**词法单行化 + 局部名随机化**（逐字符扫描，字符串/长字符串/注释
    全部按词法处理）——产物必须保持极少的物理行数，VM 里的反美化守卫（§5.14）就是靠
    「同一物理行上定义的函数 linedefined 相等」做判据的；
  - 按用户要求去掉原版的全部 `"XXS ..."` 水印与 e1..e9 错误码：`G.error()` 不再带文本、
    `pcall` 不再传 chunk name、assert 只留条件；环境探针（`string.char`/`byte` 往返、
    loader 存在）逻辑保留，失败路径不再有可读文案。
- 管线改动：
  - `src/packer/mod.rs`：`pack_lua` 换成新外壳；上一代实现原样保留为
    `pack_lua_stub_v1`（不再参与产物生成，留作对照与回退）。
  - `src/main.rs`：MB 分支不再叠一层源码级 `Compressor::compress`
    （新外壳负载已是压缩态，再解析一遍只白花时间，而且它的解析器也不认新外壳里的语法）。

#### 配对实测（8 组，每次生成的产物都不同，交叉测量抵消系统漂移）

| 指标 | 上一代 MB 管线 | 新外壳 | 差 |
|---|---|---|---|
| 产物体积（中位） | 55,631 B | **50,463 B** | **-9.3%** |
| 运行时间（解压 + 加载 + 执行，中位） | 196.4 ms | **134.9 ms** | **-31.3%** |

逐对 8/8 全部都是**更小 + 更快**（时间对：201→136、167→103、193→142、173→101、197→114、
209→134、217→138、196→139 ms）。

#### 全量验证

- `cargo test --release` 83 passed / 0 failed；
- spot 16/16、宽测 16/16（同上两节的口径，含 MB 模式的 8 个语料）；
- 三产物（普通 / MB / 伪装）lua5.1 + luau 双语法通过，前两个输出与源文件逐字节一致；
- MB 产物是**单个物理行**（反美化守卫的前提不变），产物里查不到 `XXS` / `Kryvex` 等标记；
- samples 刷新：`print.obfuscated.lua` **105,037 B**、`print.obfuscated.MB.lua` **50,658 B**、
  `U4f2aU88c5.obfuscated.lua` **139,104 B**。

> 说明：`压缩.rs` 原样留在仓库根目录作参考（不参与编译）；真正参与构建的是适配后的
> `src/packer/shell.rs`。


### 5.16 字符串加密加强 + 行号守卫去明文（已做）

用户要求（附了产物片段）：

> 把 `local nO=Un("\220\222\207\210\213\221\212",0XBB)` 这种的加密加强，至少不能一眼就算出来，
> 也不需要很复杂，不用使用 chacha 这种算法；然后再把行号检测里的所有明文字符也像刚才那样加密。
> 提示：在使用 string 这种库函数时一个作用域超过两个最好先 `local x=string.xxx`，这样代码更小。

#### 一、`Un`（探测串解码器）加强：单字节 XOR → 位置相关两字节混合

- 旧：`c = p XOR k`，密钥是**单字节常量**。破绽很直接——「密文 − 明文」在整串、所有串上都是同一个
  常量，拿两条调用比对就能反推；密文也总落在可打印区附近。
- 新（两处共用同一套）：
  ```text
  c[i] = ((p[i] + k1) % 256) XOR ((k0 * i + k1) % 256)      i 从 1 开始
  解密：先按 (k0 * i + k1) % 256 异或，再减 k1
  ```
  密钥是 **16 位**（写成 `0XNNNN`，k0 恒非 0，否则退化成单字节密钥），逐串随机；
  生成时保证密文里不出现 `\000`。位置项让「同一明文字符」在不同下标上得到不同密文，
  心算不出规律，也不需要 ChaCha 这类算法，解密仍然是纯算术（不碰 bit32 / bit）。
- 端到端验证：从**产物里**抠出解码器函数定义 + 9 个调用点，单独跑 `lua5.1`：
  输出正好是 `getinfo|what|C|source|=[C]|getgenv|getrenv|loadstring|load`。

#### 二、守卫池同源加强

- 池加密原来是 `(b + key) % 256`（单字节加法，也很容易被看出来）→ 换成上面同一套混合，
  池级 16 位密钥逐产物随机，22 个条目共用一组（解码器只需带一个常量）。
- 池解码器相应改成两步（先异或位置密钥、再减 k1），位异或仍是 8 轮纯算术循环。

#### 三、行号守卫（`§5.14` 的 guard 8）去明文

- 守卫里的**字符串**全部改走池：`'getinfo'`、`'S'`、`'l'`、`'linedefined'`、`'currentline'`、
  模式串 `':(%d+)[:\r\n ]'`，以及类型名 `'table'` / `'number'` / `'function'` / `'string'` ——
  一律 `{dec}({hash})` 取，产物里不再出现。
- 守卫里的**全局名**也改走池 + 环境表：`type` / `debug` / `tonumber` / `pcall` / `string`
  一律 `{res}({hash})`（`res` = 池解密后查环境表）。
- 采纳用户提示：一个作用域里 `string` 的成员用到 4 次，就先
  `local sb,sc,sf,ss = st.byte, st.char, st.find, st.sub` 再调用 —— 比写四遍 `{st}.xxx` 短。
- 产物实测：`getinfo` / `linedefined` / `currentline` / `%d+` / `'l'` / `'number'` 命中数**全部为 0**。

#### 踩坑（两条，都已写进代码注释）

- **Rust `format!` 的隐式捕获**：模板里写 `{k1}` 会被当成「作用域里的变量 k1」而不是字面量，
  而 `AntiTamper` 里恰好有个载荷加密用的 `k1`（随机字节）——产物里于是出现
  `189=(189+RwgdZNtVLGKla)%256` 这种给数字赋值的片段，`luac -p` 直接报语法错。
  **固定局部名必须写成裸文本 `k1`，不能加花括号。**
- **顺序**：`local {st}={res}({h_st});` 必须排在第一次用 `{st}.byte` **之前**（Rust 续行符
  只是把源码行拼起来，顺序照旧）；第一版把定义写在后面，产物里 `gX.byte` 引用了一个还没
  local 的全局 `gX`，运行时报 `attempt to index global 'gX' (a nil value)`。

#### 验证

- `cargo test --release` 83 passed / 0 failed；
- spot 16/16、宽测 16/16（普通 + MB 两种模式）；
- 三产物 lua5.1 + luau 双语法通过、前两个输出逐字节一致；
- 反美化守卫仍然两头都对：**守卫段内插一个换行 → 产物卡死（rc=124）**；
  **文件末尾加换行 → rc=0、输出一致**（不误杀）；
- samples 刷新：`print.obfuscated.lua` **104,568 B**、`print.obfuscated.MB.lua` **52,306 B**、
  `U4f2aU88c5.obfuscated.lua` **138,796 B**（池从 11 条扩到 22 条、解码器多两步，MB 产物
  比 §5.15 那版大约 +1.6 KB，换来的是产物里不再有可读的字符串特征）。

> 说明：**其它守卫**里还留着少量明文（`'S'` 1 处、`'table'` 5 处、`'function'` 5 处、
> `what`、`loadstring`）——用户这次只要求「行号检测」那段，所以那些**没有动**。


## 6. 换一台机器怎么复原

见 `环境部署.md` §0（一键脚本）与 §5（七条验收清单）。

## 7. 待用户拍板的事

1. ~~8 个 `* (1).rs` 和 `src/main.rs.bak`~~ —— 已按指示删除。
2. **§5.2 的 0.7% 残留缺陷**（压缩器）是否作为下一个工作项？已经有固定输入的复现方法。
3. ~~`src/compiler/codegen.rs` 超过 80 KB~~ —— 已按指示拆成 `src/compiler/codegen/` 6 个文件，见 §5.3。
4. 全库跑一次 `cargo fmt`？（一次性约 8,500 行 diff，之后 `test-matrix.sh` 的 fmt 门才能过）
5. `tools/test-matrix.sh` / `bench-vm.sh` 是给另一套（更新的）源码写的，是**改造脚本适配当前源码**，
   还是**保留脚本、把源码往那个结构演进**？

## ⚠️ Roblox 兼容性事故记录（V2 回退）

- **现象**：执行器（Roblox LocalScript）上产物报 `loadstring) is not available in RobloxScript context`，
  栈帧全是 Line 2 的短名闭包。裸 `loadstring("return 1+2")` 在同一执行器**可用**。
- **二分定位**（产物存 `samples/roblox-test/`，print+伪装两份/版本）：
  v1=`e9d6d8e` ✅、v2=`a181d92` ✅、v3=`a45467a` ❌ —— **分界提交 = `a45467a`
  （明文全清 + 行号校验改隐式判定「弯弯绕绕版」）**。该提交把行号校验从显式改成
  隐式链式 loadstring 调用，怀疑这种调用形态触发了执行器的拦截（不是 loadstring
  本身被禁）。
- **处置**：整库回退到 V2（`a181d92`）内容：源码、主样本、交接总结全部恢复；
  §5.19/§5.20/§5.21 的守卫/取值器/池拆分工作**全部搁置**（提交仍在历史里：
  `78c8fbe`、`d78709f`、`dd8fa25`，均含 V3 分界之后的改动，重落地前必须先修
  行号校验的调用形态——比如换成与 V2 相同的显式结构、或探测执行器是否放行）。
- **遗留**：`samples/roblox-test/` 保留作诊断记录；若后续要重做「明文全清+
  隐式行号校验」，需在 V2 基础上以执行器友好的调用形态重写，并过 Roblox 实测。

### 第一步重做实测：也报错（已回退 `df6e580`）

第一步（只拿 `a45467a` 的池清理、行号守卫保持 V2 显式结构）产物上 Roblox 实测：
**同样报 `loadstring) is not available in RobloxScript context`**。
说明触发点**不在行号校验的写法**，而在池清理部分本身——嫌疑收窄到三处之一或组合：
① 守卫/探测字符串改走池（`dec(哈希)`/`res(哈希)` 的字符串加载形态）；
② packer 探测带池（`Some(pool)`）后的调用形态；
③ 解码头/方法原型的 `__index`/`__mode`/type 改动。
若后续还要做明文清理，需在 V2 上**小步**做（每次只落 ①②③ 中的一项）并逐步 Roblox
实测；当前结论：**V2 的明文水平就是该执行器的安全边界**，不动为妥。

### 行号校验拆分管线版（V2 语义不变，明文不动）（待 Roblox 实测）

背景：用户实测确认「loadstring not available」报错 = 行号校验**触发**的表现
（校验发现被改 → 崩溃路径的 loadstring 被执行器拦下）。V2 的校验在 Roblox 上
有效（加一行 print 触发 ✓），V3/第一步的失败是校验被**误触发**。用户指令：
把行号校验拆成多个 function、打乱线性逻辑、伪装成其他功能，**判定逻辑逐条不变**，
明文先不动。

实现（`AntiTamper.rs` 守卫 8 臂重写，其余一概不动）：
- 拆成三段闭包管线：①能力采样（解 string/type/debug/pcall/tonumber + 类型名，
  逐项 fail-open）→ ②测量（f2/f3 的 linedefined、f1 报错行两种读法、当前行）
  → ③判定（u/v 双路读、(l1-l1)+l1~=l2、报错行 tonumber vs 当前行——与旧链
  **逐条相同**，fail-open 条件也逐条相同）；
- 键（0X 随机互异）乱序注册、顺序表按逻辑序驱动 `T[o[j]]()`，数据全走闭包
  upvalue（hoist 一串 local，f1/f2/f3 仍在守卫体内定义）——读起来不再是
  行号校验梯子；
- 字符串照旧走 V2 同一个池、同一批哈希，明文零新增；池、探测、解头、其它守卫
  全部保持 V2。

踩坑：`{msg}=''` 被写成 `\'\'` 过度转义，minifier 重序列化字符串字面量时
不转义内嵌引号 → 产物出现 `''''` 语法错（压缩器「缺少 ''」报错）。
**规则：守卫模板里的空字符串一律写裸 `''`，不要任何转义。**

验证：cargo 83/0；语料 28/28；print 12 连跑；普通/MB 输出一致；
篡改矩阵：未压缩 f2/f3 插换行 → rc=124 卡死；压缩产物 f2/f3 插换行（锚
`return 0X0002` 形态）→ rc=1 拦截；good 增量 (k+19→k+20) → rc=124；末尾
换行 → rc=0 不误杀；**+print 行行为与 V2 完全一致**（本地 lua5.1 两版都不
触发——Roblox 上触发是执行器行号环境差异，V2 原生如此）。

samples：print **105,581 B**、MB **51,597 B**、伪装 **138,529 B**；
Roblox 测试副本 `samples/roblox-test/step2-linefn-{print,disguise}.lua`。

### 自定义流加密隐藏明文（独立于池；待 Roblox 实测）

用户指令：写一个自定义流加密（不用太强），加密现有明文，**不用池**。
动机：V2 池（22 条）本身在执行器上是好的，step1 失败在「池扩容 + 新站点」，
所以这次绕开池——字符串不进共享表、不做哈希查找，每个调用点自己带
`DEC("\\ddd…", 0XKKKK)`（随机 16 位密钥），靠作用域里一只小解码器就地还原。
密码本体与探测块 dx 同族（位置相关双字节混合、纯算术 XOR、不用位库），
解码器形态与探测块一致——执行器已验证过的形状。

覆盖点位（目标明文 → 全部 0）：
- probe 体（Generator_util）：'S'/'table'/'function'（类型比较 + pcall 选项；
  debug/pcall/loadstring/load 裸全局访问**保持原样**——那是执行器验证过的形态，不碰）；
- 守卫 0（'nil'）、1（__add/sub/mul/call）、3（'table'）、7（'table'×2 +
  tostring/index/newindex/call + 诱饵 eq/concat，连 __ 前缀整串加密）；
- 崩溃函数（'table'）、trigger 毒表（__index）；
- Generator：'#' select、方法原型 __index、解码头 type 比较/'__mode'/'k'、
  body_consts 的 __index 与 "KryvexObf_" 前缀；
- packer：gmatch 模式 "[^~]+"、"return " 前缀、"kryvex" chunk 名。
- MB 外壳残留 1 × "function"（壳层自带，v1/v2 即有，不动）。

解码器共用：AntiTamper 在 setup 最前定义一只（chunk 顶层作用域，守卫与
Generator 全部部件同作用域复用，名字走 AntiTamperResult.sc_fn）；packer 脚本
独立作用域自备一只；stub（legacy）同。

踩坑：守卫 7 的 meta_lines/trigger 等替换曾因脚本中途断言失败而**静默丢失**
（replace 后没走到 write）——python 补丁必须「改完即写」，长脚本拆步跑。
好路径 hop (k+N) 在 if/else 阶梯里有**多份副本**，篡改实验必须全量替换同一
delta 才能测出拦截（单份替换是假阴性）。

验证：cargo 83/0；语料 28/28；print 12 连跑；普通/MB 输出一致；普通产物
目标明文全 0（KRYVEX 水印保留）；篡改矩阵：f2/f3 插换行 rc=124、good 增量
全副本 bump rc=124、末尾换行 rc=0 不误杀、+print 行 rc=0（与 V2 行为一致）。

samples：print **110,395 B**、MB **53,399 B**、伪装 **137,192 B**；
Roblox 测试副本 `samples/roblox-test/step3-streamcipher-{print,disguise}.lua`。

### ③④⑤ 重做 + 解密调用表化（dec(T[x],T[x])；待 Roblox 实测）

用户指令：做 ③守卫互锁 ④常量运行期生成 ⑤加密特征打散（参照 `908f795`），
并把流加密改成「**密文与密钥存进同一张表，调用形如 `dec(T[xxx],T[xxx])`，
`dec` 本身藏起来不显眼**」，同时打散解密代码的逻辑数据流。

**StreamTable（Generator_util）**：一张表同时装「匿名解码器 + 密文 + 密钥」：
- `st.call(plain,k0,k1)`：密文/密钥各占一个随机 u32 表键（0x1000_0000..0x7FFF_FFFF
  互不重复），返回三种等价拼写轮换的调用式 `T[dk](T[ck],T[kk])` /
  `(T[dk])(T[ck],T[kk])` / `T[dk](T[ck],(T[kk]))`；
- `st.emit()`：条目**洗牌**后渲染 `local T={[dk]=function(s,k)…end,…}`——
  解码器没有名字、和密文密钥混在同一个构造器里，首眼看不出哪项是 dec；
- 作用域：AntiTamper 一张（`AntiTamperResult.st`，替代原 sc_fn；Generator 的
  解头 fn/mode/k、'#'、原型 __index、body_consts 共用）；packer 脚本一张；
  MB stub 一张（名字也走 NamePool，无固定指纹）。

**③ 守卫互锁**（AntiTamper）：
- 9 个守卫挂在互不相同的随机 u32 键上；定义/调用/触发各写同一个值的
  **不同派生算式**（derived_num 三式：`(0XA-0XB)`/`(0XA+0XB)`/`(0XA*0X2+0XC)`），
  文本搜索对不上号；
- 全链共享一个运行期状态 `R`（`R=(R*A+B)%2^32`，A 奇 <997、B<2^32 ⇒ 精确）；
  每块入口先推进 R，再把「实算−应有」混进令牌：`net[next](k±delta+(R-0Xstate))`
  ——链路完整时该项恒为 0，少跑/改跑/换序任何一块，令牌被污染 → 链尾跳表
  （`jump[expected]=function()end` + `__index`→爆栈）卡死；
- 网表挂 `__index`→爆栈陷阱门；2 条形状一致的诱饵（从池取值、动状态、不参与链）。
- **守卫键从顺序 0..8 改为随机键**：`next_good/next_bad/single_guard_raw` 全部
  改用 `derived_num(keys[i+1])`/`derived_num(keys[i])`。

**④ 槽位键运行期生成**（`GenRng::slot_key_block(20)`）：pc/stk/top/ops/… 20 个
槽位号不再写死——产物里是一段线性同余序列 `local x=0Xs;local T={};for i=1,N do
x=(x*0Xa)%0Xm;T[i]=x end;local n1,…,n20=T[...]`，20 个局部名收运行期算出的键。
`state_fields/hot_locals` 的键类型 i64→String。

**⑤ 加密特征打散**：
- 滚动层常数逐产物随机（Rust 正向与 Lua 读取器逆向由同一组变量生成）：
  `sc_add`(奇 1..255)、`sc_rot_in/add_k1(1..7)`、`sc_mul_k2(2..7)`、
  `rot_k2/rot_k4(1..7)`。**注意**：k1 的 `+add_k1` 是**状态更新**不是逆运算，
  Lua 侧必须同样 `+add_k1`（写成一逆就 −，解密流从第 2 字节就错位）；
- 池键哈希参数逐产物随机（`HashParams{mult(奇),add,seed}`；Lua 循环
  `h=(h*M+byte+A)%2^32`），撞键重抽；毒药串防撞循环；
- g3 守卫的 djb2 常量 5381/33 换成 hash_params() 三参数。

**本轮踩坑（全部已销案）**：
- 陷阱门用了 `poly_hash("setmetatable")/("__index")` 但没把这两个串**登记进池**
  → 查池得 nil → 构造器 `[nil]=…` 报 `table index is nil`。凡 poly_hash 查池的
  串必须在 strings vec 里登记过；
- `finish_setup()` 必须在**所有 st.call 注册之后**、`out.push_str(&at.setup)` 之前
  调（emit 的表声明要领先于全部调用拼写）；
- MB 外壳 `shell.rs::finalize` 的随机短名**不过滤 Lua 关键字**，1~3 字符名约
  3%/产物 撞上 `in/or/if/do`（e9d6d8e 就有的旧病，被语料暴露）→ 加 KEYWORDS 表；
- 篡改矩阵的 bump 测试必须穷举 delta 的**全部进制拼写**（minifier 会把 75 写成
  `0X4B`/`0X004B`/`0X00004B`——只换十进制是假阴性）。

验证：cargo 0 错 0 警；语料 28/28；print/benchmark MB 各 12 连跑全过；语法套件
luac5.1+luau-compile 三样本全过；篡改矩阵：8 个 hop delta 全拼写 bump → 8/8 拦截、
末尾换行 rc=0、+print rc=0（V2 行为）、入口函数体内插行 rc=0（与 daa37dd 基线
完全一致）；明文残留逐词 == daa37dd 基线（无新增泄漏）。
顺带修掉语料暴露的旧病：MB 外壳随机名撞关键字（见上）。

samples：print **100,417 B**、MB **53,426 B**、伪装 **129,972 B**；
Roblox 测试副本 `samples/roblox-test/step4-tabledec-{print,disguise}.lua`
（用户 Roblox 实测后报精确字节数）。

### 三块解密部件的 Luraph 式打散（用户贴 beautifier 可读片段点名；待 Roblox 实测）

用户指令：Roblox 无报错后，把 beautifier 之后还能一眼读懂的三块逻辑数据流打散
（用户原话贴了 u32 拼装、滚动读取器、常量缓存三段）。全部落在 Generator.rs：

**① u32 读取族（read_u32/a5/rs/a10）**：
- 收集三选一：表+while / 逐局部独立语句（声明方向随机，反向时名字表倒写但
  **逗号不能少**，且左名字收右值 ⇒ 字节归属镜像）/ 表+repeat until；
- 拼装三选一：霍纳 / 双 u16 段拼（lo=b1+b2·W、hi=b3+b4·W，段序权重随机）/
  乱序加权和（权重拼写 2^8|256、65536|2^16、16777216|2^24|2^16·256、项序洗牌、
  零种子 0|(bx-bx)|(by·0)）；两套函数独立抽签；
- rs 循环壳 while/for 随机（while 壳计数器**必须就地 local 初始化**）；
- a10 三段变形：阈值 2^31 拼写（2^31|2^30·2|2147483648|2^16·2^15）、
  条件（2*h-h|not(v<h)|v-h>=0）、减法（v-h-h|v-(h+h)|v-2*h）。

**② 滚动读取器**：逆运算四步（-sc_add → xor k4 → rotr k3%8 → +k2 → xor k1）
与状态更新三组（k1/k2/k34）全部拆成**随机整数键**的表条目（键互不相同、注册
顺序洗牌），读取器本体只剩「原始字节 → 按调度表跑 4 步 → 按键调三组更新」；
链的真实顺序只编码在键值与调度表里；k1/k2 更新组调用序随机（实参随之对应：
k1 吃解后字节、k2 吃原始字节、k34 最后）；循环壳 while/repeat/单次 for 随机 +
永不触发的假出口。**铁律：表键必须是字面量数字或已声明的局部——未声明的标识符
在 Lua 里是全局 nil，`T[名字]=…` 直接 table index is nil。**

**③ 常量池查表（body_consts）**：tag 三路 if/elseif 阶梯 → 两张随机键调度表
（键 = obfuscate_num 展开式，运行期才是 1/2/3）：读入侧 tag→装载闭包、访问侧
tag→解码闭包；注册顺序各自洗牌；各挂一条永不命中的诱饵键；未命中一律静默吞掉
（fail-open）；缓存检查拆成独立前哨闭包（缓存值只可能是 string/number/false，
~=nil 判命中无歧义）；`mt_consts` 固定名也换成了逐产物随机名。

**本轮踩坑（教训密集，全部销案）**：
- a10 阈值抄错：原文 `v>=2*h-h` 求值 = h = **2^31**（遮眼写法），我「简化」成
  `h+h` = 2^32 → 条件永假 → 负常数永不转符号 → VM 拿巨大无符号值跑飞、随机报
  compare nil。变形前必须先算清等式两边；
- collect B 反向声明漏逗号（`local a b c d=` 语法错，间歇性生成失败）；
- rs while 壳漏了计数器 `local i=0` 初始化（间歇性 nil 比较）；
- 双 u16 段内字节序写反（lo 应 b1+b2·W）；
- 步骤表键用未声明的随机名 ⇒ 全局 nil 索引；调度表同理漏 `local x={}`。
  共性：**打散生成的每一段都要当成新手写的 Lua 过一遍语法/作用域**。
- 沙箱重置曾把工作树打回旧 main：恢复 = fetch + `git reset --mixed FETCH_HEAD`
  + `git checkout -- .`，未推送的编辑先备份 /tmp 再重放。

验证：cargo 0 错 0 警；语料 **28/28**；loops ×20 / strings ×10 / print MB 与
benchmark MB 各 12 连跑全过；语法套件 luac5.1+luau-compile 三样本全过；篡改
矩阵：全 delta bump（20 处全进制拼写）拦截、末尾换行 rc=0、+print rc=0（V2 行
为）；明文残留逐词 == daa37dd 基线。

samples：print **98,366 B**、MB **54,038 B**、伪装 **128,536 B**；
Roblox 测试副本 `samples/roblox-test/step5-scatter-{print,disguise}.lua`。

---

## 形态 1–5 落地（解密链全量控制流化，本轮收官）

**范围**：Luraph 14 类形态清单前 5 类落到解密链与读取族；不实现 Roblox/Luau
特有料（产物纯 Lua 5.1）；已有形态（二分树/双调度表/u32 头变体/a10 变体）
结构不动，新形态叠加。终态目标=整个文件都有控制流，后续轮次继续 6–14。

**在位形态**（Generator.rs，全为逐产物随机生成）：
- **① 返回码 handler 协议**：`__index` 拆三 handler 挂随机名注册表
  （kA=旗标+memo：命中→RET/未中→NEXT/旗标假→FAIL,(kobf..ix)；
  kB=装载+dsp：缺→NIL/齐→VAL,dec(PE)；kC=落缓存→RET），驱动 while-true
  按返回魔数换相（初相 kA、NEXT→kB、else→kC 且 aux=值、RET/F/NIL→return）。
  返回魔数与注册键**全部走 K 表派生**（`K[i]±K[j]` 表算术拼写，比较与赋值
  同串恒一致）。
- **② 状态常数运行期派生**：K 表 9 魔数（1<<20..1<<24 逐产物随机）→ 12 个
  派生值（m_a~m_d 读取梯子态 + h_ka/kb/kc 注册键 + 5 个 HH 返回码），
  互异重摇守卫 `vs.len()==12`（**曾经 7，扩到 12 忘改断言 → 生成期死循环
  rc=124，已修**）。
- **③ else-if 状态梯子**：u32/a5/rs 三读取器 3 态梯子（初值=K 派生式、
  两次比较换相、兜底臂执行本体后回 A）、a10 两态梯子（含单否定真臂）。
- **④ 反转臂**：rs 零检查极性修复版 `if LK~=(池(x,y)))then else return''end`
  （**极性教训：`==`错臂→非空串返''不消费载荷→流错位→nil/尾 OOB；修复=取反
  条件保反转臂形态**）。
- **⑤ 不透明谓词**：PJ 恒空表（0X10000..0xFFFFF 随机键，永不命中）双否定
  `not(not PJ[k])` 诱饵臂 ×（3 读取器+junk 臂）+ 读循环探针包裹。

**hash「挂死」真相（两案并结）**：
1. `hash.lua` 是纯数值基准（纯 Lua SHA-2+基准循环），VM 下慢约 215×
   （原始 0.18s → 产物 38s）。此前 15/25/30s 超时全是自己掐死的**假挂死**；
   60s 语料预算下稳定通过。驱动逐轮日志证实 HH 三相每常数正常解码
   （unpack/concat/byte/char/rep/sub/format/floor 全真值）。
2. 真死循环另有一起：互异断言 `vs.len()==7` 未随派生值扩容更新 → 生成期
   无限重摇 → 全语料「生成 rc=124」。断言改 12 后消失。
- 另：本轮源码里 HH 声明段曾被插两份（x_ret1 count=2，Rust 遮蔽掩盖了它），
  已删重复段。

**跨产物对比铁律（再犯再记）**：任何「产物级取证」（插桩、常数核对、矩阵）
必须先 `cp obfuscated.lua /tmp/prod_xxx.lua` 钉死单一产物；提取/生成命令会
顺手重生成 obfuscated.lua，跨代数值不可比（本轮 spin=产物A、矩阵=产物B 差点
again）。插桩三雷：python heredoc lambda 前向引用 NameError 且文件没写出；
计数全局须 `(G9 or 0)+1` 且小心压缩器把同名局部遮蔽；插入文本必须以空格结尾
（否则拼出 `endif` 语法错）。

**验证**：cargo 0 错；语料 **28/28 × 2 连跑**；loops ×20 / strings ×10 /
print MB ×12 / fibonacci MB ×12 全过；luac5.1 -p + luau-compile 三样本全过；
拦截 survey：delta bump 42 处→8 处拦截、state bump 42 处→8 处，**与补丁前
基线产物完全同水平（41 处→8 处），非回归**；头 print rc=0 与尾换行 rc=0
均为已接受现状（V2 不误杀无关编辑）。

**字节数**：Generator.rs **79,733 B**（≤80,000）；samples：print
**102,915 B**、MB **54,947 B**、伪装 **101,393 B**；Roblox 测试副本
`samples/roblox-test/step6-flow12-{print,disguise}.lua`。

**快照与重放**：权威快照 `/tmp/Gen_form15_final.rs`（沙箱重置后
`cp /tmp/Gen_form15_final.rs src/VM/VM_Backend/Generator.rs` 即可）；
`/tmp/flow_patch.py` 已标弃用仅作考古。

---

## 14 形态权威清单与完成度（用户提供原文，本节为编号基准锚点）

> 权威清单由用户在形态①~⑤收官后重发（此前仅对话存在）。约束：产物必须纯
> Lua 5.1（十六进制大写 0X+A–F），**Luau/Lua5.2+ 专属语法一律排除**——
> 二进制字面量 0b/0B、下划线数字、continue、复合赋值（*=、+=）、\xNN、
> \u{}、\z、bit32 库。下表「完成度」以当前产物（step6-flow12，1d702d8）
> 实测 grep 计数为据。

**① 返回码状态机（标志形态）— ✅ 完成**
91 个小 helper 不互相直接调用，而是 return 58351 这类随机魔数；调用方
while true do x=R:jG(...) if x==49282 then break; elseif x==0XE3E_F then
continue; end end。调用关系被「魔数协议」间接化，47 处魔数比较，值逐产物
随机。（落地：HH 三 handler+驱动换相，注册键/返回码全 K 表派生）

**② 下一状态常数=运行期派生 — ✅ 完成**
X=-15241+(j[0X2A][0B11010](...))：状态转移目标经过 buffer 读/位运算伪装成
数据依赖，不写成字面量。（落地：K 表 9 魔数→12 派生值，梯子态/注册键/
HH 码全派生式，比较与赋值同串恒一致）

**③ 主解释器=else-if 状态梯子 — ✅ 完成**
if T==3 then … elseif T==0x1 then …（T 为取指变量）；刻意混用**扁平
elseif（177 处）与嵌套 else if（202 处）**两种风格，美化后嵌套深度剧增。
（落地：u32/a5/rs 三态+a10 两态梯子，扁平 elseif+嵌套 else if 混用在位）

**④ 真代码藏进反转臂 — ⚠️ 主体完成，子项未做**
if T[0x25]==206 then else <真代码> end（161 处 then else）：恒假条件的
then 臂为空、else 臂才是本体；同样手法有 if O~=0x51 then else <真代码>
end。配 9 处 while 0xa9 do return … end 常数条件直行包装（循环体只走一次，
靠 return 出）。（落地：rs 零检查反转臂×1；**缺：反转臂扩量、while 常数
直行包装（当前产物 0 处）**）

**⑤ 常量表探测（不透明谓词）— ⚠️ 主体完成，单 not 极性未做**
if not j[31399] then / if not(not T[4720]) then：随机 u32 键查表恒 nil，
真代码与诱饵/包装分居两臂，且同键以 not/not not 两种极性出现。（落地：
PJ 恒空表双否定×4；**缺：单 not 极性（当前 0 处）、同键双极性并存**）

**⑥ for 序列跳转 — ❌ 未做**
for J=0X6f,252,0X65 do（迭代值 111→212）：怪异 start/end/step 让循环变量
走随机数值路径，体内按区间分支把直线程序切成若干段——循环计数器兼任状态
变量。（Lua 5.1 可做，只换 0X 拼写）

**⑦ while true + continue/break 跳转网 — ⚠️ 部分在位**
76 个 while true、101 个 continue、92 个 break；另有 13 处 repeat…until
false 纯换皮。三种循环壳随机轮换。（现状：while true×9、repeat…until
false×3、break×14；**continue 按约束排除**（Luau 语法），等价物=break+
循环尾布尔；**缺：换壳轮换扩大、break 网密度**）

**⑧ 形参重名遮蔽 — ❌ 未做**
59 处 function(R,R,T,y)：self 被第二参遮蔽，名字追踪直接失效；还有
_U=function(R,R)R=0X1;return R;end「常数提供者」函数（连常数都要过一次
调用）。（Lua 5.1 可做；当前产物 0 处）

**⑨ 区间二分树代替跳表 — ⚠️ 既有形态，解密链未叠加**
if not(O<=0B0101110__)then if not(O<=0X035)then…：not(x<=k) 双重否定+
嵌套区间树派发，与等值比较混用。（二分树调度早已在位且**结构不许改**；
**缺：解密链新叠加区间树**；二进制拼写排除，用 0X）

**⑩ 常数=位运算派生式 — ⚠️ 部分在位**
(-75+((R.YS(T[0X006928],(T[0x56__2c])))+T[…]))：产物里没有裸常数，全部由
随机表 w+状态经 bit32 链现算，深括号嵌套。（现状：obfuscate_num 加减/xor
派生 + K 表派生 + 隐藏 dec 流密钥已覆盖大头；**bit32 按约束排除**（5.2 库），
等价物=自写 AND/OR/XOR 闭包或纯算术链；**缺：成体系的深括号位运算链**）

**⑪ 数字全格式混拼 — ⚠️ 5.1 子集在位**
同一文件 0X 大写 1467、0x 小写 488、二进制 0b/0B 819、下划线分隔 301；
同值每次出现换拼写。（**二进制/下划线按约束排除**；现状=0X 大写与十进制
逐出现混拼已做；**缺：0x 小写是否解禁由用户定**，默认维持全大写规则）

**⑫ 字符串混合转义打碎 — ❌ 基本未做（5.1 子集可做）**
\ddd（220）、\c、\xNN、\u{XXXX}、\z 续行混用，"\114e\u{0061}d\x751"=
'readu16'。（**\x/\u/\z 按约束排除**；**可做：\ddd 十进制转义与原字符
混拼打碎字符串**，当前产物字符串基本未转义）

**⑬ 调用形式轮换 — ⚠️ 部分在位**
冒号 R:xx(...) 148 处与点索引 xx[NN](...) 400 处混用；(y[0B100])[O]=…
括号包裹索引 280 处；Luau 复合赋值 Z*=u/j+= 84 处。（现状：冒号调用×300、
括号包裹索引×9 已混用；**复合赋值按约束排除**；**缺：包裹索引扩量、
点/冒号比例刻意失衡**）

**⑭ 单行+巨型单闭包 — ⚠️ 半在位**
全文件 1 行；主解释器约 190KB 单个方法，热路径全内联；外层处处 return nil
兜底、local a,b,c,d,e,f,g,h,i 9 元联合声明、O=nil;O=nil 冗余填充。（现状：
**产物已是 1 行** ✓；**缺：return nil 兜底、N 元联合声明、冗余 nil 填充**；
巨型单闭包受 Luau 上限约束注意分寸）

## 还剩什么没完成（下一轮开工清单，按序）

1. **④ 收尾**：反转臂扩量（解密链关键位多埋）+ while 常数直行包装 ×N；
2. **⑤ 收尾**：单 not 极性 + 同键 not/not not 双极性并存；
3. **⑥ for 序列跳转**（全新，读取器/装配段适用）；
4. **⑧ 形参重名遮蔽 + 常数提供者函数**（全新，Lua 5.1 合法）；
5. **⑦ 扩量**：repeat…until false/while true/break 网轮换加密（continue 排除）；
6. **⑨ 解密链叠加区间二分树**（既有调度树不动）；
7. **⑩ 位运算派生链**（自写 bit 闭包替代 bit32）；
8. **⑫ \ddd 转义打碎字符串**（5.1 子集）；
9. **⑬ 包裹索引扩量 + 调用形式比例失衡**；
10. **⑭ 兜底/联合声明/冗余填充**（注意 Luau 200 闭包上限，勿踩 v3 覆辙）。

约束重申（适用于上面全部）：产物过 Lua 5.1；0b/下划线/continue/复合赋值/
\xNN/\u{}/\z/bit32 一律不用；已有形态结构不改；每步 Roblox 实测再下一步。

---

## 形态④⑤ 收尾（本轮，④反转臂扩量+while直行 / ⑤单not极性+同键双极性）

**④ 反转臂扩量（终版伪装样本实测）**：
- `PJ[k]==num` 恒假探针反转臂 ×4：u32/a5 收集返回（B/AC 两模板都有）、
  kA memo 命中返回、读循环派发调用——真代码全在 else 臂；
- `x~=(池(x,y))` 已知值反转臂 ×1（rs 零检查，原有）；
- **while 常数直行 ×4**（`while 0X.. do return … end`，循环体只走一次靠
  return 出）：rs 拼接返回、kA 尾返回 NEXT、kC 返回 RET、驱动 nil 返回。

**⑤ 单 not 极性 + 同键双极性**：
- 单 not ×2：a10 第二臂（包诱饵 if）、body_protos 装载循环（真在 then 臂，
  else 臂=死诱饵 `i=n; n=0`）；
- 同键双极性 ×2 键：a10 键（第一臂双否定真在 else + 第二臂单 not）、
  pkx1 键（body_ret 双否定真在 else + body_protos 单 not）；
- 双 not 总计 ×6（u32/a5/rS junk 臂 + 读循环 + a10 第一臂 + body_ret）。

**实现要点**：全部新键（pkA/pkB/pkC/pkx1）走 `0X10000..0xFFFFF` 随机；
numA/B/C 走 10 万~999 万随机十进制（后处理会重拼成 0X 大写）；while 直行
常数 `0X51..0xFFFFF` 非零保证真值。源码 12 处模板修改+3 处 args 重复修正
（读循环 format! 原有 pj 参数，勿重复传）。

**验证**：语料 **28/28**；loops×10 / strings×5 / print MB×5 / fibonacci
MB×5 全过；luac5.1 -p + luau-compile 三样本全过（注：shell 里
`$LUA/luac5.1` 会拼成 `lua5.1/luac5.1` 假 ✗，直接写全路径）；**delta bump
拦截率 8/42 → 30/42**（反转臂探针把更多位点纳入守卫网，无误杀）；
头 print/尾换行 rc=0（V2 已接受现状）。

**字节数**：Generator.rs **79,997 B**（≤80,000，压线 3 B；注释截短释放
988 B）；samples：print **103,310 B**、MB **55,076 B**、伪装 **133,706 B**；
Roblox 副本已刷新。

**快照**：`/tmp/Gen_form45_final.rs`（重置后 cp 回
src/VM/VM_Backend/Generator.rs 即可）。

**14 形态进度**：①②③ ✅；**④⑤ ✅ 本轮收官**；⑥⑧⑫ 未做；⑦⑨⑩⑬⑭ 部分
在位待扩量。下一步=⑥ for 序列跳转。

---

## 形态⑥⑧⑫⑬ + Generator.rs 拆分（本轮）

**拆分**：`src/VM/VM_Backend/Generator.rs`（原 83KB）拆为两个文件：
- `Generator.rs` **53,055 B**（主装配：header 调用、链 body、驱动器、packer 对接）
- `Generator_flow.rs` **36,371 B**（新模块，形态共享件）：build_k（K 表派生）/
  build_scatter（滚动读取器打散）/ build_readers（u32~a10 读取族梯子）/
  build_consts（HH 常量池协议）/ build_header / build_decnum / build_decstr。
  KConsts 结构体传派生值；拆分保持 rng 调用顺序不变（产物统计特性不变）。
- 重放：沙箱重置后 cp /tmp/Gen_split_main.rs 与 /tmp/Gen_split_flow.rs 回原位。

**⑥ for 序列跳转**：滚动读取器 4 步派发改 for 区间二分（`for J=S,E,P` 怪异
start/step，体内 `if J<Q2 then if J<Q1 …` 区间树经 seq 表派发，2/3 概率抽中，
老 while 壳保留 1/3）；u32/a5 收集 D 变体（1/4 概率）`for i=S,E,P do
T[(i-S)/P+1]=rd() end`（(i-S)/P 恒整）。
- **连带修复压缩器 parser 一字符 bug**：for 步进分支检查 `Some("")` 应为
  `Some(",")`——步进从未被解析过（此前无产物用三段 for 所以未爆）。

**⑧ 形参重名遮蔽**：kA/kB `function(ix,ix)`（驱动按相切换第二参
`if cur==e_kc then a2x=aux else a2x=ix end`）；memo/ld/dsp 加诱饵首参；
常数提供者 `local function X(a,a) a=0X1; return a end`（body_init）。
- **连带修复压缩器 renamer**：新增 `declare_param`（同作用域同名参数复用
  VarId），否则改名器把遮蔽参数改成不同名、形态消失。仅参数走此入口
  （局部变量复用会破坏闭包按位置捕获首绑定的语义）。

**⑫ 字符串混合转义**：Generator_util 新增 `lua_mixed`（可打印安全字符
随机原样/`\ddd` 混拼，恒 3 位定宽防数字粘连），mix_lit 与 packer 两处
出口全部改用——产物 100% 字符串为混拼形态。

**⑬ 调用形式轮换**：kexpr 括号包裹索引逐出现随机（`K[(1)]`↔`K[1]`）；
定点包裹 ca/ec/ev/dsp/ld/PJ 探针/rs shell/读循环派发约 40 处模板位——
产物实测 78~86 处包裹索引。

**验证**：语料 **28/28**（拆分前+拆分后各一轮）；loops×8 / print MB×4 /
strings×4 全过；luac5.1+luau 三样本全过；delta bump 全量 21/41（逐产物
随机波动，远高于 ④⑤ 前 8/42 基线）；头 print/尾换行 rc=0。

**字节数**：Generator.rs **53,055 B** + Generator_flow.rs **36,371 B**（两文件
均 ≤80,000）；samples：print **103,489 B**、MB **54,613 B**、伪装
**132,269 B**；Roblox 副本已刷新。

**14 形态进度**：①②③④⑤⑥⑧⑫⑬ ✅（9/14）；⑦⑨⑩⑭ 部分在位待扩量；
⑪ 5.1 子集在位（0x 小写待用户解禁）。下一步候选：⑦循环壳扩量或⑨区间树
叠加。

---

## 形态⑦⑨⑩⑭收官（本节随本轮提交）

**⑦ 循环壳随机轮换**：池初始化 4 处循环（字符串池/数字池各外层+内层）
由 `mk_shell(rng,a,b)` 逐循环抽签 while / repeat-until（repeat 用 break
前哨式 `repeat if x>=n then break end; x=x+1; … until false;`，空池不
多跑）；body_ret 态整体包 `while true do … break; end;`（单次出壳）；
body_debug 三段 dump 包 `repeat … break; until false;`。壳串必须先独立
format! 拼好再注入外层模板——嵌套花括号直接放外层不会被求值。

**⑨ 区间二分树诱饵**：`it9` 闭包产出
`if not(st<=k1) then if not(st<=k2) then else end else end;`（双重否定
嵌套，恒空转），注入 body_insts / body_protos / body_debug 三态头部；
二分调度树既有结构未动。

**⑩ 常数位运算派生链**：`deep10(rng,bx,val)` 产出
`bx(bx(a,b),c)`（a^b^c=val，a/b=range64(0x1000,0xFFFFF)），8 处落点：
pkx1（protos/ret 探针）、numA/wk1（读取族）、numB/numC/wk2/wk3/wk4
（HH）。**全部提升为加载期 local**（`local nB_=bx(bx(..))` 在 consts
体内求值一次，kA/rs/驱动闭包捕获 upvalue）——热路径零开销。深教训：
提升值必须注入**真正发射**的 family 变量——build_readers 内层
`let mut family = String::new()` 会遮蔽外层同名绑定，曾把前缀发到死
代码导致探针解析成全局 nil（rs 失步超时/崩溃双症状）。

**⑭ 兜底与声明**：consts 四表联合声明 `local ec,ca,dsp,ld={},{},{},{};`
+ `mt=nil; mt=nil;`（setmetatable 后双写）+ ret 态死代码
`local r=nil; if r then return nil end`（藏在恒假守卫内——裸
`return nil` 放链臂尾会被 lua5.1 接受成 `return nil elseif` 且**可达**，
decode 恒返回 nil）+ body_debug 九元联合 `local d0..d8=nil×9`。
注意 Luau 200 闭包上限：九元联合只占 1 个 local 槽，无风险。

**排障记录（hash 超时二分）**：hash.lua 产物 120s+ 超时（HEAD 45s），
按"全量中和→逐块恢复"定位到根因=⑩前缀误注入被遮蔽变量（探针变全局
nil）：u32/a5 探针恒假致立即返回（产物仍对、但 rs `while nil do` 跳过
返回段→失步狂读→超时；复测还会随机崩 nil 算术）。修复后 hash 38~50s
（HEAD 同机 41~46s）。二分手法：中和时保持 rng 流消耗（`let _=`+哑参），
产物语义逐字对齐 HEAD 再逐块加回。

**验证**：语料 **44/44**（test/*.lua + test/Prometheus/*.lua 去
metatable，×2 模式；hash 双模式 150s 预算 38~50s）；print/MB/伪装
luac5.1+luau 全过；print 与 MB 输出与源逐字节一致；伪装源本身依赖
hookfunction（lua5.1 必错，与产物行为一致，Roblox 实测归用户）；
delta bump 两代相似度 3.4%（全随机）；头 print/尾换行正常。

**census（print 产物）**：⑨树=3；⑩deep 链=7；⑭九元=1、mt双nil=1、
四联=1；⑦until_false=10、while_true=10（含 ret 壳）；MB 样本为自解压
外壳不计 VM 特征。

**字节数**：Generator.rs **55,200 B** + Generator_flow.rs **37,758 B**
（均 ≤80,000）；samples：print **101,938 B**、MB **55,691 B**、伪装
**133,666 B**；Roblox 副本 step7-flow791014-print **101,464 B** /
step7-flow791014-disguise **133,454 B** 已刷新（luac 双✓）。

**14 形态进度**：①②③④⑤⑥⑦⑧⑨⑩⑫⑬⑭ ✅（13/14）；⑪ 5.1 子集在位
（0x 小写待用户解禁）。下一步候选：⑪解禁或已收官形态扩量。

---

## --rob 环境检测旗标（本节随本轮提交）

**功能**：`./target/release/kryvex-simple 输入.lua --rob`（可与 `MB`
组合，顺序无关；cargo run 同样生效）。带 `--rob` 时在**编译成字节码
前**把 `samples/Check.lua` 原样插到源码顶部，随后随源码一起进字节码
被混淆——产物里不出现明文检测段。

**插入保真**：main.rs 用 `include_str!("../samples/Check.lua")` 逐字
嵌入（编译期随二进制携带，运行期不读盘），拼接
`format!("{}\n{}", check, src)`——Check.lua 尾部无换行（`end` 结尾），
只补一个分隔 `\n`，检测代码本体一字节不动。

**Check.lua（用户提供，6,889 B/134 行，Roblox 实测可用）**：~250 个
执行器全局路径（getgenv/newcclosure/hookfunction/crypt.*/bit.*/Delta.*
/pibble.* 等）`string.gmatch` 逐段 pcall+rawget 探测；任一缺失即
`while true do error(0,0) end`。纯 5.1 语法（luac5.1 -p ✓）。

**验证**：print --rob 产物 124,766 B（luac ✓）；本地 lua5.1 运行
rc=1、错误对象 `0`（=检测段在非执行器环境正确触发，预期行为）；
MB --rob 82,413 B luac ✓ 同样 rc=1；**无 --rob 路径零改动**，语料
**44/44** 无回归；Roblox 实测副本
`samples/roblox-test/step8-rob-check.lua`（162,193 B，luac ✓）。

---

## ⑮ 指令格式/派发结构改版 + ⑯ ChaCha 参数组化（本节随本轮提交）

**背景**：外部分析指出两个结构性弱点——(a) 171 个 opcode 别名很快归并成
34 类，因为派发树在**密集别名值空间**二分（阈值比较 ≈ i+常数>阈值），
别名数量本身收益不大；(b) 全部常量共用一套 ChaCha key/salt/nonce 布局
与统一解码入口，整张常量表可批量提取。

**⑮ 指令格式改版**（opcode 字段魔数化 + A 域加法编码 + B/C 奇偶交换）：
- op_magic 表：每个别名/融合/builtin/回退键（0..95、BUILTIN_OP_BASE..+256）
  分配唯一 32 位魔数（0x0100_0000..0x7FFF_0000）；线上 op 字段改存魔数；
- rewrite_chunk 三个写点（普通/融合/builtin）：A 域从 u8 改 u32 =
  (a+魔数) mod 2^32；(B,C) 按魔数奇偶预交换（奇=写 (C,B)）；
- body_insts 的 A 读取 rd()→a10()（4 字节）；execute 主循环解码：
  `inst_A=a_arr[pc]-op; if op%2~=0 then inst_B,inst_C=inst_C,inst_B end`；
- 派发树：tree_entries 按**魔数值**排序二分，阈值=魔数——类别在魔数空间
  随机散布，静态「值域→类别」归并失效（census：旧别名区间阈值 0 处，
  魔数区间阈值 56+ 处）；
- 派发条件文本换魔数：`elseif op == 别名` token 带 " or"/" then" 尾边界
  整体替换；CLOSURE 模板体内**伪指令 op 比较**（uv_inst[1] == 别名，
  MOVE/GETUPVAL 上值绑定路径选择）同批替换——同样带尾边界。
- **三个中程读下一条指令的模板同步解码**（SETLIST/VARARG 的裸 A 读、
  CLOSURE 的 uv_inst 复合体）：复合体精确文本是
  `({ ops[pc], aa[pc], bb[pc], cc[pc] })`（花括号内侧有空格），
  解码版 = A 减魔数 + B/C 奇偶还原。
- **踩坑记录**：(1) 魔数替换必须带尾边界——op_magic 的 fallback 段含
  个位数键（9/6/66/196…），无边界时 `uv_inst[1] == 9` 前缀命中
  `== 1995021881` 造成数字级联拼接（产物出现 50+ 位巨型字面量、
  上值绑定分支永不匹配、语料 26/44 失败）；(2) 伪指令消费点共三处
  （environment.rs SETLIST/VARARG、load_store.rs VARARG、
  environment.rs CLOSURE），漏一处即运行期 nil 索引。

**⑯ ChaCha 参数组化**（CG=4）：
- 每组独立：key[8]、salt、sigma 下标排列（[1..8] 洗牌取 4）、nonce
  三元布局（(salt,idx,kind) 的 6 排列 Fisher-Yates 抽签）；常量按
  池下标 mod 4 分组加密；
- 产物侧：共享 xor32/rotl32/qr（255×255 表只建一次），4 组各自的
  K 表+salt+cblock+cstream 闭包，统一入口 dec 函数接口不变——
  wrapper 按 `pool_idx%4` 路由到参数组；解密热路径单次查表开销；
- 单组 key 泄露只暴露 1/4 常量，且各组 nonce 三元排布互不相同，
  「一次恢复批量解密全表」失效。chacha8_xor 签名改为直接收 nonce
  三元组（Generator_util.rs）。

**验证**：语料 **44/44**（hash 双模式 150s 预算 41s，与基线 38~50s
持平=热路径零回退）；print/MB 输出与源逐字节一致；luac5.1+luau 三
样本全过；delta 两代相似度 1.9%（全随机）；伪指令三消费点修复后
t5/t11~t17 上值/闭包微样全过。

**字节数**：Generator.rs **61,068 B** + Generator_util.rs **53,685 B**
+ Generator_flow.rs **37,758 B**（均 ≤80,000）；samples：print
**108,348 B**、MB **61,388 B**、伪装 **145,763 B**（魔数 10 位比别名
5 位宽、+3 组 cblock/cstream 使产物增大约 6~9 KB）；Roblox 副本
step9-vmfmt-print **108,723 B** / step9-vmfmt-disguise **140,691 B**
已刷新（luac 双✓）。

---

## ⑰ 常量按原型分组内联 + 解码入口打散（本节随本轮提交）

**背景**：⑯ 后仍有两个结构性残留——(a) 字符串/数值密文仍是两张中央整表
（B/oc），且常量按「池下标 mod 4」分组；(b) 各组 cblock/cstream 之上还有
一个统一 chacha 入口（mod 4 路由 wrapper）——逆向者找到该入口+同作用域
四组参数即可一次遍历导出全部常量。

**载荷布局改版**（rewrite_chunk）：
- 常量节 = **组字节（本原型参数组下标，子原型各自重摇）+ 逐条内联密文**
  （字符串=tag+长度前缀密文；数字=tag+8B 密文；nonce 池下标改用**节内
  槽位号**，与 Lua 侧 pos-1 一致；同值复用同 (blob,li)）；中央 gs/gn 密文表
  从 payload 里彻底删除；
- EncCtx（keys/salts/layouts/kstr/knum × CG=4）传入 rewrite_chunk；
  chacha8_xor 直接吃 nonce 三元组。

**解码入口打散**（Generator.rs）：
- 每组一个**自包含簇** = K 表+salt+sigma 下标排列+cblock+cstream+专属
  dec_str/dec_num（kind 常量逐组随机值内嵌）；**统一 mod-4 路由入口废除**；
- 四簇落位洗牌：1 簇进 chacha_setup 尾、其余 3 簇作为独立 parts 插在
  decode_chunk 之前的随机位置——四个解码器在产物里四处散置；
- body_consts 首读组字节（**用字节读取器 rd**，不是 u32 的 a5——首版用
  a5 读 1 字节组号错位 3 字节全流失步，语料 0/44，改 rd 后全通），按组
  **直连**本簇的 dec_str/dec_num（`(g==0 and A or g==1 and B or ...)`），
  每个原型运行期只触碰自己组的那对解码器；
- 内建名专用第 5 簇：独立 key/salt/kind，密文以混合转义**字面量**内嵌
  （lua_mixed），与四常量簇完全分离——导出任何常量簇参数都拿不到内建名。

**踩坑记录**：(1) 组字节读器选错（a5=u32 vs rd=byte）→ 流错位、读穿
payload、reader 收到 nil 算术崩溃；(2) 上轮一处补丁只探针没写入（文件
未落盘），恢复后编译暴露——补丁必须当轮验证落盘；(3) 沙箱再次深度重置
（HEAD 掉至 9dfd4c0 + 工具链清空），fetch+reset --mixed FETCH_HEAD +
setup-env 照例恢复，⑰ 改动全数保留。

**验证**：语料 **44/44**（hash 49s，基线 38~50s 持平）；print/MB 输出
逐字节一致；luac5.1+luau 三样本全过；census（process1）：cblock 簇=5、
组直连选择器=每原型 1 个、**mod-4 统一路由=0**、boot 字面量表=1；
delta 两代相似度 2.0%。

**字节数**：Generator.rs **61,494 B** + Generator_util.rs **54,999 B**
+ Generator_flow.rs **38,675 B**（均 ≤80,000）；samples：print
**112,891 B**、MB **62,722 B**、伪装 **148,177 B**（密文内联 +5 簇解码器
再增 ~4KB）；Roblox 副本 step10-perproto-print **111,787 B** /
step10-perproto-disguise **146,575 B** 已刷新（luac 双✓）。

### ⑥ 指令格式/派发魔数化（⑮，9cba8c5）
- 所有产出 chunk 的字节码字段定界符（内建/新 VM 双路）由编译期魔数决定，不再可特征码化；运行期用魔数反解。
- 派发结构加由魔数派生的跳变，静态扫描 opcode 签名失效。

### ⑦~⑧ ChaCha20 四参数组 + 逐 chunk 派生（⑯，9cba8c5）
- `src/VM/VM_Backend/ChaCha20.rs`（新）：纯 ChaCha20 block function + 初始化，无外部 crate。
- 4 组参数（key/nonce/counter/轮数）编译期随机路由；常量/字符串密文按组标注，运行期对应解密器各持一组。
- 逐 chunk：以 chunk 序号+长度参与 nonce 派生，同源 chunk 不同密文。

### ⑨~⑩ 常量按原型分组内联 + 解码入口打散（⑰，bb6e845）
- EncCtx 进入 rewrite_chunk：常量密文按子原型分组、组字节 u8 标注，解码器读组字节选参数组后解组内密文。
- 中央常量池删除；四簇（数字/字符串/内建/布尔-表）+ boot 簇（内建名解码用）发射于 body_consts。
- 修复：组字节读取器用 a5（u32 错位 3 字节致全崩）；boot 簇为 ⑱ 预留销毁钩子。

### ⑪~⑬ 防内存 dump/插桩（⑱，e18ca51）
- **用后即焚**：body_consts e_kb 处理器解码成功后自删（`ec[ix]=nil`），坏路径同样焚。
- **proxy 化**：ec/ca 用 `newproxy(true)` + 元表转接 backing 表（无 newproxy 环境退化为普通表）；pairs 触发 error（第 5 条自动达成）；stk 热路径保持普通表（3~5 倍慢，不做）。
- **boot 簇用毕销毁**：bk/bs/bcb/bsm/bdec/bpt 内建名解码后全 nil；常量簇 K 表贯穿运行期不销毁（⑰分组限损）。
- **原型惰性解码**：body_protos 子块加 u32 长度前缀；顺序解码时逐字节喂 rd()（滚动密钥流，跳读必须推进外层 k1..k4）后跳过子块，槽位存 thunk；CLOSURE 首次引用时换入快照（A2 游标+k1..k4+防篡改旗）解码子原型、换出恢复。**注意：读取游标是 var_a2（载荷子串 P 的下标），var_chk 只是 header 校验器——seek/快照必须用 var_a2**。
- 反注入检测（④）未实现（用户明示先不做）。

## 未完成目标（已立项，暂不实施）

### ⑲ 去信号化完整性校验（立项 2026-09-25，未实现）
**原则：所有校验不得成为攻击者用来确认「破解成功/失败」的信号；禁止用 error 直接反馈。**

现有三处 fail-closed 检测（header 校验器 var_chk、防篡改旗、kX 恒等式自检簇）均为「被抓即固定位置报固定错误」，等于给攻击者免费路标，后续全部要去信号化改造：

1. **不直接 error**：篡改被检测到时不报错、不中断、不给任何确定性反馈。
2. **环境破坏替代报错**：把校验结果当错误方向的路由——篡改后引导执行流向一个完全错误的方向（错误的状态值、错误的密钥、错误的控制流），让它解出来的就是乱码、跑起来的就是错的，程序表面照常运行但结果自然错误、崩溃点远离篡改点且形态发散。
3. **校验值做密钥材料**：校验和/旗状态参与 K 表/nonce 派生——篡改不解报错，只解出垃圾，无「成功」确认点。
4. **延迟爆发 + 非确定性**：状态静默污染，崩溃远离篡改点、每次表现不同；检查点运行期随机子集执行，无必经可 NOP 的检查。
5. 存量改造点：var_chk 失败分支、Xg/flg 检查、kX 恒等式簇——改造成环境破坏路由，删除固定报错路径。

验收（届时定）：篡改任意一处后，产物应「无固定报错信号、行为劣化方向发散」，且正常路径语料 44/44 不回归。

### ⑭ 行完整性守卫（⑳，本次）
- **背景**：用户 2026-09-25 明示要求（推翻早前「放弃行数守卫/反美化 fail-open」决策）：产物只许 1 行注释 + 1 行整体逻辑，出现第 3 行即崩，报错须伪装成自然错误。
- **实现**（Generator.rs line_guard，每产物 3 个采样点：行 2 开头 / parts 随机缝隙 / main_chunk 解码前）：采样点用 `pcall(function() local x; return x.f end)` 触发**真实索引错误**，从解释器位置前缀 gmatch 取**最后一个** `:(%d+):`（防 chunkname 内含 :N: 干扰；lua5.1/luau 消息格式同构）；行号≠期望值 2 时触发同型索引错误自然崩溃——**无 error()、无自造文案、双方言报错均为解释器原生**。
- **期望行=2 的统一**：默认模式 = 注释行+VM 行；MB 模式载荷（解压后 chunk）由 main.rs 预置同一注释行使其 VM 也落在第 2 行。
- **已知边界**：最后一个采样点之后的拆行不在守卫范围（拆行若在 main_chunk 之后、`}):WE()` 之前的残余段）。
- **附带修复（既有 bug）**：AntiTamper Luau 探针把 `_VERSION=="Luau"` 误当执行器环境，断言全局 `raknet`（执行器专属）为 table——原生 luau CLI 无 raknet → `error("1%@",0)`（这就是此前 luau 只做编译检查、运行期从未测过的原因）。修复：`te~=nil` 才审计（raknet 缺席=纯 luau 环境跳过；存在但非 table 仍报）。修复后 luau 运行期与 lua5.1 输出逐字节一致。
- **验证**：前插一行/拆 3 行（luac -p 验证拆分文件语法有效，排除假阳性）双方言均以原生文案崩溃；尾部补换行不误伤；语料 44/44 双模式；MB 双方言 rc=0 输出一致；corpus_run.sh 重建（判据：rc 对齐或 obf 干净跑完而原本地环境失败；benchmark* rc-only；hash 验 All tests passed）。
- 立项 ⑲（去信号化）仍待实施——本守卫仍是「篡改→崩溃」型，⑲ 落地时一并去信号化。

### ⑮ 销毁逻辑伪装化（⑳.1，本次）
- **用户指示**：`a=nil; b=nil; c=nil;…` 连续清空运行太显眼，一看即知是「用完销毁」逻辑——把清空伪装成其他代码。
- **boot 簇销毁重写**（Generator.rs）：六变量六形态、零 `=nil` 字面赋值，nil 全部来自表达式的自然缺失——槽位表取未用键（`X[0X79BC5]`）/未命中补取（`if not g then Ir=X[..] end` 恒执行）/空表取键转存（`({})[k]`）/or 链/条件式恒 nil（`v=v and nil or v`）/间接索引——读起来就是普通池取序列，混入产物既有图案。
- **mt 双杀伪装化**（Generator_flow.rs）：`mt=nil; mt=nil;` → `mt=({})[随机键];` 单次消化。
- **九连声明去指纹**（body_debug）：`local d0..d8=nil,nil,…×9` → 纯 `local d0..d8;`（语义等价，还省字节）。
- **验证**：产物 ≥2 连 `=nil` 运行 = 0（print/MB/伪装三样本）；语料 44/44 双模式；lua5.1/luau 输出一致；luac+luau 五样本过。销毁语义经逐表达式审查仍全部成立（每形态恒 nil）。

### ⑯ 行守卫采样逻辑伪装化（⑳.2，本次）
- **用户指示**：行守卫探针 `pcall(function()local sd;return sd.OH;end)+gmatch(":(%d+):")+tonumber` 一眼即知是行号检测，要混淆伪装。
- **重写**（Generator.rs line_guard）：
  - **提取逻辑整体删除**：不再解析行号——改为「自检函数 + pcall 验证 + 错误消息内容断言」：`if not ok and type(err)=="string" and not err:find(":"..(0Xr+2-0Xr)..":") then <触发> end`；期望行 2 以随机算术内嵌于 find 针式，无 gmatch/tonumber/行号变量。
  - **探针错误族随机**：索引/调用/算术/连接四族每构建随机取型，采样探针与触发探针独立取型（同产物两处守卫形态即可不同）。
  - **形态随机**：内联 `pcall(function()…end)` vs 先 `local function f()…end; pcall(f)` 两形态五五开。
  - 语义更稳：错误位置无论落在闭包定义行还是调用行都仍是行 2（pcall 调用同在行 2），针式必命中；篡改后消息行号漂移 → find 不中 → 同型自然错误。
- **验证**：守卫区 gmatch/tonumber/(%d+)/=nil 指纹全零；正常双方言 rc=0 输出一致；前插/拆行双方言以原生文案崩溃（实测同产物两处分别崩 index 族/call 族）；尾换行不误伤；语料 44/44 双模式；MB 双方言 rc=0；luac+luau 五样本过；delta 2.0%。

### ⑰ 行守卫去线性化（⑳.3，本次）
- **用户指示**：守卫仍是「pcall→判断→触发」线性直书，太明显；要求拆成多个 function、用 `:` 调用、隐藏线性逻辑与数据流、功能不变。
- **重写**（Generator.rs line_guard）：逻辑拆进挂表的 5~6 个 function（驱动/pcall 派发/探针/针式校验/触发 + 1~2 个诱饵 `return q` 方法），定义顺序 rng 洗牌，全部 `:` 方法调用；驱动体即嵌套表达式树 `s:ht(s:ck(e))`——无 if 判断直书，数据流折进参数传递。
- **触发折叠**：`local u=h and <容纳值> or nil; <触发语句>`——h=find 命中(数字)→u=容纳值→语句无害通过；h 假→u=nil→同型自然错误。容纳值与触发语句构造期配对生成（踩坑：曾用字符串包含判别，`Uy..` 被 `Uy.` 误匹配给 concat 族配了表容纳值→正常路径崩；改为 trig_stmt 直接返回 (stmt,tolerant) 对）。
- **随机化**：探针四族（索引/调用/算术/连接）+ 触发三族（索引赋值/调用/连接）每构建独立随机；针式期望行算式随机；诱饵数量随机。
- **语义**：探针与触发均在守卫表方法体内（行 2），pcall 调用也全在行 2——正常路径消息含 :2: 针式命中；行漂移后针式不中→触发自然崩溃（消息为解释器原生、行号为 VM 实际行）。
- **验证**：守卫区无内联 pcall(function)、`:方法调用`=5；正常双方言 rc=0 输出一致；前插/拆行双方言原生文案崩溃（本轮构建触发族=call）；尾换行不误伤；语料 44/44 双模式；MB 双方言 rc=0；luac+luau 五样本过；delta 4.9%；Generator.rs 68,484B。

### ⑱ 守卫移入 return 壳内（⑳.4，本次）
- **用户指示**：检测代码不许放在 return 壳外面——行 2 头部原先直书守卫表定义+驱动再 `return({...})`，一眼可见壳外有独立逻辑块。
- **改动**（Generator.rs）：采样点 #1 从壳外（header_block 之前）移到 header_block 之后=壳方法体第一条语句；三处采样现全部位于 `return({... xx=function(...) … end,…}):…()` 壳内方法体（开头/随机缝隙/主解码前）。行 2 头部只剩 `local L=...` 与 `return({`。
- **验证**：守卫表定义位于壳内（产物实测 `return({Jf=function(…)...local VX={}…` ）；正常双方言 rc=0 输出一致；前插/拆行双方言原生文案崩溃（本轮 index 族/concat 族）；尾换行不误伤；语料 44/44 双模式；MB 双方言 rc=0；luac+luau 五样本过；delta 2.8%；Generator.rs 68,695B。

### ⑲ 保守版明文窗口（㉑，c48b71b+7e6e797）
- **目标**（用户批准「开始做保守吧，性能不要太低就行，有性能损耗可以接受」）：减少 VM 运行内存明文与解密中间明文。
- **三项机制**：
  1. **e_kb 读点判空**：`ev=ec[ix]` 读出非表（启动期污染/篡改产物）→走 nil 处理不进解码——防污染常量缓存。踩坑：曾给 vv（解码结果）也判空，把字符串/数字常量全杀（vv 是标量！），已撤——**只判 ev 容器表，不判 vv 标量**。
  2. **启动期 thunk 隔离**：NP/MD/TH/tw 四变量必须在 execute 定义之前声明（否则 execute 内是全局 nil——Lua upvalue 捕获时机）；MD=C.pr 别名（body_protos 里建立，**踩坑：format! 另生名会写成全局**，必须复用同一 Rust 变量）；TH=thunk 快照（boot 销毁后逐槽快照，密文闭包常驻）。
  3. **主循环 pc 水位周期回收**：`if Xg and pc>tw then tw=pc+随机步长(0x8000~0x40000); for j=1,#MD do if type(MD[j])=='table' then MD[j]=TH[j] end end end`——过期原型槽写回 thunk（密文），活跃闭包持有明文引用不受影响；未来 CLOSURE 经 ⑱ 的 `type(p)=='function'` 自动重解。零计数器（tw 自更新），典型短脚本不触发（零损耗），长运行计算周期性回收。
- **性能**：hash 43.9s（基线 43~50s 带，无回退）；语料 44/44 双模式。
- **设计取舍**：panic 窗口/SP 首访置旗（早期实验）经复盘防的是实验自造场景，砍掉；RETURN 处恢复（v1）因 opcode 模板独立模块拿不到 Generator 域随机名而改为主循环水位版（单文件、参数化）。
- **副本**：step13-memwin-print 116,087 / step13-memwin-disguise 147,396 B。

### ⑳ handler 数值状态机（㉒，本次）
- **目标**（用户指示）：模仿 Luraph 14.9 的 handler 形式，把热点 opcode 语句序列改写为数值状态机（`local sm=初始值; while true do if sm==S0 then … sm=恒等式(S1) elseif … else break end end`），三站点接入：FORPREP / TFORCALL / TFORLOOP（权重各 8/10）；CALL/TAILCALL/RETURN/FORLOOP 保持 plain build（冷块 staging 是死路，勿再试）。
- **build_staged**（Opcodes/mod.rs）：`split_top_stmts` 顶层语句切分——**必须同时跟踪括号深度与 Lua 块深度**（if/for/while/function/repeat 的 do..end 配对），只认双零处的分号；旧版只看括号把 if 体分号当边界，TFORCALL 嵌套 if 被切碎。k=3..6 随机分组；状态值 `0x10000+next()%0xEDFFFF` 去重；`num_lit` 仅 0X-hex/十进制。
- **恒等转移式**（ident）：`-r+(r+t)` / `(r-r)+t` / `(2*r-(r+r))+t`，r≥0x1000、r+t≤min(0x7FFFFF,0xFFFFFE−target)。**血泪**：第三式曾写成 `(2*r-r)+t`＝r+t≠t，1/3 转移断链→TFOR 访客级死循环，且链验证脚手架把同款算成 t 双双漏检——同权重产物时而过时而挂（每转移独立掷式子，全对概率 (2/3)^n）。「断链/截断论」早前被否定也是被它误导。
- **local 提升**：TFOR 双件的 `local r1..r6=call` 拆段后是 if..end 块作用域，跨段引用会读到全局 nil——所有顶层 local 声明提升到机器前（`local r1..r6;`），声明句改写为赋值，`local f=function`→`f=function`，纯声明删句。
- **调试基建**（用完即撤）：`[pass]` 三阶段机器计数探针（证伪「压缩器吃机器」——15=15=15 全程存活）；sethook stderr 追踪（stdout 会被 timeout 吞）；OBF_NOGUARD/OBF_SKIP_PASSES 曾临时旁路行守卫/压缩器定位层面，已 git checkout 还原。
- **性能**：hash 39.7s / MB 40.1s（基线 43~50s 带、㉑ 后 43.9s——不降反升，门内）；语料 44/44 双模式；luac5.1+luau-compile 双过；双方言一致。
- **备忘录式不透明谓词**（仿 Luraph 14.9 `if not O[k] then O[k]=<式> end`）：状态转移改为 `if mm[k]==nil then mm[k]=<ident式>; end; sm=mm[k];`——目标态数值仅首次算出，之后经表槽流动；键逐站点随机（0x1000~0xFFFFFF）；热路径省重复算术（hash 39.7s→33.8s）。**Rust 坑**：`format!` 命名参数的**值**里 `{{MM}}` 不做花括号转义（转义只在模板串生效），字面 `{{MM}}` 经 replace 只吃掉内层留下外壳 `{表名}` → 内部 Lua 解析器报「期望 then」且 stderr 被吞成静默无产物——闭包直接捕获真名即可。
- **结构性评估（用户问询后定案：不实现）**：Luraph 的「每状态 body 放命名方法+方法表分发(R:jG)+状态跨函数流动+魔法信号码替代 break/continue」——我们 stage body 是模板占位符在主循环闭包局部作用域展开，抽命名函数需把 STK/PC/consts/env/protos 等约 8 个 VM 局部穿参/改捕获（全生成器布局重做），且每转移多一层方法调用落在 TFOR/FORPREP 热路径（每循环迭代），30s 级热循环恐撞 15% 性能门；备忘录式已取得其核心风味（状态经表单元格流动、挫败常量折叠），结构改造不划算。
- **⑥⑦⑧+A 表面特征消除（本次续）**：A=解码器前奏五件套打散（随机键库表+洗牌序+.m/["m"] 混合访问，产物不再有 `math.floor,string.char,…` 对齐并列）；⑥=字节拼装 `X*256+Y` 算术化（**256 是高/低字节位置权重必须保值**——首版误把权重改成 1，MB 自解压壳校验和崩成静默 rc=1，差式/零和式/约简式三选一只伪装拼写；shell.rs 的 P() 带游标副作用只能用不改求值序的形式；真源在 VM/packer.rs:372，stub_generator.rs 是旁支同款）；⑦=行守卫 m_hit 去空壳（type 捕获+洗牌 1~2 句对 ""/function/nil 全安全的填充语+两种触发收尾，u=nil 时仍抛同型自然错误）；⑧=行守卫 m_chk 的 find 针式冒号字面量改 string.char(0X3A) 运行期构造（:N: 行号嗅探指纹消除）。篡改行仍自然崩（rc=1）、原产物正常（rc=0）；语料 44/44；双方言过。
- **池解码器 v2（AntiTamper，本次续）**：用户点名把池字符串解码器（`local function D(h)` 读字节表→位置键流 XOR→偏移组串）进一步拆散打乱线性逻辑与数据流。v2=①循环体拆五态数值状态机（查长/自增/键流取数/谓词+异或拼装，转移三款恒等式、状态值逐产物随机）；②循环携带数据（下标/累计串/长度/键流系数）走暂存表槽，跨态 a/b/r/p 提升声明；③键流算式拆两步、K 拆解嵌差值恒等式、判空改 return 原值(nil)；④内层 for 常量(8/2)逐处独立伪装、谓词恒真族随机。**踩坑**：末态必须转「不匹配任何分支的哨兵值」才能落入 else break——首版 else 转回 S1 自身→池表耗尽即死循环（提取验证脚本也因 O(n²) 切片超时两次，非解码器问题）。验证：24 池键全部解码正确（debug/pcall/setmetatable/__index/...）、篡改行守卫 rc=1 仍生效、语料 44/44、双方言过。
- **字节数**：print 116,162 / print MB 65,378 / hash 187,436 / hash MB 148,025 / 伪装 （见提交说明）（v2 后）。

### ㉓ 尝试作废与基线重建（重要教训，先读）
- **676de98/ad0b695 的 ㉓ 全部作废**：推送源从未编译通过（err=2 被误判为构建残留），当时报告的全部验证实跑在旧 ①② 二进制产物上——已误报。根因未定位即弃树（二分仅回撤 Lua 读侧、写侧未同步回撤，归因失效）。
- **铁律（两条）**：① 任何 src 补丁后必须同一命令 build 且 err=0 才许测试，err>0 时一切测试无效；② 双侧特性必须同一补丁内成对改，每步 err=0+冒烟再进下一步。
- **重建**：b7c871a 干净基线重套 ①②，真验证后 79aea21 已推。

### ①② 在 b7c871a 上重套（79aea21，已推）
- ① 文件值掩码：融合/常规 `(fb,fc)=(fb0^(mag^ki1)^kb, fc0^(mag^ki2)^kc)`，builtin `(mag^ki1)^kb`/`(mag^ki2)^kc`；rewrite_chunk 签名尾 4 参 kb/kc/ki1/ki2；dcb=符号入 2^32 域→bx(bx(w,k),bx(m,q))→按 2^31 还符号。② 调试剥离：头（chunk 名空、linedefined/参数数 0,0）尾（lines/locals/upvalues 只消费不落盘）。验证：44/44、守卫 rc=1、hash 45s。

### ⑱.2 尺寸前缀掩码（80b9299，已推）
- 机制：子原型 u32 长度前缀 ln=child_len ^ f(站点密钥状态, 层内序号)；mask 4 字节=(rotl8(ka^kb,r)+盐)%256（四对 (k1,k4)(k2,k1)(k3,k2)(k4,k3)），盐=(层内1-based序号*A+B)%256，12 常数逐产物随机。
- 写侧：rewrite_chunk 返回站点表（绝对偏移,层内序号），统一 keystream 循环在线取站点首字节前 k1..k4 异或（orig=异或后值，零额外模拟）；读侧 body_protos 读 ln 前快照 qa,qb,qc,qd=k1..k4 后 bxor 还原（**快照必须在 u32d() 前**——Lua 求值序）。⑱ thunk 快照位置不变。验证：44/44、守卫 rc=1、hash 40s。

### #3 常量密钥混入引用折叠（本轮重套第二次；**曾因 .git 第三次回卷丢失 8780c55/aecbae1，远程仅存 79aea21/80b9299**）
- 机制：每常量槽 li 解密键混入 F_li=Σf(pc)（RK 引用本槽的指令位置和；r>127→li=r-128）。常量解密依赖解释不依赖槽位号。
- 常数（FoldCtx）：谓词 pv(v)=(rotl32(v,r6)^p1)%100<p3（B/C 各一套，p3∈30..70=「K 模式表」）、f(pc)=(pc*f1)^f2，逐产物随机。
- 写侧：inst 三写点扫描（pc18 1-based 对齐 Lua 下标；fused 记 swap 后值；builtin 解码恒 0 不参与；死槽照扫）；nonce[0]^=F_slot；seen 键 (bytes,fold)。读侧：body_consts 扫描块（op=文件魔数、b/c=dcb 解码后明文）算 Ftbl；dsp 第 3 实参 (Ftbl[li] or 0)%2^32；sm 第 4 参 fold 混 nonce[0]（bx(a0,fold or 0)）；dec_str/dec_num 透传；builtin 域哑形参（**不可传 "nil"——function(e,p,nil) 语法错致产物静默不落盘**）。
- **血泪同余坑**：谓词 (rotl+p1)%100——Rust wrapping mod 2^32 后 %100 ≠ Lua 直接 %100（2^32%100=96）→ 部分产物坏且 p3=101 恒真时被掩盖。定案：谓词/f 必须用 32 位域同构（xor/rotl）。
- 验证（重套后）：err=0、语料 44/44、守卫 rc=1、hash 50s。字节：混淆 112,966 / MB 62,662 / 伪装 154,658。
- 基线备注：80b9299 重建后复测语料曾报 primes/default 一条 FAIL——原文/产物多次重跑全一致，判为 hash 重负载后的瞬时抖动（该轮同轮重跑 44/44）。

### ㉓ 重做要点（下轮任务；四刀模型不变）
- A 常量解密依赖解释（#3 fold 已是半步，换掉 li 分量）；B tag 逐 build 随机（两侧同源字母表）；C 死常量破尺寸预言机；D 去 KRYVEX 包裹标记（--Kryvex v2.2 水印保留）。
- 公式已证（可复用）：R=roll 初值 0x2545F491^salt[group]，r7=rotl32(roll,7)、roll=(r7^mag).wrapping_add(a_enc).wrapping_add(fb0^fc0)（inst_recs 存预掩码预交换值；builtin=(0,0)）；Lua rotl7=(x*128)%2^32+(x-x%2^25)/2^25（<2^53 精确）；双侧实证一致（group3 n=4→976283175）。nonce 混 R：s1=roll^salt、s2=r7^(slot*6+kind)、s3=salt^rotl7(r7)。build_consts 用文件值（负值先+2^32）、ld 存 {taglit,blob,roll}、分发按 tag 键控、kmark=6 随机小写字母同长。
- 环境重建配方（.git 回卷/target 与 /tmp 不随快照保留时）：git fetch origin arena/01a0ce97-obf + git reset --hard FETCH_HEAD；tools/bootstrap-rust.sh → tools/offline-crates/vendor_crates.py + stub_crates.py → ~/.cargo/config.toml（directory=/usr/local/obf-crates-vendor + offline=true）→ PATH 加 .toolchains/rust-1.88.0/bin；语料脚本重建（test/*.lua + test/Prometheus/*.lua 双模式 diff）。

### ㉓ 重做完成（四刀 D→C→B→A，0fa72ec/b78a844/ce76114/befca2a，已推）
- **刀 D 去 KRYVEX 包裹标记**（0fa72ec）：build_header `[=[KRYVEX..]=]`→`[=[..]=]`；校验循环照吃载荷头 6 字节、读取偏移 `-6`（obfuscate_num(6)）；`--Kryvex v2.2,by 1%` 水印保留，产物 KRYVEX 字面量=0。
- **刀 C 死常量破尺寸预言机**（b78a844）：每原型追加 0~min(real+1,25) 个诱饵槽（nil/bool/num 混排、blob 随机字节——惰性解码下永不访问=永不解密）；写侧单点，读侧零改动。
- **刀 B tag 字母表逐 build 随机**（ce76114）：4 值取自 0..59∪251..255（避 60..250 诱饵区），写侧 consts/omit/诱饵推送与读侧 ld 键+record 内部标记+空消费句柄同源（tag_map18）；修 DF「线上 tag 恒 0..3」缺陷。**注意**：record 内部标记与 dsp 键经 one/two/three 耦合，改键必须同步改 ld 写入的字面标记。
- **刀 A 常量密钥混入指令流滚动状态 R**（befca2a）：R 链=roll 初值 0x2545F491^组盐、逐记录 r7=rotl7(roll)、roll=(r7^文件魔数+文件A+(fb0^fc0)) mod 2^32；写侧三写点推进（常规/fused 用 b1/builtin 无 fbx）；nonce=[盐^roll^F, r7^(槽*6+kind), 盐^rotl7(r7)]——**layouts 直传退役**；每槽 roll=最后引用指令后状态（谓词集同 #3）、未引用槽=链末。**读侧重算**（body_consts 扫描同式重放 R 链，密钥完全由指令流文件值+盐决定，产物零存储）。sm 5 形参、dec_str/dec_num 4 形参、dsp 第 4 实参 (Rtbl[li] or 链末)。
- **血泪（三个）**：
  1. **跨原型 Ftbl/Rtbl 残留污染**：表在 decode_chunk 层共享，原型间同槽号残留 → #3 潜伏 bug（此前 44/44 属运气）。修：扫描块开头 `{ft}={};{rt}={};{rf}=0`。
  2. **fn_bxor 负输入坑**：dcb 输出是 i32 解释（原值 ≥2^31 时为负），多项式 bxor 对负输入行为错误（bx(5,-1)=2 ≠ 0xFFFFFFFA）——**任何 bx 前必须 %2^32 归一**。
  3. 读侧公式笔误（bv+cv 独立相加 vs bx(bv,cv)）——**双侧逐指令分量对拍**（Rust eprintln vs 产物 print 前 10 条 (mag,a_enc,fbx,roll)）是最快定位手段，本轮 10 分钟抓出两个 bug。
- **A 刀读侧数组语义**：pf_opcodes=文件魔数、pf_a_arr=a10()=i32 解释的文件 A、pf_b/c_arr=dcb 后明文（=预掩码预交换 fb0/fc0）——R 链三输入全部可从数组重算（含死槽/builtin 恒 0）。
- **验证（全刀）**：err=0、语料 44/44 双模式×2 轮、守卫篡改 rc=1、hash 45s。**字节**：混淆 111,596 / MB 63,202 / 伪装 155,500。

### #4 四数组掩码=高危①主体（本轮）
- 机制：body_insts 四数组中 opcodes 明文驻留（供派发），A/B/C 存掩码值（写侧 `bx(值%4294967296,k*)` 归一）；K 逐原型从 `kp=chunk.pf_ld、pb=chunk.pf_lld` 派生（KA18=MA18 实测同一）：`ka=bx(kp,pb); kb=bx(kp,ka); kc=bx(pb,ka)`；execute 入口派生局部（fn_bxor2 别名，无 VM 槽写入）+ **写掩码槽**（见 #5）。
- 取指还原：`inst_A=bx2(a[pc],mk1)-op; inst_B/C=bx2(…,mk2/3)`+`op%2~=0` 交换；扫描块（body_consts R 链重算）同样派生+读侧 unmask（av/bv/cv）。
- **根因链（本轮排障全记录）**：`k_mk1..3=rng.name()` 作槽键=池外裸名→运行时 nil→`o[nil]=…`「table index is nil」。修复=槽键必须走 `slot_key_block` 池（扩 20→23）。
- **符号还原（关键新坑）**：跳转类 b/c operand 原明文为带符号负值，写侧 %2^32 归一后成 4294967294/95，读侧解出 u32 直接加 pc→爆 2^32（FETCH_NIL_4294967337）。修复=全部读侧（取指 B/C、CLOSURE 四元组、扫描块 bv/cv）`if v>=0X80000000 then v=v-4294967296 end`。
- 探针法：FETCH_NIL+上一条 `_G.DBG={pc,op,A,B,C}` 记录定位负跳转；探针要放首取指处（扫描循环内 `if i==1` 永不触发）。

### #5 中程读补齐 + 查表 bx 回退（本轮；语料 8/44→44/44）
- **中程读是语料阻塞真因**：三处站点（SETLIST `{INSTS}[{PC}][2]`、EXTRAARG `[…+1]`、CLOSURE `local uv_inst=({…四元组…})`）旧式 `a[pc]-opcodes[pc]` 直读掩码数组→错。修复=统一 `bx2(数组[pc],掩码)` 还原：热块内联用 execute 局部（n_mk1..3 提前到站点改写前生成，两处同名）；冷块方法体同名局部从掩码槽自取（`body.contains(n_mk1)` 时 fetch 前缀 `local m1,m2,m3=self[k1],self[k2],self[k3]`）。
- **CLOSURE 四元组改写形态**：模板自带 `local uv_inst = ` 前缀——替换必须连前缀一起匹配（重复 local=语法错、产物静默旧盘）；四条语句化+mq1..4 临时+按 `q1%2~=0` 交换还原。
- **查表 bx 回退（性能）**：沙盒 lua5.1 无 bit/bit32，逐位回退 O(32)；4 处定义点（Generator.rs fn_bx+fn_bxor2 共表、Control_flow.rs、Generator_util.rs）改 **T4(16×16)+T8(256×256) 两级查表**：建表 ~O(4K 次内层)，每次 bx=4 轮字节查表。hash 178s→76.5s。**血泪**：①建表代码塞进 `local fn=…` 赋值串→`local x=local y` 非法；②建表放 format! 串内 Lua `{}` 必须 `{{}}` 转义（曾有 `{{{{}}` 四重残留→产物 `local qT4c={{}` 非法→**压缩器 parser 报错、产物是旧文件**——编译失败必看 stderr，勿信旧产物）；③Generator.rs 块 4 别名共用一套表=1 份建表。
- **验证（全链）**：err=0、**语料 44/44 双模式**、守卫（最长行第 3 字符翻转）原文 rc=0/篡改 rc=1、hash×3 rc=0（76.5s 带——超出旧 ~47s 带30%+，系掩码取指×无位库沙盒的口径代价，Roblox bit32 原生不受影响）。**字节**：混淆 120,423 / MB 69,293 / 伪装（#U4f2a#U88c5.lua 普通模式）160,562。**更正**：此前误把 print.lua 的 --rob 构建当伪装样本（140,950）——伪装三件套=根目录 #U4f2a#U88c5.lua（WindUI 伪装脚本）普通模式产物，无 --rob 外壳；沙盒 rc=1 静默同型为正确行为（Roblox API 缺失即错，不 print）。
- 三件套（hash 构建现场）：process1=293,224 / process2=182,879 / process3=188,463 / obfuscated.lua=188,484 bytes。
