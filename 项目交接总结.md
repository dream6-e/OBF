# 项目交接总结

更新：2026-09-27，Asia/Shanghai。

## 当前项目与入口

OBF 原有混淆样本和 `Environment.zip` 保持不变。本次整理并上传的是 Luraph 反混淆工具及实际提取出的可读逻辑，不是 Kryvex Rust 实现；仓库仍没有 Rust 工程源码。

- **完整修复后反混淆源码**：`tools/luraph/`。
- **部署、运行与测试文档**：`integrations/luraph/README.md`。
- **主库 14.8 最终逻辑**：`integrations/luraph/reports/artifacts/main-v148/`。
- **主库 14.7 最终逻辑**：`integrations/luraph/reports/artifacts/main-v147/`。
- 累计补丁、输入哈希、测试、探针及历史验证报告：`integrations/luraph/`。

## 部署与验证

Linux，Python 3.10+，需要 make、g++、ar。在仓库根目录执行：

```bash
python3 -B integrations/luraph/setup_tools.py --jobs 2
python3 -B integrations/luraph/validate.py
```

环境从本仓库 Environment.zip 和固定 Luau 0.735 的 AST 编码器补充源码离线构建。原版工具保留在 `.tools/Environment/toolchains/bin/`，独立分析版在 `deobf-bin/`；引擎 bin 链接由部署脚本建立。**不要删除已部署的 `.tools/` 工具目录**；它只是 Git 忽略，不属于待上传源码。

所有完整源文件已经打包进本仓库，不再要求恢复曾经位于 `/home/user/luraph-v15-v14.x-deobfuscator` 的外部目录。该旧路径及早期实验日志路径只在历史报告中作为证据出现。

## 用户明确的工作要求

1. 能完整反虚拟化就交付还原代码；不能完整反虚拟化，也要记录并展示全部已捕获的可读逻辑，不要只报告失败。
2. 区分原始源码字节、模拟轨迹、整理摘录和推断，标明未覆盖路径、截断及变量绑定缺失。不要虚构源码、强填动态边界或声称没有测量的覆盖率。
3. 用户原始输入不修改；实验使用隔离副本；不绕过白名单、不为具体文件名/原型编号写兼容特判。
4. 清理缓存、重复日志和旧生成物；保留工具、原始样本、最终逻辑、测试和必要来源信息。
5. 上传前检查全部交付语法并执行回归。此次用户已明确授权提交和上传 GitHub；会话固定在 `arena/01a0de62-obf`，只推送该分支，不覆盖 main。

## 已验证结果与限制

- 上游四个小样本（14.7/14.8/14.9/v15）可完整静态还原并执行，但不能把它们的成功套用到用户主库文件。
- 主库 14.7：寄存器尾部清理与常驻入口保持已改进；剩余 3 处动态数值循环边界。保留 56 条可观察操作及业务摘录。
- 主库 14.8：仍未完整静态还原；保留 328 条可观察操作、437 行含说明的记录、376 条字符串和一段 2444 字节 loadstring 包装源码。主要 UI/角色伪装相关调用已可读，不是可直接运行的完整脚本。
- 主库 14.9：GUI 布局与闭包/辅助函数问题未解决；Studio 校准探针尚无真实引擎结果。
- 各次离线记录中的 URL 没有被实际请求；远程库源代码并未因此获取。模拟回调和对象代理不代表真实 Roblox 运行验证。

## 打包来源与清理

上游固定提交 `d6158608bb512e919afe4e6485f6146598738274`，已应用累计 23 文件修复补丁。`tools/luraph/SOURCE_MANIFEST.json` 记录保留的 101 个有效源码/资源/文档/样本文件及哈希，列明排除路径。上游缓存、Windows EXE/RAR、重复 bin 源码、历史工作目录和生成结果未上传；最终输出目录的重复 raw/debug 日志已清理，关键结果保存在 metadata.json。

上游未附独立 LICENSE 文件，来源与授权不确定性在 SOURCE_NOTICE.md 中说明；Luau AST 补充代码另保留原 MIT 许可证。不要把公开可见误写成已获任意再分发授权。

以后修改打包的上游文件，需要同步来源清单及累计补丁（或清楚记录新增的本地修改），并运行统一验证命令。具体上传提交和验证结果见 Git 历史及 `integrations/luraph/PACKAGE_VALIDATION.md`。
