# OBF 项目交接总结

> 更新时间：2026-09-11（Asia/Shanghai）
>
> 核对的代码基线：`7106908 Replace Adler frame tag with keyed dual-lane integrity hash` **加上 2026-09-10/09-11 批次**（红门处置与 pin 重录、K10 结构拆分与体积门重钉、K12/ISA16 链式 operand lane key、K13 惰性解码、K13b 空闲回锁、`seed.rs` 预备拆分、K14 装载期校验器分派树、K13c 第一步（常量池坐标化）、K13c 第二步（ISA17 常量池加钥）、K15（数字池化，因改壳格式整体回退 `7b3f450`）与 K16（重复轮常数改为包装表字段，不改壳格式）、K17（十进制转义取最短安全宽度 + base86 字母池排除 `"`/`'`/`\`））；wire 为 private **ISA16**（K13/K13b/K14/K13c① 不改镜像字节），K 系列施工中期（K0/K5/K7/K8/K9/K9a 已落地，K10 = 体积 shrink + 静态表面收紧）
>
> 仓库：`dream6-e/OBF`；本次交接工作分支：`arena/01a08a7a-obf`，从 `arena/01a0866a-obf` 切出；远端另有 `arena/01a0857a-obf`/`01a08590-obf`/`01a0891d-obf`/`01a0896b-obf` 等历史批次分支。切换会话时以 `git branch --show-current` 为准，其他 `arena/*` 分支只读。
>
> 本文按**当前实现**重写，不是历史更新日志。详细协议见 [自定义字节码.md](自定义字节码.md)，支持条件和已知不等价项见 [虚拟机兼容性.md](虚拟机兼容性.md)，路线与历史记录见 [总路线.md](总路线.md)。文中出现的旧 ISA（ISA3/ISA14…）与旧 golden 数字若出现在历史小节内，是当时的记录，不是当前状态。

**阅读顺序**：首次接手先看第 1–4 节；修改实现前读第 5–9 节；验证、定位和提交按第 10–12 节执行。

### 0. 当前实测基线（2026-09-10，rustc/cargo 1.88.0，本机全量复核）

| 项 | 实测值 | 来源 |
|---|---|---|
| private wire 版本 | `WIRE_ISA_VERSION = 17`、`WIRE_INSTRUCTION_ENCODING = 1` | `src/vm/custom/semantic.rs` |
| 公开格式 | OBF v2 / ISA2，`HEADER_SIZE = 32`、prototype header 24 B、varint 2~7 B、payload Adler-32 覆盖 `[32, file_size)` | `src/bytecode/custom.rs` |
| 私有 prototype 字段 | **16 项**：`k tags u parent m p flags nu nk nc shared self code cached control routes`（`routes` 由 K8 引入） | `src/vm/fields.rs` |
| golden 与构建一致性 | 用 seed 7001/7351 重新生成，两份 golden **逐字节 `cmp` 相同**；stdout 与对应 fixture 逐字节一致；`luac5.1 -p`、`luau-compile` 与 `obf check` 全部接受 | `virtualize` + `cmp` + 参考工具 |
| golden 体积 | `vm_lua51.out.lua` **100,619 B**、`vm_luau.out.lua` **111,608 B**（K17 纯拼写瘦身各 −1,289 / −728 B；K16 把 5/4 个重复轮常数改成包装表字段各 −353 / −269 B；K13c② 常量池加钥与逐帧重建各 +1,467 / +2,338 B；K13c① 池坐标化各 +82/+88 B；K14 校验器分派树各 +1,279/+1,070 B；K13b 空闲回锁各 +207 B；K13 惰性解码各 +136 B；K12/ISA16 曾 +201/−283 B，−283 B 是链把更多字面量吸进同一段、Luau 侧反而变短） | `wc -c` + `cmp` |
| golden SHA-256 | Lua51 `fc0e33036750fd142b2f6a492180fb9291d5120b143a07745b53a62c6cc7bf56`；Luau `d9c2754471cedacef250819ed45986a1f007e785cbf25d5898aa594ff4b8113c`（2026-09-11 K17 后重录；K16 的 `21f0f649…`/`14d3d430…`、K13c② 的 `43f86a71…`/`891bdb4b…` 与 K13c① 的 `4d9ee226…`/`b1a37291…` 均已作废——那两行原本按各自批次记录，统一以本行为准；每批 golden 变更后以 `sha256sum vm_lua51.out.lua vm_luau.out.lua` 复核） | `sha256sum` |
| 对应公开 `.obf` | 5,525 / 6,575 B，SHA-256 `a33b5dcb81b02d1f9f2e00ce71c10d55706020e2b28fd516fc69f6444bb81140` / `def67d59b5c79832709c4dbaa33293058195c2dc347f9578c8aaf61cd45fa1d7`（自 varint 批次以来未变） | `compile` + `sha256sum` |
| LZW（含 16 B header） | K12/ISA16 实测 Lua51 seed 7001 `14,071 → 13,479 B`；Luau seed 7351 `17,713 → 17,482 B`（链改变 varint 分布：Luau 压缩后反而小 45 B；Lua51 大 4 B，仍严格小于 image，硬门照过） | `compression_reduces_bytecode_while_script_budget_is_independent` 实测 |
| 门数 | **177 单元 + 116 集成 = 293**（K12 两条 operand-lane 门，K13/K13b 各一条惰性解码/空闲回锁门，K14 校验器分派树门 + 边界条件互补门，K13c① 常量池坐标门，K16 八条常数字段化门 + K17 两条转义/字母表门（256 字节值 × 15 种邻居的真解析器往返、86/99/缺 13 与两字符转义类排除）：改写/覆盖体内与兄弟字段、格式守恒、幂等、遮蔽体不改、构造式不改、表键不改、非规范拼写不做候选、短/稀有不改、键名避让、近壳不改），**2026-09-11 全绿**（红门与 K10 体积门均已关闭；§0.1 逐条记录） | `cargo test --all-targets --no-fail-fast`（体积门 enforce 中，无需 `off`） |
| 执行覆盖 | Lua51 **46/46**、Luau **49/49** 仍为绿 | `every_supported_opcode_is_actually_executed_in_the_target_runtime` |
| 15-run bench | K17 提交树：15 次最佳 VM Lua51 **346 ms**、Luau **114 ms**（native 2/3 ms ⇒ 173.0x / 38.0x；K16 提交树中位 344 ms / 112 ms，同轮 native 2/3 ms ⇒ 172.0x / 37.3x；K13c① 提交树（`target/k13c1-matrix.log`）为 Lua51 **323 ms**、Luau **108 ms**；同轮 native 漂到 5/4 ms，故比值（64.6x/27.0x）不可与前批比较。K14 为 311/104 ms（native 2/3 ms ⇒ 155.5x/34.7x）、K13b 332/112 ms、K12 323/103 ms ⇒ 惰性解码 + 空闲回锁在本 fixture 上各加约 9 ms（34/31 次解码是主因），K10 时 326/106 ms。噪声区间见 §0.1，未据此调门 | `tools/bench-vm.sh` |
| 整脚本体积门 | **120,000/120,000 B 硬门（K17 后实测 10 种子最坏 Lua51 101,134 B、Luau 111,608 B；K16 那批是 102,377 / 112,701 B）**（K10 钉 112,000 B，2026-09-11 K14 经用户指示重钉 120,000 B）**：`tools/bench-vm.sh` 默认值与 `src/vm/custom/tests/semantic.rs` 双目标预算同数，矩阵不导出 `off`。K13c 第一步后实测最坏 10 种子 Lua51 100,794 B ；第一步后曾为 100,794 / 111,528 B。加钥批次实测 +1.5 KB 量级，容纳得下，未删任何校验。暂停开关仍是 `OBF_BENCH_SCRIPT_CAP=off` | `tools/bench-vm.sh`、`tools/test-matrix.sh` |
| 源文件上限 | 全部 `src/**/*.rs <= 81,920 B` 通过；K12 给 `emit.rs` 加了链语句（64,639→**65,225 B**），并把 `tests/semantic.rs` 顶到 83,419 B 超限，因此按 `include!` 惯例拆出 `src/vm/custom/tests/lane.rs`（5,685 B）→ 回落 78,029 B。当前最大仍是 `seed.rs` 80,646 B，其后 `tests/runtime.rs` 79,020 B、`parser.rs` 78,439 B | `tools/test-matrix.sh` ceiling 门 |

### 0.1 三个红门的处置记录（2026-09-10 已全部清掉）

frame-tag 批次 `7106908`（Adler-32 → keyed dual-lane hash）留下 3 个红灯；同日两批（红门处置 + K10）已全部关闭，逐条如下。

1. `vm::custom::tests::k7_bit_library_names_are_never_spelled` —— **真实契约破口，不是 pin 问题**。`emit.rs` 把字面量 `"buffer|bit32|table.freeze|debug.info"` 直接拼进 Luau key probe 的 transcript（Rust 侧 `cipher.rs::runtime_probe_witness` 折叠同一串），等于交付 3 份共 19 B×3 的静态锚点，违反 K7/T7“库名与反射名不得明文”。
   **改法**：prelude 用隐藏名池里的 6 个名字本地量拼出 transcript 后缀（`v_a.."|"..v_b..`）成为单个 local，再作为**额外参数**下发给三个 probe（probe 是 VMS 兄弟条目，看不见 prelude local，所以必须走参数，与 `SB/DB/GI/LS` 同一通道）；名字列表 `PROBE_TRANSCRIPT_NAMES` 同时喂给 Lua 表达式与 Rust fold，两侧**无法漂移**。transcript 值随池槽位逐 seed 变化，且产物中只出现一次。
   **不放宽断言**：K7 门反而扩到 18 个词（新增 `table.freeze`/`debug.info`/`getinfo`/`loadstring`），并同时审计**两份 golden 文件**（旧门只审计探针程序的 emit，这正是它能抓到问题、而更早的批次没能抓到的原因），另加两条结构断言：产物中不得出现 `A=A..` 紧跟字符串字面量的拼接；Luau 必须恰有 5 个池名连接点（6 个名字一次拼装成单份 local），Lua51 必须为 0。
   **影响面**：wire image 与求值语义不变，只有 share/witness 数值随 transcript 文本变化；Lua51 golden `cmp` 零 diff，Luau 只多 111 B 文本（删 3 份 36 B 字面量、加 1 个池拼装 local）。
2. `heuristic_surface_pins_hold_on_both_audit_configs` —— pin 漂移：dual-lane tag 改了 emitted 常量集并把下游 rng 流位移，M2/M3b/M4/M5/M7 全动。两套 pin 按观测重录，注释写明“哪些项为什么动、哪些项必须继续稳定”（M1=86、KAT 词计数、M6=(0,0) 未变，即没有新增可疑 API 面）。
3. `product_text_audit_pins_hold_on_both_goldens` —— 也是 pin 漂移，但**归因不是 frame tag**：用 git 里的历史 golden 直接数过（`count_decimal` 同算法）：`89dcc91` 的 golden 里 `256` 已经是 199/177，所以 +18 是 pin 之后 K8/K9 批次把脚本从 93,044 长到 98,586 B 时新增的 base-256 打包拼写；tag 批次只移动 `65536`（102→99 / 94→91）和 `4294967296`（18→20 / 16→15）；本批的 probe 修复对 check1/check5 数值**零影响**（与 `7106908` 的 golden 对数验证）。check9 `rem5` 0→1 / 0→4 意味着流长离开“全部是 5 的倍数”这条病态路径，是安全方向，`fail` 仍为 false。

4. 体积预算门（待办，非破口）——**同日 K10 批次关闭**。`tools/test-matrix.sh` 不再导出 `OBF_BENCH_SCRIPT_CAP=off`，整脚本预算从 98,000 B 重新钉到 **112,000 B/目标**（`tools/bench-vm.sh` 默认值与 `src/vm/custom/tests/semantic.rs` 同数，取实测最坏 + 约 2% 余量）；裸跑 `cargo test --all-targets` 不再有预算红灯。同批把 `emit.rs` 按发射阶段拆成 3 个模块，恢复源文件余量（见 §12 约束与第 8 节 K 系列表）。
   **“不删步骤的体积 shrink”经实测判定无空间**，所以走不了“先 shrink 再收紧”，只能按实测重新定标：两份 golden 的 5,737 / 5,801 个空格 **100% 是必需的 token 分隔符**（可省 0 B）；最长的重复片段全部落在设计内噪声上（寄存器窗口按 lane 轮换重读：Luau `{5 名}=k(h,q,g,l)` 共 414 次、每种拼写 103–104 次；Lua51 对偶形式 `then 5 名=e(p,d,…)` 共约 405 次、每种 101–102 次；数字三形态/谓词四拼写；控制流扁平化的状态跳转）；两份 golden 的嵌入字符串段合计 21,323 / 27,225 B，承载的是 LZW frame（13,475 / 17,527 B）加 ISA14 池/元数据；按 base86 每字符至多 log2(86)≈6.43 bit 计，frame 的编码下界就是 16,775 / 21,819 B，余下 4.5 / 5.4 KB 是池数据本身加每段 4 字符长度前缀与变宽组，不是可挤掉的空白。再压必须删混淆步骤或改分发形态。**同日 K12 已把 ISA 推到 16，但走的是加安全强度而不是减体积的路**：operand lane key 折上按镜像顺序推进的链（详见 §8 末尾的 K12 记录）；体积随之变为 99,090 / 108,766 B（Luau 因为 varint 分布变化反而缩短 283 B），预算门未动。

## 1. 先记住这十一件事

1. 这是 **Rust 2021、std-only、零第三方 crate** 的 Lua 工具链；最低 Rust 版本是 **1.88.0**，不是一个尚待实现的 VM 原型。
2. 目标固定为 **Lua 5.1.5** 和 **Luau 0.735**。Roblox 是适配方向，**尚未完成 executor 实机验证**。
3. 默认 `virtualize` 已经是 **源码 → owned AST/BindingId → typed register IR → 自定义 OBF v2 → Register VM**，不调用外部 compiler，不存 native word，也不静默 fallback。
4. 旧 compiler/native chunk/OBF v1 路径只通过 **`--backend native`** 或 `vm::virtualize_native` 显式使用，必须继续单独回归。
5. 公开文件格式仍是 **OBF v2 / ISA 修订2**（32-byte Header、24-byte prototype header、2~7 byte opcode+varint）；修订1仍可读取/执行/原样round-trip。生成脚本从已验证Program重编码为private **instruction encoding 1 / ISA15**：保留camouflaged recipe/edge-token/fragment、global segment graph、runtime-witness与share-masked control，使用per-prototype operand/register/field-order ABI，把安全邻接项变为carried-value pair、把entry接线变为六状态图，把record/dictionary/segment/metadata字段顺序逐seed重排，把capture/constant移入两个全局洗牌池（ISA14），再叠加K8 per-prototype indirect route（新增私有字段`routes`）、K9 per-lane payload-derived affine operand value key（Form 1–5；Form 4 用16-bit逆元）与K9a/K1逐image混合base86传输（ISA15），最后进入 `8 KiB bounded LZW → inner ChaCha8 → frame v2（keyed dual-lane integrity tag）→ outer ChaCha8 → 混合base86三段`。它不会嵌入可`pc+=4`一对一翻译的canonical指令串。
6. VM **全部生成完后**才做：私有字段短名 → 最终 local 短名 → generated-VM 限定的无歧义可选分号省略/单行化 → 解析与绑定校验。公共 `minify` 仍严格保留完整语句分号，歧义 `;(` 也不得删除；finalizer 后不能追加代码。
7. 所有可安全改名的 local 使用不同于原名的 `[a-z]{1,2}`；同域唯一、跨域安全复用。VM 的 **16 个私有 prototype/control 字段**（含 K8 新增的 `routes`）也统一缩短，当前全部为随机单字母。
8. 字段压缩仅用于生成器明确授权的私有表；用户字段、导出键、字符串、宿主 API、元方法和静态 NAMECALL 名称不能一起改。
9. private **ISA17**（`WIRE_ISA_VERSION=17`：K13c② 给常量池 payload 加钥并把常量重建移进 `DC`；K16 只改发射脚本里常数的表示位置——5/4 个重复轮常数成为包装表字段，`luac5.1 -l` 仅 max upvalues 16→17；K17 只改字符串字面量的转义宽度与 base86 符号集；两批镜像与公开 `.obf` 都不变 ⇒ 均不升 ISA，**镜像字节因此改变**；K12 把 operand lane key 改成吃已解记录前缀的链，K13/K13b 只改运行时词表生命周期、K14 只改装载期校验器的分派形状）golden 是 **102,261 / 112,605 B**；整脚本预算自 K10 起为硬门、K14 按用户指示重钉为 **120,000 B/目标**（K13c① 后实测 10 种子最坏 100,794/111,528 B）；历史整脚本关系不是 compression 门。脚本 SHA-256 为 `4d9ee226f900c4215d00acf5e704e17323a4f697a9aa2c82662345b07a31b8fd` / `b1a372919e327013a9ab727d59be05321658fa0251bbe32b8358b1fb8b79df66`；对应公开 `.obf` 仍是 5,525 / 6,575 B（SHA `a33b5dcb81b02d1f9f2e00ce71c10d55706020e2b28fd516fc69f6444bb81140` / `def67d59b5c79832709c4dbaa33293058195c2dc347f9578c8aaf61cd45fa1d7`，自 varint 批次起未变）。完整矩阵为 **282 门（166 单元 + 116 集成），2026-09-11 全绿**（frame-tag 批次的 3 个红灯处置记录见 0.1；K10 起整脚本预算门恢复 enforce，“全绿”已包含体积门），15-run bench（`target/k13c1-matrix.log`）VM 为 Lua51 323 ms、Luau 108 ms（本轮 native 漂到 5/4 ms，比值不作跨批引用）；覆盖仍为 Lua51 **46/46**、Luau **49/49**。这些数字不是语言兼容率或密码学安全证明。
10. ISA12-A/B分别提供完整private id域互异的operand profile与register profile，删除固定`6+3*i`、actual-opcode及全像register-key桥。ISA12-C只融合结构证明安全的邻接项：producer保存逻辑目标/物理key/一次求值结果并保持write，consumer register reads经四family `RF`直接转发（含nil/false）；其他形态保持single。entry另改为六状态seed-shuffled graph。ISA13把record 4槽与segment 3 token按per-id/slot factorial置换重排，dictionary/metadata/tuple用per-image置换baked进parser；fetch/validator的`I[2]/I[3]/I[1]`静态下标随seed变化，错序在用户代码前fail closed。ISA14把capture/constant移入两个全局洗牌池（3匿名u16 token/记录，per-record factorial profile，池顺序按image翻转），header仅保留24字节metadata。ISA15把transport换成逐image混合base86（4-char 段长前缀、链式4/5/6字组、`86^width`冗余内随机高位填充、逐seed 86字母表）。K8让每次fetch再经一层per-prototype payload-derived indirect route（新私有字段`routes`，重复route在parse期fail closed），K9对Form 1–5的operand wire value做per-lane affine key（runtime用`FM[op]`取Form后执行`AK`逆变换，Form 4用模65536逆元），K7把目标端字运算分叉为多态工具箱（Lua51纯算术、Luau隐藏名捕获bit32/buffer）。frame v2的tag已由Adler-32换成以运行时key state为输入的双lane（模65521）链式keyed hash；LZW header与semantic image的Adler门仍在，二者不要混称。ISA11 source-witness/ChaCha/anti-hook全部保留。规范环境、profile/fusion/置换规则、`RF`、handler、parser字段与全部inverse仍随客户端交付；不能声称客户端秘密或已解决全部静态恢复。
11. **永久 80 KiB 源文件门**：任何 `src/**/*.rs` 必须 `<=81,920 B`。超过即先拆分，保持逻辑，并在功能修改前用 fixed-seed golden 证明拆分零生成差异。`tools/test-matrix.sh` 已自动检查；原 124,624 B 的 `src/vm/custom/tests.rs` 已拆为三个 include 子文件。

## 2. 接手后的第一组操作

以下命令在仓库根目录执行。本次工作区是 `/home/user/OBF`，其他环境不要照抄绝对路径。

```bash
# 先辨认当前工作线和已有改动，不要先清理目录
pwd
git branch --show-current
git status --short
git log -5 --oneline

# 安装/确认固定 Rust，安装目录已在 .gitignore 中
./tools/bootstrap-rust.sh
export PATH="$PWD/.toolchains/rust-1.88.0/bin:$PATH"
rustc --version
cargo --version

# 仓库带有 Linux x86_64 参考二进制；先探测是否能运行
toolchains/bin/lua5.1 -v
toolchains/bin/luau --obf-runtime-version

mkdir -p target
# 必须先有一份完整基线，不能只看 cargo build 成功
./tools/test-matrix.sh >target/handoff-matrix.log 2>&1
tail -15 target/handoff-matrix.log

# 体积门自 K14 起是 120,000 B/目标的硬门，直接跑即可（只有施工窗口才需要 OBF_BENCH_SCRIPT_CAP=off 暂停）：
cargo test --all-targets --no-fail-fast
```

K10 之后矩阵在 `cargo test --all-targets` 处不再被体积门卡住（275 门全绿）；需要单独确认生成物时按第 10.2 节的两条 `virtualize` + `cmp` 命令跑，或用 `./tools/bench-vm.sh` 看体积与耗时。

### 环境说明

- `tools/bootstrap-rust.sh` 通过 npm 的固定 `@rustbin` 包安装 rustc、rust-std、cargo、rustfmt 1.88.0；需要 npm、tar 等基本工具。不是安装最新 Rust；自定义路径可用 `OBF_RUST_PREFIX`，但必须指向专用工具链目录：重装时脚本会删除并重建该路径，不能指向仓库、home 或已有工作目录。
- `tools/test-matrix.sh` 优先用 `OBF_CARGO`，其次用 `.toolchains/rust-1.88.0/bin/cargo`，再查 PATH，并把对应目录加入 PATH。手动运行 Cargo 时也必须让它找到同目录 rustc。
- 固定 bootstrap **不安装 Clippy**，不要声称 `cargo clippy` 已通过。历史上另有保持 rustc 1.96.0 源码兼容的要求，但本次实际构建与测试的是 1.88.0，不是 1.96.0 实测。
- `toolchains/bin/{lua5.1,luac5.1,luau,luau-compile}` 是参考可执行文件，**不是 shell wrapper 或文本配置**。缺失、平台不符或不能运行时再重建：

```bash
# 需要 gcc/g++、make、strip 等；JOBS 默认 2，不使用 cmake 流程
JOBS=2 ./tools/build-reference-tools.sh
```

- 重建脚本会安装并 strip 四个参考工具，退出时清理 vendor 下的中间产物。工具二进制受 Git 跟踪，重建后检查 diff，不要随普通功能修改无意提交一批二进制变化。
- `toolchains/bin/luau` 来自固定 Luau `Repl.cpp` 加 `tools/luau_runner_main.cpp`：提供 CLI 相容的标准库、`loadstring`、文件系统 `require` 和 sandbox 初始化。版本探针应输出 `Luau 0.735 (OBF CLI-compatible runner)`。不要换成只调用 `luaL_openlibs` 的简化 runner。
- 测试环境提供 `loadstring/require` 不表示默认 VM 用它们执行源码，也不表示 AST 后端保证反射/动态加载等价。
- 工作区恢复后，`.toolchains/`、`target/` 或历史日志可能不存在；从脚本重建，不把临时路径当成永久交付物。如果 Git 显示大量似曾相识的历史改动，先备份并对照远端/文件内容，不能直接 `reset --hard`、`clean` 或重新提交全部文件。

## 3. 当前入口与输出契约

命令实现集中在 `src/main.rs`，目标名称和保守命名保留集在 `src/target.rs`。

| 命令 | 输入 → 输出 | 关键说明 |
|---|---|---|
| `check` | UTF-8 源码 → 成功/诊断 | 词法、语法及部分绑定检查，不等于原生编译成功 |
| `minify` | 源码 → 单行源码 | 默认安全随机 local；`--no-rename` 保留名称 |
| `virtualize` | 源码 → 单行 VM | 默认 `--backend ast`；旧路径须显式 `native` |
| `dump-ir` | 源码 → 可读 IR | 用于定位 lowering，不是 bytecode |
| `compile` | 源码 → OBF v2 binary | 直接走 AST，不需要原生 compiler，不接受 seed |
| `wrap-bytecode` | OBF v2 binary → 单行 VM | 支持 ISA 修订 1/2；不接受 native chunk 或 OBF v1 |
| `inspect-bytecode` | OBF v2/native chunk → 文本报告 | 自动识别，显示文件实际 ISA 版本；不是旧 OBF v1 reader |

通用参数是 `--target lua51|luau`、一个输入路径（`-` 表示 stdin）。可输出文件的命令支持 `-o/--output`；`check` 和 `inspect-bytecode` 拒绝 `-o`。目标还接受 `lua5.1/5.1`、`roblox` 别名，但请在回归脚本中使用规范名称。

- `--seed` 仅用于 `minify/virtualize/wrap-bytecode`，接受十进制或 `0x` 十六进制 u64；与 `--no-rename` 互斥。`--backend` 仅用于 `virtualize`，`--no-rename` 仅用于 `minify`。
- 上述使用 seed 的命令省略 seed 时生成新 seed（`minify --no-rename` 除外），CLI 只向 **stderr** 输出 `seed: N`。stdout 必须保持纯脚本、binary 或所请求的报告，不能夹带调试日志。
- 相同源码/目标/配置/seed应逐字节复现；宿主 transcript 不参与生成。默认 v2 的 seed 不影响 IR 或公开 canonical bytecode，但会影响private ISA15 wire、per-prototype operand/register/field-order profile、capture/constant池洗牌/池序与fragment binding、record/segment factorial参数、dictionary/metadata/tuple字段序、recipe/token/fragment/segment graph、segment切点与全局物理顺序、K8 per-prototype route表与K9 per-lane affine operand key、K9a混合base86字母表/段宽填充/段序、prototype/record/section布局、LZW helper的二/三字段拆分、ChaCha salts/context/domain、frame states（含keyed dual-lane tag的salt与lane初值）、ciphertext以及最终local/私有字段名；执行时的实际transcript/attestation另影响shares、control mask、final material与能否通过frame门。有限名称空间不保证任意两个 seed 的短名结果必不同。
- 脚本必须没有物理 CR/LF；IR、inspect 文本及 binary 不受“单行脚本”限制。
- 生成、解析或验证失败时不能写出失败结果。当前 CLI 在结果就绪后用 `fs::write` 写文件，**并不是事务性/原子文件替换机制**，不要扩大已有“失败不覆盖”回归的承诺。

### Rust 公共 API

```rust
let ast = obf::parse(source, target)?;
let ir = obf::ir::lower(&ast)?;
let bytes = obf::bytecode::custom::encode(&ir)?;
let program = obf::bytecode::custom::decode(&bytes, target)?;
assert_eq!(obf::bytecode::custom::serialize(&program)?, bytes);
let script = obf::vm::custom::emit(&bytes, target, seed)?;
```

- 便捷入口：`ir::compile`、`vm::custom::{compile,virtualize}`；默认字节切片入口是 `vm::virtualize(source_bytes, target, vm::Options { seed })`。
- 源码压缩：`obf::minify`、`minify_with_options`；优先用 `MinifyOptions::seeded(seed)` / `MinifyOptions::lexical()`，不要假设 Options 只有一个字段。
- 作用域：`scope::analyze`；低层 `minify::minify(source, tokens, target)` 仍是验证外部 token 后的词法模式。
- 直接构造 IR/Program 的调用者需提供 `Function.shared_closure`、`Program.isa_version` 等当前字段。不能删这些字段来恢复旧 struct literal 的编译。

## 4. 架构与文件导航：先找对层再改

```text
默认 AST 后端
源码
  └─ lexer / parser → owned AST + 全局 byte span
      └─ scope → BindingId、引用/写入、作用域、传递 upvalue
          └─ ir/lower → typed register IR / CFG / cell / pack
              └─ bytecode/custom → 选指令、回填标签、canonical OBF v2
                  └─ vm/custom/semantic → private ISA15 token/fragment/pool graph（含 K9 per-lane affine operand value key）
                      └─ vm/custom/lowering → per-prototype operand/register/field-order ABI + handler access lowering
                      └─ vm/custom/seed + seed_deform → opcode 0–48 逐 seed 7-op 汇编与模板等价变形
                      └─ per-prototype context segments → global shuffled code pool
                          └─ bounded LZW → inner ChaCha8（字运算走 vm/custom/bitops 多态工具箱）
                          └─ runtime source witness + anti-hook → shares/attestation → final material
                          └─ share-derived mask → fetch/dispatch state；K8 indirect route → pc→rid 二次映射
                          └─ strict frame v2（keyed dual-lane tag）→ outer ChaCha8 → 逐 image 混合 base86 三段
                          └─ vm/custom emit → decoder / runtime / handlers / adapters
                              └─ vm/fields → 私有字段双射
                          └─ minify::finalize_vm → 最终 local / 分号 / 单行 / 绑定复核

显式 native 后端
源码 → vm/compiler → 原生 chunk → vm/lua51 或 vm/luau → OBF v1
     → 旧解释器/handler → minify::finalize_vm
```

| 文件/目录 | 负责什么；容易混淆的地方 |
|---|---|
| `src/lib.rs`、`src/main.rs` | 公共入口、CLI 参数/输入输出；不是 VM 指令实现 |
| `src/lexer.rs`、`src/parser.rs`、`src/ast.rs` | 双目标前端、owned AST、插值 fragment、私有语句结束位置 sidecar |
| `src/scope.rs` | 绑定、声明生效、引用写入、globals/upvalues、反射屏障 |
| `src/scope/rename.rs` | 名称分配、跨域干涉、同域匹配修复、最终验证 |
| `src/minify.rs`、`src/random.rs` | 字面量/分隔 emission、最终处理；共享确定性 PRNG 与新 seed |
| `src/ir.rs`、`src/ir/lower.rs` | 新 IR 和 AST 编译；target-specific 求值顺序、循环、捕获、标量分析 |
| `src/bytecode/custom.rs` | **新 OBF v2** Header/ISA/codec/校验，不是原生 reader |
| `src/vm/custom/` | decoder、frame/closure/pack runtime、分发和最终组装；`semantic.rs`负责canonical Program→seed-specific ISA15 semantic/segment/pool graph与K9 per-lane affine value key（`WIRE_ISA_VERSION`在此，改wire版本只改这里）；`lowering.rs`负责per-prototype operand/register/field-order profile、Lua store/getter/mapper、field factorial置换、fragment binding与handler token-level register-access lowering；`seed.rs`/`seed_deform.rs`负责opcode 0–48的逐seed 7-op汇编发射与模板臂/站点等价变形（`seed_deform.rs`是从`seed.rs`为过80 KiB门拆出的，逻辑同一）；`compress.rs`负责8 KiB bounded LZW与inner ChaCha8 body；`chacha.rs`负责标准ChaCha8、runtime KDF/attestation及五个Lua crypto fields；`bitops.rs`负责K7按目标分叉的多态字运算工具箱（每操作3变体、hot双闭包、`2^32`模数拼写抽取）；`cipher.rs`负责runtime shares与strict frame v2（含keyed dual-lane integrity tag）；`emit.rs`负责目标parser/LZW/ChaCha组合、K8 indirect route与semantic fragment——K10 已按阶段拆成 64,639 B（另加 `emit_decode.rs` 8,923 B 解码器文本、`emit_prelude.rs` 10,915 B prelude 阶段），`generate_semantic` 只剩阶段编排；`transport.rs`负责逐image混合base86/分段/水印及宿主逆链；`structure.rs`负责state/dispatch；`tests/{runtime,transport,semantic,seed_v1,bitops,heuristic_audit,stream_audit}.rs`为拆分单元测试（根`tests.rs`只做include）。 |
| `src/vm/fields.rs` | 只压缩显式授权的私有 prototype 字段 |
| `src/vm/opcode/{lua51,luau}/c*.rs` | **自定义 ISA** handler；各子目录 `mod.rs` 注册 |
| `src/bytecode/{lua51,luau}.rs` | 原生 chunk 的 reader/inspect，不要改它们来“修复”默认 IR lowering |
| `src/vm/compiler.rs` | **旧路径**找原生工具并调用，默认 v2 不经过这里 |
| `src/vm/{binary,lua51,luau}.rs` | **旧 OBF v1** 打包及 VM 生成器 |
| `src/vm/opcode/lua51_*.rs`、`luau_*.rs` | 旧原生 opcode handler；不要与子目录的 `c*.rs` 混改/互换 |
| `tests/support/mod.rs` | 固定原生编译/执行与临时工作区；差分优先复用它 |
| `tools/test-matrix.sh` | 完整验收入口；不是只跑 Cargo 的薄壳 |
| `tools/luau_runner_main.cpp` | 固定 CLI 相容 Luau 测试环境，不能随意缩减 |
| `src/anti/.gitkeep` | 通用Anti保留位；当前decrypt anti-hook因参与KDF而在`vm/custom/chacha.rs`，不要重复搬运或误称不可绕过 |

旧 compiler 调用方式：Lua51 用 `luac5.1 -s -o ...`，Luau 用 `luau-compile --binary -O1 -g0`。工具优先取 `OBF_LUAC51` / `OBF_LUAU_COMPILE`，再查当前目录与可执行文件祖先目录下的 `toolchains/bin`，最后查 PATH。设置了错误的显式路径时应报错，不能偷偷换工具。

## 5. 前端、作用域和源码压缩：必须保留的不变量

### 5.1 AST/解析

- AST 完全拥有文本并保留 **UTF-8 byte offset**；不能把字符下标当 byte span。插值中的节点也使用整份源码的全局坐标。
- 外部 token-array 入口会验证边界、顺序、EOF、位置/目标并重新词法化核对。不要把不可信 span 直接拿去切字符串。
- 递归深度之外还有限制线性运算/后缀链，防止迭代建出的深树在递归析构时爆栈；不要只限制 parser 调用栈。
- `Target::is_reserved_name` 是保守的**命名分配**策略，不是 lexer keyword 文法。尤其不能用它拒绝 Luau contextual `continue/type/export` 的合法标识符用法。
- 已审计调用基底/后缀、换行调用、类型断言次数与 unary 优先级、const 初始化/写入、模块 return/export 冲突、type function 捕获等；改文法先查 `tests/parser_audit.rs`。
- `FunctionBody.has_vararg` 区分无类型标注的 `...` 与非变参；`Call.type_arguments` 不能在新 AST walker 中漏掉。Luau 函数签名按固定 0.735 的外层值作用域解析；`typeof` 和 `Module.Type` 的模块前缀可能包含值引用。
- `check` 不做完整 Luau 类型检查，也不承诺原生 compiler 的所有控制流/资源限制。不要为“让 check 和 compile 一样”硬塞一套不正确的词法拒绝规则。

### 5.2 local 改名与安全复用

- 身份依据是 **BindingId**，不是拼写。普通 local 的初始化器先读旧作用域，再引入新绑定；local function 则需要递归可见自身。
- `Scope.name_scope` 是同名分配约束组，不是 lexical scope：参数与直接 body、for 变量与直接 body 共组，但不合并词法作用域。
- 同组中未使用/被遮蔽的声明也不能引入新撞名；仅验证引用图不足以发现 unused-local 冲突。
- 跨域复用依赖声明真正生效的 reference ordinal、可见性及捕获/写入干涉，不能逐块清空名字池，也不能只按 source span 判断。
- 候选是一/两字母，排除保留词、globals、类型/泛型和受保护名字；原本已很短的可改名 local 也必须换名。允许使用其他**同时改名** local 的旧拼写。
- 同域迭代匹配仅在外部颜色固定的情况下修复冲突；不能做无界回溯或跨域任意交换。失败返回 Diagnostic，不偷偷扩为三字母或只改一部分。
- 用户导出、隐式 `self/arg`、type function 的保守保留规则仍要遵守。`Reference.is_write` / `LocalBinding.is_const` 参与最终核对，修改表字段不等于重新赋值 const 绑定。

### 5.3 分号与字面量

- 完整语句之间、非空块末语句与 `end/else/elseif/until` 之间优先用 `;`。已有分号不重复，EOF 不额外补，空块不添加空语句。
- `local a`、`return a`、运算符/数字的必要间隔、表字段语法、函数表达式后的调用后缀仍须保留。不能把空格批量替换成分号，也不能靠遇到 `end` 推断语句结束。
- 分隔依据是 parser 私有、可选、有界的语句结束位置 sidecar，覆盖嵌套函数、类型表达式、type function、插值 fragment；改名会改变长度，必须重新收集位置。
- 保留字符串的值而非任意改写内容：NUL/非 UTF-8 字节、长字符串首换行、CR/LF、Luau 转义/插值边界都已有回归。
- 普通 minify 不做 AST 常量折叠/DCE；不要把 `ir/lower` 为目标语义所做的优化顺手搬进通用压缩器。

## 6. IR 与 VM：不要“简化”掉目标语义

### 6.1 核心表示

IR 是**非 SSA typed register IR**，基本块有符号后继，编码阶段才回填地址。local/参数用 heap cell，临时值是普通寄存器；闭包持有 cell。退出作用域 `Clear` 只释放寄存器引用，循环新轮次/槽位复用创建新 cell，不能覆盖已逃逸闭包的旧 cell。

多返回值、vararg、实参和返回值通过显式 **pack.n** 搬运。不能用 `#pack`、`ipairs` 或 nil 终止规则代替长度，否则尾部 nil 会丢。`Call` 处理 VM/宿主函数和回调；VM→VM `TailCall` 替换 frame，普通非尾递归仍依赖宿主栈。

生成器会预先捕获所需宿主基础函数；不要在用户已改写全局后重新从全局取 VM 必需能力。闭包 descriptor 的弱表与 shared-closure 缓存用途不同，不能统一改成强引用/弱引用来“优化 GC”。现有回收测试并不保证所有 GC 时点一致。

### 6.2 两个目标不能机械共用的行为

| 位置 | 维护注意事项 |
|---|---|
| 赋值 | Lua51 重复 store 右到左；Luau 有提前写入、冲突延迟与多返回值 staging。先保留原生差分再改 |
| 操作数 | `Operand::Cell` 在消费时读 local，其他值可能先存 temp；不能把所有操作数都提前快照或都延后读取 |
| 方法调用 | Luau 的 receiver/方法查找时机与参数求值耦合；userdata 通过静态 NAMECALL adapter，不能直接改成 `object[key](...)` |
| 表构造器 | 列表/记录字段刷新顺序和最后一个 multret 不同；当前 Lua51 按 50、Luau 按 16/记录边界处理，不能随意合并 |
| numeric-for | initial/limit/step 求值与准备分目标；Lua51 的 initial-step 再加 step 与 Luau 不同，浮点边界有意义 |
| Lua51 数值/arg | 保留纯数字面折叠、`+0/-0` 常量去重，以及固定 `LUA_COMPAT_VARARG` 的隐式 arg slot/table 行为 |
| Luau integer | i64 编码和重建不经过 double；`-Ni`、变量的动态 unary、可传播常量不是同一规则 |
| 插值 | 所有插值参数先求值，再转换为字符串；不能把用户重写的 tostring 偷换进其原生规则 |
| 广义迭代 | raw `__iter`，nil 与 false 不同；无 `__iter` 时保留 callable table/userdata，再处理普通 table；元表自身的 `__index` 不应被触发 |
| 模块导出 | export cell 链接到导出表，读写保持 live binding；最终 freeze/显式返回路径不能混淆 |

### 6.3 可达性、运行时捕获与共享闭包

- Luau 的只读标量/纯条件分析不运行用户函数、表访问、元方法或全局读取；支持范围、字符串上限和工作预算不能随意扩大。
- 已证明死分支中的 `continue` 不再导致合法 repeat 误拒绝；活跃 continue 可能跨过 until 所用 local 初始化时仍保守拒绝。不能删 guard 来“提高支持率”。
- 编译结束后，`finish_captures` 从实际 IR 的 Read/WriteUpvalue 和子函数 capture 自底向上裁剪类型/死代码捕获，并重编号自身及传递捕获。**公开 scope/resolver 仍需检查原始类型与绑定**，两者职责不同。
- Lua51 按其规则新建闭包；Luau 根据写入、作用域、初始化器和实际运行时捕获决定可共享性。不能只看“有没有 upvalue”，也不能把 readonly、参数、循环、递归 self 混作一种捕获。
- `Function.shared_closure` 和 `Capture::RecursiveLocal` 参与编码；缓存比较用原始值相等，不调用用户 `__eq`；递归初始化不能拿尚未写入的 self cell 去比较旧闭包。

## 7. OBF v2 数据格式与双侧校验

详细表格只维护在 [自定义字节码.md](自定义字节码.md)。这里列会影响兼容的关键点：

- magic 是 `OBF` 加**数值字节 2**；端序为 little-endian。Header 的 offset 24 保存 ISA 修订，不是外层格式版本；offset 6 宽度码为 `0`（varint 变长指令流）。
- 默认 ISA 修订 2；修订 1 保持读取、运行和原样序列化。`Program.isa_version` 必须保留真实值，CLI 不能用当前编译器常量冒充文件版本。
- prototype 按 ID 顺序，父先于子，entry 固定 0。寄存器、常量、prototype、upvalue 从 0 编号；pack 元素从 1 开始。指令流为 `[opcode][Form 对应字段 varint]`（2~7 bytes），prototype header 带 `instruction_byte_count`（区间 `2n..7n`），无 AUX/trailing data word。
- varint 必须最小编码（末组非零，≤4 组），字段值域 A/B/C ≤ 255、Bx ≤ 65535、Ax ≤ 0xFFFFFF；解码必须恰好消耗全部指令字节。逻辑 word 仍是 (opcode,A,B,C)，`Jump.Ax` 是该 prototype 内**绝对指令编号**，不是 byte offset，也不是 native sBx。IR branch 编为 `Test + Jump + Jump`。
- 49 个稳定 opcode 编号不能重排；Lua51 拒绝 26/43/48（FloorDivide/Export/Freeze）。未实现指令不得变成 NOP。
- flags bit0=variadic，bit1/2=Lua51 legacy arg slot/table，bit3=修订 2 Luau shared closure。bit3 只允许非入口 Luau prototype。
- capture 是两字节 `[kind,index]`；kind0=父 local cell，kind1=父 upvalue，kind2=递归初始化 local cell。kind2 只与合法 shared flag 同用，每 prototype 最多一个。
- binary64 按位编码、i64 精确编码、字符串为原始 bytes；Rust codec 的位级 round-trip 不意味着目标 Lua 能保留任意 NaN payload 的运行时观察。
- Rust reader/validator **和生成的目标 decoder**都验证版本、checksum、长度、reserved、父子/捕获关系、opcode/目标、操作数、jump/test 和末指令。修好 checksum 也不能绕过结构检查。
- Adler-32 只用于损坏检测；字符串转义不是加密。结构验证也不是 sandbox：合法 bytecode 仍可死循环、类型错误或调用宿主能力，不能执行不可信内容。

## 8. 最终生成阶段：local 和私有字段是两套策略

实际顺序位于 `src/vm/custom/mod.rs::finalize`：

```text
generate 完成所有模板、handlers、NAMECALL adapters、执行尾部
    （整体包进 local x={};return setmetatable({...},x):m()：
      载荷表 = 5 个随机数字键 section 函数 + 随机字母键入口函数）
    → fields::shorten
    → minify::finalize_vm
    → 返回结果（不再追加任何源码）
```

### 整体方法调用包装（generate 阶段）

默认后端与 `wrap-bytecode` 的 chunk 只有 `local x={}` 和 `return setmetatable({...},x):m()` 两条语句；全部代码以多个函数存放在载荷表内：随机数字键的 5 个 section 函数（宿主捕获预导 `return SC,Z,...`、bytecode decoder `return P,np,entry`、操作数校验、CV/SV/Lookup 辅助 `return CV,SV,Lookup`、含 W/Call/Make/H 的解释器簇 `return H`），以及随机单字母字符串键的入口函数 `(VMS,...)`——按顺序串联各 section、执行 `H(entry,Z(...),{})` 并返回结果。section 之间用参数/返回值传递状态；互相递归的 Call/Make/H 保持同一函数作用域。`:m()` 直接命中载荷表自有键，chunk 真正使用 `...`（入口 prototype 含 Varargs 指令）时才写 `:m(...)` 转发。方法名与数字键走独立 seeded 随机流，不影响 bytecode、最终 local 或私有字段名。`scope::generated_vm_plan` 会下降到各 section 函数体内寻找唯一环境捕获；`vm::tests::no_inline_metadata` 允许数字/字符串键的函数字段。旧 native 后端不做此包装，仍按顶层语句审计。

### 私有字段：新接手最容易漏掉的部分

`src/vm/fields.rs::PROTOTYPE_FIELDS` 当前有 **16 项**：

```text
k tags u parent m p flags nu nk nc shared self code cached control routes
```

`routes` 是 K8 加入的 per-prototype indirect-route 表：fetch 处先按 `(pc×mul + token×add + fid×salt) % 65521` 选 route，再用 `route_info[pc]` 校验 `rid` 与 bundle 宽度（1–4），缺失或不符即 `E()`。它与其余 15 项走同一 schema/双射/复核链，**新增或改这个名字必须同时改 constructor、decoder、parse 期重复检查与 handler 读点**——历史上把 `PT[op]` 当成 Form 表用就是在这一层踩的坑（见 `总路线.md` 2026-09-10 K9 条目）。

- 模板通过 `__obf_proto_` identifier marker 明确授权；constructor、decoder、操作数校验、缓存、alias 和 handler 的读/写必须使用同一 schema。
- 用独立 seeded 随机流分配互不冲突的一/两字母名，当前 16 项均单字母；不是嵌入 runtime 映射表，也不修改 bytecode。不要固定写死某个 seed 下的 `code → z`。
- 按 token/span 改写，二次 lex 核对未标记 token 和所有字面量不变。未知/误用 marker、池耗尽等应拒绝，不做字符串全局替换。
- **不动**用户 key、公共 pack/arg 的 `n`、模块导出、宿主库/元方法名称和静态 NAMECALL。即便用户方法恰叫 `__obf_proto_code`，`object:__obf_proto_code()` 也必须保留。
- 当前内部 VM 模板不使用 Luau 插值；若将来引入，先实现 fragment 处理，不能漏改内部访问。这条限制不影响用户 Luau 插值编译。
- 新增私有字段时同时补 schema、所有构造/访问点及回归。不要仅把 `.code` 换掉、漏了表构造器或子函数 alias。

### 最终 local 与环境例外

`minify::finalize_vm` 仍是 crate-private；它与普通 minify 不同，要求每个**显式** VM binding 最终已换为不同的一/两字母名，保留同域唯一、绑定图和解密后 payload 校验。生成器环境例外共两类，均形状固定、逐字符审计：

```lua
local G=(getfenv and getfenv(1))or _G      -- 唯一环境捕获（1 处）
local A=DB and GI(LS,"s")                  -- Luau × 6（份额 3 + 分段 3）
local A=DB and GI(LS,"S")                  -- Lua 5.1 × 6（份额 3 + 分段 3）
```

前三个 key probe 折叠实际 source transcript 并派生 share；后三个 segment probe 保持规范 source/what 门；anti-hook字段另有6个直接source observation，检查native/generated crypto函数同源。`scope::generated_vm_plan` 因此要求observation总数**恰好 12 个或 0 个**（旧 native 后端为 0），任何部分集合都拒绝。custom probes 只接收 prelude 已捕获的 `DB/GI/LS` 参数，最终脚本不拼出这些反射名称；rename barrier 必须恰好是唯一 capture span 内的三个 `getfenv/_G` 引用，capture 外任何屏障都拒绝。不能泛化成“VM 可以任意反射”，也不能开放跳过安全检查的用户参数。普通 minify 检测到已知反射时保留名称，默认 AST 编译对用户源码的已知反射报诊断；正常 `_G/_ENV` 数据名称访问与任意环境/栈反射不是一回事。

### ISA3 semantic virtualization（2026-09-07）

这是对纯静态破解报告的主修复，不是另一层字符串加密。旧产物即使解开 transport，也能把每个重编号 opcode 的 Lua handler 一对一归类，prototype code 仍接近原线性 chunk；当前 `generate` 忽略用于传输的 canonical byte slice，只从已验证 `Program` 调用 `semantic::encode(program,seed)`：

- 按 CFG leader 切边；非控制直线段组合为 1~4 primitive recipe，控制操作只允许在 singleton/final 位置。
- recipe 全程序去重并分配随机非零 u16 ID；dictionary primitive 还由 recipe ID、序内位置和 seed mask 编码；use-site 只有 operand varint，不带 opcode。
- record 格式为随机 `label,next,skip,recipe_id` + operands，物理顺序 seed 洗牌；Jump Ax 改目标 label，Test 使用两条显式后继。target parser 建 `code[label]`，解释器不再线性 `pc+=4`。
- sibling prototype 以 parent-before-child 约束重排，parent 与 Closure Bx 同步 remap；另加入 2~4 节点 synthetic subtree（内部 Closure 链真实，首节点无 live Closure 入边），改变 prototype count/tree topology；code count 也改为 bundle count。
- 每个 image 有四个 graph 未引用但 descriptor 合法的 decoy recipe；其 dictionary 声明与 Lua superhandler 执行序列故意不同。分析器必须先恢复 graph reachability，不能把所有 syntactically valid arm 当 ground truth。

该批次目标端只接受 encoding 1 / ISA3 embedded image，并严格检查 recipe、canonical varint、label/后继、控制位置和 prototype/Closure/capture 关系。公开 `.obf` 仍为 encoding 0 / ISA1|2，`compile`/`decode`/`serialize` 契约不变；当前目标端已迭代到 ISA14，`decrypt_embedded` 返回 ISA14 image，测试仍与同 Program/seed 的 deterministic re-encode 对照。覆盖测试插桩 live recipe ID 后展开 primitive，仍为 46/46、49/49。结构门还断言至少三分之一 canonical words 属于多 primitive superoperator、存在四 primitive recipe、record/prototype 洗牌、2~4 个 synthetic prototype 及 recipe decoy 不可达；破坏性测试直接修改 ISA3 dictionary/graph/prototype metadata并证明用户代码前拒绝。

已知边界必须如实保留：primitive handler 文本仍在 Lua 中，常量及 prototype 元数据仍可分析，攻击者可以实现真正的 devirtualizer；改进是把“解码+逐条翻译”升级为 recipe 甄别、CFG 恢复和重新线性化问题，不是不可破解证明。

### ISA4 context-token 深层状态机（分阶段第一批，2026-09-08）

本批先实现用户列出的低到中风险结构项，不在一次变更中同时引入分组密码和跨函数语义变换：

- 每个 record 的第四个 u16 从稳定 `recipe_id` 改为五阶段 context token；每阶段为模 2^16 的奇数乘法可逆层，并混入 label、next、skip、prototype、随机系数及交叉项。Rust encoder/decode 回归逐上下文验证严格往返。
- 目标 parser 与运行解释器共享同一个 `RD`。验证 record 时先恢复 ID 才能选 operand schema；运行时每次 fetch 再动态恢复，code table 不缓存明文 recipe ID。
- `RD` 是 seed-shuffled 11-state machine：5 个 live inverse state、1 个 return state、5 个无 live 入边的 dead state。每个 live state 的真实运算再埋入 4~5 层依赖已验证整数上下文的不透明谓词；每层死侧和五个死 state 都携带算术更新与单次 `repeat ... until false`。
- 同一 recipe 在不同 graph context 下生成不同 token，不能再用 use-site u16 直接分组；私有 header 升级为 encoding 1 / ISA4。公开 OBF v2/ISA1|2 完全不变。
- 专项测试覆盖五层参数、奇乘数逆元、64 组上下文差异、损坏 token fail-closed、嵌套/死路径密度、固定 seed 复现，以及 Lua 5.1/Luau 语法编译和真实执行。固定 seed golden 为 68,344/75,615 B，预算为 72,000/80,000 B；完整门禁 PASS174（62+112），bench 40/57 ms。

这是第一批而非最终完成；其明确遗留的明文 successor 与 orphan decoy 问题由下一节的 ISA5 第二批处理。

### ISA5 successor token 与 reachable neutral CFG（分阶段第二批，2026-09-08）

- record 的物理四个 u16 槽位保持不变，但第 2/3 槽从明文 `next/skip` 改为三阶段 edge token。每层为奇数乘法可逆仿射层，绑定 source label、prototype ID、edge kind（next=0/skip=1）、seed 系数与 `((source+kind)*(prototype+1)) mod 65536` 交叉项。
- target validator 与每次解释器 fetch 都先调用独立 `ED` 动态解出 next/skip，再以真实后继调用 `RD` 恢复 recipe ID。持久 `code[label]` 仅保留 token；明文后继只作为短生命周期 local，绝不缓存回 record。
- `ED` 是 seed-shuffled 7-state machine：3 个 live inverse state、1 个 return、3 个 arithmetic/repeat dead state；每个 live stage 另有 2~3 层动态 opaque predicate。`RD` 原有 11-state、每层 4~5 重谓词契约继续保留。
- 每个**真实** prototype 入口插入 2~4 个 neutral bundle，并按函数规模从原 next/test edge 抽取至多 5 条、各插入 1 个。通常给 frame 增加一个私有 scratch register并执行 `Nil/Not/Move/Clear` 序列；若 frame 已用满 256 slots，则安全退化为 `Move r0,r0`。插入后重洗 physical records，因此这些节点既 graph-referenced 又会真实 fetch/执行，不能再仅靠 Closure/record reachability 删除。
- 四个 dictionary-unreferenced poison recipe 与 2~4 个 live-tree-unreachable synthetic prototype 仍保留，但文档和测试明确区分它们与 reachable neutral recipe。运行插桩证明 root entry neutral ID 实际出现，同时 poison ID 不执行。
- private header 升到 encoding 1 / ISA5；公开 canonical OBF v2 / ISA1|2 不变。破坏性回归新增 edge token 解出不存在 label 的场景；双目标语法/runtime、三/五层双向 token、64-context 差异、state/dead-code 密度、debug/release/native/VM parity 和体积门继续执行。固定 seed golden 为 71,084/78,081 B，bench 46/62 ms，完整门禁 PASS175（63+112）。

这仍不是总目标结束：decoder 算法开源可逆，prototype 元数据与 primitive 模板仍可分析。其当时遗留的 dictionary 自描述和连续 superhandler 问题由下一节 ISA6 第三批处理。

### ISA6 live descriptor camouflage 与 global semantic fragments（分阶段第三批，2026-09-08）

- `SemanticRecipe` 不再以一个 `ops` 同时承担 wire 描述和执行真值，而是明确分离 `descriptor_ops` / `execute_ops`。wire dictionary 只写 descriptor，生成 Lua handler 只读 execute。
- live descriptor 的替换契约是**三项同时等价**：canonical `encoding_form`、目标端 `validation(op)` 的实际 operand 边界、control/terminal graph class。当前六类可替换 family 覆盖 A-register、numeric window、AB-register、upvalue、global/constant-key 和 ABC-register 操作；Jump/Test/Return/TailCall 及其他 singleton validator class 保持原值。不得为了提高统计比例放宽 class。
- 双目标/多 seed 回归要求超过三分之一 live recipe、至少 45% live primitive 被 camouflage；每个替换项逐一比较 descriptor class/Form，且 layout 必须随 seed 变化。poison recipe 的 descriptor/execute mismatch 继续独立存在。
- 连续 superhandler 改为 fragment planner：长度 1 保持单 fragment；2 拆 `1+1`；3 随机 `1+2|2+1`；4 随机 `2+2|1+1+2|2+1+1`。所以所有多-op recipe 必定被拆，3/4-op 又至少保留一个 2-op fusion。
- 每个 fragment 获得唯一随机三位 `sid`。ED/RD fetch 恢复 `rid` 后，`rid` grouped chain 只将 `sid` 设为该 recipe 的随机入口；另一套独立 grouped `sid` chain 包含全 image 的 fragment arms，并在发射前跨 recipe 全局洗牌。中间 fragment 只写 next `sid`，final fragment 才恢复 fetch state；Return/TailCall 不追加不可达语句，避免 Lua 5.1 terminal-statement 语法错误。
- private header 升为 encoding 1 / ISA6。公开 OBF v2/ISA1|2、primitive 编号、`.obf` 字节与 SHA 均不变。固定 seed golden 为 80,369/89,149 B，SHA-256 为 `1be51fd30c946c4803d311b9c9cb3cbb7fbfe05b7d09826a5afc316209e06f78` / `3009575f7aef69b4ecfa331c09cfdc68fb0e9c4791efa3c8b88e0abf08508cfa`；预算 85,000/94,000 B，bench 47/64 ms，完整门禁 PASS176（64+112）。

这批直接破坏“读 dictionary 即得 primitive 序列”和“一个 recipe ID 对应一个连续 Lua body”两个静态恢复假设，但 `sid` arm、转移和 primitive 模板仍存在于最终脚本，完整 devirtualizer 仍可关联并恢复。当时动态密钥/自定义分组传输加密、全局 prototype/function segment pool 与函数 inlining/outlining/reorder 尚未完成；不得把 ISA6 单独宣称为不可恢复。

### 体积预算调整为双目标统一 95,000 B（2026-09-09）

- lua51 整脚本预算 87,000 → 95,000 B（compression 单元门 + `bench-vm.sh` 同步更新）；luau 本已是 95,000 B，保持不变。golden 与 SHA 不变。
- 此举为全 handler 迁移预留 headroom：按最坏 seed 85,522 B 计剩 9,478 B，47 形态约 3.8KB 可直接装下；下条 P0 条目中“否则预算在 slice 4 前告罄”预警就此解除，字节打包不再阻塞 slice 2。

### P0 seed-ISA 完成：47 形态迁移 + classic 明文删除（2026-09-09，当前）

- 基线：wire 保持 private ISA14；opcode 0–48 全量 seed 化，`src/vm/opcode/lua51/` 与 `luau/` 共 95 个 classic 明文 handler 整体删除，matrix 计数门改为缺席 pin。
- 发射：`SEED(prog,site,expect)`（SEED 内移至 H 闭包捕获 R/RX/K；site 按 lane 引用裁前缀；expect 编码 nil=0/1/2/9；value-first 返回，TailCall 短返回 `act or v2`）；模板 CALL 改直调，callee 报错原样传播（`seedfail:28` 退役）。
- 门禁：85 向量 + 40 向量双目标差分、lane 子集、活性探测 sweep、tailcall 臂 pin、控制流/全 op 执行；预算维持 95,000/95,000 B（headroom 约 10.3KB/1.5KB）。
- 数字：golden 84,727/93,502 B（SHA `d503c6981bc11536444cc007585a375bb99b74bd7f978bb27f8c9527c37c873f` / `50a501471daee0efbaf2f59b5948f864a841e83f1f83605c4ee6aec6a293a77e`）；PASS220（105+115）；bench 120 ms/60.0x、135 ms/45.0x。
- 下一步：luau headroom 较薄（~1.5KB），后续增长先做 SEEDT 打包或模板 arity 表；字节打包（routine 引用字节化）暂缓（当前 margin 可支撑）。


### P0 seed-ISA slice 1：seed loop + jump/test/return 迁移（2026-09-09）

- 基线：wire 保持 private ISA14，`WIRE_ISA_VERSION=14`、`14/1436/87` 与 canonical `.obf` SHA 全部不变；本批只改发射端（`src/vm/custom/seed.rs` 新增，`emit.rs` 接线，`transport.rs` hidden 门扩展 `pcall`）。
- 发射：jump/test/return 走 `seed_arm_lua` dual-form（`SEED(SEEDx,{a,b,c,k,j,skip1,pc},R,RX,F.__obf_proto_k,STAB,SEEDH)` + 动作应用），routine 数据构建期验证；其余形态保持 classic 体。
- 门禁：`cargo test seed` 24 项全绿；79 向量双目标直接差分（`SEEDOPS PASS 79/79`，含 fdiv/mod 负数 parity）；8 类 routine 手术腐蚀双目标均编译通过、运行失败、零输出；`stages` 九机 pin 不变。
- 数字：golden 84,626/92,850 B（SHA `7469993baba87a364ef60086e65f0c4729f04ce5bfa1c357bb3af32f7105e695` / `0d87bd8afbca99e44c39a199bbdd587e49029e002375741d8bea807e5a4e2481`）；预算 87,000/95,000 B；PASS214（99+115）；bench 101 ms/50.5x、120 ms/40.0x。
- 下一步：slice 2（ALU 簇）前先做字节打包与 arm 瘦身设计，否则预算在 slice 4 前告罄；P0 末删除 classic 路径并更新 46/46、49/49 覆盖口径。

### ISA14 capture/constant pools（第九阶段第五个结构子阶段，2026-09-08）

- `WIRE_ISA_VERSION`从13升到14；公开OBF v2/ISA1|2、record/segment factorial与dictionary/metadata/tuple per-image置换、operand/register ABI、carried-value fragment、六状态entry、bounded LZW与双ChaCha8/frame-v2 transport不变。本批只把capture/constant从header后连续区移入两个全局洗牌池，header仅保留24字节metadata。
- 池布局：24字节header后先capture池后constant池（`pools_flipped`按image翻转）；每记录3匿名u16 token携带掩码owner/slot/index/tag并按per-record factorial profile重排，再接payload。`nu/nk`留作池计数。
- `emit.rs`目标parser先验证两池总量/掩码/token合法与全局洗牌coverage，再按owner切片；`CU/CK`经`slot_rewrite`g-slots在`PU/PK`间共享，无真实upvalue。`local TU,TK=0,0;`计数，`for slot=1,TU/TK`消费。
- 新增`capture_constant_pools_exist_in_wire_image`（翻转seed行为差分，一致输出`1`）、`target_decoder_rejects_every_pool_corruption_on_both_targets`（池错序双目标无输出拒绝）与`generated_parser_reads_global_capture_constant_pools`；旧walker迁移到`semantic_pool_layouts`，全单例池豁免交错要求。
- fixed output为 **81,515/89,777 B**，SHA-256为`d6392170a448f3e400ea5786920f5517e00dd80cbd96a030149f3bed93154eee` / `ca21e582f5d3cbcb24eb0646728ed200f2b2fb163bb0421bbc2f73852dfacde1`；完整LZW frame为`12,332→11,327 B` / `15,559→15,104 B`。完整矩阵为 **PASS200（85单元 + 115集成）**；15-run bench为Lua51 102 ms / 51.0x、Luau 122 ms / 40.7x。
- 边界：池掩码/置换规则与全部inverse仍在客户端，静态分析可对每image重建池映射。该批只完成capture/constant全局池化；函数结构变换仍须独立评估，不得宣称不可逆或静态恢复已解决。

### ISA13 parser field-order de-canonicalization（第九阶段第四个结构子阶段，2026-09-08，历史）

- `WIRE_ISA_VERSION`从12升到13；公开OBF v2/ISA1|2、operand/register ABI形状、carried-value fragment、六状态entry、bounded LZW与双ChaCha8/frame-v2 transport不变。本批只重排record/dictionary/segment/metadata/tuple字段顺序，不改opcode/edge/recipe语义与固定slot宽度。
- `lowering.rs`新增`FieldLayout`/`field_perm`/`emit_field_perm_lua`：`field_seed`从独立salt派生`(mult,add)`，乘子固定于与阶乘互素的候选集（4槽M=1,5,7,11,13,17,19,23；3 token M=1,5），factorial数`(id×M+A)%n!`决定逐项置换。record用prototype id逐prototype变化，segment用物理slot逐segment变化；Rust两端与生成Lua用同id/slot实时重算，不输出逐项对照表。
- dictionary entry header、metadata的u32×4/u16×3/u8×2宽度组与内存`__obf_proto_` record tuple因无逐项id，用per-image置换，以字面顺序直接baked进parser文本；`record.rs`测试支架与Lua `sont` helper都走keyed查找而非固定位置。
- 固定宽度slot读取位置不变，只有语义归属随profile变化；record-slot/segment-token/dictionary-header/metadata-slot四类错序（checksum修复后）全部落入既有label/token/operand/count门，在用户代码前fail closed。validator的`I[2]/I[3]/I[1]`静态fetch下标不再跨image稳定。
- 新增`field_order_permutations_decouple_parser_from_canonical_layout`（固定seed parser字段序期望+跨seed变化）与`target_decoder_rejects_field_order_confusion_on_both_targets`（双目标四类错序无输出拒绝）；旧ISA12 private version pin测试同步升到13。
- fixed output为 **79,426/87,873 B**，SHA-256为`f6ece30c4bbdea852a8fd365d8902a1285d03618c7e0d1636469443f6904bd92` / `d5980b569759fead1099e88802890373328528d2bb281c21162fdb89c2ea49c8`；完整LZW frame为`11,872→10,703 B` / `15,203→14,653 B`。完整矩阵 **PASS192（77单元 + 115集成）**，bench 98/116 ms。
- 边界：factorial置换规则与全部inverse仍在客户端，静态分析可对每image重建字段映射。该批只完成parser/recipe/CFG字段顺序去自文档化；captures/constants全局池、函数结构变换与全部静态恢复问题仍是后续独立阶段。

### ISA12-C carried-value dataflow fusion + entry graph（第九阶段第三个结构子阶段，2026-09-08，历史）

- `WIRE_ISA_VERSION`仍为12；公开OBF v2/ISA1|2、semantic/segment bytes、bounded LZW与双ChaCha8/frame-v2 transport不变。本批只改变fragment emission和entry wiring，不伪造新的wire revision。
- `lowering.rs`以目标lexer token span识别融合资格。producer必须是唯一direct write的单语句`R[a]=expr`；consumer必须至少有一个register read，direct write若存在只能是开头单赋值。control、multi-write、重叠`R[R[..]]`与未证明形态返回`None`并拆成single，畸形模板返回Diagnostic。
- planner对最长4项recipe枚举最大非重叠匹配；2-op fragment因此必定是真实dataflow pair。producer先保存逻辑target并求`RX(target)`，只求值一次RHS、保持register write；再执行第二项`OG` binding。consumer所有安全read改为`RF(index,target,value)`，依赖命中时绕过register reload。
- `RK(fid,R)`共同返回`RX/RF`。四register family分别使用逻辑相等、反向不等、物理key相等和shifted-mod-257相等；显式value参数不会像Lua `and/or`选择那样丢失nil/false。handler仍按recipe全局共享，不复制per-prototype body。
- entry把prelude、三share probe、base86 segments+watermark、decrypt/LZW/parser、validator/runtime bind、execute六阶段放入新增状态机；state值、文本arm顺序、比较拼写由seed变化。依赖仍严格，所有inverse仍客户端可见。
- 新增`carried_value_fusion_rewrites_both_primitive_boundaries`和`fused_dataflow_preserves_nil_false_aliasing_metamethods_and_errors`；fragment门验证`fragments+fusions=operations`、每pair至少一个forwarded read，sampled完整fixture为50~60 fusion/65~85 reads，超过四poison recipe最多8 pair，故live recipe实际使用。entry总machine数由8增至9并禁止旧线性export锚点。
- fixed output为 **78,706/87,167 B**，SHA-256为`a9ef0ee9782615c9ca2eaa5c7b5a36aaa83b8e0900fc37ec0f47b1b9db26477b` / `d43482096f2bb4f9f89e69f0607f4c508a4d82b5498c5d65a1ff8b689967c68a`；完整矩阵 **PASS190（75单元 + 115集成）**，bench 96/117 ms。
- 边界：逐primitive“operand bind+原handler”桥和顶层稳定线性入口被削弱，但fusion eligibility、`RF`、primitive运算、semantic parser字段及全部inverse可静态模拟。captures/constants全局池、函数结构变换与全部静态恢复问题未完成。

### ISA12-B per-prototype register ABI（第九阶段第二个结构子阶段，2026-09-08，历史）

- `WIRE_ISA_VERSION`保持12；公开OBF v2/ISA1|2、semantic/segment wire、bounded LZW、ISA11 transport及ISA12-A operand ABI均不变。`src/vm/custom/lowering.rs`继续承载独立register planner，未批量修改两目标约95个primitive模板。
- `register_layout(seed)`通过独立salt派生family/stride/shift仿射参数：family模4乘数为1/3，stride模127与shift模257使用任意非零乘数。三模两两互素，周期`4×127×257=130,556`，故允许的32,767个private prototype id无profile碰撞。
- family选择四个互斥257-key bank；域内分别使用正向仿射、反向仿射、逻辑逆序仿射、shift-before-multiply公式。stride产生`1+2×stride`非零乘数，因此逻辑寄存器0..255在每profile内单射，物理key严格小于1,028。目标只保留常数规模公式，每frame由`RK(fid)`创建一次`RX` mapper，不输出256项表。
- `lower_register_accesses`先按目标lexer取得token span，只识别crate-owned handler的标识符`R`后接`[`并按括号深度包裹索引；字符串/注释不做全局文本替换，畸形模板返回Diagnostic。它覆盖`a+1/a+2`、`d[2]`和`Clear`循环。`SETUP`显式使用同一`RX`初始化参数、legacy arg槽及vararg table cell，随后把mapper交给当前递归frame；嵌套closure/pcall不会共享可被覆盖的全局slot。
- `per_prototype_register_abi_lowers_every_primitive_access`对8 seeds遍历完整id域，抽查256-slot单射/界限，验证无表factory与两目标全部supported handler access；raw生成物禁止旧`R[a]/R[b]/R[c]/R[i]/R[d[2]]`面。`register_abi_preserves_cells_captures_callbacks_loops_and_tailcalls`对两目标及Lua51 legacy/Luau vararg执行3 seeds原生差分。
- fixed output为 **74,540/83,040 B**，SHA-256为`927b84824299ab9a45df3a5aad3db67d016ed11a7ab31bc2155ea1029e4e7dd1` / `135514905e4e101cc282b0d58b098426fb2dc2bee5a9e4424bb46113685f758c`；完整LZW frame仍为`11,872→10,678 B` / `15,203→14,644 B`。完整矩阵 **PASS188（74单元 + 114集成）**，bench 96/115 ms。
- 边界：mapper可从脚本计算，primitive算术/表语义和共享`OG`仍可识别。本批切断规范register key桥，但不等于真正handler/dataflow fusion；parser/入口去自文档化、captures/constants全局池、函数结构变换仍是后续独立阶段。

### ISA12-A per-prototype operand ABI（第九阶段首个结构子阶段，2026-09-08，历史）

- `WIRE_ISA_VERSION`从11升到12；公开OBF v2/ISA1|2、semantic recipe/edge/segment schema、bounded LZW和ISA11 transport不变。新模块`src/vm/custom/lowering.rs`隔离operand ABI规划，避免继续膨胀`emit.rs`。
- `operand_layout(seed)`派生三个仿射分量：family模4使用乘数1/3，rotation模3使用1/2，lane模2,731使用任意非零乘数。2,731为素数且`4×3×2,731=32,772`，所以private image允许的32,767个prototype id全部profile互异；不是把完整profile列成明显运行时表。
- 四种物理record family为旋转packed-u24、反向AC+B双区、旋转transposed columns、反向nested tuple；`ob=6+13×lane`避开record固定头。parser只在原varint解码和validator通过后写入这些布局；graph validator和runtime fragment共用`OG(fid,I,qi,form)`恢复原`a/b/c/k/j`。
- fragment不再发射`a,b,c=I[6+3*i],...;k=...;j=...;o=<actual opcode>`。四种`form`分别改变getter的五返回值顺序，调用处用匹配LHS；planner按operation轮换并在每四项后重新shuffle。共享store/getter避免为每个函数复制整套handler，脚本反而因删除重复prelude降到72,198/80,636 B。
- 新单元门覆盖Lua51/Luau、8个边界seed、完整32,767 id域无碰撞、4 family/3 rotation/4 binding、旧bridge/marker消失、semantic payload逐byte往返；既有native syntax/runtime、46/49 coverage、debug/release、semantic/segment/cipher corruption、compression与golden门全部通过。实现时发现`slot_rewrite`不能全局加入`v`（会改坏ED函数参数），故保持`o`槽化并让`OG`中会被改写的local显式初始化。
- fixed output为 **72,198/80,636 B**，SHA-256为`5defa5829298f395b1f91c64b5926ed510cc7a87c48cc1272ad98e627b30dc6c` / `7fd96ee94bb83fe7b27470e74927162c9e6f4828e0eb9ef0f30a3265cecfeefc`；完整LZW frame为`11,872→10,678 B` / `15,203→14,644 B`。完整矩阵 **PASS186（73单元 + 113集成）**，bench 95/114 ms。
- 边界：profile/getter与primitive handler仍在客户端，静态分析可计算每个函数布局并恢复语义。该批只完成operand ABI与actual-op marker删除；register ABI及handler register-access lowering已由上列ISA12-B完成，真正cross-primitive fusion及parser/入口/recipe/CFG进一步去自文档化仍是后续子阶段。

### ISA11 ChaCha8 + runtime material + decrypt anti-hook（第八阶段，2026-09-08，历史）

- `WIRE_ISA_VERSION`从10升到11；公开OBF v2/ISA1|2和ISA10 semantic/control schema不变。当前transport为`bounded LZW → inner ChaCha8 → frame v2 → outer ChaCha8 → watermark/base86`，旧custom XOR streams与generalized Feistel均已删除。
- `chacha8_block`实现标准256-bit key、96-bit nonce、32-bit counter、8 rounds，published zero-state vector锁定；字运算按目标分叉：Lua51走纯算术多态工具箱（3变体/操作、双闭包），Luau走隐藏名捕获的`bit32`/`buffer`多态工具箱（同样3变体/双闭包）；模数拼写与求和项序逐use-site抽取，`2^53`发射门禁。
- final key/nonce/counter不是Lua literal：runtime shares、forms `pv`、domain/context与anti-hook attestation先形成base state，再经ChaCha8 block KDF。inner/outer domain、nonce salts、counter与context互相分离。
- wrapper numeric keys增至29；word ops、quarter round、block、KDF/stream、anti-hook五个fields与其余payload fields共同洗牌。outer与inner每次decrypt前各执行anti-hook；source metadata、helper同源、primitive behavior与ChaCha KAT任一失败即停止。
- frame v2严格检查descriptor/version/header width、length、cookie、tag和0..3 padding。测试覆盖published vector、domain/context/material差分、wrong attestation、全部outer ciphertext byte corruption、行为等价native-byte wrapper、source/loadstring hook、helper/KAT/attestation常量tamper；scope为12-or-0 direct probes。
- fixed output为 **82,201/90,866 B**，SHA-256为`8192a5df629f8db9730bf6536dcdfe1012e408a268216ad057545289eb4403be` / `e40507fff0de952a29d7a66fed6fac4c5e249a7c0b7fd97ba6ca1745f0186f24`；完整矩阵 **PASS185（72单元 + 113集成）**，bench 94/113 ms。
- 边界：ciphertext、decoder与规范环境同时交付；攻击者仍可执行或精确模拟。不要把动态material或anti-hook写成不可提取秘密，下一结构批应处理每函数异构lowering与parser去自文档化。
- **压缩口径纠正**：完整LZW frame必须小于其输入semantic bytecode；整份Lua只保留独立预算，不再与ISA7比较。golden seed实测Lua51 `11,872→10,679 B`、Luau `15,203→14,646 B`，均包含16-byte压缩header。`tools/bench-vm.sh`已删除predecessor gate，单元测试改为Lua51/Luau多seed直接验证正确边界。

### ISA10 runtime witness key + data-dependent control（分阶段第七批，2026-09-08，历史）

- `WIRE_ISA_VERSION` 从 9 升到 10；公开 `.obf` 的 OBF v2/ISA1|2、canonical bytes 与 SHA 不变。ISA9 segment graph、ISA8 bounded LZW、block transport v1、outer/base86、水印和六个 audited probe 全部保留。
- Rust `runtime_probe_witness(target)` 对规范 source（Luau `[C]`、Lua51 `=[C]`）执行 `w=(w×257+byte) mod 2147483647`。Lua 三个 key probe 对 debug API **实际返回**的 source 做同一完整 byte fold，并在各 probe 的 seeded Lehmer rounds 后以 `1+(state+witness×mix) mod 2147483646` 形成 share；fold/share/final keys 不作为数字字面量保存。
- key probe 仍为 scope auditor 允许的 `DB and GI(LS,"s"|"S")` 固定调用形状；Lua51 从返回 table 的 `.source` 取 transcript。nil/非字符串 transcript 在长度或 byte fold 中报错而 fail closed；任意形状合理但错误的字符串（包括空/短值）产生错误 share，随后 outer/block/frame 门拒绝。三个 segment probe 保留独立规范门，因此集成测试能只伪造前三次 source 而让后三次正常。
- 入口将 `(c1+c2+c3)%65520` 写入新增私有 `control` 字段。fetch/dispatch 各有两套互异三位 seeded state，`control%2` 的短路条件选择实际物理对；初始化、比较和转移再表示为 `(selected_state+control)%65521`。这不是普通状态机前的一次性 guard，也不是可统一约掉的公共平移。final recipe fragment 只恢复 `sid`，统一出口集中切回 masked fetch state。
- 单元门 `runtime_probe_witness_is_required_by_every_share_and_control_mask` 逐个将 witness 加一，要求仅对应 share 改变、mask/parity 改变且旧 block frame 不能打开；`emitted_probe_transcript_masks_interpreter_control_states` 检查三份完整 fold、双物理 state 选择与 masked state dataflow。集成门 `encrypted_payload_probes_fail_closed_on_tampered_environments` 新增 source-only hook，Lua51/Luau 都要求 status failure 且 stdout 为空。
- 固定输出 **82,514/91,344 B**，SHA-256 为 `d987f38e79b2c1d4b47748367f67a67ba804cb47e3d06ed6489bdb8077741e47` / `d6321eb2cb6906c33b7e37ebcdbd8549f516d53e4744c85bf3a2baa8db40592f`；相对 ISA9 少 215/378 B，相对 ISA7 少 1,126/772 B。完整矩阵 **PASS184（71 单元 + 113 集成）**；15-run bench 67 ms / 33.5x、88 ms / 29.3x。
- 安全表述必须准确：这一批把短 seed-only replay 升为必须执行或精确模拟 target transcript/share/control dataflow，但规范 transcript 可从标准环境建模，客户端没有秘密，不能宣称不可静态恢复或密码学保密。

### 80 KiB 源文件永久门（2026-09-08，当前）

- 任何 `src/**/*.rs` 单文件必须 `<=80 KiB`（81,920 B）；`tools/test-matrix.sh` 在 Cargo 前扫描并失败。生成的 `.out.lua` 是产物，不属于实现源文件门。
- 原 `src/vm/custom/tests.rs` 为 124,624 B，已按完整 item 边界拆成 `tests/runtime.rs`、`tests/transport.rs`、`tests/semantic.rs`，根 module 仅保留 imports/native support 与三个 `include!`。相对路径随子文件层级调整，但测试 module scope/逻辑不变。
- 拆分完成后、ISA10 功能修改前，重新生成 seed 7001/7351 并与 ISA9 golden `cmp`：82,729/91,722 B 及 SHA `be0757...` / `b7963...` 完全一致。今后任何超限拆分也必须先做同类零生成差异证明。

### ISA9 global prototype code-segment graph（分阶段第六批，2026-09-08，历史）

- private header 从 ISA8 升为 ISA9；公开 OBF v2/ISA1|2、canonical `.obf`、既有 descriptor/edge/recipe/fragment 语义、LZW 和 transport 格式均不变。ISA9 改的是压缩前 semantic image 的 prototype code 物理布局。
- 每个 prototype 先完成完整 dictionary 与 record code，再以 `token_layer[0].add/multiplier` 和 owner 派生 `1..code_len-1` 的切点，生成恰好两个非空 segment。segment ID 固定使用 `2×owner+1/2` 的独立域，prototype header 原 reserved u16 改存 context-masked 唯一 root。
- 所有 prototype metadata/capture/constant 结束后，全部 segment 作为一个全局物理 pool 做 seed-specific 全记录 shuffle。每条 record 显式写分别绑定 image、物理槽、SID 和 owner 上下文的 masked `id/owner/next`，terminal 也必须显式编码并解出 `next=0`；因此 parser 不靠物理邻接推断归属或结束。
- 目标 decoder 固定读取 `2×prototype_count` 条 record，在进入任何 recipe/CFG 验证及用户代码前，检查 SID 范围/唯一/完整覆盖、claimed owner、每 owner 唯一 root、恰好两节点 next 链、terminal、cycle、每函数总长、全局 used、截断、尾随与 pool 精确消费，再按链逐字节重建原 code。
- 私有 segment ID 形式将生成脚本上限收紧为 32,767 prototypes；公开 canonical v2 仍可表示 65,536。semantic label 仍为非零 u16，并为 neutral entry 明确保留 4 个值。
- 结构测试跨 seed 证明非空 owner-derived 切点、跨 owner 物理混排、ID/owner/root/next 完整性和重组结果精确；双目标 corruption 门覆盖 duplicate、missing、out-of-range、wrong-owner-token、wrong-root、cycle、truncation 与 trailing，均要求用户 stdout 为空。
- 为抵消新增 graph decoder 体积，仅 crate-owned generated-VM finalizer 在块关键字边界及 `)`/`]`/`}` 后接 identifier 的安全边界省略可选分号；公共 minify 仍强分号，歧义 `;(` 保留，最终 parser/binding 复核不变。
- fixed-seed goldens 为 **82,729/91,722 B**，SHA-256 为 `be0757fd570a13cb2262a315e96eceeaac2ed78b64463de11f9b8e5dc9e9ff2f` / `b7963ed55553718179c8e4a22376adc1638fc6a45dc801bec0ce27360060bf14`；独立整脚本预算仍为85,000/94,000 B。与ISA7的83,640/92,116 B关系仅作历史体积信息。完整矩阵 **PASS182（69 单元 + 113 集成）**；默认 15-run bench 为 Lua51 68 ms / 34.0x、Luau 88 ms / 29.3x。
- 这一层破坏了 metadata 后连续 code body 的函数边界，但 inverse、primitive 模板与多数 metadata 仍随脚本交付，静态分析可完整恢复。captures/constants 全局池、安全 inlining/outlining/reorder、跨 primitive dataflow fusion 和不可逆静态防护均未完成。

### ISA8 bounded LZW（分阶段第五批，2026-09-08，历史）

- `semantic::encode` 的结构保持 ISA6/7 语义契约，只把 private header 版本升到 8。`emit` 在任何 cipher 前调用 `compress_bytecode`：Welch dictionary 每 8,192-byte 解压输出边界重置，所有 code 用一个连续 LSB-first canonical prefix bitstream。
- compression frame 固定 16 bytes：`LZW\1`、`original_len:u32`、`bit_len:u32`、完整 semantic image 的 Adler-32。原长限 16 MiB；frame 长度必须等于 `16+ceil(bit_len/8)` 且严格小于原像，否则生成失败。没有损失性替换、部分输出或伪造总能缩小的 fallback。
- reference 用 `00 + bounded offset`，低 literal 用 `01 + 5 bits`，其余 literal 用 `1 + 8 bits`。Rust decoder 用 prefix/suffix dictionary 与有界展开栈；严格拒绝 first/forward/out-of-range reference、非 canonical literal、phrase 越界、非零 padding、未完全消费、长度/checksum 不符。全部 truncation、每个 frame byte 单 bit corruption、256 个结构合理随机畸形 frame及不可压缩随机输入均有测试；另以重新计算的合法上游 transport 将 checksum/body 畸形 frame 直接注入两端 Lua inverse。
- 原 constant stream 的 wrapper key pair、round count 和三族 arithmetic family 保留，但覆盖范围扩为整个 compressed body。clear header 的原长、bit 数、checksum、reset count和 body length全部绑定 inner state，header 自身受 block tag 保护。semantic parser 改回普通常量读取，不再维护 selective constant ranges。
- Lua inverse 分为 bit reader、prefix/suffix LZW 与 frame/inner stream。按独立 seed stream选择 bit reader 独立或与 LZW 合并，所以解压逻辑占二或三个 numeric-key sibling fields；wrapper key 扩为 24 个，全部 field 一起洗牌。共享 `X8/AD/L32` exact-integer helper 减少 decoder 体积且不依赖 bit library。
- pipeline 为 `semantic→LZW→inner stream→block→outer→XXS:/base86`。`extract_embedded` 返回 inner-encrypted LZW frame；`decrypt_embedded` 再解密并严格展开为 deterministic ISA8 image。固定goldens **82,572/92,022 B**；相对ISA7 **83,640/92,116 B** 的整脚本关系仅作历史信息；完整矩阵 **PASS179（66 单元 + 113 集成）**；SHA-256 为 `50917035eeaf57c78f44f4efd5b2f3073b1c72e3360d499574da4a901b03b34c` / `fb3a850f5087685d0ffd3563d374312def42fdda503c2158f32bfc752a3ff7ea`。
- 这一批不引入 ZSTD/第三方 crate，也不改变公开 `.obf`。LZW 和 cipher 都可逆；不能描述为修复静态 semantic recovery。当时全局 prototype/function segment pool 与 inlining/outlining/reorder 明确暂缓；前者已由下一批 ISA9 首期完成，后者仍未完成。

### ISA7 dynamic block transport v1（分阶段第四批，2026-09-08，历史）

- private semantic header 从 ISA6 升至 ISA7；inner dictionary/record 继承 ISA6 schema，公开 OBF v2/ISA1|2 与 canonical `.obf` 不变。生成顺序现在固定为 `semantic→constant cipher→block frame/Feistel→outer stream→XXS:/base86`。
- `cipher.rs` 新增独立 `BlockParams`：每 seed 为 7~10 rounds，且 schedule 必含 quadratic、low/high-byte bilinear、two-stage quadratic 三 family；所有 round 系数/salt/position、两套 dynamic-key coefficient/salt 与 IV/chaining salt 走专用随机流，不扰动旧 stream 参数。
- frame 为 16-byte header + payload + 0~3 byte padding，整体 4-byte 对齐：descriptor 编码 transport version 1、round count、block width 4 和 dynamic high byte，另有原长、cookie、tag。32-bit generalized Feistel 以两个 16-bit half 运算，先加 chain、逐轮 `(L,R)=(R,L+F(R))`，ciphertext half 链到下一 block；目标端逆轮完全展开。
- 两个 31-bit final key state 由 canonical `c1/c2/c3` probe share、forms 重建的 `pv` 与 padded ciphertext length 使用两套非对称奇系数运行时计算。入口仍按 seed 洗牌 probe 调用顺序，但赋值改为所属 probe 的 canonical 槽；否则旧 outer state 只看 share 求和不会暴露顺序错误，而新非对称 key 会与 Rust 端失配。
- `d_done` 只有在 block ciphertext 长度/4-byte 对齐、descriptor、原长/精确 padded length、cookie、payload tag 和每个 padding byte全部通过后才返回 constant-encrypted image；上限相应为 16,777,232 bytes。`transport.rs` 的六种 segment order 候选同样先 outer inverse 再 block verify，错误 seed/损坏无 winner。
- 新单元测试对 513-byte ciphertext 的**每个 byte**逐一翻转，并覆盖 0~3 padding、wrong share/`pv`/seed、截断/扩展、schedule/round family/ciphertext diversity 与 final-key 数字零泄漏；新集成测试在 Lua51/Luau 上保持 watermark/base86 合法，只改三个 segment 首/中/尾完整组，全部无 stdout 失败。
- golden 更新为 83,640/92,116 B，脚本 SHA-256 为 `499ebc32d10ba9543f233380aa20b894a4b921740bac4fc7e77e4e31e2f87afa` / `35c80e5e890c42a7b8f029aacab790d781cc8fcb672eeaffdb55b637c62e8607`，预算保持 85,000/94,000 B。完整门禁 **PASS178（65 单元 + 113 集成）**；bench 为 Lua51 58 ms / 29.0x、Luau 81 ms / 27.0x。tag/动态 key/Feistel 均是提高恢复与篡改成本的可逆机制，不是密码学认证。

### 嵌入 payload 传输加密（M6 历史基础，2026-09-06）

`vm::custom::generate` 在生成阶段以 Lehmer 流（`48271 mod 2147483647`，初态 `1+(s1+s2+s3+31*#B)%2147483646`，混入密文长度）对内嵌 blob 逐字节 XOR；三个份额**不在脚本中存储**——由三个 payload 函数在运行时对入口传入的结构键对计算 `x=(a*31+b)%2147483647` 叠 3/5/7 轮 Lehmer 步进得出（零密钥字面量），计算前先做上表探针（Luau 断言 `"[C]"`，Lua 5.1 断言 `what=="C"`）；入口按 seed 洗牌顺序调用，decoder 段结合解密后走原有 magic/Adler/结构校验。`.obf` 文件与 `compile` 输出保持明文 canonical、与 seed 无关；seed 决定表键（即份额的推导输入）、密文与探针调用顺序。份额、外层密钥流初态与常量层密钥初态的十进制串都不出现在脚本任何数字字面量中（单元测试逐多 seed 断言）。

**常量池第二层加密（同日追加）**：`generate` 在外层加密前，先对嵌入镜像中每个常量记录的 payload（布尔值字节、数字/整数 8 字节、字符串内容；字符串长度 u32 保留明文作框架）用独立密钥流 XOR——密钥来自另一对 wrapper 键 `(keys[4],keys[7])`（`31` 混合 + 11 轮 Lehmer + `31*#B` 长度混合），并重算嵌入镜像的 Adler-32。`constant_ranges` 是有界防御性扫描（截断/坏计数/未知 tag 均为诊断错误，新增单元测试覆盖）；F2 段用 db8/db32/dstr/dnum 专用读取器在解析时逐条解密，`fin(lo,hi)` 复用原浮点重组。该历史批次曾建立“剥掉后续 transport 后常量仍是密文”的契约；ISA8 已把同一 inner stream 扩为覆盖完整 compressed body，并删除 selective constant scanner/reader。当前 `extract_embedded` 返回inner-ChaCha8-encrypted LZW frame，`decrypt_embedded`再解流并严格展开为同Program/seed的**ISA15** semantic image（含全局segment graph、capture/constant池、K8 route与K9 affine operand key），而不是 canonical `.obf`。破坏性测试更新：破坏 framing 的载荷（如 cs 超界）现在由生成器扫描直接拒绝，其余仍由目标端拒绝。如实声明：字符串长度与常量种类仍可见（框架元数据），该层是混淆而非加密。

**base86 传输层与分段打乱（同日追加）**：双重加密镜像整体 base86 编码（字母表 = 可打印 ASCII 35..121 去反斜杠，共 86 字符；4 字节小端组 → 5 字符，尾部 1..3 字节 → 2..4 字符）后切成三段，seed 洗牌分放到三个 `[数字]=function` 分段函数（wrapper keys 8..10）。每个分段函数 = loadstring 原生探针 + 自己的 base86 段 + 解码循环（四重校验：字符域/尾长≠1/组值≤2^32-1/尾宽上限），只解码自己的段；入口按流序 `Y1..Y2..Y3` 拼出外层密文传给 decoder 段（F2 签名改为接收 B）。生成器审计的探针计数从 3 改为 6（份额×3 + 分段×3，native 后端仍为 0）。验证 API `extract_embedded`：取三个最长字母表内字面量，试 6 种段序，当前以 watermark+outer ChaCha8+frame-v2+magic/目标字节/全镜像 Adler-32 取唯一有效序（实现中发现仅凭 magic 无法区分共享首段的置换，已改为全 Adler 判定）。副作用：嵌入成本从每字节约 2.9 个转义字符降至 1.25 个可打印字符，golden 显著缩小。base86 是编码不是加密。

**固定传输水印 `XXS:`（同日追加）**：base86 流解码后前 4 字节 = `XXS:`（明文水印，其后才是外层密文 body；运行时入口用 `SS(流,5)` 剥离后才传 F2，外层 `31*#B` 长度公式不受影响）。检测按用户要求拆成两个 function 隐藏：`W1(keys[11])` = 首 4 字节大端打包器（无水印常数），`W2(keys[12])` = 与不透明 u32 `1482183482`（= 0x5858533A，Rust 侧由 `u32::from_be_bytes(*b"XXS:")` 派生）比较，不等即 E() 静默。脚本全文无 "XXS:" 字符串（单元断言）。`extract_embedded` 要求水印前缀并剥离——水印把流首段钉死，配合全镜像 Adler 使有效段序唯一。集成回归：外科手术式只翻转流首段首字符（base86 组独立 → 仅水印 4 字节改变、密文其余逐字节不变），脚本必须无输出中止——证明中止由水印检测触发而非密文损坏。途中修复：W1 最初未把 SB 作为参数接收（尝试调用全局 SB → nil 报错），改为 `function(S,E,SB)` 显式传参。

**M7/ISA7 结构随机化**：`structure` 流仍提供四种比较、三种整数边界及元方法分支变体；`dispatch_condition_for(variable,...)` 现在分别为 F3 `o`、recipe entry `rid`、semantic fragment `sid` 生成条件。三维 dispatch 各自拆为 2~4 个模数子链，`rid` 不再内联语义，只选择入口 `sid`。`compression_reduces_bytecode_while_script_budget_is_independent`与`tools/bench-vm.sh`分层看护；live descriptor camouflage 与双层语义路由是有意识的空间换结构，硬上限为 **85,000/94,000 B**，运行灾难阈值 1500ms。

**不透明真假分支与 semantic camouflage**：入口恒真/恒假包装及 primitive validator 的不可能值死臂仍保留；旧 F5 一对一 opcode 假臂不再是主要契约。`semantic_descriptors_and_fragments_poison_dictionary_only_translation` 检查 poison descriptor/execute mismatch 与 live fragment routing；`live_recipe_descriptors_are_validation_equivalent_but_not_semantic_truth` 检查 live camouflage 的 class/密度/seed 变化；`reachable_neutral_decoy_bundles_execute_but_poison_recipes_do_not` 在 ED/RD 后证明 neutral 真执行而 poison 不执行。`decrypt_embedded` 返回当前private ISA14，且 runtime witness/control/compression/semantic/transport/segment graph 整体仍是可逆混淆而非密码学加密。

自定义 VM 的 graph-fetch/坏 semantic image 测试先插桩，再走 **`custom::finalize`**，不能直接调用旧 finalizer 而绕过字段阶段。旧 native backend 不走 `fields::shorten`，它的原短字段与测试继续保留。

### K 系列（2026-09-09 ～ 2026-09-10，当前工作线）

命名对照：K1–K9 对应静态技术文档 §3 的九项检查 T1–T9（避开历史 imitation T1/T6 撞号）。目标是把**静态可直接抠出结构的锚点**逐项归零，不是再加一层加密。

| 批次 | 做了什么 | 门禁 |
|---|---|---|
| K0（09-09） | `tests/product_audit.rs` 落库 §3 九项审计（对两份 golden 文本跑，pin 观测值）；`src/vm/custom/tests/stream_audit.rs` 在真实字流上跑 check 8；`tools/bench-vm.sh` 新增 `OBF_BENCH_SCRIPT_CAP`（默认 98000，`off` 暂停门但仍报尺寸） | 基线 pin：[1] 漂亮常数 8/7 项、[2] 字母表连续 FAIL、[9] 流长整除 FAIL、[5] 同型模板 4/5 形；[3][4][6][7][8] 干净。PASS249（133 单元 + 116 集成） |
| 09-10 红门清账 | Luau probe transcript 改为隐藏名池运行时拼装 + 额外参数下发；K7 静态门扩到 18 词并直接审计两份 golden；`heuristic_audit`/`product_audit` pin 重录并写明归因。Lua51 golden 零 diff，Luau +111 B；275 门全绿 |
| K9a/K1（09-09，= ISA15） | `base86_encode_mixed`：每段 4-char 长度前缀 + 字节驱动链式 4/5/6-char 组 + `86^width` 冗余内随机高位填充；逐 image 从 28..=126 取 86 字母表（强制含 28/29/125/126、跨度恒 99、逐 seed 丢 13 后重排），8 个 sub-12 片段 baked，Lua 侧 `VAL` 表解码；基数/`MM`/`M24` 以随机加法对隐藏；`~` 标记字节改 `\127` | 9 个新单测（字母表/填充/往返/六段序/状态号/slot/转义/污染/长度上界）；`product_audit` [2][9] 重 pin。golden 93,044/102,734 B，PASS258（142 + 116） |
| K7（09-09，提前插入） | `src/vm/custom/bitops.rs`：目标端字运算多态工具箱。Lua51 纯算术（X8/X32/rotate/L32 各 3 变体），Luau 经隐藏名池捕获 `bit32`/`buffer`（直接调用/buffer 组合/De Morgan 各 3 变体，库名方法名不拼写）；hot xor/rotate 双闭包；`2^32` 模数拼写与求和项序逐 use-site 抽取；独立 `bitops_rng` 流，wire image 逐字节冻结 | 15 个 K7 门（wire 冻结 pin、RED 签名、发射器单测、真解释器 1300 断言向量、`LS` 遮蔽碰撞回归、10 seed×双目标执行）。golden 94,553/103,728 B，PASS273（157 + 116） |
| K5（09-09） | 验证为**已满足**：外层/内层 keystream 均 position-dependent（counter ChaCha8），无位置无关 XOR 残留；旧 custom stream 已在 ISA11 删除 | `pin(0,0)` + `pin(848,0,0)/(677,0,0)` 零重复，census 门 |
| K8（09-09/10） | per-prototype payload-derived **indirect route**：fetch 处 `(pc×mul + token×add + fid×salt) % 65521` 选 route，`route_info[pc]` 校验 `rid` 与 bundle 宽度 1–4；新增第 16 个私有字段 `routes`（`src/vm/fields.rs`），parse 期重复 route 检查 fail closed | `k8_payload_driven_indirect_routes_are_bound_and_checked`；与 K9 同时 fail closed |
| K9（09-10） | per-lane **payload-derived affine operand value key**：encoder 按 record recipe token、prototype id 与 lane 对 Form 1–5 operand wire value 做 affine；runtime 在 semantic validator 中用 `FM[op]` 恢复 Form 后执行 `AK` 逆变换（保留 `PT[op]` 作 opcode permutation validator 输入）；Form 4 `bx` 用模 65536 逆元，Form 1 packed Ax 按 3 个 byte lane 处理 | `k9_affine_lanes_round_trip_for_all_forms_and_contexts`；`custom_vm` 20/20；矩阵 159/159 单元 + 全部集成 |
| 09-10 frame tag | transport frame 的完整性字段从 Adler-32 换成 **keyed dual-lane integrity hash**：两条模 65,521 lane（`left×257+byte` / `right×263+byte+left`）以运行时派生 key state + salt 起始，混合 cookie/descriptor 后写入 32-bit tag；公开 `.obf` 的 Adler-32 与 LZW header 的 semantic Adler 门不变 | ⚠️ 本批**未重录 pin**，留下第 0.1 节 3 个红灯；接手第一步应先处理 |
| K10（09-10） | `src/vm/custom/emit.rs` 按发射阶段拆分：5 个解码器文本构造函数 → `emit_decode.rs`，prelude 阶段（环境捕获/隐藏名池/单元语句/K7 工具箱/返回表，9,364 B）→ `emit_prelude.rs`（返回 `PreludeStage { text, header_end, ret_names, probe_transcript }`），`mod.rs` 用 `pub(crate) use` 重导出；`validation()` 有意留在 `emit.rs`（单元测试按 `super::emit::validation` 调用）；整脚本预算 98,000 → 112,000 B 并恢复 enforce | 拆分前后 seed 7001/7351 两份 golden `cmp` 逐字节相同；159 单元 + 116 集成全绿（含恢复后的体积门）；不删步骤的 shrink 经三项实测判定无空间；公开 `.obf`/wire/pin 全不变 |
| K12（09-10，= ISA16） | operand lane key 折上**按镜像顺序推进的链状态**：`st0=(id*7919+nr*4099+start*131+salt)%2^31-1`，逐条 `st=(st*213+token*1031+at*7517+salt)%2^31-1`，AK 加性项加 `st%m`；host/lua 共用 `semantic::K9_*` 常数 | `operand_lane_chain_is_order_dependent_and_double_exact` + `emitted_runtime_replays_the_operand_lane_chain`（对改名/省分号免疫）+ 双目标执行等价；image 1,159→1,141 / 882→870 B，golden 99,090 / 108,766 B，公开 `.obf` 不变 |
| K13/K13b（09-11） | K13 把 `semantic_validator` 的逐 prototype 解码抽成共享 `DC(id)`：装载期仍跑完整解码+全量校验，随后把 `__obf_proto_code` 退回链态串，解释器改为“首次执行才物化词表”；K13b 加帧引用计数 `LVC`/`LVE`，归零即把链态串写回并清 `__obf_proto_routes`（尾调用换帧处必须在 `fid` 重绑**之前**销账） | 镜像字节不变 ⇒ 仍 ISA16；golden 各 +136 / +207 B；只动 `heuristic_audit` M4 两个类与 Return 臂文本 pin；新增两条生命周期门（惰性解码、空闲回锁，含“释放不得停在分派循环尾部”的反向 pin） |
| K14（09-11） | 装载期操作数校验器的 opcode 分派由“逐残差桶线性 `if/elseif` 等值链”改为**逐桶种子化二叉搜索树**（`structure.rs` 的 `grouped_tree`/`search_tree`：桶内按 opcode 值排序、种子选切分点/分支左右/叶链长 2–4，内部节点 `if <边界> then <左> else <右> end;`；新 `Prng::boundary_condition` 给 9 种等价边界拼写）；同批按用户指示把体积门 112,000 → 120,000 B 并放开 Luau 直用 `bit`/`buffer` | 臂谓词与 `validation(op)` 逐字未变 ⇒ 与旧链在 0..=255 上逐位同值，未知 id 仍唯一 `E()` 出口；`k7_wire_image_bytes_are_pinned` 不红（镜像/`.obf` 不变，不升 ISA）；golden 100,712 / 110,179 B；新增 2 门（边界互补性逐拼写、树结构与拓扑随种子变化）；`heuristic_audit` M4 与 `product_audit` check1/check5/check6 按观测重录并写归因，`stream_audit`/`private_fields` 原样通过 |
| K13c 第一步（09-11，T4 第三步的管道批次） | 常量池镜像由 `{tg,val}` 改为 `{tg,off,len}` 字节坐标（相对 `KBase=pos()`，区长 `KLen`），值在池循环内直写所属 prototype（含 `val==1` 布尔归一内移）；切片态 `PK` 从搬运工改为区间/覆盖门（记录存在、`tg>5`、`off+len<=KLen`、非 nil 常量必须已落值） | 零行为差异：池序 plain/flipped 行为对照门、ISA14 池 corruption 门、graph corruption 门、`k7_wire_image_bytes_are_pinned`、`stream_audit`、`private_fields`、`product_audit` 九项全部原样通过 ⇒ 不升 ISA；golden 各 +82/+88 B（100,794 / 110,267）；新增 `constant_pool_mirror_holds_coordinates_not_values`（含“`KBase`/`KLen` 声明必须早于 defs，否则 `PK` 里的 `KLen` 是全局 nil”这条顺序锁）；`heuristic_audit` M4 仅类 0 +2、类 3 +1（两目标同形，逐字面量核对） |
| K13c 第二步（09-11，T4 第三步收尾 = **private ISA17**，`WIRE_ISA_VERSION` 16→17） | 常量池 **payload 不再以明文进镜像、也不再常驻 prototype**：`semantic.rs` 新增 `pool_key_pair/pool_key_fold/pool_key_byte`，池密钥对由镜像自身的 `mask_salt/mask_mul/token_layers[1]` 推出（独立解码器与 `wrap-bytecode` 仍能复算），写入时逐记录加钥，读取时在 `DC` 内按 `{tg,off,len}` 坐标重建值，常量只在帧激活期内存在（`LVE` 释放） | 加/解两侧共用同一递推 ⇒ 逐字节确定性不变；golden 100,794 / 110,267 → 102,261 / 112,605 B（+1,467 / +2,338），仍在 120,000 B 门内；bench 无可测执行代价（320 / 107 ms 中位）；改了镜像字节 ⇒ 受影响的审计 pin（`heuristic_audit`、`product_audit`，以及镜像流与私有字段两套）在同批按观测重录并写归因 |
| K16（09-11，不改壳格式的常数字段化） | 发射脚本里重复出现的规范十进制轮常数改为**包装表字段**：新模块 `src/vm/custom/constant_fields.rs` 挂在 `finalize()` 最前（`lift → fields::shorten → finalize_vm`），写入 `t.e,t.h,t.u,t.ab,t.ac=65536,4294967296,16777216,65521,1001000` 是入口函数体的第一条语句，其余可改写使用点读 `t.<键>`；四条门（壳形逐条对上 / 只在写入之后执行的位置插读点 / 遮蔽包装名的作用域保持原拼写 / 只改规范十进制字面量）+ 三条守恒自检，任一不成立即 `Err` | 不改镜像、不改公开 `.obf`（5,525/6,575 B）、不升 ISA；golden 102,261 / 112,605 → **101,908 / 112,336 B**（−353 / −269），10 种子最坏 102,377 / 112,701 B；`luac5.1 -l` 仅 max upvalues 16→17；`;` 总数与换行数一字未增；新增 8 门 ⇒ **175 单元 + 116 集成 = 291 全绿**，矩阵 PASS，bench 中位 344/112 ms；`heuristic_audit` 只有 M3b/M4 动（差值恰为被改写常数的加权和）、`product_audit` check1/check2/check5/check8 按观测重录，`stream_audit`/`private_fields`/镜像 pin 原样通过；`semantic.rs` 守卫普查与 `fields.rs` token 双射改读新增的 `#[cfg(test)] emit_unlifted` |
| K17（09-11，转义宽度与字母表池的纯拼写瘦身） | **A1**：非可打印字节的十进制转义由固定 `\ddd` 三位改为"按后一个源字符允许的最短拼写"（`\28`，只有后随数字时才 `\028`），规则收在 `minify::push_decimal_escape`+`next_byte_is_digit`，三处发射点共用（K9a 镜像 `transport::lua_escape_string`、用户源 `minify::encode_content`、遗留 `lua51::emit_byte_string`）；**A2**：`base86_image_alphabet` 的池按构造先排除 `"`(34)/`'`(39)/`\`(92)、随机丢弃数 13→10 ⇒ 仍恰好 86 个符号、span 99、`86^5>2^32` 分组余量与解码器文本形状不变；新增 2 条门（256 字节值 × 15 种邻居经真解析器 `literal_bytes` 逐个往返并断言"零位不多余"；86/99/缺 13 + base86 文本不含两字符转义类） | 不改镜像、不改公开 `.obf`、不升 ISA；golden 101,908 / 112,336 → **100,619 / 111,608 B**（−1,289 / −728），10 种子最坏 101,134 / 111,608 B；`luac5.1 -p`/`luau-compile`/`obf check` 全过、双目标真实执行与 fixture 逐字节一致、两次生成逐字节相同；**177 单元 + 116 集成 = 293 全绿**，矩阵 PASS，15 次最佳 bench 346/114 ms（K16 中位 344/112 ⇒ 无可测代价）；`heuristic_audit` 只有 M5/M7 两条拼写度量动、`product_audit` 只有 Lua51 check4 的 distinct 2→3（Luau 九项一字未动）、check9 与 `k7_wire_image_bytes_are_pinned` 证明镜像未动；`tools/size-probe.py`（只读画像器）同批落库 |

（K13c 第二步=T4 第三步收尾 **已于 09-11 完成**，原计划文字保留如下）

原第二步计划：**K13c 第二步=T4 第三步的收尾——给常量表示加钥并升 ISA17，把 `DC` 改成"解词→按 `(tg,off,len)` 从保留区重建常量→逐记录校验"，并让 `LVE` 在归零时连 `__obf_proto_k`/`__obf_proto_tags` 一起释放（回锁可重放靠 K12 那条链；须缓存 mask，外部分析实测反推 key 48/64 失败；体积已不是瓶颈：预算 120,000 B、K13c① 后 Luau 最坏 111,528 B，余约 8.5 KB；第一步已把坐标与区间门禁就绪）**、K9b（token-layer pool 的审计“漂亮值”归零）、K6（打散）、K2、K3-FULL（transport 保池、消费侧逐记录化已做 lite，全去池另批）、K4（≈E1+B6）、K1 余项、K10（整脚本体积 shrink + 静态表面收紧，之后重新钉 `OBF_BENCH_SCRIPT_CAP`）。已完成不再列在这里的：K13（首次执行才解码 + 校验后释放）与 K13b（帧引用计数空闲回锁）。仍然冻结的区：F1、D4-D5 key-from-payload（2026-09-11 用户解除“Luau-buffer 直用”这一条：Luau 侧可直接用 `bit`/`buffer`，某变换若 Lua 5.1 表达不了就优先实现 Luau，5.1 侧只允许保留较粗但正确且 fail-closed 的变体）。

**红线**：K 系列不得把上述机制写成不可逆或客户端秘密；每批都要同时给出「发射侧改了什么 + 哪些 pin 重录 + 双目标 fail-closed 门」，并按第 10 节流程用 fixed-seed golden 复核。

### 2026-09-10：K12 private ISA16 — operand lane key 吃已解记录前缀（T1 第一步，已实现）

- **动机**：外部分析（`T1`）指出，只要"字流→指令/常量"是 `(密文, 位置, 静态常数)` 的纯函数，离线整段解出就总是可行。K9 之前 lane key 只吃 `token/id/lane`，每条记录自洽 ⇒ 可并行、可跳读、可逐条试解。K12 把 lane key 折上一条**按镜像顺序推进的链状态**，链的输入是**本条已解出的 recipe token**。
- **递推（两侧共用同一组常数，`src/vm/custom/semantic.rs`）**：`st0 = (id*7919 + nr*4099 + start*131 + mask_salt) % 2147483647`，逐条 `st = (st*213 + token*1031 + at*7517 + mask_salt) % 2147483647`，AK 加性项由 `(v*257+id*911+lane*193+mask_add)` 变为 `…+st%m`（m=256/65536 逐形）。逆元表不变；仿射只用可乘性逆，不引入模逆预算。
- **可执行性**：链只用整数且中间量 `2^31*213 + 2^16*1031 + records*7517 < 2^53`（`operand_lane_chain_is_order_dependent_and_double_exact` 锁死），Lua 5.1/Luau 双精度可精确表示 ⇒ 不改变求值语义；前缀不符时 operand 解错会被既有 `vld()` 边界检查捕获，仍在用户代码前 fail closed。链**不掺**时钟/随机/宿主执行计数，因此同 seed 仍逐字节确定。
- **漂移锁**：`emitted_runtime_replays_the_operand_lane_chain` 直接数最终产物里 6 个乘子的 `*N+` 片段各出现 1 次，并确认 AK 加性项里出现 `%`（折叠发生）。这些乘子刻意选成在整个产物中唯一，因此**对最终改名与分号省略免疫**（finalizer 会把 `stL`/`AK` 都改成 1–2 字母）。
- **归因（全部按观测重录，不是"顺手调数"）**：
  - 私有 image（K7 探针）：Lua51 1,159→**1,141 B**、Luau 882→**870 B**，FNV `0xd96c049c46c02c21` / `0xb4e955d8ec85a75c`；`k7_wire_image_bytes_are_frozen` 因此改名 `k7_wire_image_bytes_are_pinned`（K7 时代"逐字节冻结"的语义在 ISA 变更后不再成立，改成回归指纹）。
  - `stream_audit`（真实内层字流）：`(987,0,0)→(975,0,0)`、`(784,0,0)→(778,0,0)` —— 只有字流量变化，重复对/gcd 仍 0。
  - `heuristic_audit`：M2/M3b/M5/M7 位移（多一条链语句）；**M1=86、KAT 词表、M6=(0,0)、M4 残差表在 Lua51 全表不变**，Luau 仅 `23` 残差 −1 ⇒ 没有新增可读值类，也没有新增静态 API 面。
  - `product_audit`：`2147483647` +2（就是链的两处），check8 配对 +2（gcd 仍 1），check9 流长 +15/+7；check1 的 `86`、check2/3/4/5/6/7 不变 ⇒ 无新模板族、无新别名串。
  - 公开 `.obf` 5,525 / 6,575 B 与两份 SHA **未变**；`private_fields.rs`/`semantic.rs` 的 image 头断言 15→16（`wire_isa_version_is_16`）。
- **源文件上限**：新增发射语句把 `src/vm/custom/tests/semantic.rs` 顶到 83,419 B（上限 81,920 B），因此按 `include!` 惯例把 K9/K12 lane 与版本 pin 四个测试拆到 `src/vm/custom/tests/lane.rs`（5.8 KB，拆前后测试集合不变）。注意：被 `include!` 进来的文件**不能**用 `//!` 头（`E0753`），只能普通注释。
- **下一步（T4）**：链已经"吃已解数据"，剩下的缺口是**一次性解密**——启动即整镜像解密，dump 一次拿全。T4 用同一递推做段级 unlock/re-lock（回滚必须缓存 mask，纯靠 `inv19` 反推已被实测证伪）；预计 +1–2 KB 文本，而 Luau 预算余量只剩 1.7%，所以 T4 那批要么先做 K9b 腾体积，要么按同一实测流程重钉预算。

## 9. 安全门限和不能宣称的能力

| 层 | 当前门限/策略 |
|---|---|
| 前端 | 源码 64 MiB；token、AST 节点各 1M；递归/插值 64；每层线性运算/后缀链 64 |
| scope | scope/binding/reference 各 1M；显式工作栈预算 8M |
| 名称复用 | 8M 工作、1M 跨域干涉边；一/两字母池最多 702 个候选，实际排除保留项；总绑定数不受 702 限制 |
| IR lowering | 1M 工作、64 层下降；Luau 字符串常量分析不超过 64 KiB，使用 Rc 避免声明别名复制膨胀 |
| OBF v2 | 文件 16 MiB；聚合指令+常量+捕获 1M；prototype 最多 65536 |
| private ISA15 | prototype 最多 32,767；operand profile周期32,772且全域互异，register profile周期130,556且逻辑0..255单射/物理key<1,028；record 4槽按`(pid×M+A)%24`、segment 3 token按`(slot×M+A)%6` factorial置换，乘子与阶乘互素；dictionary/metadata/tuple用per-image置换；capture/constant池每记录3匿名u16 token、per-record factorial profile且池顺序按image翻转，header仅24字节metadata；固定slot宽度不变，错序由既有门fail closed；每owner恰有两个context-bound code segment；原始bundle每prototype最多65,531、每bundle最多4 primitive、多primitive recipe最多96；fragment总数`<900`、宽1~2，宽2只允许单语句producer+安全consumer的真实carry pair；每pair至少1 forwarded read，四family RF显式保留nil/false；entry六stage，整份生成体状态机总数9；edge token 3层、recipe token 5层；每真实prototype入口2~4 neutral bundle并抽样至多5条边；每image 4个unreferenced poison recipe；目标端精确operand/graph/pool验证；K8 per-prototype indirect route（`routes`字段，`%65521`选表 + `route_info[pc]`校验rid/宽度1–4）与K9 per-lane affine operand value key（Form 1–5，Form 4为模65536逆元，逐lane乘子绑定recipe token/prototype）均并入同一validator；K9a/K1逐image混合base86（字母表28..=126的86子集、强制含28/29/125/126、跨度恒99、逐seed丢13、每段4-char长度前缀+链式4/5/6字组+`86^width`内随机高位填充），六段序中仅一种可通过完整逆链 |
| bounded LZW | semantic 原长 `1..16,777,216`；16-byte header；8,192-byte 输出 reset，最多 2,048 段；prefix/suffix dictionary 与 phrase expansion 单段有界；frame 必须严格缩小；bit 长度/padding/完整消费/Adler 全校验 |
| ChaCha8 / frame v2 | IETF 256-bit key/96-bit nonce/32-bit counter、8 rounds；inner/outer domain分离；LZW payload≤16,777,216 bytes，transport frame≤16,777,232 bytes且4-byte对齐；16-byte header、0..3 padding；descriptor/version/length/cookie/tag/padding全验证；**tag 自 2026-09-10 起不再是Adler-32**：`frame_tag` 用两条 keyed lane（`left×257+byte`、`right×263+byte+left`，均模 65,521，初值取运行时派生的两个 key state + salt）混合成 32-bit tag，再混入 cookie/descriptor；仍是 fail-closed 损坏门，不是 AEAD |
| 每个函数 | 寄存器/捕获最多 256、常量最多 65536；参数与各 opcode 操作数还受实际编码位宽约束 |
| 生成源码/字段 | 遵守lexer源码/token上限；固定16字段prototype schema（含`routes`）；wrapper numeric keys为29，其中24..28为ChaCha/anti-hook fields；scope只接受12或0个direct probes；有限一/两字母池和二次token复核 |
| Rust 实现源文件 | 每个 `src/**/*.rs <=81,920 B`；超限先拆分并证明拆分前后 fixed-seed 生成逐 byte 相同 |

这些是不同层的防护，不是对原生 compiler 资源范围的模拟。超限拒绝，不截断、不绕过验证、不增加 AUX、不静默调用原生 compiler。CLI 当前先读入文件/stdin，不能把前端的 64 MiB 检查误当作流式输入内存上限。

目标是相同宿主/输入下，受支持确定性程序的 stdout bytes、返回值/数量、写入和回调顺序一致；**不是任意原生可编译源码全部等价**。以下仍有边界：

- debug 栈/名称反射、环境/加载反射、dump、源文件/行号和错误位置；动态/宿主别名未必都能静态识别。`--no-rename` 保留名称，不保留原始布局。
- 隐藏元表和任意宿主扩展；table-valued 的 `__metatable` 锁定值不能可靠识别。Roblox executor、其他 Luau 版本/编译选项不能视为已验证。
- GC/弱引用可回收时点、内存、普通递归预算、地址型 tostring、时间/随机输入。
- 含洞表的 `#`、数组/hash 容量、未规定的遍历顺序。普通 table 分配未模拟所有原生容量提示；使用明确长度、连续数组和排序后的键做确定性对照。
- 所有优化相关的函数身份、大常量/vector 常量等未覆盖的共享路径，以及 NaN payload 的位级观察。
- 仍无法证明安全的 repeat/continue 数据流。

不要通过删除反例、把失败项忽略、改 expected 输出或宣称“99% 兼容”掩盖这些差异。普通受支持行为的真实 bug 应最小化并加入永久原生差分。

## 10. 测试基线与生成物

### 10.1 当前完整结果

本阶段在既有semantic/segment/LZW/witness/ChaCha与ISA12-A/B/C operand/register/fusion门上加入ISA13 parser field-order置换、ISA14 capture/constant池、ISA15 混合base86 transport，以及K5/K7/K8/K9门。矩阵先检查80 KiB源码门，再覆盖Rust 1.88.0 debug/release、两端原生语法/运行、两后端、多seed、公开ISA修订1/2包装到private ISA15、operand/register/field-order全域profile、capture/constant池洗牌与池序翻转、factorial置换合法与双端重算一致、handler token lowering、producer/consumer eligibility、fusion/read恒等式、四family RF、entry graph、record/segment/dictionary/metadata/pool五类错序双目标fail-closed、nil/false/alias/元方法/errors/cells/captures/callbacks/tailcalls/legacy vararg、golden、semantic-bytecode→完整LZW-frame strict-smaller与独立bench。`.obf`仍是canonical 7-bit varint。K 系列在此基础上追加：K0 九项静态审计 pin（`tests/product_audit.rs` + `src/vm/custom/tests/stream_audit.rs`）、K5 位置相关密钥流验证、K7 多态字运算工具箱（`tests/bitops.rs` + 真解释器 1300 断言向量 harness）、K8 indirect route、K9 per-lane affine operand key round-trip、K9a/K1 混合 base86（字母表/填充/往返/六段序/状态号/slot/转义/污染/长度上界）与 `heuristic_audit` 表面 pin。**当前实测（2026-09-10，HEAD `7106908` + 红门处置与 K10 批次）为 159 单元 + 116 集成 = 275 门，全绿**（红门处置记录见第 0.1 节；体积门自 K10 起以 112,000 B/目标恢复 enforce，裸跑 `cargo test --all-targets` 不再有预算红灯）。15-run bench 为 Lua51 326 ms / 163.0x、Luau 106 ms / 35.3x（K10 提交树复测）；LZW 门为 Lua51 `14,073→13,475 B`、Luau `17,763→17,527 B`（含 16-byte header，seed 7001/7351）。

| 集成 suite | 数量 | 主要关注 |
|---|---:|---|
| `custom_bytecode` | 11 | Header/varint 布局与 canonical、round-trip、损坏/修复 checksum、版本、捕获、宽跳转、尾随/越界拒绝 |
| `custom_cli` | 4 | 默认/显式后端、无 compiler、compile/wrap、失败输出保护 |
| `custom_vm` | 20 | 原生差分、求值顺序、循环 cell、multret、尾调用、host/GC/模块、semantic graph、register ABI路径、fusion的nil/false/alias/元方法/error/capture/callback、ChaCha/frame ciphertext、source-witness与anti-hook fail-closed |
| `parser_audit` | 11 | 固定目标接受/拒绝、语法与编译器边界 |
| `private_fields` | 5 | 字段/公开 API/NAMECALL/导出保护、bytecode、CLI/旧 ISA |
| `random_names` | 6 | 短名、seed、保护名称、池和 CLI |
| `safe_minify` | 13 | 字面量、作用域、反射、双目标实际输出 |
| `scope` | 18 | BindingId、声明时序、捕获、类型与安全门限 |
| `scope_reuse` | 7 | 同域/跨域冲突、逃逸、10,000 相邻块、生成式程序 |
| `semicolons` | 7 | 语句边界、必要间隔、函数后缀、类型/插值、两 VM 入口 |
| `product_audit` | 1 | K0 §3 九项静态泄漏审计对两份 golden 文本的 pin（漂亮常数/字母表/噪声/阈值/同型模板/别名/死表/字流 gcd/流长）；每个 K 批收敛后重 pin |
| `vm_parity` | 13 | 145 生成式组合、常量共享、应用语料、模块/回调/1000 项 pack |

77项单元测试还包含完整32,767-id operand/register profile唯一性、operand四family/rotation/binding、register 256槽单射/物理界限/全handler token lowering与旧静态桥消失、record/segment factorial全范围置换合法与双端重算一致、parser字段序固定期望与跨seed变化、carried-value eligibility/producer/consumer改写、live fusion/read下界、entry第九个状态机与旧线性锚点消失、runtime transcript→share→control mask差分、masked fetch/dispatch、真实ED/RD + fragment graph-fetch覆盖、live descriptor class/密度、split/fusion、reachable neutral/poison、global segment精确重组、坏semantic/segment metadata拒绝、ChaCha8 published vector/domain/context/material差分、wrong-attestation与frame-v2逐ciphertext byte corruption、字段双射/池/未标记token证明、分配器小图穷举，以及整体包装AST形状与原生执行差分。

- 自定义 ISA **执行**覆盖：Lua51 46/46、Luau 49/49；不要用只出现在字节码里的 opcode 冒充执行覆盖。
- 旧 native/reference 覆盖：Lua51 38/38、Luau 63/91；不代表旧 Luau 后端完整支持 91 条语义。
- 应用语料 `parity_common.lua`（两端）和 `parity_luau.lua` 另做原生 stdout、debug/release binary 与生成 VM 的逐字节对照。
- 矩阵还先检查全部 `src/**/*.rs <=81,920 B`，再检查 CLI-compatible Luau 环境、损坏输入、无外部 compiler 的默认生成、默认 virtualize 与独立 wrap 一致、所有显式短名和脚本无 CR/LF；witness门覆盖逐share差分与control mask；ChaCha/anti-hook门覆盖published vector、wrong attestation、native wrapper/source/helper tamper、12-or-0 probe及每个outer ciphertext byte；segment门覆盖exact reassembly与duplicate/missing/out-of-range/wrong-owner/root/cycle/truncation/trailing；operand ABI门覆盖全private id域、四种物理layout/binding、旧`6+3*i`/actual-op marker消失与双目标payload round-trip；register ABI门覆盖130,556周期、完整id域、256逻辑槽单射/物理界限、无映射表、全部双目标handler token lowering与旧规范`R[...]`面消失；fusion/entry门覆盖pair结构、`fragments+fusions=operations`、live fusion、四RF family、nil/false/alias/元方法/errors/cells/captures/callbacks及六状态入口；field-order门覆盖factorial全范围/双端重算/固定seed parser顺序/跨seed变化与双目标四类错序无输出拒绝；compression覆盖多seed下完整LZW frame严格小于对应semantic bytecode、exact round-trip、全部truncation/畸形frame、wrong inner material、padding/完整消费/checksum和incompressible rejection；整份Lua不参与该压缩比较。
- 固定输出标记包括 `OBF|matrix:13|`、`OBF:luau:165|`、`vm:lua51:ok|`、`vm:luau:ok|`。只看到标记不等于整套门禁通过，必须检查退出码和最终 `[matrix] PASS`。

### 10.2 根目录 golden

这两份文件是**真实自定义解释器 + private ISA17 semantic/global-segment/pool image + per-prototype operand/register/field-order ABI + capture/constant pools + carried-value fragments/entry graph + K8 indirect route + K9 per-lane affine operand key + runtime-witness/control + bounded LZW + 双ChaCha8/frame-v2（keyed dual-lane tag）/anti-hook + 逐image混合base86**，不是源程序文本，也不是原生/custom opcode只换编号。对应公开`.obf`仍为canonical ISA2，2026-09-10复核确认两份`.obf`与varint批次逐字节相同。

| 文件 | fixture | seed | 单行脚本 | bytecode | prototypes / instructions / constants |
|---|---|---:|---:|---:|---|
| `vm_lua51.out.lua` | `tests/fixtures/vm_lua51.lua` | 7001 | **100,619 B** | 5,525 B | 14 / 1436 / 87 |
| `vm_luau.out.lua` | `tests/fixtures/vm_luau.lua` | 7351 | **111,608 B** | 6,575 B | 14 / 1816 / 67 |

整脚本预算门：K10 钉 112,000 B/目标，**2026-09-11 K14 起按用户指示为 120,000 B/目标**。K16 后实测最坏 10 种子 Lua51 102,377 B、Luau 112,701 B（K13c 第二步后曾为 102,576 / 112,994 B（余量 17,424 / 7,006 B，Luau 5.8%），两份 golden 与全部种子都在门内，Luau 余量约 7.6%（K13b 期为 100,224 / 110,437 B，余 1.4%；K12 期 99,981 / 110,094 B，余 1.7%）。生成侧源文件上限仍是 81,920 B/文件。

对应**完整 `.obf` 文件** SHA-256（不是 Lua 脚本哈希）：

```text
Lua51 a33b5dcb81b02d1f9f2e00ce71c10d55706020e2b28fd516fc69f6444bb81140
Luau  def67d59b5c79832709c4dbaa33293058195c2dc347f9578c8aaf61cd45fa1d7
```

对应当前**生成脚本** SHA-256（K12/ISA16 后复核）：Lua51 `18ead8b34b5ee0b4bfee0edb3591244c2d5beef572bab903edcdc4f94b8c55cf`；Luau `5aa72862902d0170d68ccffdda5aa675254bb1f82c2dd262754ce2371ac6f1ad`。用 `--seed 7001/7351` 重新生成后与仓库内文件 `cmp` 逐字节一致，两份产物在 `toolchains/bin/{lua5.1,luau}` 下运行并与 fixture 输出一致。

字段短名批次只改动两个 golden 的 170/176 个字段 token，脚本减少 164/159 B；原 local、其他 token、字面量和内嵌/独立 bytecode 不变。这是该批次的证明，**不是今后语义/编译器修改都不得改变 bytecode**。

整体包装批次把两份 golden 改为 `local x={};return setmetatable({...},x):m()` 的分函数载荷形式，相对私有字段批次各增加 484 B；seed 7001/7351 对应的方法名分别为 `p`/`e`。varint 批次把指令流改为 7-bit 编码后，bytecode 缩小 19.7%/20.4%（6,879→5,525 / 8,257→6,575 B）；加密批次再把内嵌 blob 换为密文（密文中可打印字节比例高于 `\000` 密集的原明文，Lua51 反而缩小），密钥结构动态推导（零字面量）后为 26,322/29,999 B；常量池第二层加密后为 27,223/30,888 B；base86 传输层把嵌入成本降到约 1.25 可打印字符/字节；水印批次后为 20,938/22,950 B；M7 变体后为 21,191/23,395 B；不透明真假分支后为 21,401/23,647 B；全代码随机化（字段序整体 seed 洗牌 + 加密参数 seed 派生：乘子/混合常数/轮数双端同步变化，同 seed 字节复现）后为 21,464/23,652 B；特征拆分隐藏（验证器一分为三：打包重建形态表、varint 解码、边界验证，均独立字段并随机布局）后为 21,654/23,809 B；随机 opcode 重编号（分派/边界/终止检查全部跑 per-seed 单射置换编号，目标端打包串重建并在展开时重写操作码字节，`.obf` 与差分不变）后为 22,081/24,318 B；分派链二级拆分（2..4 条模数子链、选择器四拼写、链长逐 seed 变化，子链各自 fail-closed）后为 22,289/24,582 B；全字段解除锚定+分隔符/prelude 序微变体（17 字段位置完全随机、`,/;` 逐字段变体、prelude 三方洗牌）后为 22,185/24,408 B；全局名隐藏（26 个名字经字符函数池拼装+块级环境索引，探针参数化，明文归零；体积门有意识上调 25,500/27,000 B）后为 23,449/25,938 B；随机 section 拆分（解码器扁平化为解密+2..4 随机 helper 簇+core 平级字段，字段总数 20..22 逐 seed 变化）后为 23,799/26,410 B；全阶段控制流扁平化（四类逻辑状态机化+SETUP/PH/PU/PK 函数拆分）时曾为 24,933/27,315 B；后续 M7/A1/A2 历史值见路线日志。ISA3 semantic virtualization 以 program-specific superhandler 换去一对一同构后曾为 **64,599/71,598 B**；ISA4 五阶段 token/深层谓词/死状态批次后为 **68,344/75,615 B**；ISA5 edge-token/可达 neutral 批次后为 **71,084/78,081 B**（门 72,000/80,000 内）；ISA6 live descriptor/global fragment 第三批后为 **80,369/89,149 B**；ISA7 block transport v1 第四批后为 **83,640/92,116 B**；ISA8 bounded-LZW 第五批在包含 Lua decoder 后降为 **82,572/92,022 B**；ISA9 global code-segment 第六批加入全局两节点图及目标 parser 后为 **82,729/91,722 B**；ISA10 runtime witness/control与集中final-fragment回边后为 **82,514/91,344 B**；ISA11双ChaCha8/frame-v2/anti-hook后为 **82,201/90,866 B**；ISA12-A删除重复fixed-slot/actual-op prelude并加入共享operand getter后为 **72,198/80,636 B**；ISA12-B加入frame-local register mapper与全handler访问lowering后为 **74,540/83,040 B**；ISA12-C加入carried-value fragments及六状态entry graph后为 **78,706/87,167 B**；ISA13加入record/segment factorial与dictionary/metadata/tuple字段序置换后曾为 **79,426/87,873 B**；ISA14加入capture/constant全局池后曾为 **81,515/89,777 B**；K9a/K1（ISA15 混合base86）为 **93,044/102,734 B**；K7 多态字运算为 **94,553/103,728 B**；K8/K9 与 09-10 keyed dual-lane frame tag 批次为 **98,889/108,938 B**，同日 probe transcript 批次 +0/+111 B → 当前上表 **98,889/109,049 B**（整脚本预算已于 K10 按实测重钉为 112,000 B/目标并恢复 enforce，暂停开关 `OBF_BENCH_SCRIPT_CAP=off` 保留）。这些历史整脚本大小不充当compression ratio；正确门位于semantic bytes→完整LZW frame。公开`.obf`的SHA-256仍是varint批次值；private ISA14 operand/register/field-order/pools/segment/LZW/transport按seed/semantic image改变。

需要更新生成物时，先确认行为正确再用固定 seed 重建：

```bash
cargo build
target/debug/obf virtualize --target lua51 --seed 7001 -o vm_lua51.out.lua tests/fixtures/vm_lua51.lua
target/debug/obf virtualize --target luau --seed 7351 -o vm_luau.out.lua tests/fixtures/vm_luau.lua
./tools/test-matrix.sh
```

不能为了消除 golden mismatch 先覆盖 expected 文件；纯文档变更不应改动 golden。

## 11. 日常修改与定位问题的流程

### 修改前后必须做什么

1. 记录分支、HEAD、初始工作树和基线；保留别人已有修改。
2. 先加入/保留原生最小复现，确定是 parser 接受性、lowering、codec、runtime、字段还是最终命名/分隔问题。
3. 做最小范围修改；先跑相关 suite，随后跑完整矩阵。每次更新都要有目标语法检查，不能只靠 Rust 编译。
4. 验证同 seed、默认新 seed、bytecode 是否应变化、两目标和两个 backend；必要时检查 compile/wrap 及旧 ISA。
5. 涉及生成结果才重建 golden，并同步 README/协议/兼容性/路线中的实际能力、限制、测试数和体积。
6. 检查 UTF-8/U+FFFD、`git diff --check`、最终 diff；文档说明必须对应真正执行过的测试。
7. 只提交本次相关文件，完整门禁通过后推送当前工作线，核对远端 SHA。不要把本地保存、commit 和 push 混为一谈。

### 按修改类型找检查点

| 要改什么 | 至少检查哪些位置 |
|---|---|
| 新语法/AST 节点 | parser/ast、scope 所有 walker、type/插值 fragment、sidecar、minify、IR lower；先查原生可接受性 |
| local 名称/复用 | scope/rename、激活时序、protected/global/type、同域独立检查；小图穷举及实际运行 |
| 分号/字符串 | parser sidecar、改名后位置重收集、emit_tokens/fragment；不要修改 payload |
| 求值/闭包/循环 | ir/lower 的 Place/Operand、cell/pack/CFG、目标 handler、捕获裁剪/共享元数据；双目标差分 |
| 新 opcode/格式 | 稳定编号、encode/decode/serialize、Rust validate、目标 validation、目标 registry/handler、旧版本拒绝/兼容及真实执行覆盖 |
| private operand/register/dataflow/field-order ABI | `lowering.rs`三类profile全域唯一性、operand store/getter、register单射/界限/无完整表、field factorial合法/双端重算、handler token span、producer/consumer eligibility、carry/RF nil/false、`SETUP`同ABI、fragment最大匹配、求值顺序/alias/递归frame、entry stage graph、四类field-order corruption、双目标runtime与golden |
| 私有字段 | schema + 每个 marker 访问点 + custom::finalize；用户/宿主同名项、词法证明和 payload |
| 新宿主能力 | 固定 runner 的真实 API/行为、NAMECALL/元方法/回调边界、平台测试；不要泛化成 executor 已兼容 |

### 可复制的定位命令

```bash
export PATH="$PWD/.toolchains/rust-1.88.0/bin:$PATH"
cargo build

# 假定最小复现保存在 target/repro.lua；Lua51 版本同理替换目标与工具
toolchains/bin/luau-compile target/repro.lua >target/repro.native-listing.txt
toolchains/bin/luau target/repro.lua >target/repro.native.out

target/debug/obf check --target luau target/repro.lua
target/debug/obf dump-ir --target luau -o target/repro.ir target/repro.lua
target/debug/obf compile --target luau -o target/repro.obf target/repro.lua
target/debug/obf inspect-bytecode --target luau target/repro.obf
target/debug/obf wrap-bytecode --target luau --seed 735 -o target/repro.vm.lua target/repro.obf

toolchains/bin/luau-compile target/repro.vm.lua >target/repro.vm-listing.txt
toolchains/bin/luau target/repro.vm.lua >target/repro.vm.out
cmp target/repro.native.out target/repro.vm.out

cargo test --test vm_parity
cargo test --test private_fields
# 这些是定位步骤，不能代替最终完整矩阵
```

定位提示：

- 原生本身不接受：先区分 SyntaxError/CompileError、固定版本和编译选项，不要归咎 VM。
- `check` 通过但 `compile` 拒绝：看反射、控制流证明或资源门限，未必是 parser bug。
- wrap 阶段拒绝：看 target、外层格式/ISA、结构与 checksum，不要跳过 validator。
- VM 可编译但输出不同：优先看操作数读时机、store 顺序、cell 生命周期、pack.n、原生优化和宿主差异。
- 只在最终压缩后失败：在 Rust 内部测试里保留生成的 raw/intermediate 源码，分别检查字段映射、绑定与语句 sidecar；不要往最终输出临时追加局部声明。
- 只在 Luau require/NAMECALL 失败：先核对固定 runner、模块相对路径/调用环境和 userdata 适配器，不要改成 `loadstring` 兜底。

### 2026-09-11（同日第七批）：K17——十进制转义取最短安全宽度 + base86 字母池排除引号类字节（体积 −1,289 / −728 B；293 门全绿）

**触发**：K16 之后用户批准的一次全量体积审计（要求"不删步骤、不改结构、一次性报告全部可挤项"）。审计里排第一的第二、第三项被批准实施，其余留在待批清单。**关键约束**：只准改文本表示，不准动值、结构、步骤与格式。

- **A1 的根据**：`minify::literal_bytes` 与 `src/lexer.rs` 的十进制转义分支都是"最多读三位、遇非数字即停"，且 Lua 5.1 明确拒绝 >255——所以 `\28` 与 `\028` 在两个目标上都严格等价。两份 golden 里被转义字节的 99% 是字母表强制包含的 28/29（外加 127/165 这类），它们全都能省 1 B。规则写成"后随一个 ASCII 数字才补零"，用 `next_is_digit` 看的是**发射流**的下一个字符而不是 payload 的下一个字节（chunk 切在 11 字符上 ⇒ `...\00` + `3` 这类跨界必须仍补零；新门里 `\028` 后紧跟裸数字 5 的用例就是钉这个）。
- **A2 的根据与代价**：字母表原先只按"避开边界类"筛池（28..=126 去掉 28/29/125/126 再随机丢 13），于是 `"`/`'`/`\` 有约 86% 概率入选，每 6 KB 段招来 ~70 处两字符转义。改为按构造先扣掉这 3 个、把随机丢弃数 13→10 ⇒ 符号数与 span 不变，只改"哪个符号代表哪个值"。**明确付出的代价**：字母池从 95 选 82 缩到 92 选 82（集合随机度略降），且 A2 换映射会改变 `base86_padded` 里"填充分支"的命中次数，共享的 transport RNG 前进量随之变化 ⇒ 逐种子标签会整体挪位（Lua51 的 check4 distinct 2→3 就是它，Luau 九项反而一字未动）。
- **体积与结构边界**：Lua51 101,908 → **100,619 B**（−1,289），Luau 112,336 → **111,608 B**（−728），10 种子最坏 101,134 / 111,608 B ⇒ 120,000 B 门内 Luau 余 8,392 B。A1 每字节发 `min(所需位数) ≤ 3`，结构上不可能变长；A2 的文本长度是映射的函数，实测会 ±数百字节抖动（Luau 本轮实省比静态预估少 554 B）。镜像与公开 `.obf` 一字未动（`k7_wire_image_bytes_are_pinned`、`stream_audit`、`product_audit` check9 `(18777,2,false)` 全部原样通过）。
- **门与重录（全部写归因）**：新增 2 条 ⇒ **177 单元 + 116 集成 = 293 全绿**，矩阵 PASS（源码 ≤81,920 B 逐文件过，最大文件 `constant_fields.rs` 44,294 B），15 次最佳 bench 346/114 ms，覆盖率仍 46/46、49/49。`heuristic_audit` 只有两条拼写度量动：Lua51 M5 `(5,1914)→(5,1842)`、M7 `[557,549,540,134,134]`→`[527,525,522,134,134]` gap 406→388（134/134 那对不动 ⇒ 没有新的长串）；Luau M5 `(5,1523)→(5,1509)`、M7 `[419,418,418]→[419,412,410]` gap 284→276。M1（字母表 86 / span 99）、M2、KAT、M3b、M4 的整个 top12、M4b、M6 一字未动。
- **测试侧的两处"跟着改"，都不是放松门**：`semicolons.rs` 的字面量形态 pin 从 `\000\255` 改 `\0\255`（字节等值由程序自带的 `string.char(…,0,255)` 断言 + 与原生解释器的差分继续钉住）；`custom_vm.rs` 的篡改靶子查找原先硬编 `\029` 并按固定 4 字符替换（A1 之后只剩 2 处命中 ⇒ "no stable tamper target"），改为两种宽度都认 + 断言替换后源宽不变，反 hook 的 fail-closed 断言原样成立。

**测出来的"已经没有余量"清单（写下来免得重复调查）**：代码区 6,132 个空格一个都不能安全删（删一个就破坏 `needs_separator`/`vm_boundary_can_omit` 的充分性）；分号已最小；`x[N]` 读取 1,740 次共 3,202 B（平均 1.8 位）；`.obf` 与压缩帧无免费余量（14,071→13,479 B 的 4.2% 是"输入已被加钥置换"的下界，换更强编码器榨不出东西，把压缩挪到加钥之前等于让字典看见明文）；base86→base94 只省 ~0.4% 却要改分组宽度与全部长度门；跨串字面量池化 ~255/242 B、`0x`/`0b`/下划线数字拼写 ~1.1 KB/1.5 KB、相邻 `local` 合并 ~918/948 B（需豁免"每条语句以分号结尾"这条规则）——**全部仍是未批项**。画像器落库为 `tools/size-probe.py`（只读，不进矩阵）。

### 2026-09-11（同日第六批）：K16——重复轮常数改为包装表字段（不改壳格式；291 门全绿）

- **动机**：用户要求"在不改变格式的情况下，把数字改成 `local f={} return setmetatable({随机字母=256,g=function(k) print(k.随机字母) end},f):g()` 这种调用"。先按字面做了一版（数字挂进 payload 表的入口字段、经 `self` 读），实测只覆盖 2,224/117,812 B 且省 0 B——payload 表只有入口拿得着，其余 section 字段是被显式实参调用的；随后改挂到 **chunk 级包装表**上：所有函数都能以 upvalue 读到它，读点写成 `t.<键>`。
- **改动**：新模块 `src/vm/custom/constant_fields.rs`，`finalize()` 变为 `lift → fields::shorten → finalize_vm`（`lift` 是唯一钩子，`emit`/`generate` 不参与）。写入 `t.e,t.h,t.u,t.ab,t.ac=65536,4294967296,16777216,65521,1001000;` 是入口体第一条语句；读点遍布入口与兄弟字段体内。
- **格式证据**：证据可核：新增写入语句自带的 `;` 被 `finalize_vm` 当冗余分隔符剥掉 ⇒ 两份 golden 的 `;` 总数与上一版完全相同（377），换行数仍为 0，入口体前缀除被改写的字面量外与上一版逐字符相同（`if 65536%256==1` → `if t.e%256==1`）。
- **四条门**：壳形逐条对上（两条语句、`setmetatable(payload, wrapper)`、被调方法名 == 入口字段的键）⇒ 像壳的用户程序不改；改写范围限于 payload 表函数字段的体内 ⇒ 构造式自身与 `:m(args)` 实参在入口之前求值，那里读到 nil；遮蔽包装名的作用域保持原拼写 ⇒ 绑定范围由 AST 精确算出（`local` 到块尾、形参与 `for` 变量到所属体）；只改规范十进制整数字面量 ⇒ 词法级跳过字符串/long bracket/注释，`[N]=` 键位不动，前导零不绑。自检是三条守恒式（字面量总数、每键读取数、包装名出现数），不成立即 `Err`；随后 `lex` + `parse_source` 复核。
- **定价与实测**：读价 = 名长 + `.` + 键长（两份 golden 实测 3 B），写价 = read + 值 + 分隔；脚本自身几乎占满单字母名，第 4 个字段起键长 2 ⇒ `2000`(17 次) 被留着、`256`(208 次) 按定义不动。`MAX_FIELDS=14` 未触及（实际 5/4 个）。golden 102,261 / 112,605 → **101,908 / 112,336 B**，10 种子最坏 102,377 / 112,701 B；`luac5.1 -l` 只有 max upvalues 16→17（上限 60）；双目标真实执行 stdout 与 fixture 逐字节一致；矩阵 PASS、bench 344 / 112 ms、覆盖率 46/46 与 49/49。
- **pin 归因**（重录前逐条测得并记录）：`heuristic_audit` 只有 M3b/M4 动——Lua51 大常数 256→228 且 `big_sum` 差恰为 19×4294967296 + 9×16777216，Luau 259→244 且差恰为 15×4294967296；`(65536,100)`/`(65536,91)` 整类退出 top12。`product_audit` check1 `65536` 99→3 / 93→3、`16777216` 10→1、`4294967296` 21→2 / 16→2（后两类退出锚点表），check8 83→49 / 78→57 而 gcd 仍 1，check5 一类模板规范化成 `%X.X`；check2/3/4/6/7/9、M1/M2/KAT/M4b/M5/M6/M7、`stream_audit`、`private_fields`、镜像 pin 一字未动 ⇒ 不改镜像、不改公开 `.obf`、不升 ISA。
- **普查测试改读 `emit_unlifted`**：`semantic.rs` 的 opaque 守卫普查（`truthy+falsy==1`）与 `fields.rs` 的"只改被标记字段"token 双射看的是**拼写**，本批正是把 `65536%256==0` 换成 `t.a%t.b==0`（值不变）⇒ 两条改读 `#[cfg(test)] emit_unlifted`（generate→shorten→finalize_vm，跳过 `lift`），不是把门放松。
- **诚实边界**：只改表示位置，语义/值/错误边界/镜像/`.obf` 全不变；被遮蔽作用域（`2147483647` 的 27/29 处）与构造式里的常数仍拼在原地，读字段本身仍可还原。收益是静态锚点面收缩（三类漂亮常数退出 golden）+ 353/269 B。
- **上一批的账**：K15（把重复字面量归拢成 chunk 级 local 池，实测 −1,799 / −1,642 B、无运行时代价）因改变 `setmetatable` 首参与 chunk 头格式被用户否决并**整体回退**（`7b3f450`，与 `65d3d84` 逐字节相同），本批是同一目标下不改格式的重做。

### 2026-09-11（同日第四批）：K13c 第一步——常量池镜像改存字节坐标（零行为差异；282 门全绿）

- **动机**：ISA17 要给常量值加钥，加钥后 `__obf_proto_k` 里只能留"可由保留字节推出"的东西；因此先把发射体的表示层改成 `(tag, off, len)` 坐标形式，并让"区间自洽"成为被门禁的不变量。本批**不加钥、不改生命周期**，属纯管道。
- **改动**：`src/vm/custom/emit_decode.rs::pool_loops_lua` 的常量循环开头 `KBase=pos()`，逐条 `local ko=pos()-KBase`、按 tag 走原有解码分支后 `T[ix]={tg,ko,pos()-KBase-ko}`，并把值当场写入 `OW.__obf_proto_k[ix]` / `OW.__obf_proto_tags[ix]`（`tg==1` 的 `val=val==1` 归一从 `PK` 内移进来，位置等价）；循环尾 `KLen=pos()-KBase`。`emit.rs` 的 `PK` 变成门禁：`if not rec then E()end;local tg=rec[1];if tg>5 or rec[2]+rec[3]>KLen then E()end;if tg~=0 and F.__obf_proto_k[j]==nil then E()end`。
- **踩坑（已入门禁，值得复述）**：`PK` 与 `PH`/`PU` 同在 defs 块里定义、且 defs 被逐 seed 洗牌，所以 `local KBase,KLen=1,0` **必须**声明在 defs 之前；第一版把它跟在 `local P={};…;local w=…` 那行（defs 之后）发射，`PK` 里的 `KLen` 解析成全局 nil，装载期第一条常量就 `attempt to compare nil with number`，fixture 直接无声中止。新门把"声明位置早于 `local PK=function`"写成断言。
- **证明零行为差异**：`generated_parser_reads_global_capture_constant_pools` 用 plain 与 flipped 两种池序对同一小程序**真实执行**并比对原生输出；ISA14 capture/constant 池 corruption 门、semantic graph corruption 门、逐 outer ciphertext byte 损坏门、`k7_wire_image_bytes_are_pinned`（image 字节未动 ⇒ 不升 ISA）、`stream_audit`、`private_fields` 原样通过；`product_audit` 九项一字未动。
- **实测**：golden 100,712 / 110,179 → **100,794 / 110,267 B**（各 +82/+88 B；SHA-256 `4d9ee226…b8fd` / `b1a37291…9d66`），10 种子最坏 100,794 / 111,528 B（120,000 B 门内，Luau 余约 7.6%）；`vm:lua51:ok`、`vm:luau:ok`、`luac5.1 -p`、`luau-compile` 全过；矩阵 **166 单元 + 116 集成 = 282 门，0 失败**（`target/k13c1-matrix.log`）。
- **审计归因**：`heuristic_audit` M4 双目标同形位移——类 0 +2（新增 `tg~=0` 与 `local KBase,KLen=1,0` 各一个 0）、类 3 +1（`rec[3]`），类 1 净值 0（池内新增 `val==1` 与声明处的 `1`，抵掉 `PK` 删除的两处 `==1`）、类 2 净值 0（`rec[2]` 前后各一个）；其余类与 M1/M2/KAT/M3b/M5/M6/M7 逐字节相同。**为什么 `product_audit` 完全不动**：新增的都是个位数小整数，不落入漂亮常数类（86^k / 2^8 / 2^16 / 2^24 / 2^31± / 2^32±），也没有产生新的 `;` 片段形状类。
- **下一步（第二步，未开工）**：区串加钥 + `DC` 按 `(tg,off,len)` 重建 + `LVE` 释放 `__obf_proto_k`/`_tags` + `WIRE_ISA_VERSION` 16→17（同步 `private_fields`、`bitops` 镜像 pin、`stream_audit`、两套 `heuristic_audit`、两套 `product_audit`）。开工前先解决"DC 里没有浮点重构器 `fin`"这一条（见施工单 (A)/(B)）。**诚实边界不变**：本批之后常量明文仍常驻 `__obf_proto_k`，重入不会重建 ⇒ T4 未完成。

### 2026-09-11（同日第三批）：K14——装载期校验器分派改为种子化二叉搜索树；体积门 120,000 B（已实现）

- **动机与来源**：用户在看过已发射的 `vld(...)` 分派字段后直接指定“在这个基础上加入二分树，让这个更复杂一些”。原形态是每个残差桶一条线性 `if/elseif` 等值链，形状齐整到可以正则扫出“opcode→谓词”整表。
- **改了什么**：`src/vm/custom/structure.rs` 的 `grouped_chain` → `grouped_tree` + 递归 `search_tree`。桶内成员按 opcode 值排序（`debug_assert` 断言两两互异 ⇒ 分派是纯函数，节点求值顺序无关），用 `structure` 种子流选切分点（`1+rng%(len-1)`）、按种子位翻转左右分支、按种子选叶链长度（`2+rng%3`）；只在叶子保留旧式 `if/elseif … else E()end` 短链，内部节点统一发 `if <边界条件> then <左> else <右> end;`。`src/vm/custom/emit.rs` 的调用点改为 `grouped_tree(&mut structure, f3_arms, f3_groups, "o", program.target.is_luau())`。
- **新增的发射侧原语**：`src/random.rs` 的 `Prng::boundary_condition(variable, bound, below, luau)`——`<` 有 5 种等价写法（`o<K`、`K>o`、`not(o>=K)`、`o-K<0`、`K-o>0`），`>=` 有 4 种（`o>=K`、`K<=o`、`not(o<K)`、`o-K>=0`），边界值仍走与 arm 相同的 `integer_literal`（十进制/hex，Luau 另可选带下划线的 bin），故不引入新值类。**踩过的坑**：第一版把两棵子树都塞进 `then` 分支、漏了 `else`，fixture 静默不执行；补上显式 `else` 后双目标才 `vm:*:ok`。
- **为什么语义不变**：臂谓词字符串、`validation(op)` 表体、外层 `local ok=false; … if not ok then E()end;return true` 全部逐字未动；树只是把“哪条臂被试”换成“按值域二分走到哪条臂”，0..=255 上与旧链同值，未知/越界 id 仍只有 `E()` 一个 fail-closed 出口。私有镜像与公开 `.obf` 一字未动 ⇒ **不升 ISA**（`k7_wire_image_bytes_are_pinned`、`stream_audit`、`private_fields` 原样绿）。
- **门**：`random::tests::boundary_conditions_partition_the_byte_range_exactly`（每种拼写在 0..=256 逐值核对恰为 `<`/`>=` 互补，且 9 种拼写必须全部出现——否则等于悄悄缩减覆盖面）；`src/vm/custom/tests/lane.rs::validator_dispatch_is_a_seeded_binary_search_tree`（两份 fixture、四颗种子：臂数恒为 48 / 51，` then if ` 节点 ≥20，叶链数 ≥ 节点数，`E()` 数 ≥ 叶链数，且 (节点,叶链) 组合跨种子不得全等）。
- **实测**：golden 99,433 / 109,109 → **100,712 / 110,179 B**（SHA-256 `13c34a7a…6b03` / `05842492…0aa1`），10 种子最坏 100,712 / 111,440 B；`vm:lua51:ok`、`vm:luau:ok`、`luac5.1 -p`、`luau-compile`、`obf check` 全过；矩阵 `[matrix] PASS`，**165 单元 + 116 集成 = 281 门，0 失败**（`target/k14-matrix.log`）；bench Lua51 311 ms / 155.5x、Luau 104 ms / 34.7x；告警画像与基线同为 8 条既有。校验器体文本实测每种子 4.5–5.4 KB、节点 20–34 个随种子变。
- **审计 pin 的逐条归因（按观测重录，非“顺手调数”）**：`heuristic_audit` 只有 M4 残差表位移（Lua51 类 1 461→469、0 396→410、20 104→74，类 9 退出 top12、41 以 68 进；Luau 1 460→492、0 394→401、23→16、`skipped` 32→39），M1=86 / KAT 词表 / M2 / M3b / M5=5 / M6=(0,0) / M7 top5-gap 全不变；`product_audit` check1 Lua51 `256` 199→210、`65536` 99→102，Luau `86` 7→5、`256` 177→187（切分点字面量在既有 opcode 值域内换类，无新值类），check5 Lua51 11→12 类（树节点本身就是一种新语句形状；最大类仍 21，top-8 只换第 8 项），check6 Lua51 1→0 / Luau 0→1（校验器第一个 `;` 位置移动，把脚本头那个 `local` 起始密集槽片段切短/切长——已逐片段实测确认），check2/3/4/7/8/9 与全部 `fail` 标志不变。
- **同批的门与口径**：`tools/bench-vm.sh` 默认 `OBF_BENCH_SCRIPT_CAP` 112,000 → **120,000**、`src/vm/custom/tests/semantic.rs` 两处预算同数、`tools/test-matrix.sh` 注释同步；用户解除“Luau-buffer 直用”冻结项（Luau 可直接用 `bit`/`buffer`；Lua 5.1 表达不了的变换优先实现 Luau）。
- **诚实边界**：树换来的是“静态形状不再齐整”和装载期每记录 log2 次比较；它不是新秘密、不提供抗 dump 保证。仍在执行的 prototype 词表与常量/捕获池依旧是明文 ⇒ T4 缺 K13c。

### 2026-09-11（同日第二批）：seed.rs 预备拆分——只拆分、零生成差异

- 把 `seed.rs` 里 32,985 B 的**逐 opcode routine 数据**（`routine_jump`/`routine_test`/`routine_return`/`routine_for`，L480–1434）整段移入新兄弟模块 `src/vm/custom/seed_routines.rs`；编码、标签汇编器、构建期校验器、Lua 发射器与 `SEED_LOOP` 模板留在 `seed.rs`。
- 接线方式沿用 `seed_deform.rs` 先例：`mod.rs` 只加一行 `mod seed_routines;`（不加星号再导出），`seed.rs` 用 `pub(crate) use super::seed_routines::routine_for;` 保住既有 `seed::routine_for` / `custom::*` 路径，测试里三个手写 routine 改由 `use super::super::seed_routines::{…}` 引入。新模块对 `super::seed::*` 做 glob 导入，`asm_ins/asm_lab/asm_jif/asm_jmp` 四个 helper 由私有放宽到 `pub(crate)`（可见性变化不触及生成文本）。
- **零生成差异证明**：重录后 `cmp` 两份 golden 与提交树逐字节一致（`486963e5…` / `0402d8c4…`，99,433 / 109,109 B 未变），双目标执行仍 `vm:*:ok`；`cargo build --release` 的告警画像与 HEAD 完全相同（拆分曾多出的“未使用再导出”已消除，8 条为既有基线）；`tools/test-matrix.sh` **PASS / 279 门 0 失败**，bench 317 ms / 158.5x 与 99 ms / 33.0x（同码抖动区间内）。
- 拆分后 `seed.rs` 46,798 B、`seed_routines.rs` 35,480 B，两者对 81,920 B 上限各有 >35 KB 余量，为 K13c（常量/捕获池惰性化）腾出施工面。

### 2026-09-11：K13c 第二步 — 常量池 payload 加钥 + 常量生命期（private ISA17，T4 收尾）
- **做了什么**：见 `总路线.md` 同日条目。要点：镜像里的常量 payload 已非明文；解析期常量循环只走位并记 `{tg,off,len,acc}`；值由 `DC` 在帧激活期重建，加载期 dry-run 与 `LVE` 都释放；字段池 16→18；`WIRE_ISA_VERSION` 16→17。
- **同步 re-record 的账**（逐项实测后改，未静默调数）：`WIRE_ISA_VERSION` 断言（`lane.rs`、`tests/private_fields.rs`）；`k7_wire_image_bytes_are_pinned` image FNV（Lua51 `0xd907c58e3ca3fb7e`、Luau `0x50d0f585b7c36b71`，**长度 1,141 / 870 B 不变**，说明加钥只改内容不改形状）；`stream_audit` 内层词数 975→997 / 778→799（重复与公因子仍 0）；`heuristic_audit` 两配置（+47/+23 流长、M3b 大常量 +6/+6、M4 尾部换形、M5/M7 转义密度抖动、M6 保持 (0,0)）；`product_audit` 两配置（check1 圆整数计数 Lua51 ↓2/↓3、Luau ↑1/↑2，check5 类别 12→11——**本批唯一轻微负面**，check8 gcd 仍 1，check9 流长 +351/+230）；`lane.rs` K13b 生命周期锚点与 `transport.rs`/`semantic.rs` 的字段签名与 `SETUP` 顺序锚点。
- **新增能力断言**：`constant_payloads_are_not_plaintext_in_the_image`（4 个字符串常量在两个目标、4 个种子的 image 字节里都不得作为子串出现；关掉加钥该测试立刻失败 ⇒ 非空断言），并在池门里加"`NU` 与 `fin` 的 double 重建式全文只能出现两次"的防漂移锁。
- **64 位整数回归教训**：`DC` 的 tag4 分支一开始写了不存在的 `acc`（应为逐记录 `ak`），症状是 Luau 专属 `attempt to perform arithmetic (add) on nil and number`；`luau_integer_constants_are_exact_and_not_float_coercions` 抓到它——两份 fixture 都不含 `xi` 常量，所以只有集成语料能覆盖这条路径。

### 2026-09-11：K13b T4 第二步——帧引用计数空闲回锁（已实现）

- **做了什么**：K13 让词表"首次执行才解码"，但解完就常驻；本批补上"用完退回"。`emit.rs` 里 H 字段作用域加 `local LVC={}` 与助手 `LVE=function(f,v)`（帧计数增减 + 归零回锁），帧进入在惰性解码后 `LVC[fid]=(LVC[fid] or 0)+1`；`seed.rs` 里两条帧出口把值经助手转一手：`Opcode::Return` ⇒ `return LVE(fid,SEED(SEEDT[i],{…},2));`，尾调用的宿主侧返回 ⇒ `return LVE(fid,v1);`，尾调用换帧 ⇒ `LVE(fid,nil);fid,args,ups=v1,v2,v3;break;`。`DC` 在 `code[-1]` 留下链态原始串作回锁句柄，计数归零时写回 `F.__obf_proto_code` 并再清 `__obf_proto_routes`。
- **正确性关键**：释放**不能**放在分派循环 `end` 之后——尾调用路径在 `break` 前就重绑了 `fid`，账会记到被调方，调用方永久泄漏；实测 Lua 5.1 fixture 立刻 `attempt to perform arithmetic on local 'z' (a nil value)`。词表是帧局部，早释放仅导致下次重解；必须计数保护的是每次 fetch 都读的 `F.__obf_proto_routes`（被清成 nil 会当场炸）。
- **证据**：临时给四个点位插计数器后跑 fixture——帧进入 16 = 销账 16（Lua51）、15 = 15（Luau），解码 34 / 31 次 ≫ 所用 prototype 数，回锁 16 / 15 次 ⇒ 解码—回锁循环真的被反复触发，且两目标仍 `vm:lua51:ok` / `vm:luau:ok`。
- **门禁与 pin**：`tests/semantic.rs` 的 Return 臂 pin 改为含 `LVE(fid,…)` 包裹（去掉包裹必须红）；新增 `tests/lane.rs::prototype_words_relock_when_a_prototype_goes_idle`（原始文本门，4 seed × 2 target：助手唯一、句柄 1 铸 + 2 用、每帧入账 1 次、尾路径释放 1 次、以及"释放不得在分派循环尾部"的反向 pin）；`heuristic_audit` M4 类 1/0 由 455/395→461/396（Lua51）与 454/393→460/394（Luau），逐字面量核对为 6 个 `1` + 1 个 `0` + 1 个池索引位移，其余 M1/M2/KAT/M3b/M5/M6/M7 与另外 10 个类不变；`product_audit`/`stream_audit`/`private_fields` 原样通过。
- **体积与 ISA**：golden 99,433 / 109,109 B（各 +207 B），10 种子最坏 100,224 / 110,437 B ⇒ 112,000 B 预算仍绿，不需要援引"T4 完成前忽略体积门"；私有镜像字节不变 ⇒ `WIRE_ISA_VERSION` 保持 16。
- **边界**：本批只消灭空闲/未执行路径的明文指令词。常量池、捕获池与"正在执行"的词表仍是明文 ⇒ T4 未完成，下一步 K13c；不得据此宣称抗 dump。
- **注意**：本批让 `seed.rs` 触到 81,068 / 81,920 B（余量 852 B）⇒ 同日已追加预备拆分批次（见上），K13c 前不必再动结构。

### 2026-09-10：K13 T4 第一步——per-prototype 惰性解码 + 校验后释放（已实现）

- 把 `semantic_validator` 里"每个 prototype 解词"的整段逻辑抽成**一个共享函数 `DC(id)`**（validator 字段多导出一个 sibling：`return RD,ED,OG,DC`，entry 侧 `RD,ED,OG,DC=VMS[..](...)`，H 字段参数同步加 `DC`）。装载期仍然逐 prototype 调 `DC(id)` 做**完整解码 + 全量校验**（`vld()`、边界、图连通性、`p~=#CD+1` 消费到底），随后把 `P[id].__obf_proto_code` 退回原始链态字符串并清空 `__obf_proto_routes`，因此解码结果是一次性丢弃的：fail-closed 语义与"用户代码前无 stdout"完全不变。
- 解释器侧 `local code=F.__obf_proto_code;if not code[0] then code=DC(fid) end`：**首次执行到某 prototype 才真正物化它的指令词表**，`DC` 内 `if CD[0]~=nil then return CD end` 保证幂等（同帧/多次进入不重复解）。未被调用到的函数（大脚本里通常占多数）在内存里永远只有链态字节。
- 与 K12 的关系：链态 `stL` 是 per-prototype 从 `id/nr/start` 起算的，所以任何 prototype 可以在任意时刻被独立重放——这正是"惰性 + 可重放"的前提；也正因此，**下一次解码不依赖上一次留下的东西**，K13b 的空闲回锁只需在引用计数归零时把表退回字符串即可。
- 面：`emit.rs` 71,146→71,282 B；私有 image 字节**不变**（`k7_wire_image_bytes_are_pinned` 原样通过，故无需再升 ISA 版本）；golden 各 +136 B；只有 `heuristic_audit` 的 M4 残差 `0/2` 三项位移（多出的 `nil`/索引 0/2 字面量），`product_audit`、`stream_audit`、`private_fields` 全部原样通过。
- 门禁：`cargo test --lib` **162 全绿**；全量集成（`custom_vm` 20/20、`vm_parity`、corruption、双目标语法/运行）全绿。
- 诚实边界：这消灭的是"**空闲/未执行路径的明文常驻**"，不是"运行时不可 dump"——正在执行的 prototype 词表仍是明文；段级预算回锁与常量/捕获池的惰性化留给 K13b/K13c。不宣称不可逆、不宣称抗 dump 完成。

## 12. 工程约束、提交和下一步

### 不可破坏的约束

- std-only/MSRV、固定双目标、最终一/两字母/分号/单行/验证要求继续有效。不得擅自用第三方 parser/solver/packager 替换现有实现。
- **所有 `src/**/*.rs` 单文件必须 `<=81,920 B`**。超限先按逻辑边界拆分，功能改动前先证明 fixed-seed 生成逐 byte 不变；不得通过移除测试或把代码挪到生成物绕过门。
- 源文件上限现状（2026-09-11 拆分后复核）：`src/vm/custom/semantic.rs` 71,099 B、`src/vm/custom/emit.rs` 65,701 B、`src/vm/custom/seed_deform.rs` 51,295 B、`src/vm/custom/seed.rs` **46,798 B**、`src/vm/custom/seed_routines.rs` **35,480 B（新）**、`src/vm/custom/tests/runtime.rs` 79,020 B、`tests/semantic.rs` 78,342 B、`src/parser.rs` 78,439 B、`src/vm/custom/tests/lane.rs` 12,353 B。经验规则不变：给接近 81,920 B 的文件加逻辑前，先做一次“只拆分、零生成差异”的预备批次（先例：`seed.rs` → `seed_deform.rs`、`tests.rs` → 三个 include 子文件、`emit.rs` → `emit_decode.rs`/`emit_prelude.rs`、本次 `seed.rs` → `seed_routines.rs`）。**现在 `seed.rs` 与 `seed_routines.rs` 都有 >35 KB 余量，K13c 可直接在其中加字面量，不必再拆。**
- 默认自定义后端不依赖原生 compiler；旧后端显式且持续可用。不要把 OBF v1 13-byte/6-byte 数据布局套进 OBF v2。
- 不大幅重组项目、不搬走旧 opcode 文件、不顺手修改 vendor；允许在既有目标子目录增加相应 handler。
- 不以“优化”为由减弱有界解析、工作预算、token/绑定、结构或执行回归；安全验证不是可公开关闭的选项。
- `src/anti/`仍是通用Anti保留位；已完成的decrypt anti-hook位于`vm/custom/chacha.rs`，因为attestation必须直接参与KDF。不要搬成装饰性入口guard，也不得把ISA11/12/13 ChaCha/witness/segment/LZW/operand/register/field-order ABI误写成不可绕过安全模块。后续结构/通用Anti/captures/fusion均须先写威胁模型、体积性能预算与双目标差分。
- `target/`、`.toolchains/`、vendor 中间产物、`.obf/.luac/.luauc` 等遵守 `.gitignore`，不要提交日志、缓存、大数据和临时补丁。根目录两份 `.out.lua` 是明确保留的例外。

### 本次工作线的交付方式

当前会话（2026-09-10）绑定分支是 `arena/01a08a7a-obf`，只在它上面工作和推送；`arena/01a0804f-obf`、`arena/01a0866a-obf` 与用户提供的破解报告分支 `arena/01a07abc-obf` 都是历史/只读参考，不能向其提交。以后独立会话先确认实际绑定分支，不把本文中的历史名字当作可以切换分支的指令。

```bash
git status --short
git diff --check
# git add <本次逐项确认的文件>，不盲目 add 整个脏工作区
git diff --cached --stat
git diff --cached --check
git commit -m "<说明本次变化>"
git push origin arena/01a08a7a-obf
git rev-parse HEAD
git ls-remote origin refs/heads/arena/01a08a7a-obf
# 最后两个 SHA 应一致，再确认工作树状态
```

本地 Git 用 `git`；GitHub PR/issue/check/release 用 `gh`。未要求时不要代改历史 PR。认证失败应恢复 GitHub 连接，不在聊天或仓库保存凭证，也不使用 force push 掩盖同步问题。

### 后续优先级

0. **当前必须先做**：无红灯欠账。2026-09-11 依次完成 K13、K13b、`seed.rs` 预备拆分（零生成差异）、K14（校验器分派树 + 体积门重钉 120,000 B + 放开 Luau 直用 `bit`/`buffer`）、K13c 第一步（常量池坐标化）与第二步（ISA17 常量加钥 + `DC` 重建 + `LVE` 释放）、K15 数字池化批次（因改壳格式整体回退 `7b3f450`）与其重做 K16（轮常数改为包装表字段）、K17（转义宽度 + base86 字母池，纯拼写瘦身），当前 **177 单元 + 116 集成 = 293 门全绿**。技术路线上的下一步仍是 **K6（打散，T2：20+ 写点 + 运行期替换 handler 实现）与 K3-FULL（T3：每 handler 独立混合 + Hull–Dobell 全周期断言 + 读宽/间隙多样化）**；体积侧还有 K17 审计里那份未批清单（跨串池化、数字拼写池、`local` 合并等），动它们之前要先拿到批准。
1. **当前安排**：ISA11 ChaCha8/anti-hook、ISA12-A/B operand/register ABI、ISA12-C carried-value fusion/entry graph、ISA13 parser field-order、ISA14 capture/constant pools 与 ISA15 混合 base86 transport 已完成；K7 多态字运算、K8 indirect route、K9 per-lane affine operand key 也已落地。fixed operand bridge、actual-op marker、全像统一 register key、逐 primitive 原 handler 拼接、顶层线性入口、统一 record/segment/dictionary/metadata 字段顺序、per-prototype capture/constant 邻接与统一 transport 分组/连续字母表均已针对性削弱。frame-tag 批次留下的 3 个红灯已在 2026-09-10 全部清掉（第 0 步完成，见第 0.1 节），当前 275 门全绿。**K10 = 整脚本体积 shrink（Lua51 98,889 / Luau 109,049 B，bench 已退化到 325 ms / 162.5x（上一批 383 ms），必须一并回看）+ 重新钉体积门 + 回补 emit.rs 的 59 B 余量**；函数结构变换（inlining/outlining/reorder）另批独立评估，先写 fail-closed 不变量、尺寸性能预算和双目标 semantic corruption 门。
2. **后续独立项**：通用anti-debug、多组合理假状态/假记录、captures/constants全局池与inlining/outlining/reorder。已有decrypt anti-hook和operand/register/dataflow ABI不代表通用Anti或全部静态结构问题完成，也不能靠继续叠加transport替代这些结构工作。
3. **继续语义差分**：真实业务语料、最小反例、repeat/continue数据流、剩余常量/闭包共享、宿主边界；不要重新实现已交付compression/transport/segment/witness层，也不能把它们宣称为不可逆语义防护。captures/constants全局池与inlining/outlining/reorder另作独立评估。
4. **测量后再优化**：cell/pack 分配、寄存器压力、编译工作量、生成体积与运行成本。先写预算和原生对照，守住 nil、求值顺序、逃逸和 GC 边界。
5. **持续证明最终生成安全**：新字段/节点/handler 必须经过同一 finalizer、字段/token/绑定验证及两目标语法和运行测试。
6. **明确尚未验证的环境**：executor 实机、不同版本/选项、隐藏元表或动态反射都需要独立证据，不能用标准 runner 的 PASS 替代。

### K13c 施工单（ISA17：常量值加钥，明文只在执行中的 prototype 内存在）

2026-09-11 逐点核对发射文本后的结论与设计，供实现批次直接取用。**状态：两步均已落地**——第一步（管道，坐标化）与第二步（加钥 + DC 重建 + `LVE` 释放 + 升 ISA17，见 §8 表 K13c 第二步行与本文件同日批次小节）；下面保留的是开工前的核对过程与当时选定的 (A)/(B) 方案取舍，含"DC 里没有浮点重构器"这条当时待定项的最终结论，**不再是在办事项**。原核对过程（同日稍后的 K14 批次改了同一份 `emit.rs` 里的**校验器分派形状**——把线性 `if/elseif` 链换成种子化二叉搜索树，见 §8 表 K14 行——但 `CK`/`CU` 的记录内容、`validation(op)` 的谓词文本与臂的数量都没动，本条列出的两处耦合仍然原样成立）。

- **明文在哪儿**：常量在 parser 的池读取态成型——`tg==0→nil`、`1→b8()`、`2→num()`、`3,5→str()`、（仅 Luau）`4→IF('%08x%08x',hi4,lo4)`，逐条写入 `CK[owner][ix]={tg,val}`；捕获写 `CU[owner][sl]={tg,ix}`。切片态 `for fid=0,np-1 do id=fid;F=P[id];PU();PK() end` 再把它们织成 `F.__obf_proto_k/_tags/_u`。
- **"删掉池镜像"不是可收获项**：`CU`/`CK` 是 parser 字段 `g` 槽里的局部表，字段末尾 `g=nil;return rP,rnp,ren` 之后就已不可达、可回收。也就是说 ISA14 的洗牌在装载尾部并未留下第二份按 prototype 索引的值表；要隐藏常量只剩一条正路——**让值的持久形态变成密文**，即动私有镜像格式（ISA17）。
- **可行的 ISA17 形状**（与 K13/K13b 完全同构，避免另造机制）：
  1. ~~Rust 侧对每条常量记录的表示层加钥后，`CK[owner][ix]` 只保存 `{tg, off, len}`~~ **第一步已做完这半条**：`CK[owner][ix]={tg,ko,pos()-KBase-ko}` 已落地（不含值），值改为池循环内直写 `OW.__obf_proto_k[ix]`；**剩下的**是把 `KBase..pos()` 这段区间的字节**加钥**保存（`SS(B,KBase,pos()-1)` 拷出后逐字节 XOR/置换），并让 `__obf_proto_k` 在装载校验后被清掉。
  2. 装载期照旧逐条解一遍做**完整校验**（数值可解析、tag 合法、bool∈{0,1}、长度自洽），验完即弃明文，与 K13 对词表做的"解→校验→释放"一模一样，fail-closed 契约不破。
  3. `DC(id)` 增加一段"按 `CK[id]` 的 `(off,len)` 回到镜像缓冲、`take` 出字节、反加钥、构造值并再校验"，于是重入时仍能重建，且重入路径同样会因篡改而 `E()`。
  4. `LVE` 在计数归零时连同 `__obf_proto_k`/`__obf_proto_tags` 一起释放（现在只释放 `code`+`routes`）。
- **Luau 侧允许直接用 `bit`/`buffer`（用户 2026-09-11 放开）**：Luau 的 int64 tag 反加钥可以用 `bit.bxor`/`buffer.read_i32` 直接写，不必再降级成纯算术半字模拟；若某个变换 Lua 5.1 无法表达，优先把 Luau 路径做完，5.1 侧保留较粗但正确且 fail-closed 的变体（不得因为 5.1 做不了就整项放弃，也不得让 5.1 输出错误结果）。
- **加钥必须落在表示层，禁止用算术重建 double**：Lua 5.1 没有位→浮点的重解释手段，`sign*2^exp*(1+m/2^53)` 之类写法在 subnormal、NaN、`-0.0` 上不保证与宿主 `tonumber` 逐位一致，属于语义回归，不能做。安全的分层是：字符串与 method 名做逐字节 XOR（沿用 K7 的 `SB`/`NCH` 双目标工具箱）；Luau int64 已经是两个 u32 半字进 `IF('%08x%08x',…)`，对半字做模 2^32 加性/异或即可；浮点只对 `num()` 读到的**十进制文本**做可逆置换（值不经过任何算术改写，`tonumber` 结果逐位不变）；nil/bool 只吃 tag lane。
- **第二步唯一的硬工程问题：DC 里没有浮点重构器**。`fin`（`lo/hi → double`，含 `render_u32_atom` 的种子化形态）定义在 reader cluster 字段的返回里，绑定发生在 **decode stage 那个 state 分支内**（`local fin,num=VMS[..](MF,b32);`），而校验字段 `VMS[2](…)` 的调用在 **bind stage** ⇒ `fin` 不在作用域。两条路，先量再选：(A) 把 `fin` 提升进 `ret_names`（prelude 声明 + cluster wiring 去掉 `local`，与 `FMt,PT`/`P,np,entry` 同一个 hoist 技巧），然后作为实参传进 `VMS[2]`——零重复文本，推荐；(B) 在校验字段内复制 `fin` 的渲染文本——必须复用**同一次** `render_u32_atom` 结果（两处文本逐字相同才 bit-exact），代价是脚本里多一段完全重复块，`product_audit` check5 与 `heuristic_audit` M5/M7 会各 +1 类，需按实测归因重录。**禁止**在 Lua 侧用 `sign*2^exp*(1+m/2^52)` 之类算术重建 double。
- **两个必须同批处理的耦合**：① `validation()` 的 `ReadGlobal|WriteGlobal` 条件读 `F.__obf_proto_tags[k]`、`Constant` 条件读 `__obf_proto_nk`，所以 DC 内顺序必须是"解词 → 解常量 → 逐记录校验"，不能把校验搬出 DC；② `__obf_proto_u` 不能跟着空闲释放——closure 由**父帧**创建并读**子** prototype 的 `_u`，父帧不为子 fid 记 `LVC` 账；要么 `_u` 保持常驻，要么把 Closure 臂改成读 `CU` 原始记录（而 `CU` 常驻又与"值不落盘"冲突，须在同一批里定）。
- **成本已被测量放行（且第一步已落地）**：门是 120,000 B/目标，K13c 第一步（坐标化，各 +82/+88 B）后 10 种子最坏 Lua 5.1 100,794 B、Luau 111,528 B ⇒ Luau 余约 8.5 KB，够 DC 侧那一套反加钥重建（预计数百到 1 KB）与 K9b 并行使用。**第一步做完的事实**：`CK` 记录已是 `{tg,off,len}`、`KBase`/`KLen` 区间门禁已在 `PK` 里、值由池循环直写 prototype；**第二步只做加钥与生命周期**，不必再动池结构。开工前先跑最坏种子实测，必要时按既有纪律以实测重钉门并书面归因，**不得靠删校验或缩小常数覆盖换体积**。另一侧代价是热 prototype 被反复重入时要重解它的全部常量（`nk` 上限 65,536），这与 K13b 已接受的"重解词表"同源，应在 bench 里单独看。
- **升 ISA17 要同步的账**：`semantic::WIRE_ISA_VERSION`、`tests/private_fields.rs` 的 ISA 断言、`tests/bitops.rs` 的镜像字节 pin（当前 1,141 / 870 B + FNV 两值）、`tests/stream_audit.rs` 字数、`tests/heuristic_audit.rs` 两套 pin、`tests/product_audit.rs` 两套 pin——全部以实测重录并写归因，不许"顺手调数字"。公开 `.obf`（5,525 / 6,575 B，SHA 自 varint 批次未变）**不应**受影响，若受影响即说明改动越界到了公开格式。
- **批次切法与进度**：①管道批次 **已完成（09-11）**——`CK` 改存 `{tg,off,len}` + `PK` 区间/覆盖门，值仍明文，`CU` 无需改（捕获记录本来就只存 `{tag,index}`，实测它已不含值）；DC 侧一项未动，故本批零行为差异。②加钥批次 **已完成（09-11，ISA17）**——落点与原计划有 3 处差异：`fin` 不搬家，改为把 `NU`（连同 `U32`/`UK`）放进 prelude 工具箱随 `ret_names` 提升，避开簇形两种（单簇/多簇）下的 `local` 剥离脆弱性；池循环**连解码都不做**，只按 tag 跳过 payload；`acc` 随记录存进 `__obf_proto_rec` 第 4 位，`DC` 于是可按槽位顺序走而不重放全局链；`PK` 的取值检查随值消失而撤销（只剩覆盖/范围门）。②加钥批次——区串 `KImg=SS(B,KBase,pos()-1)` 加钥后**经 parser field 的第 4 个返回值**交出去（`return rP,rnp,ren,rKImg`，wiring 处 `local P,np,entry,KImg=VMS[..](…)`，再作为 `VMS[2]` 的新增实参传给校验字段）：**不要新增 `__obf_proto_*` 字段**，那会动 16 字段双射与 `private_fields` 的字段池；值仍明文、行为完全等价，用固定 seed `cmp` 证明零生成差异（体积会变，需实测）；②加钥批次——Rust 侧表示层加钥 + Lua 侧反加钥 + 空闲释放 `k/tags`，升 ISA17 并重录上述 pin。③（可选）捕获侧：先改 Closure 臂读 `CU`，再谈 `_u` 的惰性化。

交接时应交付的是：当前代码/分支、实际可复现命令、最新完整结果、已更新的生成物与文档、明确的剩余限制。不要再把历史的“待接入默认 AST”“尚无私有字段压缩”当成当前任务，也不要把暂缓项误写成已完成。
