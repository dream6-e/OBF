# OBF 项目交接总结

> 快速交接文档，面向第一次进入仓库的开发者/Agent。
>
> 快照日期：2026-09-06（Asia/Shanghai）
>
> 仓库：`dream6-e/OBF`
>
> 当前开发分支：`arena/01a074dc-obf`
>
> 历史 PR：<https://github.com/dream6-e/OBF/pull/2>（head 为旧分支 `arena/01a071aa-obf`，本会话不更新它）
>
> 本会话接手基线：`0803946`（owned AST 交接）；已交付 M3 安全压缩，已交付完整生成后随机一/两字母命名、解析器对照修复、安全作用域复用、AST→IR→OBF v2 寄存器 VM 、分号优先源码分隔及运行语义兼容性、VM 私有字段短名增量（PASS149）

## 1. 一分钟了解项目

OBF 是一个 **Rust、std-only、零第三方 crate** 的双目标 Lua 工具链，只支持：

- Lua 5.1.5；
- Luau 0.735 / Roblox injector 方向。

现已实现 AST→IR→4-byte 自定义寄存器 VM 和安全随机短名。当前以可执行、固定格式、验证为优先，按用户要求暂不做加密、压缩、随机 section 或随机 opcode。

默认主链路：

```text
UTF-8 source → 目标 lexer / owned AST / BindingId
            → ir::Module（寄存器、常量、捕获、basic blocks）
            → 指令选择 / 符号标签回填 → OBF v2（32-byte Header / 4-byte instruction）
            → Rust 校验 / target decoder 校验 / register VM
            → 全部生成后一次 finalize_vm：随机短名 / 分号分隔 / 单行化 / 绑定图校验
```

默认 `virtualize` 不调用原生 compiler，也不 fallback。`--backend native` / `vm::virtualize_native` 才走旧 compiler→native chunk→OBF v1。两套后端都不能用 `load/loadstring` 委托执行源码。完整新协议和 ISA 见 [`自定义字节码.md`](自定义字节码.md)。

## 2. 当前可用能力

### 2.1 CLI

```text
obf check --target <lua51|luau> <input|->
obf minify --target <lua51|luau> [--seed N | --no-rename] [-o FILE] <input|->
obf virtualize --target <lua51|luau> [--backend ast|native] [--seed N] [-o FILE] <input|->
obf dump-ir --target <lua51|luau> [-o FILE] <input|->
obf compile --target <lua51|luau> [-o FILE] <input|->
obf wrap-bytecode --target <lua51|luau> [--seed N] [-o FILE] <input.obf|->
obf inspect-bytecode --target <lua51|luau> <input|->
```

示例：

```bash
./tools/bootstrap-rust.sh
export PATH="$PWD/.toolchains/rust-1.88.0/bin:$PATH"
cargo build

target/debug/obf check --target lua51 script.lua
target/debug/obf check --target luau script.luau
target/debug/obf minify --target lua51 -o script.min.lua script.lua
target/debug/obf minify --target lua51 --seed 123 -o script.seeded.lua script.lua
target/debug/obf minify --target luau --no-rename -o script.lexical.luau script.luau
target/debug/obf virtualize --target lua51 --seed 123 -o script.vm.lua script.lua
target/debug/obf virtualize --target luau --seed 0x735 -o script.vm.luau script.luau
target/debug/obf dump-ir --target lua51 -o target/script.ir script.lua
target/debug/obf compile --target lua51 -o target/script.obf script.lua
target/debug/obf inspect-bytecode --target lua51 target/script.obf
target/debug/obf wrap-bytecode --target lua51 --seed 123 -o script.from-bytecode.lua target/script.obf
```

`--seed` 对 `minify`、`virtualize`、`wrap-bytecode` 有效，接受十进制或 `0x` 十六进制 `u64`。省略时每次生成新 seed，仅在 **stderr** 输出 `seed: N`，stdout 保持纯脚本；同源、同目标、同配置、同 seed 可逐字节复现。`compile` 输出 binary，`dump-ir` 输出可读 IR，二者不接受 seed；`--no-rename` 只用于 minify，`--backend` 只用于 virtualize。

默认 AST/v2 路径的 seed **仅影响最终变量名**，bytecode 与布局不随机化；不做加密、压缩或随机 section。显式 `--backend native` 才保留旧 OBF v1 的随机 opcode/dispatcher/数字写法。脚本输出均是单物理行；IR/inspect 报告和二进制文件不适用“脚本单行”的限制。随机短名不是加密，有限名称空间不保证任意两个 seed 都产生不同文本。

### 2.2 Rust 公共接口

关键入口位于 `src/lib.rs`：

- `obf::parse(source, target) -> Result<ast::Chunk, Diagnostic>`；
- `obf::check(source, target) -> Result<(), Diagnostic>`；
- `obf::minify(source, target) -> Result<String, Diagnostic>`（每次使用新 seed 的安全随机短名）；
- `obf::minify_with_options(source, target, MinifyOptions::seeded(seed))` / `MinifyOptions::lexical()`；
- `obf::scope::analyze(source, target) -> Result<scope::Analysis, Diagnostic>`；
- `obf::ir::compile(source,target)` / `ir::lower(&ast)`；
- `obf::bytecode::custom::{encode,decode,serialize}`；
- `obf::vm::custom::{compile,emit,virtualize}`；
- `obf::vm::virtualize(...)`（默认 AST） / `vm::virtualize_native(...)`（显式兼容）；
- `obf::bytecode::inspect(...)`。

`check` 是兼容旧调用者的验证接口；需要语义结构时使用 `parse`。`MinifyOptions` 现在包含 `rename_locals` 和 `seed`，推荐构造方法而非旧 struct literal。`MinifyOptions::default()` / `vm::Options::default()` 每次创建新 seed；重用同一 options 对象就是复用 seed。

### 2.3 AST 前端

`src/ast.rs` 是公开、owned、保留 span 的 AST。当前覆盖：

- chunk、block、statement、expression；
- name、binding、function/function name、parameter、vararg；
- table list/record/computed field；
- unary/binary operator 与 assignment/call；
- Lua 5.1 完整核心语法；
- Luau type annotation、type alias、type function；
- type/type pack、union/intersection/optional、function/table type；
- generic/default、table `read`/`write` access；
- `if` expression、compound assignment；
- attribute、`const`、value `export`；
- `f<<T>>` 显式 type instantiation 与 `receiver:m<<T>>(...)` 方法类型实参；
- Luau 插值字符串及其中每个嵌套表达式。

所有 span 都是原始 UTF-8 source 的半开 byte range。插值字符串内部 expression 会恢复成相对于完整 source 的全局 span，不再依赖外部 Luau 编译器兜底。

安全门限：

- source：64 MiB；
- token：1,000,000；
- AST node：1,000,000；
- parser/interpolation nesting：64 层；
- 每层线性运算/后缀链：64 次，防止迭代构造的深 owned AST 析构时栈溢出。

解析器审计已补齐 contextual `continue`、prefix/suffix 基底限制、跨行括号调用（Luau 只按 LF，Lua 5.1 也按裸 CR）、单次类型断言和 unary 优先级、模块 return/export 双向冲突、const 初始化数量和写入规则、type function 外层捕获限制。`Call.type_arguments` 必须被每个 AST/作用域检查者遍历；`LocalBinding.is_const` / `Reference.is_write` 参与最终绑定图校验。const/type-function/exported-function 语法使用有界 `scope::analyze_chunk` 做 postparse 校验，不能再次调用 parse 形成递归。

`check` 不是完整编译器：Luau `continue` 跳过 repeat 局部初始化的控制流检查和编译器资源上限不在其承诺内。`tests/parser_audit.rs` 固定记录一个原生 CompileError（非 SyntaxError）案例，避免把这类边界误当普通语法错误，也避免用朴素词法规则拒绝原生可编译的 optimized/deferred 情况。有限测试不能证明所有输入都无 bug。

公开 token-array parser 会检查 UTF-8 边界、span 顺序、EOF、行列位置，并重新词法化逐 token 对比，避免畸形 token stream 造成 panic 或错误 AST。

已完成第一阶段 lexical scope、local/parameter/upvalue 身份绑定、shadowing/name resolution 与保守安全改名。`FunctionBody.has_vararg` 独立保留无类型标注的 `...`，用于正确区分 Lua 5.1 的隐式 `arg`。

跨作用域复用、AST/IR 到自定义 bytecode 均已实现。minify 仍不做常量折叠/DCE；AST compiler 做 Lua51 必要的数值/signed-zero 处理，并新增 Luau 有界只读标量/纯条件传播、可达性和实际捕获裁剪。完整数据流/优化证明、外部宿主反射模型未完成。

### 2.4 Lexer、作用域分析与安全单行 minifier

`src/lexer.rs` 分目标处理：

- identifier/keyword/contextual keyword；
- Lua/Luau number-like token 和目标合法性；
- short/long/interpolated string；
- long/line comment；
- Luau attribute、type 符号和 compound operator；
- source span 与行列诊断。

`src/scope.rs` 为每个 chunk/function/block 建立显式 scope，声明获得稳定 BindingId，引用映射到该绑定或 global。作用域遍历使用显式栈；scope/binding/reference 各限 1,000,000，工作项限 8,000,000。公开结果包含声明位置、引用位置、const/写入标记、捕获标记和每个函数的传递 upvalue 集合。

`src/minify.rs` 在完整生成后统一调用 `src/scope/rename.rs`，对全部安全绑定分配随机 `[a-z]{1,2}`：单/双字母池分别按 seed 洗牌，再按引用频率、声明位置、BindingId 分配，优先单字母。原本已是短名的绑定也必须改变拼写。同域唯一、跨域安全复用；避开 globals、类型/泛型、protected locals、目标/宿主 API，将同时改写的 locals 的旧名可释放。不是文本替换，不做折叠或 DCE。

`Scope.name_scope` 表示命名唯一性分组：函数参数+直接函数体 locals、for 变量+直接循环体 locals 分别共组，原 lexical scope ID/parent/可见性不变。一个组内的所有可改名声明都互斥，包括未使用和原来已被同名遮蔽的声明；通过颜色占用表而不是平方规模边集实现。原先必须保留的同名遮蔽不被强行拒绝。

跨域干涉使用私有 `Analysis.activations` 的语义 reference ordinal，不用源码声明位置猜测：某外层绑定 A 的引用在内层 B 生效后、且位于 B 的作用域内，则 A/B 不得同色；同 scope 已遮蔽的旧声明也需检查。初始化/签名先于声明、闭包、传递 upvalue、typeof、方法类型实参、插值和 repeat 条件因此与 resolver 保持一致。

最多 702 个候选（还要排除保留名字），但总绑定数不受 702 限制。同域迭代增广路径可修复多步名称冲突，其他域保持固定；不是全局最优着色或无界回溯求解器。工作预算 8,000,000，跨域稀疏边上限 1,000,000；超出池、当前有界分配找不到安全方案或超过门限时返回 Diagnostic，不扩展到三字母、不部分输出。最终源码重新解析并核对全部绑定、引用/写入、声明生效顺序、globals/upvalues，同时独立检查同域唯一性（捕获未使用变量撞名），失败拒绝输出。

关键语义：

- local 的全部 annotation/initializer 引用旧环境，再引入新 locals；local function 的函数体可递归引用自己；
- Luau 函数的整个 signature 在外层值作用域解析，参数和递归新函数名在 signature 后才进入作用域；
- repeat 局部变量对 until 可见，for 的循环变量对初始化表达式不可见；
- 方法 self 在显式参数前引入，Lua 5.1 兼容 arg 在显式参数后引入；
- 全局/字段/方法/未限定类型/泛型/value export 名称不改；typeof 和限定类型前缀中的值引用会同步改写；
- type function 暂不改其内部名称；它的签名、函数体及嵌套函数不允许捕获外层运行时 locals；
- 插值内嵌表达式、嵌套函数和注释均按原始全局 spans 处理，允许表达式中的物理换行并最终输出单行；必须避免 table 表达式紧跟插值左括号形成非法 `{{`。

发现 `debug`、动态环境、加载 API、dump 或静态可识别的反射字段时，整份源码自动只做词法压缩。`--no-rename` / `MinifyOptions::lexical()` 可显式关闭名称改写，适用于从未知宿主回调传入的反射能力；不能声称已分析任意动态宿主。两种模式都不保留源代码行号、dump 字节或错误位置文本。

旧低层 `minify::minify(source, tokens, target)` 保持不改名，并先验证外部 tokens；它与普通 minify、`--no-rename`、两套 VM finalizer 共享分号分隔规则。

分隔策略：完整语句之间，以及非空块末语句与 `end/else/elseif/until` 之间输出 `;`。已有分号不重复，EOF 不额外补分号，空块不插入空语句。`local a`、`return a`、块头/循环头、逻辑运算、`- -`、数字/点号与插值 table 起始间隔仍按语法保留；字符串/bytecode 字节、表字段分隔和调用后缀不变。不保证比分号增量前更小。

实现只在 formatter 请求时启用 parser 的私有 `statement_ends`：`block_inner` 在消费已有 `;` **之前**记录完整语句结束的全局 byte offset；升序表与节点数量同样有界，emitter 二分查找，不靠关键词猜边界，也不另写可能漏节点的 AST walker。插值子解析器移动同一 Vec，覆盖嵌套函数、`typeof` 与 type function，不跨层复制。改名改变源码长度后，必须重新解析改名文本获取边界；不得复用旧 offset。公开 AST/语法不变，普通 check/compile 不收集该表，最终输出仍重解析、验证绑定并执行资源门限。

VM 只能经私有 `minify::finalize_vm`：严格匹配顶层单声明 `local G=(getfenv and getfenv(0))or _G`，所有三个 barrier span 必须在该 initializer 内，且 `getfenv` / `_G` 引用都必须是 global。多一个已知环境/反射 barrier、受保护显式绑定、绑定遮蔽或不同捕获结构都拒绝；普通 user-source API 不提供此例外。所有代码先组装完，再统一改名，随后不许追加模板。最终每个显式 VM local 必须 `[a-z]{1,2}`、同域唯一且不同于旧名；隐式 `arg` 保留。不承诺虚拟化任意宿主反射/调试栈语义。

`src/random.rs` 复用原 deterministic PRNG，布局算法不变。新 seed 由 std 进程随机 hasher 初始化并用原子计数/双射 mixer 派生，进程内到 u64 回绕前不重复；不是密码学机制。命名流与布局隔离，目标域分别为 `0x6e61_6d65_0000_0051` / `0x6e61_6d65_0000_0735`，显式 seed 不经过 fresh 路径。

### 2.5 原生 bytecode parser

- `src/bytecode/lua51.rs`：Lua 5.1 header、端序、prototype、constant、debug table、子函数和 38 个 opcode；
- `src/bytecode/luau.rs`：Luau bytecode v3–v14、type version、string table、prototype、constant、debug/coverage/cost 等结构；
- 所有 reader 必须继续使用边界检查、checked arithmetic、计数和递归上限；
- 截断输入已进入强制矩阵。

固定 Luau 0.735 编译器默认生成 bytecode version 14，不要误以为是 base version 9。

### 2.6 自定义 bytecode 与 VM

```text
source → 现有 AST / BindingId → typed register IR / basic blocks
       → 自定义指令选择 / 标签回填 → OBF v2
       → 校验 / decoder / register VM → 完整生成后随机短名 / 单行化
```

这条链路**不调用原生 compiler，不存 native word，也不默默 fallback**。新 `ir::Module` 包含函数、常量、cell/upvalue 捕获和带符号后继的基本块；IR 的 branch 生成 `Test + Jump + Jump`，每条均为 4 bytes。Header 固定 **32 bytes**，含版本、目标、端序、宽度、文件长度、prototype 数量、入口、ISA 版本和 Adler-32。完整逐字段规范与 **49 条 ISA** 见 [`自定义字节码.md`](自定义字节码.md)。

- Lua 5.1：46 个 `src/vm/opcode/lua51/c*.rs`；Luau：49 个 `src/vm/opcode/luau/c*.rs`。每条有效 opcode 有一份独立固定 handler，不以 NOP 代替未实现语义。
- 每 frame 为寄存器文件；local 使用 heap cell，临时值为普通寄存器，闭包引用 cell。循环的新一轮/复用寄存器不会破坏逃逸闭包。
- 显式 pack.n 处理多返回值、尾部 nil、vararg、调用/返回；VM→VM 尾调用替换 frame。支持宿主函数、元方法、回调及 coroutine。
- 两端分别处理赋值/方法求值顺序、numeric-for、表构造器刷新和 Lua51 隐式 `arg`；Luau 另有 `//`、插值、泛型擦除、`__iter`、userdata NAMECALL、精确 i64 及冻结导出表。
- Rust reader 与生成的 decoder 都做范围/格式验证；指令流为 byte string，每步直接 fetch 4 bytes，不是源码 table 中的伪字节码。

Rust API：`ir::compile/lower`、`bytecode::custom::{encode,decode,serialize}`、`vm::custom::{compile,emit}`，以及默认 `vm::virtualize`。`inspect-bytecode` 自动区分 OBF v2 与原生 chunk；`wrap-bytecode` 可把已保存的 `.obf` 独立包装为 VM。

所有 decoder、runtime、dispatcher、字节码中出现的 handler 和执行尾部组装完后，只调用一次私有 `minify::finalize_vm`；之后不追加代码。该阶段统一随机一/两字母名，重解析复核绑定图、同域唯一性、名称长度、确实换名和 bytecode 字节不变。唯一生成器环境例外仍是严格审计的 `local G=(getfenv and getfenv(0))or _G`，没有公开忽略反射的开关。

#### 运行语义增量 / ISA 修订 2

`src/ir/lower.rs` 以 BindingId 收集全局写入事实、初始化器、root/loop 信息；Luau 的已证明纯条件可剪枝，CFG 标记可达性，不再把死 continue 当作实际跳转。只读标量通过 `Rc<Constant>` 共享，string 推断上限 64 KiB，操作/复制及最终 IR 扫描计入 1M 工作预算，不能调用用户函数/表来推断常量。

编译完成后自底向上根据 IR 的 Read/WriteUpvalue 和子函数 capture 裁剪类型/死代码引用，重编号自身和子函数的传递捕获；公开 scope/resolver 不改。随后根据运行时捕获判断 Luau 共享策略；mutable/参数/循环捕获与递归初始化不能混为一谈。固定目标的负 integer literal 与动态 unary 行为分别保留。

`ir::Function.shared_closure`、`Capture::RecursiveLocal` 对应 prototype flag bit3 与 capture kind2；**ISA_VERSION=2**，外层版本/长度/opcode 宽度与 46/49 编号不变。`Program.isa_version` 保存实际版本，修订 1 继续读/执行/原样 round-trip，CLI 不能打印编译器常量冒充文件版本。新增组合限制在 Rust validate 和 Lua decoder 双侧执行。

Luau 迭代器使用 raw `__iter`，不触发 metatable 的 `__index`，false 不得退回容器，无 `__iter` 时保留 `__call`。完整支持条件/已知不等价项和验证方法见 [`虚拟机兼容性.md`](虚拟机兼容性.md)，尤其不能将含洞表 `#`、GC/布局、隐藏宿主或所有优化相关函数身份算作已保证。

#### 显式兼容后端

`virtualize --backend native` / `vm::virtualize_native` 保留原来的 `compiler → native reader → OBF v1`。旧根目录 `lua51_*.rs`（38）与 `luau_*.rs`（91）不移动、仍单独测试。只有这个后端使用旧 13-byte Header、6-byte `u16 private-opcode + u32 native-word` 及随机布局。

旧 backend 的 compiler 查找依次为 `OBF_LUAC51` / `OBF_LUAU_COMPILE`、仓库 `toolchains/bin`、`PATH`。默认 AST 后端在这些变量指向不存在文件时仍可正常编译/生成；原生工具仍用于门禁的语法/运行对照。

#### 已提交示例

| 文件 | 来源 | seed | v2 bytecode | 最终单行脚本 |
|---|---|---:|---:|---:|
| `vm_lua51.out.lua` | `tests/fixtures/vm_lua51.lua` | 7001 | 6,879 B | 32,832 B |
| `vm_luau.out.lua` | `tests/fixtures/vm_luau.lua` | 7351 | 8,257 B | 33,582 B |

生成器、命名或分隔策略变更后必须再生成两份示例。矩阵比较默认生成、独立 compile/wrap、debug/release 及 golden 的逐字节一致性。本版优先完整可执行与格式清晰，不声称体积比旧 native backend 更小。

## 3. 固定环境与复现

仓库包含：

| 组件 | 版本 | 位置 |
|---|---:|---|
| Lua source | 5.1.5 | `vendor/lua-5.1.5/` |
| Lua interpreter | 5.1.5 | `toolchains/bin/lua5.1` |
| luac | 5.1.5 | `toolchains/bin/luac5.1` |
| Luau source | 0.735 | `vendor/luau-0.735/` |
| Luau runner | 0.735 | `toolchains/bin/luau` |
| Luau compiler | 0.735 | `toolchains/bin/luau-compile` |
| Rust bootstrap | 1.88.0 | `tools/bootstrap-rust.sh` |

Rust 约束存在两个必须同时记住的版本：

1. `Cargo.toml` 的 MSRV 是 Rust/Cargo 1.88.0，仓库内可离线复现；
2. 用户还要求兼容 rustc 1.96.0（源 tarball 版本）。当前环境无法直接取得完整 1.96 工具链，所以新增代码至少必须在 1.88 编译，且不要写出预计会破坏 1.96 的实现。

`.toolchains/rust-1.88.0` 体积约 550 MiB，不提交 Git；环境重置后运行：

```bash
./tools/bootstrap-rust.sh
```

目标解释器/编译器和必要 vendor source 已纳入仓库，不应改为依赖在线下载。

## 4. 强制验证流程

**每次修改后都必须运行两个目标的完整矩阵：**

```bash
./tools/test-matrix.sh
```

矩阵当前包括：

1. rustfmt check；
2. `cargo test --all-targets`；
3. debug/release build；
4. Lua 5.1 basic + focused AST corpus 的 check/minify/luac/runtime 对比；
5. Luau basic + focused AST corpus 的 check/minify/compiler/runtime 对比；
6. 两套原生 bytecode inspect 和截断拒绝；
7. Luau runner 的 `loadstring`、`require`、sandbox 探针；
8. Lua/Luau VM 同 seed 可复现、异 seed 不同；
9. 每份 VM seed 变体目标编译、执行对比、单物理行检查；根目录输出与固定 seed 7001/7351 逐字节核对；
10. 私有 blob 的 magic/version/target/长度/Adler、命名前后字节一致、AST inline metadata 检查，以及 opcode 布局/禁止 `loadstring` 的断言；
11. 新 ISA 实际执行覆盖 Lua51 46/46、Luau 49/49；显式 native 后端另保留 38/38 和至少 60 条核心 opcode coverage；
12. scope corpus 的原始/词法/seed 735/736 执行差分、固定 seed 确定性、异 seed 换名及这些 fixture 的体积缩短；
13. 两套 reflection corpus 的可观察 local/upvalue/function 名称与 `--no-rename` 一致；
14. `[a-z]{1,2}`/同域唯一性/旧名改变，跨域复用、多步匹配修复、650 个短名、耗尽、CLI 默认 seed/可复现/参数/失败文件保护，私有 VM 环境例外正反例；
15. 多 seed `0/1/0x735/u64::MAX` 原生执行差分与并发 fresh seed 回归；
16. 固定编译器 parser audit、10,000 相邻块复用、小图穷举重解析、96 种生成式遮蔽/初始化运行差分及干涉工作门限；
17. compile/inspect/wrap、binary stdin、缺失 compiler、debug/release binary 和脚本逐字节一致；
18. 新 bytecode 恶意结构/截断/round-trip/大 jump、tail call、GC/coroutine/回调、精确 i64、冻结模块、userdata NAMECALL 等回归；
19. `tests/semicolons.rs` 的分隔、必需空格、函数后缀、类型/嵌套插值、字符串字节、空块/已有分号、非法源码、CLI/两后端，以及内部边界完整性/改名偏移/10,000 相邻块/幂等性测试。

2026-09-06 VM 私有字段短名增量后的最近一次完整结果：

- Rust tests：**149/149**（40 单元 + 109 集成；此前 140 项全部保留，新增 4 项私有字段内部测试和 5 项集成）；
- matrix：PASS；
- Lua 5.1 自定义 ISA executed coverage：46/46（legacy/reference 38/38）；
- Luau 自定义 ISA executed coverage：49/49（legacy/reference 63/91）；
- Lua 5.1 VM：**32,832 bytes**（seed 7001）；
- Luau VM：**33,582 bytes**（seed 7351）；
- Lua 5.1 scope fixture：4,131 B 原始 → 3,584 B 词法 → 1,979 B 安全压缩；
- Luau scope fixture：4,194 B 原始 → 3,685 B 词法 → 2,283 B 安全压缩。

两个可直接检查的根目录输出必须继续提交到 GitHub：

- `vm_lua51.out.lua`；
- `vm_luau.out.lua`。

生成器/命名/分隔策略修改后，先重新构建 `target/debug/obf`，再更新示例并跑矩阵：

```bash
target/debug/obf virtualize --target lua51 --seed 7001 -o vm_lua51.out.lua tests/fixtures/vm_lua51.lua
target/debug/obf virtualize --target luau --seed 7351 -o vm_luau.out.lua tests/fixtures/vm_luau.lua
./tools/test-matrix.sh
```

对非 vendor 修改可额外运行：

```bash
git diff --check -- . ':(exclude)vendor/**'
```

不要直接对整个 `vendor/**` 做全量 whitespace 修复；上游代码存在预先存在的格式问题，会制造无关大 diff。

## 5. 不可破坏的要求

下面这些不是建议，而是项目硬约束：

1. 只支持 Lua 5.1 与 Luau/Roblox，不扩展到 Lua 5.2/5.3/5.4。
2. Rust std-only；不得增加任何第三方 crate。
3. 每次修改同时测试 Lua 5.1 和 Luau，任何一边失败都不能接受。
4. 每次更新同步维护 `总路线.md`。
5. 最终压缩/VM 输出必须是一条物理行。
6. Lua 5.1 输出不得出现 Luau binary literal、数字下划线、type annotation 等语法。
7. 未证明安全时不改 global、field、method、API、`_G`、`_ENV`、`getfenv` 等动态名称。
8. 私有 bytecode 必须是真实连续 serialized byte stream，不能用 Lua source table 冒充。
9. wrapper 可以 decode 私有 bytecode，但不能通过 `load`/`loadstring` 委托执行原生 chunk 或解密源码。
10. 新指令按用户许可放在 `src/vm/opcode/{lua51,luau}/`，每指令一文件/固定模板；旧兼容指令留根目录，不大幅移动项目结构。
11. 只生成当前 chunk 需要的 handler。
12. 默认 v2 每指令严格 4 bytes、32-byte Header；不保存 native word/AUX。v1 的 6-byte 格式仅用于显式兼容后端。
13. 数字随机表示必须 target-aware。
14. 未来使用原生 `load`/`loadstring` 时，每次调用都必须重新验证，不能只在初始化时检查一次。
15. 本阶段明确不做复杂加密、压缩、随机 section；未来数据隐藏必须等用户推进后另行设计和测试。
16. Anti/anti-debug/anti-dump 暂不实现；`src/anti/` 必须保持只有空占位，等待用户给出具体实现。
17. 生成后的错误检查可以保留，但不要恢复冗长、固定、易识别的错误消息格式。
18. 所有代码生成结束后才统一随机一/两字母局部名；显式 seed 可复现，默认新 seed；不可为追求“全部”而改动 API、反射敏感名称或字符串，也不能跳过最终绑定图验证。

## 6. 常见陷阱

### 6.1 Luau contextual keyword

`type`、`export`、`const` 在特定位置有声明意义，但仍可能作为普通 identifier/API 使用。例如内建 `type(...)` 必须能通过。修改 lexer/parser 时不要简单把所有 contextual word 都变成永久 reserved keyword。

### 6.2 Luau interpolation

插值中可能嵌套 table brace、quoted/long string、comment 和另一个 interpolated string。不要退回“扫描到下一个反引号/右大括号”的简单逻辑；现有实现带 nesting limit，并对 fragment 重新词法化/解析。

### 6.3 Lua 5.1 与 Luau 数字不同

- Lua 5.1 不支持 `0b...` 和数字下划线；
- Luau 允许非常宽松位置的 `_`，还支持 integer suffix；
- Lua 5.1 reference lexer 会先扫描宽泛的 number-like token，因此 `1..2` 是 malformed number，不应自动当成 `1 .. 2`；
- 输出阶段的随机数字 spelling 也必须遵循目标语法。

### 6.4 旧 native backend 的 AUX/data word 与跳转

Luau 很多 opcode 后跟 AUX word；Lua 5.1 `SETLIST C==0` 也有 trailing data word。修改 instruction walk、jump target 或 lowering 时，必须区分真正 instruction 和 data/AUX word。

### 6.5 调用/返回/闭包语义

VM 最容易回归的区域：

- 多返回值；
- vararg；
- tailcall/call frame；
- closure/capture/upvalue close；
- method/namecall；
- FASTCALL/FASTPCALL fallback；
- generic/numeric loop；
- 比较跳转和元方法求值顺序。

不要只做“能编译”的测试，必须比较原始与 VM 的实际输出。

### 6.6 临时调试代码

不要恢复硬编码 `/tmp` 的 VM source dump hook，也不要提交临时大文件、完整 Rust toolchain、build/target/node_modules 等产物。

### 6.7 Luau runner

上游 Luau CLI 没有标准 `--version`；仓库自定义 runner 使用 `--obf-runtime-version`。runner 环境由固定 `Repl.cpp` 设置并包含 `loadstring`、filesystem `require` 和 sandbox。

### 6.8 名称复用不能靠“逐块清空名字池”

必须使用实际声明生效顺序及跨域干涉，不能只比较源 span 或只看当前拼写可见的绑定。参数/body 和 for 变量/body 共用唯一性组，但不要合并它们的 lexical scope ID。最终绑定图校验不足以发现 unused-local 撞名，必须保留独立同域唯一性验证。匹配修复只能在同域、外部颜色固定的条件下进行，不能无条件交换两个不同作用域的颜色。

### 6.9 最终命名与测试不能依赖旧拼写

不能分段提前改名再追加裸 handler；只能在完整生成后一次 finalizer。不要硬编码 blob 的 `B` 或 prototype 的 `P`：全都会换名。宽泛的 `变量[数字]={...}` 正则还会误拒 Luau 合法 NAMECALL 包装表；使用现有 AST/字节检查，区分函数包装与 inline metadata。新增模板引入反射会被私有审计拒绝，不要扩大为通用“忽略所有 barrier”。

### 6.10 VM 私有字段压缩的维护约束

`src/vm/fields.rs` 是自定义 VM 专用，不是公开 minify 开关。14 个 prototype 字段使用 `__obf_proto_` identifier marker，由 `custom::finalize` 在全部 decoder/校验/runtime/handler/NAMECALL/尾部生成后做 token/span 双射替换，再调用现有 `minify::finalize_vm`。字段名随机流与 local 随机流分离；当前 14 字段全单字母，池支持双字母并排除关键字/同表重名/原名，超限拒绝。

所有已授权字段的 constructor/alias/read/write 必须同步标记；新增字段要加入 schema，不得用字符串全局替换或猜测用户 `.field`。只支持私有点字段与构造器 key；未知/误用 marker 拒绝。当前内部模板不使用 Luau 插值，若引入需先补全 fragment 处理，不能静默漏改；这不限制用户源码的插值编译。

所有字符串（包括 bytecode）、公共 `n`、标准库/元方法、模块导出和用户 NAMECALL 必须保持，即便方法名恰好是 `__obf_proto_code` 也不能动。字段后再次 lex 证明其他 token 未改；随后 local/作用域/分号验证照旧。自定义 VM 的 fetch-loop/畸形 decoder 测试插桩后须走 `custom::finalize`，不能绕过字段阶段。旧 native backend 的原短字段及其 finalizer 路径不改。

## 7. 关键文件导航

| 路径 | 作用 |
|---|---|
| `Cargo.toml` | Rust 2021、MSRV 1.88、零依赖 |
| `src/lib.rs` | 公共 parse/check/minify/bytecode/VM 入口 |
| `src/ast.rs` | owned、span-preserving AST |
| `src/lexer.rs` | 双目标 lexer、插值 fragment、安全门限 |
| `src/parser.rs` | AST 构造、目标规则、诊断、资源限制 |
| `src/scope.rs` | 作用域、绑定/引用、声明生效顺序、upvalue、最终图验证和私有 VM 环境审计 |
| `src/scope/rename.rs` | 同域唯一性、跨域稀疏干涉、seeded 着色、匹配修复及防撞名验证 |
| `src/minify.rs` | 公共/私有 finalizer、图校验、递归插值单行 emitter |
| `src/random.rs` | 共享 deterministic PRNG 与 std-only fresh seed |
| `src/diagnostic.rs` | offset/line/column 用户诊断 |
| `src/bytecode/lua51.rs` | Lua 5.1 binary chunk parser |
| `src/bytecode/luau.rs` | Luau bytecode parser |
| `src/vm/compiler.rs` | 仅显式 native 后端的固定 compiler 调用/查找 |
| `src/vm/binary.rs` | 旧 OBF v1 writer（非默认 v2） |
| `src/vm/lua51.rs` | 旧 Lua 5.1 native lowering/generator |
| `src/vm/luau.rs` | 旧 Luau native lowering/generator |
| `src/vm/opcode/` | 根目录旧 38+91；子目录新 Lua51 46 / Luau 49 个独立 handler |
| `tests/parser_audit.rs` | 固定编译器语法对照、运算符组合、const 写入/方法类型 AST 与 parser/compiler 边界 |
| `tests/support/mod.rs` | 原生编译/执行与临时目录测试工具 |
| `tests/scope.rs` | 绑定图、名称策略、链深度安全回归 |
| `tests/random_names.rs` | seed/短名/同域唯一性/耗尽/末位冲突与反射策略回归 |
| `tests/scope_reuse.rs` | 作用域复用、逃逸/传递闭包、类型/插值/循环、10,000 块和生成式原生差分 |
| `tests/safe_minify.rs` | 双目标运行差分、类型模块导入、CLI 回归 |
| `tests/fixtures/scope_{lua51,luau}.lua` | 安全压缩语义语料 |
| `tests/fixtures/reflection_{lua51,luau}.lua` | 自动保留名称/词法 opt-out 语料 |
| `tests/fixtures/ast_lua51.lua` | Lua 5.1 focused AST corpus |
| `tests/fixtures/ast_luau.lua` | Luau focused AST corpus |
| `tests/fixtures/vm_lua51.lua` | Lua 5.1 38/38 VM coverage corpus |
| `tests/fixtures/vm_luau.lua` | Luau 63/91 core coverage corpus |
| `tools/test-matrix.sh` | 不可跳过的双目标验收门禁 |
| `tools/bootstrap-rust.sh` | 固定 Rust/Cargo 1.88 安装 |
| `tools/build-reference-tools.sh` | 重建 Lua/Luau reference tools |
| `README.md` | 用户使用说明与当前能力 |
| `总路线.md` | 必须持续更新的总体路线/日志 |
| `vm_lua51.out.lua` | 已发布 Lua 5.1 生成输出 |
| `vm_luau.out.lua` | 已发布 Luau 生成输出 |
| `src/anti/.gitkeep` | 按要求保持空白 |

Luau grammar/AST 对照：

- `vendor/luau-0.735/Ast/src/Parser.cpp`；
- `vendor/luau-0.735/Ast/include/Luau/Ast.h`。

新增核心文件：`src/ir.rs` / `src/ir/lower.rs`（typed IR/AST lowering）、`src/bytecode/custom.rs`（v2 ISA/codec/校验）、`src/vm/custom.rs`（4-byte fetch/runtime/完整生成后命名）、`src/vm/opcode/{lua51,luau}/`（新 handlers）、`tests/custom_{vm,bytecode,cli}.rs` 与 [`自定义字节码.md`](自定义字节码.md)。

运行语义增量新增 `tests/vm_parity.rs`、`tests/fixtures/parity_{common,luau}.lua` 与 [`虚拟机兼容性.md`](虚拟机兼容性.md)；矩阵增加应用语料的原生/debug/release 对照。

## 8. 里程碑状态

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 可复现环境/门禁 | 完成 | 固定工具、runner、矩阵 |
| M1 source frontend/minify | 第一阶段完成 | owned AST、span、Luau 类型/插值、安全限制完成；已接入 M3 |
| M2 bytecode frontend | 新 v2 已交付 | 原生 reader 保留；独立 IR/v2 reader、operand/index/jump/捕获校验 |
| M3 scope/safe minify | 含随机命名/复用增量 | local/upvalue、seeded 一/两字母、跨域干涉/同域唯一性、反射退回与最终验证 |
| M4 private bytecode | v2 可执行 | AST→typed IR→4-byte ISA，32-byte Header，reader/round-trip；不做加密 |
| M5 register VM | v2 默认 | 46/49 handler 实际执行覆盖、最终随机短名；旧 native 随机布局显式保留 |
| M6 staged data hiding | 按最新要求暂缓 | v2 明文固定布局 + checksum，无复杂加密/压缩 |
| M7 advanced randomization | 暂缓 | 少量变体只在旧 native 后端；新 v2 不随机化布局 |
| Anti | 暂停 | 等用户提供实现 |

## 9. 推荐的后续优先级

### 优先级 A：巩固 M3 安全压缩

第一阶段已交付。继续扩展语义语料、宿主能力边界与性能测试，不要重新实现按文本替换：

1. 持续覆盖 local initializer、shadowing、捕获写入、repeat、隐式 self/arg 与 Luau signature/type/interpolation；
2. 一/两字母名字已支持跨域复用，同域唯一、所有可改名绑定都换名；继续扩展复杂干涉/压力语料，不弱化 global/outer local/upvalue 约束和最终防撞名验证；
3. type function 内名称和 export API 继续保守保留；未知宿主反射必须可显式 `--no-rename`；
4. 常量折叠/DCE 尚未启动，不能把普通语法树构造当作已证明无副作用；
5. 每次更改重跑原始/词法/安全压缩的双目标运行差分及整个 VM 门禁。

### 优先级 B：巩固新 IR/VM，而不是重新实现

1. 继续扩展 CFG/寄存器资源压力、求值时机、nil/变参、元方法及双目标差分；
2. 在已交付的纯条件/只读标量可达性基础上扩展 repeat/continue 数据流；活跃路径可能跨初始化的 until 引用仍保守拒绝；
3. 衡量全部 locals 使用 cell、临时 pack 的分配成本；优化须守住逃逸闭包、GC 和求值顺序；
4. 坚持 `compile → inspect → wrap` 与默认 virtualize 的等价、无 compiler 生成、debug/release 确定性和逐 opcode executed coverage。

### 优先级 C：宿主语义边界与安全输入

1. 不宣称 debug 栈/环境反射可完全模拟；明显反射入口拒绝，动态别名不承诺识别；
2. 当前 generalized iterator 依赖可见元表。被隐藏的元表/任意宿主 userdata 不属于完整等价保证，详见协议限制；
3. 扩展 format mutation/计数/字符串/跳转/捕获测试；结构校验不是运行不可信 bytecode 的沙箱；
4. 未做 Roblox executor 实机验证，不把固定 Luau runner 结果当成实机覆盖。

### 暂缓：加密、压缩、随机 section、多模板和 Anti

本次用户明确要求先完成可执行 AST/IR/4-byte register VM。上述复杂变换不应抢在正确性、资源和接口测试之前；Anti 仍等待用户实现。

## 10. 新接手者的第一组命令

```bash
cd /home/user/OBF

git status --short
git branch --show-current
git log --oneline -8

./tools/bootstrap-rust.sh        # .toolchains 不存在时运行
./tools/test-matrix.sh

gh pr list --repo dream6-e/OBF --head arena/01a074dc-obf
```

在当前 Arena 工作会话中，必须继续使用 `arena/01a074dc-obf`，每个通过完整测试的修改批次只向该分支 push。旧 PR #2 属于 `arena/01a071aa-obf`；不切换或推送到旧分支，不把本分支修改误称为已更新 PR #2。

开始新功能前先阅读：

1. `README.md`；
2. `总路线.md`；
3. 本文档；
4. 对应模块和现有 fixture；
5. Luau 语法修改时同时对照固定 0.735 `Parser.cpp`。

每轮完成标准：

```text
实现 -> cargo fmt -> focused tests -> tools/test-matrix.sh PASS
     -> 更新 README/总路线/交接文档（如状态变化）
     -> git diff --check（排除 vendor）
     -> commit -> git push origin arena/01a074dc-obf -> 核对远端 HEAD
```


## 2026-09-06：AST → IR → OBF v2 核心批次

新增独立 typed register IR、32-byte Header / 4-byte 指令的 OBF v2 codec 与可执行双目标 VM。新路径不调用外部 compiler；49 个稳定 opcode 中 Lua51 支持 46 个、Luau 支持全部 49 个，fetch-loop 实际执行覆盖已建立。旧原生路径暂留 CLI 默认，下一批接入默认命令和示例。新增目录只涉及 `src/ir/` 与现有 opcode 下两个目标目录，不移动旧实现。

完整格式、资源门限、源码/反射/受保护 `__iter` 等边界和 API 见 [`自定义字节码.md`](自定义字节码.md)。按最新要求，暂不做复杂加密、压缩、随机 section 或随机 opcode；此前路线中的相关随机化项只描述旧 backend，不能当作新 v2 的实现要求。

核心批次最终门禁：`./tools/test-matrix.sh` **PASS 110**（32 单元 + 78 集成）；其中新增 26 项 AST/IR/v2 VM 回归，含两端逐 opcode 实际执行覆盖。debug/release 与原有门禁均通过。


## 2026-09-06：默认后端与独立 bytecode CLI 接入批次

在已推送核心提交 `e5447e8`（PASS110）上，将 `virtualize` / 公共 API 默认切换到 AST/v2；旧路径显式 `--backend native`。新增 `dump-ir`、`compile`、`wrap-bytecode`，inspect 自动识别 v2。新接入 4 项 CLI 测试，完整门禁 **PASS114**；矩阵额外验证无外部 compiler、独立 wrap、debug/release binary/VM、两后端多 seed 与两份新 golden。示例更新为 32,524 / 33,980 B；新 bytecode 为 6,879 / 8,489 B。此前日志中的“旧路径默认/下一批接入”仅描述核心提交当时的状态。


## 2026-09-06：源码分号优先分隔增量

在 `6c54026` 上按用户要求将语句分隔统一为 `;`，不盲目替换空格。核心修改限于 `parser.rs` 的私有可选边界表与 `minify.rs` 的共享 emitter；不修改公开 AST/语法、命名策略或 IR/bytecode/VM 指令。已有精确文本测试更新分隔符预期，新增 3 个内部测试和 `tests/semicolons.rs` 的 7 个原生差分/入口测试。

完整矩阵 **PASS124**（35 单元 + 89 集成）：Lua 5.1/Luau 语法与运行、所有 seed/绑定/类型/插值、两套 VM、46/49 fetch-loop 覆盖及 debug/release/golden 均通过。示例更新为 **32,568 / 34,033 B**（较前版 +44/+53 B）；逐 token 对比除分号与间隔外完全一致，包含原 seed 名称与所有字面量。

两份内嵌及独立 `.obf` 修改前后均逐字节相同：Lua51 **6,879 B**，SHA256 `21489c95d5d7f9aa2110c005febbf74e90cc8d9305e39109571f921365b30db9`；Luau **8,489 B**，SHA256 `1046275a6bbe4bdfefe3424aebb7b54823d5e8c80b607142511f81c5ada590ef`。没有新增加密、打包、随机布局或依赖。前面的 PASS110/PASS114 与旧体积记录是相应历史批次的结果。

## 2026-09-06：自定义 VM 运行语义兼容性增量

基线 `0d2d40e` PASS124。完成 raw `__iter`/callable 迭代器、Luau 闭包共享/递归身份、死 continue 误拒绝、实际 upvalue 裁剪/重编号及只读标量传播。新 ISA 修订 2 以受验证的 flag/kind 明确共享语义，修订 1 保留完整读写/执行兼容；32-byte Header、4-byte 指令和 46/49 stable opcode 保持。

完整矩阵 **PASS140**（36 单元 + 104 集成）：13 项 `vm_parity` 中含 145 个生成式组合、多 seed、两份应用语料、类型/元方法/闭包/模块/1000 项 pack；新增 2 codec + 1 独立 decoder；原有全部回归、debug/release、原生输出和 golden 一致性仍通过。真实旧修订 1 fixtures 另手动 wrap/运行成功。

golden 更新为 **32,996 / 33,741 B**（7001/7351）。两端 bytecode 为 **6,879 / 8,257 B**，SHA256 分别 `acb33193bb00e6cdadd1bcf2d9108de6d6dbc3e63de83d1bf2346153edf8e963` / `a7ca14eff736e33a480055aa579a7f56a05f15bbef66ae3ce3b29984cb1a0a61`；均为 14 prototypes，1436/1816 instructions。本轮编译产物有意变化，前面“字节码不变”只描述历史分号批次。

新增 [`虚拟机兼容性.md`](虚拟机兼容性.md) 明确“受支持源码”和逐字节输出验收：GC/弱引用时点、含洞表长度、布局敏感遍历、反射/位置、隐藏元表及未模拟的原生优化仍不能宣称完全一致。不能把 PASS140 或 46/49 指令覆盖当作任意输入等价证明，也不能编造普遍兼容率。


## 2026-09-06：VM 私有字段一/两字母压缩

基线 `8efa9b6`（PASS140）。`code/tags/parent/flags/shared/self/cached` 等 14 个私有 prototype 字段统一压缩为随机单字母，allocator 支持双字母；闭合 schema 的模板标记只在完整生成后的私有阶段替换，不更改通用 minify 用户字段策略。新增 `src/vm/fields.rs`、`tests/private_fields.rs`；opcode 变更只涉及相应私有字段访问，ISA/IR/数据字节未改。

完整门禁 **PASS149**（40 单元 + 109 集成）。新增 4 内部 + 5 集成，包括字段双射/短名/关键字/门限、全部未标记 token 复核、原 local 名称保持、用户字段和导出、host/metamethod、marker-like NAMECALL、CLI/无 compiler/修订 1/多 seed。此前语义差分、debug/release、两个后端、46/49 fetch-loop 执行和 golden 均通过。

新 golden **32,832 / 33,582 B**（seed 7001/7351），分别减少 **164 / 159 B**；只变动 170/176 个字段 token，其他所有 token、原 seed 的 local 和字面量不变。内嵌及独立 bytecode 仍 **6,879 / 8,257 B**，SHA256 仍为 `acb33193bb00e6cdadd1bcf2d9108de6d6dbc3e63de83d1bf2346153edf8e963` / `a7ca14eff736e33a480055aa579a7f56a05f15bbef66ae3ce3b29984cb1a0a61`。本轮只是受验证的源码私有字段压缩，不是新加密/随机 section，也未扩大语义等价承诺。
