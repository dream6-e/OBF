# Luraph v15 混淆技术全清单
### —— 45 项技术逐条拆解 + 最小实现例子，供其它混淆器改进参考

样本：`Luraph15💀💀.lua`（171,753 B，3 行）。本文所有"样本实录"均为**逐字摘录**（含真实常数），
所有结论来自纯静态分析（未执行样本）。标注方式：**[实测]** = 从代码直接读出；**[推断]** = 由结构/统计反推。
分析过程与证据链见同目录 `REPORT.md`；工具 `tools/an.py`（结构解析）、`tools/ops.py`（dispatch 树重建）、
`tools/lph_codec.py`（容器解码）可复跑。

---

## 0. 先度量：这套方案到底"重"在哪

| 指标 | 实测值 | 说明 |
|---|---|---|
| 顶层字段数 | 227 = **145 函数 + 82 值 + 0 位置参数** | 每个函数=一个扁平化基本块/工具函数 |
| 标识符长度 | 1 字符 49 个，2 字符 96 个 | 高密度随机名 |
| 状态号集合 | **136 个不同值，范围 1..236** | 344 个 `return <state>,…` 站点 |
| 主解释器 | `b[18]`：32,695 B，503 次内部 handler 调用，377 个 if | 单函数就占全文 19% |
| 次核心 | `b[73]`：3,366 B，5 个 callee | 调用桥 + 惰性操作数解码 |
| dispatch 树数 | **4 棵**（按帧字段 `u[7]` 分派：`S==251/99/8/140`） | 多态解释器 |
| 比较热点 | 状态变量 269 个阈值；opcode 变量 38 个阈值 | 后者 → 约 250 个操作码规模 **[推断]** |
| 运行期 import | 68 个数字槽 + 14 个字符串常量 | 全脚本可见 API 的**全集** |
| 载荷 | `RC` 74,566 字符 → 去壳 74,690 → 读窗 74,685 = **5×14,937** → 14,937×u32 = 59,748 B | 熵 7.9958 bit/B |
| Luau 特征 | `+=`25 `-=`36 `*=`8 `/=`10 `^=`7、`continue`45、if-表达式 17、**goto 0 / 标签 0** | 目标语言被钉死为 Roblox Luau |
| 大整数面量 | **612 个，229 个不同值**（top：4294967295×111、2097152×68、2147483648×51） | 算术混淆密度 |

> 给设计者的第一个结论（**别乱报膨胀率**）：文件体 171,676 B；其中载荷字符串 74,566 字符，解码得
> 59,748 B 字流（**仍是加密态，不是明文**）；145 个 handler 源码合计 95,373 B = BODY 的 **55.6%**，
> 剩下 44.4% 几乎全是那条载荷 + 噪声 handler。因为明文大小未知，"膨胀 3x"这类说法我无法证实 ——
> 但"CFF 本身吃掉一半体积"是确证的：**如果只做算术混淆而不做 CFF，体积成本可以砍掉一半以上**。

---

## 1. 家族 A —— 程序形态 / 打包层

### A1 · 单表达式打包 + 空 metatable 装饰 **[实测]**
```lua
-- 样本首尾逐字
return setmetatable({jC=function(b,u,z,Z,o,w,K,G,q,M,F,H,E,V,f)if z<=206 then ...
                  ...,[51]=buffer.create},{}):FC()(...);
```
整个程序是**一个表达式**：构造表 → `setmetatable(t, {})`（第二参数是**空表**，没有任何元方法）→
用 `:FC()` 把表自身当 self 调用 → 返回值再 `(…)` 调用一次。

* 意图：① 无 `local`/无顶层语句 ⇒ `load` 后的 chunk 体只有一条 `return`，脚本级 hook 点消失；
  ② `self` = 全局 handler 表，所有符号解析变成 `b[X]`；③ 空 metatable 让工具"看到 setmetatable 就以为有沙箱"。
* 最小例子：
```lua
return setmetatable({
  A=function(S) return S.f(S, 0) end,
  f=function(S,st) if st==0 then return 1 else return nil end end,
},{},nil) -- 元表装饰
```
* 泄漏特征：`setmetatable(...,{})` 的第二个参数是空表 → **元表是纯噪声**。别写装饰性元表；
  若要保留，就真放一个 `__newindex` 来拦截写入（当前版本可被静态确认"无人读取元表"）。

### A2 · 全局符号数字槽化（import 表）**[实测]**
```lua
[66]=buffer.readu8,[47]=buffer.writeu8,[8]=bit32.bxor,[20]=bit32.band,[99]=bit32.lshift,
[10]=bit32.rshift,[122]=bit32.bor,[71]=bit32.bnot,[57]=buffer.writeu32,[51]=buffer.create,
[107]=buffer.fromstring,[93]=string.sub,[118]=string.gsub,[22]=table.create,[13]=table.pack,
[102]=table.move,[15]=next,[34]=getfenv,[25]=setfenv,[81]=pcall,[16]=error,[38]=coroutine.yield,
[84]=type,[58]=typeof,[17]=Vector3.new,[60]=Vector2.new,[126]=vector.create, ... (68 项)
```
所有环境读被替换成"对 self 表的整数下标读"。
* 意图：消除 `buffer`/`bit32` 这类**语义关键词**，让 grep/关键字规则/导入分析失效。
* 最小例子：`local T={ [7]=string.char }; return setmetatable(T,{}):boot(T)` + 发射时把 `string.char` 换成 `b[7]`。
* 泄漏特征：**下标分布是"少数键高频 + 大量键低频"**，且整表可以在一个构造式里被完整枚举。
  我一次扫描就把 68 个槽全部还原成语义名。改进方向：**把 import 表本身放进载荷**（运行时逐条写入 `b[N]`），
  或对同一 API 发射多个槽（`66` 和 `200` 都是 readu8），使"槽↔API"多对一，静态映射不再是双射。

### A3 · 单行无格式输出 **[实测]**
第 1 行是 banner，第 2 行空，第 3 行 171,676 字符。无换行、无缩进。
* 意图：干掉所有基于行的工具（diff、按行断点、`head/tail` 抽样、编辑器高亮、以及"数行数估计规模"）。
* 泄漏特征：无。这一条**几乎零成本**，值得照抄。唯一副作用是浏览器 devtools 卡死——而那正是它想要的。

### A4 · 遮蔽形参 + 高熵短名 **[实测]**
```lua
[73]=function(b,u,z,z,z,z,z) ...      -- 7 个形参里 5 个同名
[18]=function(b,u,z,Z,Z,Z,Z) ...
-- 局部别名串：local B,j,d,Y,_=B+j,j<=0,...   (字符集 b,u,z,Z,o,w,K,G,q,M,F,H,E,V,f,B,j,d,Y,_,g,l,k,m,x,R,t,J,S,O,y,A,a,X,N)
```
* 意图：① `function(b,u,z,z,z,z,z)` 让"参数个数=arity"的推断失效，也让朴素 re-formatter 生成非法代码；
  ② 单字符名让任何命名统计/模式学习无信号。
* 泄漏特征：重复形参在合法 Lua 里罕见 → **本身就是"这是被加固函数"的标记**（我就是靠它一眼挑出 `b[73]`/`b[18]`
  是核心）。若要更强：把遮蔽参数**真的用起来**（当作 scratch 寄存器），而不是让它恒为 nil。

### A5 · self 表三重身份（handler + 常量池 + 运行期可写内存）**[实测]**
同一个 `b` 既是函数表、又是常量表（`b.RC`/`b.pC`/`b.h`）、又被运行时写入（`b[94]=o`、`b.k=z`、`b[2]=nil`）。
* 意图：省一个 upvalue 层（Luau 里表索引比 upvalue 慢，但比 `_ENV` 快），同时让"表内容"在运行期可变 →
  静态求值不可能定值。
```lua
eL=function(b,...) ... b.k=z; local z=b:L(b.k,b[94]); return 2,7,w,b[z[4]](b,z,b.s,nil),... end
```
* 泄漏特征：写入点极少（全文只有 `b[94]`、`b[2]`、`b.k` 三处）。**改进：把运行期写入分散到 20+ 个槽**，
  并对同一槽多次改写（我现在能穷举写入点，正因为太少）。

### A6 · 厂商 banner + 载荷 magic + 垃圾头 **[实测]**
```lua
-- This file was protected using Luraph Obfuscator v15.0 [https://lura.ph/]
RC=[=[LPH:9u2u6@...(74,566 字符)...]=]
```
且解码从 `string.sub(RC,5)` 开始（跳过 `LPH:`），而**读流又从第 5 个字符开始**（再跳 4 字符）。
* 意图：签名/水印 + 让"直接复制载荷"的工具少拿 4 个字节（长度就不被 5 整除，解码直接错位）。
  我第一版解码算出 14,938 组（错），补上读窗后才得到整除的 14,937 —— 这条**确实让我错过一次**。
* 建议保留，且加强：把"头长度"本身做成 per-build 随机（当前是固定 4）。

---

### A7 · 全局→局部一次性别名化（每个巨型函数开场一条"反解串"）**[实测]**
```lua
-- b[18] 开场，逐字：
local Z,o,w,K,G,q,M,F,H,E,V,f,B,j,d,Y,_,g,l,k,m,x,R,t,J,S,O,y,A,a,X,N =
  b[22],b[34],b[102],b[15],b[13],b[53],b[99],b[20],b[10],b[8],b[71],b.AL,b[25],b[114],
  b[66],b[47],b[122],b[31],b.UL,0
-- b[73] 开场：local z,Z,o,w,K,G,q,M,F,H,E,V,f,B,j,d,Y,_,g,l = b[22],b[34],b[81],b[99],... (20 项)
```
之后函数体内 **`b[66]` 只出现 1 次**（就是这条别名串本身），其余 858 处 `[…[Q]]` 访问全部走局部量。
实测：`b[66]/b[47]/b[8]/b[20]/b[10]/b[99]` 在 `b[18]` 内各只出现 **1** 次，在全文分别出现
257/91/93/4/4/3 次 —— 也就是说**"按槽号统计热度"这种找核心函数的方法在单函数尺度完全失效**。
* 意图：① Luau 里局部变量访问远快于表索引（这是 VM 的性能必需）；② 把 `b[N]` 语义噪声换成随机字母，
  使"这个函数在调 bit32.bxor"变成"这个函数在调 `E`"；③ 名字复用（同一字母在不同块含义不同）。
* 我的破法：**开场那条"纯 `b[数字]` 列表"就是最好的解析锚点**——它右边只有下标访问，没有别的表达式，
  一个正则就能把 20~30 个字母全部绑定回语义（我把它写成了自动 `rd()`，之后每个 handler 都直接可读）。
* 改进建议：① 别名串里混入非平凡表达式（`b[8]` 与 `b[20](b[8],…)` 交替、把两个槽合成一个 `local bxor=…`
  后再 `local E=bxor`），让"右边全是下标"这个模式不成立；② 分两条赋值、中间插常量；
  ③ 让**同一函数内多次重新别名**（局部遮蔽），破坏"一条串=全表映射"。

---

## 2. 家族 B —— 控制流层

### B1 · 完全控制流扁平化（CFF），块=表字段，寄存器轮转 **[实测]**
```lua
SL=function(b,u,z,Z,o,w,K,G,q) if w<=30 then
    if w<=29 then local M=b[66](z,u+1); return not(M>=128)and 28 or 19,u,M,G,q;
    else Z[G]=q; return 33,u,Z,G,q; end
  elseif w<=31 then local b=q-128; local z=o*128+b; return 30,u+2,Z,G,z;
  elseif w<=32 then local b,z=G-128,(q-128)*128; local w=o*16384+z+b; return 3,3+u,Z,w,q;
  else ... end end
```
每块返回 `(下一状态, 轮转后的寄存器元组)`；调用方 `V,b:xxx(...)` 接收。**没有一条 `goto`（0 次）、
没有标签**，全部靠"表字段名 + 状态整数"完成转移。
* 意图：把 CFG 变成一张 136 状态的表；反编译器输出"一坨 while+if"，不可读、不可再优化。
* 最小例子：
```lua
local T={} 
T[0]=function(S,a) print"do0" return 3,a end
T[3]=function(S,a) if a>10 then return 1,a-1 else return 0,a end end
T[1]=function(S,a) return nil end
local s,a=0,100 while s do s,a=T[s](T,a) end
```
* 泄漏特征：**"每个块都以 `return <小整数>, <同一批局部名的排列>` 结束"**是极强的结构指纹。
  我据此立刻确认 CFF；且状态数的取值域（1..236）暴露了状态机规模。改进建议：
  ① 状态号用 32 位随机值 + 每跳转前 `state ^= K[i]` 解算（当前是明文小整数，可直接枚举全部节点）；
  ② 让部分块**不返回状态**而是尾调用另一个块（引入真正的间接跳转）；
  ③ 混合两种分派：一半状态走表、一半走 `if` 链。

### B2 · 数值比较链代替跳表（不用 `[T[state]]`）**[实测]**
上面 `if w<=30 / elseif w<=31 / elseif w<=32` —— 状态分派本身也是**二叉比较树**。
* 意图：避免 `T[state]` 这种"一搜就能找到分派器"的模式；比较链对模式匹配不友好。
* 代价：每跳转多 2–5 次比较。
* 泄漏特征：`<=` 与**连续整数**阈值成簇（28/29/30/31/32）→ 阈值序列本身就是状态表的键。
  改进：把阈值也做仿射变换 `if w<=K+30`，让阈值不再是小整数密集区（现在我用"阈值的数值分布"就估出了状态图规模）。

### B3 · `while true do … continue … end` 状态循环，禁 goto **[实测：45 处 continue]**
```lua
while true do
  if E<=3 then if E<=1 then ... else E,j=0,K; end
  elseif E<=5 then ... else E=q(V)==M and 6 or 7; end
  else w(V,1); E=2; end
end
```
* 意图：只用一种循环骨架 ⇒ 全文**控制流形态高度一致**，让"找异常结构"的工具没有锚点；
  且 `continue` 是 Luau 独有，直接把 Lua 5.x parser 挡在门外。
* 建议：这是低成本高收益项，照抄。附带好处：任何依赖 luaparse/Lua 5.1 语法的去混淆器都要先实现 Luau 方言。

### B4 · 多态 dispatch（同一 VM 四棵树）**[实测]**
```lua
if S==251 then while true do local O=J[Q]; ... end
elseif S==99 then while true do local q=J[Q]; ... end      -- 另一套 opcode 空间
elseif S==8  then ...
elseif S==140 then ...
```
`S = u[7]`（帧字段）决定用哪棵分发树；`[18]` 的 38 个 `O` 阈值只覆盖第一棵树。
* 意图：**同一个语义在不同模式下用不同 opcode 编号 + 不同 handler**，把"逆向出一个表就通吃"变成
  "要逆向 4 个表"。这是本样本里我认为设计最好的一条。
* 建议：再加"per-function 模式"（同一脚本里不同函数用不同树）与"运行时按帧切换"（当前是 `u[7]`，可改由载荷给出）。

### B5 · 分支无关条件跳转（cmov 化）**[实测，17 处]**
```lua
Q=if n[y[Q]]then R[Q]else m[Q];end
Q=if n[y[Q]]<=R[Q]then m[Q]else Q;end
Q=if R[Q]<n[J[Q]]then m[Q]else Q;end
```
用 Luau 的 **if-表达式**把"测试+跳转"折成一次赋值。
* 意图：① 消灭 `if … then … else … end` 块 ⇒ 基于块边界切分 CFG 的工具全部失效；
  ② 语义上是"无分支跳转"，指令计数/基本块划分都被打乱。
* 副作用（真实代价）：这类语法**任何标准 Lua parser 都过不了**，我的 luaparse 路线就是在这 17 处炸掉的。
  如果你的目标包含"让工具链报错"，这条性价比极高。
* 泄漏特征：`X=if` 前缀极稀疏（17 次），一眼可枚举 ⇒ **想更强就把同样的思想用 `and/or` 表达**
  （`Q = n[y[Q]] and R[Q] or m[Q]`，对不真值敏感的场景不适用，但可用来处理非布尔情形）。

### B6 · 自修改 dispatch / 惰性操作数回写 **[实测]**
```lua
J[Q]=12;                                                   -- 执行完把自己改成 handler 12
R[Q],y[Q],m[Q],J[Q]=197,77,228,12;                         -- 四通道一起改写
J[x],R[x],t[x],m[x]=231,200,146,2;                         -- 段解密完成后改写
x-=1;                                                      -- PC 后退 1 → 重新执行被改写的槽
```
* 意图：① 线程化 dispatch（省一次取指）；② **操作数"第一次用到才解码"，解完写回原槽**，
  所以静态看到的字节码永远是"未解码态"，且每个槽的值取决于执行顺序；③ `x-=1` 让 PC 不再单调，
  线性扫描反汇编直接失败。
* 建议：这条**值得抄**。加强版：把回写做成"部分回写"（只解 A 通道，B/C 到别处解）+
  定期**反向擦除**（用完后重新加密回去），这样 dump 内存也拿不到完整明文指令。

### B7 · 循环回边的"三元件"退化写法 **[实测]**
```lua
-- HL 的 for 回边（原语句 for pos=5,#u-1,5 do … end）
local K,G,q=o[2],o[3],o[4]; local M,F=K+G,G<=0;
local G,H,E=not F,M>=q,M<=q; K=F and H or G and E; o[2]=M;
if K then return 1,... else return 6,... end
```
`step<=0` 决定用 `>=` 还是 `<=` —— 把"升序/降序 for"统一成一个模板。全文 `IL/SL/jL/nC/n/N/TC/VC` 都是同型。
* 意图：让 `for` 循环在扁平化后仍以固定三件套（累加、符号位、双向区间测试）出现，**不给循环识别工具任何
  "for 语句"的语法锚点**。
* 泄漏特征：**正因为它是模板**，我一次认出就把它当作"这不是 opcode，是回边"的证据 —— 反过来提醒设计者：
  **同一个退化模板被复用了太多次**（≥9 处一模一样）。要么在每个实例里随机插入等价噪声（`+0`、`*1`、
  双条件冗余），要么每循环换一种写法。

---

## 3. 家族 C —— 载荷 / 常量编码层

### C1 · 自定义 ASCII-armour：radix-85，digit=ord−40 **[实测]**
```lua
-- qL 状态0（每 5 字符 → 1 个数）
local K,F,H,E=(G-40)*614125,7225*(z-40),(u-40)*85,o-40; local u=w+H+F+E+K; Z[M]=u
-- eL 状态5（最高位那一项在别处算）： 52200625*(H-40)
```
`614125=85³, 7225=85², 85=85¹, 52200625=85⁴`，`digit = ord(c)-40`，MSB first。字符集恰为 `(`(40)…`|`(124)。
* 意图：① 载荷全部是可打印字符，能塞进长括号字符串、过审、过复制粘贴；② 不用 `string.char`/`base64`
  关键词；③ **radix 85 不是随便选的**：`(2^32)^(1/5)=84.45 ⇒ 85 = 让 5 个字符能覆盖一个 u32 的最小整数进制`。
  相比 base64 的 4字符/3字节（1.33x），这里是 5 字符/4 字节（1.25x），比 Lua 的 `\ddd` 转义（4x）好得多。
* **[实测·强] 一个可以直接复用的"我对不对"判据**：85⁵ = 4,437,053,125，比 2³² 多出 3.3% 冗余，
  所以"均匀随机的字流"必然有 `1-2³²/85⁵ = 3.2023%` 的组 ≥ 2³²。**我实测 489/14,937 = 3.2737%（0.5σ 内）**
  ⇒ 定量证明：(a) 我的分组/边界是对的（错位的话这个比例会偏离）；(b) 解出来的字流**确实是均匀随机 = 白化态**，
  不是"帧读错了"。任何做同类分析的人都该把这个比例当成免费的自检位。
* 泄漏特征（重要教训）：**字母表是连续的 85 个 ASCII**，且权重常数以字面量形式散落在代码里
  （`52200625` 全文只出现 1 次，正好指到解码函数）。改进：
  ① 字母表**非连续/乱序**（对 digit 加一张 per-build 替换表，权重保持不变）；
  ② 位权不要用整常数，改成 `(85^4)` 的运行期计算（`614125` 可写成 `85*85*85` 或 `X^4*Y` 的复合表达式），
  这样"grep 2^32 附近的漂亮常数"这条找解码器的路就断了。**这是我找到解码点最快的单一特征。**

### C2 · 表外字符做单字符快捷转义 `pC` **[实测]**
```lua
XC="[ -'}~]"                    -- 恰好是字母表之外的 10 个字符
pC={ ['"']="=W8+C", ["'"]="*cZ7D", ["%"]="]JEL>", [" "]="{5Cw,", ["$"]="--vv@",
     ["!"]="2[,6K", ["~"]="ix|L9", ["#"]="3;MLo", ["}"]=",;^6{", ["&"]="kTXL7" }
u = string.gsub(string.sub(RC,5), XC, pC)   -- 1 字符 → 5 字符
```
* 意图：两层。① 压缩：把 10 个高频 5 字符组各压成 1 字符（省 ~30% 载荷）；② **反提取**：任何工具
  不先解析这张表就直接解载荷，得到错位数据。标记字符落在字母表外 ⇒ 永不与数据混淆。
* 观察证据：全文只有 32 次使用，且**全部集中在同一窗口**（组 13162..13684），而那段正好是唯一具有
  "64 组 = 256 B 周期重复"的区域。
* 建议：抄。但可以更强：让替换表**依赖索引**（第 k 次出现用不同展开，即多表轮转），
  这样"1 字符 ↔ 固定 5 字符"的双射不成立，静态展开就会失败。

### C3 · 带界读窗口（跳过 IV 头 + 末字符不读）**[实测]**
```lua
local z,Z,K=b[107](u),#u-1,5+0;  return 2,{nil,0-K,K,Z+0,o},0,{},z,w
-- reader 元组 = {nil, -5, 5, #u-1, buf}：起点 -5+5=0，步长 5，上界 #u-1
```
* 意图：让"长度必须被 5 整除"成为校验；复制载荷时少一个字符就整体崩。
* 泄漏特征：窗口边界是从**返回值元组 + 回边模板**读出来的。改进：把上界也做成运行期计算值
  （例如从载荷首 4 字节读出的长度），静态就无法确认窗口在哪结束。

### C4 · base-128 varint + 续字节 +128 偏置 **[实测]**
```lua
if u<128 then y,Q=u,B+1; else S=C(j,B+1);
  if S<128 then y,Q=u-128+S*128,B+2; else local T=C(j,B+2);
    if T<128 then y,Q=u-128+(S-128)*128+T*16384,B+3;
    else local c=C(j,B+3); y,Q=u-128+(S-128)*128+(T-128)*16384+c*2097152,B+4; end end end
```
以及位宽选择器：`H=F<16384 and 7 or(F<2097152 and 14 or 21); E=band(F,(1<<H)-1); V=rshift(F,H)`。
* 意图：变长编码让**每条指令的字节数不定** ⇒ 线性反汇编不能确定下一条边界（必须顺序求值）。
  `+128` 偏置（而不是标准 LEB 的"最高位为续标志"）额外破坏了"每 7 位一段"的位对齐直觉。
* 建议：好设计，保留。加强版：续字节标志位位置随机（有的用 bit0 作标志、有的用 bit7），
  或"标志位数量 = 载荷里再读一个数"。

### C5 · 手写 base-256 拆字节，绕开 `math.floor`/`string.char` **[实测]**
```lua
local M=K%256; local F=(K-M)/256; local H=F%256; F=(F-H)/256; local E=F%256;
return 2,0,o,w,M,E,(F-E)/256,52200625*(H-40)
```
* 意图：整型除法用 `%`+减法实现 ⇒ 不出现 `//`、`math.floor`、`bit32.extract` 这些"位操作工具"关键词，
  同时在 double 域上语义完全正确。
* 建议：低成本，值得抄。（并且注意：它把 radix-85 的最高位权值**放在同一条 return 里当参数传递**，
  等于把常数搬进了寄存器轮转 —— 让"常数→用途"的关联断裂，这招很聪明。）

### C6 · 用原始位模式做 memo 缓存表 **[实测]**
```lua
else local u,o=b[108](q,G),b[66](q,G+4); local b=u+o*4294967296;
     local w=Z[b]; if not not w then return 4,w,z,M; else return 5,u,o,b; end
```
把 5 字符当作 40-bit 整数直接做 key 查缓存。
* 意图：性能（多数组要复用）；**副作用是给分析者一个强标记**：`<数>*4294967296` 与 `Z[<拼接位模式>]`
  几乎明示了"5 字节一组、小端、40 位宽"。
* 我认为这是本样本**最亏的一条**：一个几乎没人会命中的缓存（因为 key 是 40-bit 原始位模式，
  表 `Z` 却按 1..N 顺序写）换走了一个"暴露分组结构"的指纹。改进：删掉，或改成对 key 先做
  `bxor`/取模的哈希（保留缓存效果但不暴露位宽）。

### C7 · 常量分尺寸类 + 惰性解密 + 类型化回读 **[实测，25 个同型 handler]**
```lua
-- CC 状态200（size=16 的常量）
local u,Z,o,w,q,M = K+1,213,225,(z+224)%256, b[51](16), 0
local z=(w*Z+o)%256
b[47](q,M,(b[8](b[66](G,u+M),224,z)))   -- writeu8(dst,0, bxor(readu8(src,pos), 224, k))
M=1; return 81,H,u,224,Z,o,q,M,(z*Z+o)%256,b[47],b[66],u+M
-- 解完按类型读：readu32[108] / readu16[69] / readi16[68] / readf32[125]×3 → vector.create[126]
--               readf64[91] / readstring[42] / Vector2.new[60] / Vector3.new[17]
```
观测到的 buffer 尺寸类：1, 2, 4, 8, 12, 16（+ 动态）；盐值：10,18,36,52,53,54,83,88,155,185,201,224,234。
* 意图：① 常量**不在任何池子里连续存放**，而是按类型走不同 handler ⇒ "找常量表 = 找一个大表"失效；
  ② 静态看不到明文，也看不到 `string.byte` 这类关键词；③ 类型化回读顺带完成反序列化。
* 泄漏特征：`{create→逐字节 bxor→按类型读}` 这条三步流水线的**形状**就是标记。改进：把密钥推进
  与写入交错（现在 `k=(k*213+225)%256` 与 `writeu8` 的次序在 25 个 handler 里完全一致），
  并对不同 handler 使用**不同 PRNG**（LCG/xorshift/查表轮转混用），破坏"同型"。

### C8 · 每对象一条 LCG 密钥流 **[实测]**
```lua
k = (salt + c) % 256 ;  下一轮 k = (k*213 + 225) % 256      -- 213 出现 25 次，225 出现 28 次
```
* 意图：单表小密钥流，零查表成本，且 `%256` 与 Lua number 域天然契合。
* 泄漏特征：**乘数/加数是全局常量**（213/225）。我一次 grep 就确认了密钥流形态。改进（重要）：
  **per-build 随机化乘数/加数**（LCG 只需 a≡5 mod 8、c 奇数即满周期，随机化零成本）。
  这一条改动可以直接让"识别 LCG"失效，是本文 ROI 最高的三条建议之一。

---

## 4. 家族 D —— 算术 / 表达式混淆

### D1 · 模归一化原语 `vL` **[实测]**
```lua
vL=function(b,b)return b%4294967296;end   -- 注意：两个形参同名！
```
全文 239 次调用。
* 意图：把一切值钉在 u32 域，同时给"每个算术结果都套一层"的模板提供最小单元。
* 建议：保留。成本为零。

### D2 · 用位运算拼出来的 32×32→u32 乘法 `iL` **[实测，330 次调用]**
```lua
iL=function(b,u,z)local Z=b[20]; u,z=Z(u,4294967295),Z(z,4294967295);
  local o,w=Z(u,65535),b[10]; local K,G,q=w(u,16),Z(z,65535),w(z,16);
  return Z(o*G+b[99](Z(o*q+K*G,65535),16),4294967295)%4294967296;end
```
即 `(lo_u*lo_z + ((lo_u*hi_z + hi_u*lo_z)&0xFFFF)<<16) mod 2^32`（16-bit 部分积，避开 double 精度）。
* 意图：① 让 `*` 不出现在需要 mod 语义的位置（Luau 的 `*` 在 double 上会丢精度）；
  ② 把"乘一个常数"变成一段 10 行的位运算，掩埋常数。
* 泄漏特征：**它是全文最高频 handler**（330 次），且**定义唯一、只有 200 字节**。
  一旦把它当成"乘法黑盒"，所有含它的表达式都能符号折叠。改进：发射时**多态化乘法**
  （3–4 个不同实现轮流用：部分积版 / `(a*b)%2^32` 版 / `2^32` 减法版），别把唯一实现暴露成"折叠锚点"。

### D3 · opaque predicate：`x + ~x ≡ 2³²−1` **[实测 + 数值验证]**
```lua
m[x]=b:vL(p(b:vL(V),113) + b:iL(815525529,4294967295)
        + (b:iL(3479441767,113) + b:iL(3479441767,(L(113)))));   -- L = bit32.bnot
```
拆解：`iL(K,c)` ≡ `K*c mod 2³²` ⇒ `iL(3479441767,113)+iL(3479441767,~113) = 3479441767·(2³²−1) ≡ −3479441767`，
`iL(815525529,2³²−1) ≡ −815525529`；而 **815525529 + 3479441767 = 4294967296 = 2³²** ⇒ 两个噪声项互相抵消，
整式 = `V ^ 113`。我用 Python 只对自己的折叠式在 11 个探针点上求值，确认 6 个站点里 5 个塌成 `^K`。
* 意图：往每个"取操作数"的地方塞 3 个虚假项，值域上完全等价，且常数对的和 = 2³² 是**随机生成**的。
* 泄漏特征（关键）：
  1. **`iL(K,4294967295)` 出现 111 次**，是"我在读一个噪声项"的招牌；
  2. **两个噪声常数之和恒等于 2³²**，且 `+ ( iL(c2,X) + iL(c2,bnot X) )` 这个括号形状原样重复；
  3. 结构模板化：`vL( bxor(operand,K) + noise1 + (noise2) )`。
  改进建议：① 噪声项的模数不要总是 2³²，混用 2³¹/2³²−1/2^32+K；② `bnot` 换成 `band`/`bxor` 等价式
  （如 `iL(K,x)+iL(K,(x xor 0xFFFFFFFF))` 已经用了，就再叠一层）；③ 让噪声项**真的参与计算**（例如
  最后再 `+ (c1+c2-2^32)` 被另一个分支用掉），这样"抵消即噪声"的启发式失效。

### D4 · per-lane XOR key（每个操作数通道一个密钥）**[实测：`[18]` 内 52 个、`[73]` 内 9 个]**
```lua
R[Q],y[Q],m[Q],J[Q]=197,77,228,12            -- 改写时用的就是明文小常数
D[Q]=b:vL(E(i,101) + ...)                     -- 折叠后： D[Q] = i ^ 101
O[Q]=b:vL(E(i,111) + ...)                      -- 折叠后： O[Q] = i ^ 111
m[x]=... b:vL(p(b:vL(V),113) ...)              -- 折叠后： m[x] = V ^ 113
```
* 意图：同一份载荷在不同 handler 里需要不同 key ⇒ 不存在"全局解密密钥"，也就无法一次性 dump 出明文指令流。
* 我认为**这是全样本里最值得抄的一条数据层设计**。改进：key 目前嵌在**站点局部**（可静态读出，只是需要折叠
  D3）；若把 key 改成"从载荷里读一个数 + 站点常数"，静态就完全拿不到 key 了。

### D5 · 仿射 lane key（乘+加 mod 2³²）**[实测]**
```lua
B[j]=b:vL(b:iL(2820270878,d)+b:iL(2820270878,4)
        + (b:iL(2949392836,(W(d,4)))+b:iL(2820270879,(p(d,4))))));  -- W=lshift, p=bxor
```
数值验证结果：该项不是 `d^K`，而是**仿射** `R = (1291219581·d + 1087330548) mod 2³²`
（对所有探针点斜率恒定，我已验证；解码需模逆）。
* 意图：把 XOR（可交换、可用差分解码）换成 GF(2³²) 之外的环结构 ⇒ `差分 = 差分` 这类攻击失效。
* 建议：**全面推广这一条**。当前只有 1–2 处用了仿射，其余都是 XOR，导致"先折 XOR、剩下就是它"。
  乘性混淆比 XOR 明显更难静态处理（这是我这次唯一没能塌成 `^K` 的站点）。

### D6 · 值域冗余：85⁵ > 2³² **[实测]**
`85⁵ = 4,437,053,125`，而载荷每 5 字符解码出一个字；实测 **489/14,937 组的值 ≥ 2³²**（3.3%，正好是
radix 冗余 1.033x 的量级），而回写路径是 `buffer.writeu32`（截断）。
* 解读：这说明"每组约 0.046 bit 的无信息位"——**编码器可以随机化最高位**，从而让同一份明文的密文不唯一。
* 我的判定：**[推断]** —— 同时存在 `Z[M]=u`（不截断）与 `writeu32`（截断）两条路，我无法静态确定
  哪条喂给解析器（这是我最想让你知道的未决点）。
* 设计建议：**这是好特性，但要用满**。既然允许冗余位，就应当：① 显式在高位塞随机 padding；
  ② 把"截断与否"做成 per-build 二选一（正是这点让分析者卡在"我的对齐错还是你多态"）；
  ③ 更狠：让每组权重表随机（`85⁴/85³/...` 换成一组 per-build 的混合权重），彻底断掉"整常数反推 radix"。

---

## 5. 家族 E —— 代码 / 数据就地加密（本次最难的部分）

### E1 · 运行期就地解密"指令块" + 自改写 **[实测]**
```lua
-- b[18] 中 O==8 的 handler，逐字：
p=R[Q]+1;
for q=1,m[Q],1 do
  I=F(E(y[Q],q),127);                 -- mask = (B ^ q) & 127
  R[p]=E(R[p],I); y[p]=E(y[p],I); m[p]=E(m[p],I); J[p]=E(J[p],I);  -- 四条通道全部 XOR
  p+=1;
end
J[Q]=12;                              -- 把自己改成 handler 12（不再二次解密）
```
一条 VM 指令（我暂命名为 `DECRYPT_BLOCK`）的语义 = **"从下一条开始，连续 `A` 条指令的 4 条通道，
各自 XOR 一个由 `B` 逐条变化的掩码"**，然后把自己 patch 成 nop/下一条 handler。
* 意图：这是**整层防御的核心**：指令流是"每段用段首 key 加密"的，且**只有被执行到的段才会变明文**。
  静态看到的 `J[]/R[]/y[]/m[]` 永远是密文；内存 dump 只能拿到"当前被解锁的那一段"。
* 我认为这是本样本**最难、也最该抄**的一条。改进：
  ① key 不要由本条指令的 `B` 直接给出，改成 `B ⊕ 上一条的 D` 之类**链式**（当前是可预测的 `(B^q)&127`）；
  ② 加"重新加密"（用尽即锁回），彻底废掉一次性 dump；
  ③ 让解密作用于**跨函数**的指令区间（当前看起来是同一 handler 内连续区间）。

### E2 · 段解密 + 指令改写为快速路径 **[实测]**
```lua
-- b[73] f==6：读一个 varint 长度，然后对 [Q, Q+y-1] 逐字节 XOR，并把当前指令改成 (op=2, lane 231,200,146)
local S=p(B[j],287072149); B[j]=S;                  -- 顺手把 A 通道 XOR 一个常数（就地改通道）
...
for u=Q,Q+y-1,1 do I(j,u,(p(C(j,u),d))); end          -- writeu8(j,u, bxor(readu8(j,u), d))
J[x],R[x],t[x],m[x]=231,200,146,2;
```
* 意图：字符串/段解密 + **"解密完成后换成一条更便宜的指令"**，同时让 opcode 本身在运行期发生迁移
  （静态看到的 `op=6` 永远不会以 `op=6` 结束）。
* 泄漏特征：改写后的目标四元组 `(231,200,146,2)` 是**明文常量**。改进：改写值也从载荷取，
  或把 4 个值编码成"lane key 掩码后的值"（D4 已经对部分通道做了，这里漏了）。

### E3 · 载荷白化：per-lane key 让 base-85 解出的数据看起来是随机 **[实测统计]**
把整条静态链跑到最后：59,748 B，**熵 7.9958 bit/byte**、零字节占比 0.0048（基线 0.0039）、最长常量 run=1、
14,895/14,937 个字互不相同 —— 看起来完全是随机数。
但一个统计事实出卖了它：**42 处重复值之间的距离 gcd = 64 组 = 256 B**，且集中在一个窗口内。
* 结论（对设计者有用）：**"看起来随机"不等于"加密有效"**。只要 per-lane/周期性的 key 与位置无关，
  重复距离的 gcd 就会暴露结构。我正是用这个信号反证了"我的分组/窗口是对的"。
* 改进：密钥必须依赖**位置**（如 `key_i = K(i, section)`，或对每 64 字换 key），
  让"相同明文在不同位置得到不同密文"，重复距离统计就归零。这是本次分析里我认为
  **对通用混淆器最有价值的一条改进**。

---

## 6. 家族 F —— 运行时防御 / 反分析

### F1 · 全局访问被劫进 VM（`__index` 懒解析）**[实测]**
```lua
Y=function(b,u,z,...) ...
  local z={[b.BL]=function(q,M)            -- b.BL = "__index"
      local x,A,a,...=b:zL()                -- 取初始状态，进入第三套状态机（73 个 handler）
      while F do if R<=118 then ... end end  -- 每次"读一个全局"都跑一遍 VM
    end, ...}
```
配合 `A=getfenv()` 与 `l[R[x]]=A[k[x]]`（GETGLOBAL 的实现），
以及 `eL` 里的 `b[z[4]](b,z,b.s,nil)`（`b.s={}` 作为共享环境传给构造函数）。
* 效果（对我而言是决定性的）：**静态看不到原始脚本用了哪些 API**。全文没有 `game`、`workspace`、
  `getgenv`、`syn.*`，一切全局读都推迟到运行期。
* 我认为**这是对"静态还原源码"这一目标最有效的单条防御**，优先级高于所有算术花招。建议照抄并推广到
  **常量读**（现在常量只做到了惰性解密，没做 `__index` 化）。

### F2 · 自擦 / 防 dump **[实测]**
```lua
M=function(b,b)b[2]=nil;return true,19,nil,nil,nil,nil,nil,nil,nil,nil,nil,nil,nil,nil;end
K=function(b,...)(...)[...]=nil;end
-- buffer.fill = b[1]，全文 142 次调用（注意：在 b[18] 内部出现 0 次，见 A7）
```
* 意图：用完即清 ⇒ "跑完之后 dump 环境表"拿不到完整状态；`b[2]`（chunk 引用）被显式抹掉。
* 建议：抄，并推广为**引用计数式擦除**（每次 handler 结束就擦掉不再需要的槽）。当前全文只有 3 个写点，
  擦除做得太稀疏，反而成了"这里在防 dump"的标记。

### F3 · 反 traceback + 插桩探测 **[实测]**
```lua
nL=function(b,u,z,Z) local o,w,K,G,q,M,F,H,E,V,f,B,j = b.EL,b[16],b.PL,b.dL,b[84],b.cL,b[67],b.gL,5,u,Z,z
  while true do ...
    elseif E<=5 then if E<=4 then z=f[B]; if z then E,f,B,j=0,w,G,z; else E,f,B=3,w,G; end
                 else E = q(V)==M and 6 or 7 end            -- type(msg)=="string"
    elseif E<=6 then E = F(V,H) and 4 or 1                  -- string.match(msg, ":(%d+)[:\13\10]")
    else w(V,1); E=2 end                                      -- error(msg,1)
  end end
-- 成功路径：error( "LPH:" .. "?" .. ": " .. msg, 0 )     ← level 0 抹掉 file:line
```
* 意图：① `error(msg,0)` ⇒ 报错里不出现文件名/行号，不给"通过报错定位载荷位置"的机会；
  ② **先看消息是否已经带 `:123\n` 形状**，带与不带走不同分支 ⇒ 这是一个"我的错误是否被别人的
  xpcall/hook 改写过"的探测（traceback 里若出现别人的 `file:line` 前缀 = 有人在插桩）。
* 建议：好想法，抄。当前弱点：探测条件太简单（一个 pattern），把探测结果"上报"到密钥流里
  （例如让 `b[94]` 或某 lane key 依赖探测结果）才是完全体 —— 那样插桩会直接导致解密出错误数据。

### F4 · 运行期 vtable / 槽位覆写，打断静态调用图 **[实测]**
```lua
-- DL:  b[94]=o                     （载荷里读出来的下标）
-- eL:  local z=b:L(b.k,b[94]);  return 2,7,w, b[z[4]](b,z,b.s,nil), ...
```
被调用者 = `b[z[4]]`，其中 `z[4]` 来自**已解码的载荷数据**。初值 `b[94]=(0)`。
* 效果（对我而言是硬墙）：静态无法确定谁调用谁 ⇒ 反扁平化只能重建"每棵 dispatch 树内部"，
  跨树的边不可见。我 `tools/ops.py` 只覆盖了 4 棵树里的第 1 棵，就是这个原因。
* 建议：**这是性价比第二高的防御**（仅次于 F1）。加强：让 `z[4]` 经过 D3/D5 一类的算术混淆
  （当前是明文下标），并且**多处**使用这种间接分派（本样本只有一处 `b[z[4]]`）。

### F5 · `rawget/rawset/getmetatable/setmetatable` + `getfenv/setfenv` **[实测：槽 75/64/43/124/34/25]**
* 意图：① `rawget/rawset` 绕过别人的 `__index/__newindex`（防 hook 观测）；② `getfenv/setfenv` 换环境
  而非用 `_ENV`（Luau 无 `_ENV`，同时让"扫 `_ENV`"的规则失效）。
* 建议：注意 Luraph v15 自己**没把这套用在防御上**（只在构造时用），所以是"可选未强化项"——
  你可以更进一步：每次 handler 入口 `rawget` 校验自己的表是否被替换（自检）。

### F6 · 可 yield 的 VM（协程化解释器）**[实测]**
```lua
UL=function(b,u,z,Z) local o=b[38]; o();  for b,w in u,z,Z do o(true,b,w); end; end
-- 并用到了 coroutine.resume/wrap/yield/status/close/running/isyieldable（8 个槽）
```
* 意图：① 支持被混淆脚本里的 `wait()`/`task.*`（否则 Roblox 脚本根本没法用）；
  ② **让 traceback 只暴露 VM 自己**（协程边界切断调用栈）；③ `close` 用于退出时清场（配 F2）。
* 副作用：`isyieldable` 这类调用会成为"这是 VM 而非业务代码"的指纹。权衡：值得，但把
  协程槽的使用混进常量池 handler 里（现在高度集中在 `UL` 一个函数，太醒目）。

### F7 · `pcall/xpcall/assert` 包裹 + 类型自省 **[实测：14/81/76 + 84/58]**
`type()`/`typeof()` 用在各处做"值种类"分派（VM 的类型标签），`pcall` 用在 `b[73]` 的调用桥：
```lua
local Z,w,K,G,q = o(function(...) ... end, ...)     -- o=pcall：整个 handler 体被包在 pcall 里
```
* 意图：吞掉异常 ⇒ 分析者用"故意制造错误看栈"的手法拿不到内部结构。
* 建议：抄。并且注意：`pcall` 的返回值元组被当成"状态+寄存器"继续走状态机（这里 Luraph 顺带把
  错误处理也扁平化了，很干净）。

### F8 · 用 `buffer` 库做不可变字节对象 **[实测：12 个 buffer.* 槽]**
载荷与常量都存在 `buffer` 里，用 `readu8/readu32/writeu8/...` 逐字节访问，配合 `buffer.create/copy/fill`。
* 意图：① Luau `buffer` 是**裸内存对象**，不是 Lua 表 ⇒ 不能靠 `__index` 观测、不能 `pairs()` 遍历、
  外部脚本无法直接读到（除非知道大小）；② 天然规避 Lua 字符串不可变带来的"临时字符串留在内存"问题。
* 副作用：`buffer.` 前缀本身成为指纹——但 Luraph 已经把方法名换成 `b[66]/b[47]` 数字槽，
  所以**明文里其实看不到 `buffer.`**（除了 import 表里那一次）。这就是正确做法：
  **API 名只允许出现一次（建表时），其余全是槽号**。

### F9 · 结构级噪声：重复形参 / 元组轮转 / 别名串 **[实测]**
```lua
local V,f,B,j,d = z,Z,o,w,K;      -- 寄存器轮转：几乎每个 handler 结尾都做一次平移
local B,j,d,Y,_ = B+j, j<=0, ...  -- 变量名复用 + 多重赋值打包
```
* 意图：破坏"数据流分析"里最基础的 SSA/def-use 关联 —— 每个块都换名，跨块的变量跟踪成本爆炸。
* 评价：便宜有效。我在做区间传播（`tools/ops.py`）时被它拖了很久，因为**同一个物理槽在不同块里叫不同字母**。
* 建议：抄，并把轮转周期随机化（现在轮转是"整表循环移位"，可以按位模式猜出来）。
  与 A7 是同一件事的两面：A7 管"名字→语义"的隐藏，F9 管"名字→名字"的漂移；两者都要，否则只挡一半。

---

## 7. 家族 G —— 诱饵 / 蜜罐

### G1 · 死码"密钥表"**[实测：零引用]**
```lua
h={24123,4121909815,113887313,3257834490,808597170,2484518445,3813836768,1149546688,3906211916}
```
9 个 u32 的表，命名成 `h`（看起来像 handle/hook），**但全文件 `b.h` / `b["h"]` 出现 0 次**
（我用 `[.:]h\b` 复核，所有命中都是方法调用噪声）。
* 意图：钓鱼 —— 让分析者以为找到了密钥表，花时间去试 XOR/base85 key。
* 我的真实反应：确实先怀疑过它（因为长度和数值范围都像 key 材料），最后靠引用计数否掉。
* 建议：**蜜罐要"可被局部证实为真"才有价值**。当前它太容易被 `grep 引用数` 干掉。改进：
  把死表**真的读一次**（用在永不执行的分支里，或用在被 D3 噪声保护的计算里再丢弃），
  让"零引用"判据失效。

### G2 · 空元表 `setmetatable(t,{})` **[实测]** — 见 A1。同 G1 逻辑：装饰性。
### G3 · `b.s={}` 传给构造器当"共享空环境" **[实测]** — 看着像沙箱配置，实际是空表。
### G4 · `b[94]=(0)` —— 带括号的表达式而非数字 **[实测]** — 让朴素表扫描把它读成字符串/调用；
   同时它其实是"运行期覆写"的占位（见 A5/F4）。**这条我一开始判错**，值得抄这种"看似语法错误实为语义差异"的写法。
### G5 · 蹦床函数群 `_L/KL/zL/M`：**返回 `(true, 状态号, nil×N)`** **[实测]**
```lua
_L=function(b)return true,3,nil,nil,nil,nil,nil,nil,nil,nil,nil,nil;end
zL=function(b)return true,120,nil,nil,...(15 个 nil);end
```
* 意图：① 把"入口状态"藏在函数调用后面（静态不能把状态号当常量传播）；② `nil` 尾巴撑满寄存器窗口，
  模拟真实 handler 的多返回值协议 —— **让"handler 的形状"失去区分度**。
* 我认为这是**便宜且有效**的一致性伪装（比裸写 `return 3` 好得多）。建议推广：让所有"常数返回"
  都走这种蹦床，且返回值数量向真实 handler 分布对齐（本样本真实 handler 的 arity 分布是 1..15，众数 10-11，
  而蹦床固定 12-15 个 nil，稍微扎眼）。

---

## 8. 强度 × 成本矩阵（我的实战评分）

评分：`难度` = 我破掉它花的力气（1 易 → 5 需模拟）；`成本` = 性能/体积代价；`保留价值` = 对通用混淆器的建议。

| ID | 技术 | 难度 | 成本 | 破它所用的特征 | 保留价值 |
|---|---|---|---|---|---|
|F1|`__index` VM 化全局读|5|中|元表由 `Y` 现场构造，可静态发现|★★★★★|
|E1|就地指令块解密|5|中|`J[Q]=12` 自改写 + 掩码式可读|★★★★★|
|E3|位置无关 per-lane key|4|低|重复距离 gcd=64 组|★★★★★（改为位置相关即完美）|
|F4|运行期 vtable `b[z[4]]`|4|低|`b[94]` 只有 3 个写点|★★★★★|
|D5|仿射 lane key（乘+加）|4|低|唯一站点，未塌成 `^K`|★★★★★（应全面替代 XOR）|
|B6|自修改 dispatch / 惰性回写|4|中|`x-=1` + 四元组回写|★★★★☆|
|B4|多态 dispatch（4 棵树）|3|中|`if S==NNN then`|★★★★☆|
|C7|常量尺寸分桶+惰性+类型回读|3|中|三步流水线形状一致|★★★★☆|
|C2|表外字符单字快捷转义|3|低|频次（10 字符 ×3–4）|★★★☆☆（改多表轮转→5★）|
|C4|varint +128 偏置 + 位宽选择器|3|低|`(x-128)*128/16384/2097152` 字面量|★★★☆☆|
|D4|per-lane XOR key 集合|3|低|折叠 D3 后浮现 52+9 个|★★★★☆（key 从载荷取→5★）|
|C8|LCG 密钥流 213/225|2|低|两个常数 grep 即可定位|★★★☆☆（**必须 per-build 随机**）|
|D3|opaque predicate `x+~x`|2|低|`iL(K,2³²−1)` 111 次 + 常数对和=2³²|★★★☆☆（模数需多样化）|
|D2|位运算拼乘法 `iL`|2|低|最高频 + 唯一定义|★★★☆☆（实现需多态）|
|B5|if-表达式做条件跳转|2|低|`=if` 前缀仅 17 次|★★★☆☆（语法级阻断面大）|
|C1|radix-85 + offset40|1|低|85 连续字符集 + 权重字面量|★★★☆☆（**字母表乱序**即 4★）|
|C5|手写 base-256 拆位|1|低|`%256` + 减法链|★★★☆☆（零成本）|
|B1|完全 CFF（块=表字段）|1|中|`return <int>,<寄存器排列>` 模板|★★☆☆☆（状态号需加密才升 4★）|
|B3|while+continue，无 goto|1|低|形态统一|★★★☆☆（零成本）|
|B7|for 回边三元件|1|低|≥9 处同型模板|★★☆☆☆（每实例注入噪声）|
|A2|数字槽 import|1|低|整表可枚举|★★★☆☆（多对一映射→4★）|
|A3|单行输出|1|零|—（无法防）|★★★★☆|
|A4|遮蔽形参|1|零|反而暴露核心函数|★★☆☆☆|
|A6|4 字节垃圾头 + magic|1|零|长度整除性|★★★☆☆|
|C3|带界读窗口|1|零|元组 + 回边|★★★☆☆（上界改运行期值→4★）|
|C6|40-bit memo 缓存|0|低|明示位宽/小端/组大小|**建议删除**|
|G1|零引用死密钥表|0|零|引用计数=0|★☆☆☆☆（需"可证实"版）|
|F2|自擦 `b[2]=nil`|1|低|写点极少|★★★☆☆（要做密才有效）|
|F3|`error(msg,0)`+形状探测|2|中|pattern 明显|★★★★☆（结果参与解密→5★）|

---

## 9. 给混淆器作者的 21 条改进建议（按 ROI 排序）

**必做（低成本，直接封掉我这次的主要入口）**
1. 字母表乱序 + 位权不要用整常数字面量（C1）：「52200625 / 614125 / 7225 各只出现 1 次」是把我直接引到解码函数的那根线头。
2. PRNG 参数 per-build 随机（C8）：「213」/「225」全文各 25/28 次，一次 grep 就定形；LCG 只要 a≡5 (mod 8)、c 为奇数即满周期，随机化零成本。
3. 噪声常数对的模数多样化（D3）：禁止「两个噪声项之和恰为 2³²」；混用 2³¹ / 2³²−1 / 2³²+K，并让噪声项参与后续分支。
4. 删除 40-bit memo 缓存（C6）或先哈希 key：「readu32 + readu8*2^32」把组大小=5、小端、位宽 40 全说破了，而命中率≈0。
5. 打散开场别名串（A7）：右边全是 b[数字] 的一条赋值就把 30 个符号全交出去了，是全场最便宜的一处泄露。
6. 状态号不要明文小整数（B1/B2）：「return 33,…」+ 阈值落在 1..236 连续区，等于自带状态图节点表；改仿射/XOR 加密并离散化阈值。
7. 蜜罐必须「可被局部证实」（G1）：零引用死表是负分；要么让它在某个分支里真被读一次，要么别放。

**该做（中等成本，显著提升静态还原成本）**
8. XOR key → 仿射 key（D4+D5）：v^K 可交换、可用差分解；(a·v+b) mod 2³² 两者都不成立。本样本只有 1–2 处用仿射，而它正是我唯一没能塌掉的站点。
9. lane key 从载荷读，不要写死在站点（D4）：形成「要先解码才能拿到解码参数」的循环依赖，静态就取不到 key。
10. 密钥流依赖位置（E3）：只要 key 与位置无关，「相同明文→相同密文」就会让重复距离的 gcd 暴露结构（我实测出 gcd=64 组 = 256 B）。
11. 回写目标值也要编码（E2）：「J[x],R[x],t[x],m[x]=231,200,146,2」是明文四元组；改成从载荷取或经 D3 混淆。
12. 间接分派多点化（F4）：b[z[4]](…) 全样本只有 1 处太浪费；每 5–10 个块放一个，并对下标做算术混淆。
13. 退化模板注入实例级噪声（B7）：≥9 处一模一样的「三元件 for 回边」就是循环识别锚点；每实例插 1–2 条等价噪声（+0、*1、冗余双条件）。
14. 乘法实现多态化（D2）：iL 唯一实现 + 全文最高频（330 次）= 天然的「当黑盒然后符号折叠」锚点。
15. import 槽多对一 + 表体进载荷（A2）：现在一次枚举构造式就还原了 68 个槽；同一 API 给 2–3 个槽号即可让静态映射不再双射。

**可选（用体积换防御）**
16. 解密区间跨函数 + 用尽即锁回（E1）：现在解密范围像「同一 handler 内连续若干条」，且解完不回锁 ⇒ dump 当前段仍有价值。
17. __index 化常量读（F1 推广）：全局读已经 VM 化了（本样本最有效的一条），常量还停留在「惰性解密」，可以再进一步。
18. 插桩探测结果参与解密（F3）：目前探测只改变错误消息；改为让探测结果进入 lane key，被 hook 就直接解出错误数据。
19. 多表轮转的转义展开（C2）：让「1 字符 ↔ 固定 5 字符」的双射不成立，静态展开必然失败。
20. 分派树数量 per-build 随机（2..7 棵）且树选择值来自载荷（B4）：让「一共有几套 opcode 空间」本身成为未知量。
21. 载荷组大小 per-build 随机（4/5/6 字符混用）：现在「74,685 能被 5 整除」是我验证框架正确的免费自检位（见 §10 末行）。
## 10. 反向清单：我实际用到的"识别特征"（防守方也照这份自检）

| 我的手段 | 针对的技术 | 你要改什么才能失效 |
|---|---|---|
| 数字槽的频次分布 + 一次枚举构造式 | A2 | import 表放进载荷 / 多对一 |
| 巨型函数开场"右边全是 `b[数字]`"的别名串 | A7 | 别名串混入非平凡表达式 / 分段遮蔽 |
| 字符集恰好 85 个**连续** ASCII，10 个表外字符各 3–4 次 | C1/C2 | 乱序字母表 + 表内标记（用转义位图代替） |
| `52200625/614125/7225` 字面量各 1 次 | C1 | 权重运行期计算 |
| `#u-1` + `5` + 元组 `{nil,-5,5,Z+0,o}` | C3 | 窗口边界来自载荷 |
| `(x-128)*128 / 16384 / 2097152` 组合反复出现 | C4 | 偏置值 per-build 随机 |
| `iL(K,4294967295)` ×111；噪声常数对和=2³² | D3 | 模数多样化 + 噪声参与计算 |
| 最高频 handler 只有 200 B 且定义唯一 | D2 | 多实现轮换 |
| 213×25 / 225×28 | C8 | 参数随机 |
| `return <int>, <同名局部排列>` 模板 + 状态域 1..236 | B1/B2 | 状态号加密、阈值离散化 |
| `Q=if … then … else …` 前缀稀疏（17 次） | B5 | 换 and/or 表达 |
| `J[Q]=12` / 四元组明文回写 | B6/E2 | 回写值从载荷取 |
| `if S==251/99/8/140` | B4 | 树数随机 + 树号来自载荷 |
| `{create→bxor 循环→typed read}` 三步同型 ×25 | C7 | PRNG 混用 + 步序随机 |
| 重复距离 gcd=64（=256 B） | E3 | 位置相关密钥流 |
| `b.h` 引用数 = 0 | G1 | 删掉或"用一次" |
| `b[94]`/`b[2]`/`b.k` 只有 3 个写点 | A5/F2/F4 | 写点分散到 20+ |
| 长度整除性（74,685 = 5×14,937）| C3 | 组大小 per-build 随机（4/5/6 混用）|
| 语法方言（`+=`/`continue`/if-expr）| A3/B3/B5 | 这是"目标语言指纹"，不用改——它已经帮你挡住 luaparse |

---

## 11. 附：把这些串起来的最小流水线骨架（伪码，供实现参考）

```python
def emit_protected(src_ast, seed):
    R = PRNG(seed)
    T = { }                                        # b: handler 表

    # --- A2: import 表（多对一 + 载荷内写入，见建议①②）---
    slots = assign_slots(API_LIST, R, allow_alias=True)

    # --- D1/D2: 归一化与乘法（≥3 个实现，轮换）---
    mul = [emit_mul_partial(), emit_mul_mod(), emit_mul_shift_add()][R.int(3)]

    def noise(expr, bits=32):                      # --- D3: opaque predicate ---
        K = R.u32_odd(); c = R.u32()
        return f"vL( {expr} + iL({c}, -1) + (iL({K},X) + iL({K}, bnot X)) )"  # 和≠2**32 更优
    def lane_key(lane):                            # --- D4+D5: 仿射，key 从载荷取 ---
        return f"(a_{lane}*v + b_{lane}) mod 2^32"   # a 取模逆存在的最小奇数

    # --- 载荷编码 C1..C6 ---
    words = pack_instructions(src_ast, lane_key)   # 每条 = OP/A/B/C，SoA
    words = [w ^ K[i & 63] for i, w in enumerate(words)]      # E3: 位置相关（建议⑨）
    digits = [b85(w, weights=R.permuted_weights()) for w in words]   # 乱序字母表（建议①）
    esc    = build_escape_table(digits, R, rotating=True)             # C2 多表轮转
    blob   = "LPH:" + R.bytes(4) + concat(esc.encode(digits))        # A6 垃圾头，长度 per-build

    # --- B1/B2/B6: 扁平化 + 自改写 ---
    blocks = flatten(src_ast)                      # 每块 → T[<130 个 handler>]
    for blk in blocks:
        blk.head = f"local st = {enc_state(blk.state)}"           # 状态号加密（建议⑤）
        blk.exit = f"return {enc_state(blk.next)}, {rotate_regs(blk.regs)}"
        if blk.needs_operand: blk.body += lazy_decode_and_writeback(lane_key)
        if blk.is_region_start: blk.body += xor_decrypt_region(len_key=chain_key)  # E1 链式
        if R.chance(0.15): blk.exit = indirect_dispatch(T, from_payload=True)        # F4 多点化

    # --- C7/C8: 常量分桶 + PRNG（参数 per-build）---
    consts = encrypt_constants_by_size(src_ast.consts, lcg=R.lcg(), salt=R.byte())

    # --- F1/F2/F3/F6: 运行时防御 ---
    return wrap_single_expression(
        table=T | consts | {blob_field: blob},
        env_guard=metatable_index_vm(blocks),          # F1 全局读进 VM
        self_erase=erase_after_use(written_slots),     # F2 密集擦除（建议：全部写点）
        error_fmt=lambda m: f'error("LPH:?: "..{m}, 0)',# F3（建议：结果参与解密）
        yieldable=coroutine_bridge(),                   # F6
        pretty=False, newline=False)                    # A3 单行
```

**验收标准（用我这次的实测反推）**：若实现后，"字符集频次/整除性/权重字面量/`2^32` 常数簇/LCG 常数/
重复距离 gcd/引用计数=0 的表"这 7 项静态启发式**全部无输出**，那么这份纯静态分析将在
第 7 步（radix 判定）就停住 —— 也就是把本次 40 步压缩到 3 步。

---

## 12. 一句话总结

Luraph v15 的强度**不在** CFF、也不在算术噪声（这两类我有把握说 30 分钟内任何合格 RE 都能折掉），
而在 **F1（全局读 VM 化）+ E1（就地指令段解密）+ F4（运行期 vtable）+ D4/D5（per-lane 仿射 key）**
这四者的组合：它们共同把"静态可确定的信息量"压到接近零，而其余 40 项里有相当一部分
（A4、G1、C6、B7、B1 的明文状态号）**留下了比它们本身更多的信息**。
