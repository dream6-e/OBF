# OBF v2 运行语义兼容性

更新：2026-09-11。当前实现为 private ISA17（在 ISA16 之上：K13c 第二步给常量池 payload 加钥、常量值只在帧激活期内存在，**镜像字节因此改变**；K12 折入链式 operand lane key，K13/K13b 只改词表生命周期、K14 只改装载期分派形状，均不改求值语义；K16 只把发射脚本里重复的规范十进制轮常数搬成包装表字段，K17 只改字符串字面量的转义宽度与 base86 符号集（86 个符号的数量、分组宽度与 86^5 > 2^32 余量不变），K18 只改生成器侧的抽样族（xorshift64* / 全周期 LCG / sfc 形三族按区域分工，`index()` 由取模改为乘法-位移）与校验谓词形状（头部 5 条语句 13 项等式检查各自改写），三者的值、求值顺序与错误边界均不变；K18 会让私有镜像按种子重排（Lua51 1,141→1,118 B、Luau 870→1,126 B）**但镜像格式与公开 `.obf`（5,525 / 6,575 B）不变 ⇒ 不升 ISA**；K19 只把 transport 三段的数字表改成由上一段解出的明文回灌出来的旋转量（首段 `VAL[SB(ALPHA,j)]=j-1`，后续段 `VAL[SB(ALPHA,j)]=(j-1+B)%r`，`B` 是段内已有的槽位，折算是 `for j=1,#PR do B=(B+SB(PR,j))*MM%r end;B=(B+#PR)%r`），求值语义、错误边界、镜像格式与公开 `.obf` 全不变 ⇒ 同样不升 ISA；K20 只把 payload 段函数从 `setmetatable` 的表字面量搬进入口状态机各阶段之间的注入点，在运行期用 `VMS[k]=function...end` 分多次装载（同一键可先装 A 再装等价的 B，最后写入者生效），并让每条装载语句折叠一句 `ck=ck+key`、在 run 段调用解释器之前比对总和；求值语义、错误边界、镜像格式与公开 `.obf` 全不变 ⇒ 同样不升 ISA）：在 ISA14 capture/constant 全局洗牌池基础上，再把 transport 换成逐 image 混合 base86（求值语义与兼容性不变）；池每记录 3 匿名 u16 token 携带掩码 owner/slot/index/tag，per-record factorial profile 重排，池顺序按 image 翻转；header 仅保留 24 字节 metadata；固定 slot 宽度与 opcode/edge/recipe 语义不变，fetch/validator 的静态字段下标随 seed 变化，错序在用户代码前 fail closed。ISA11 双 domain ChaCha8/runtime material/decrypt anti-hook、ISA10 runtime-witness/data-dependent control、ISA9 global segment graph 与 bounded LZW 全部保留；K7 目标端多态字运算、K8 per-prototype indirect route（私有字段扩到 16 项）、K9 per-lane affine operand value key 与 2026-09-10 的 keyed dual-lane frame tag **均不改变求值语义、操作数值、错误边界或公开 `.obf`**。
>
> 复核基线（2026-09-12，HEAD `7106908` + 红门处置、K10 结构批次、K12/ISA16、K13/K13b 惰性解码—回锁批次、K14 装载期校验器分派树批次、K13c 第一步坐标化、第二步加钥批次，以及 K15 数字池化批次（因改壳格式整体回退 `7b3f450`）与其重做 K16 常数字段化批次、K17 转义/字母表瘦身批次、K18 抽样族与校验谓词形状批次、K19 段密钥回灌批次、K20 运行期分段装载批次）：固定 seed（7001 / 7351）golden 为 **102,028 / 111,721 B**（K20 把段函数改为运行期装载各 +479 / +576 B；K19 段密钥回灌各 +158 / +268 B；K13c② 加钥与逐帧重建各 +1,467 / +2,338 B；同日 K16 把重复轮常数改为包装表字段各 −353 / −269 B，K17 把十进制转义改到最短安全宽度并从 base86 字母池里排除 `"`/`'`/`\` 各 −1,289 / −728 B；K18 换抽样族并把校验谓词改形后各 +772 / −731 B）；10 种子最坏 102,548 / 111,721 B；矩阵 PASS 307（191 单元 + 116 集成）0 失败，覆盖率仍 46/46、49/49，bench lua51 344 ms / luau 112 ms（K16 树 15 次中位，同轮 native 2/3 ms；K13c② 曾为 320/107 ms）——加钥、逐帧重建与常数字段化均无可测执行代价；K12 把 operand lane key 折上链后 image 变化，Lua51 +201 B、Luau −283 B；求值语义与逐字节确定性不变）；整脚本预算经 K10 重钉为 112,000 B/目标、K14 按用户指示重钉为 **120,000 B/目标**；**K18 批次内按用户指示暂停 enforce**（`tools/test-matrix.sh` 临时 `export OBF_BENCH_SCRIPT_CAP=off`，`tools/bench-vm.sh` 保留 `CAP_PIN=120000` 只报告不判负，并新增 `CAP_RUNAWAY=180000` 兜底：越 1.5× pin 直接失败。实测 K20 后 10 种子最坏 Lua51 102,548 B、Luau 111,721 B，**没有一处越出 120,000 B**，删除那行 `export` 即恢复；③ K6 打散已于同日落地（K20），下一批（声明式段长对账 + `UNPINNED_TAIL_SLACK`）开始前恢复 enforce 即可）。门数 **191 单元 + 116 集成 = 307，实测全绿**（体积门的 pin 仍在，只是本批不判负）；frame-tag 批次留下的 3 个红灯（两套审计 pin + Luau probe 明文拼出库名/反射名）已在同日处置完毕，逐条记录见 [`项目交接总结.md`](项目交接总结.md) 第 0.1 节。15-run bench 为 Lua51 **241 ms**、Luau **105 ms**（K19 树；上一批 K18 是 351/152 ms，同一对 golden 种子 ⇒ 这一格摆动仍是装载期尺寸参数的抽签结果，不是 K19 做了优化：K19 只加了两条折叠循环与一个形参。引用时把它当方差看，别当收益看）；K18 树曾测到（`tools/test-matrix.sh` 内嵌 bench 的 15 次最佳，K18 树复测；K17 树曾为 346/114 ms，同机同种子重测 K17 golden 得 348/115 ms ⇒ K18 的 Luau +32% 是装载期尺寸参数随换流重新抽签的结果：K18 自己的 10 种子 Luau 区间 114–152 ms，最快种子即回到 K17 量级，稳态执行量不变，`VM_BOUND_MS` 预算未破；K19 后同一对 golden 种子 241 / 105 ms，K20 的运行期装载加 20 条赋值语句后是 **232 / 103 ms**（best-of-15，同机同种子）⇒ 多点装载没有留下可分辨的装载期代价。K16 树中位 344/112 ms、K13c 第一步树曾为 323/108 ms，K14 为 311/104 ms、K13b 为 332/112 ms、K12 为 323/103 ms。本轮 native 基线为 2/3 ms，故比值只与本批同轮数据并列引用；链与树只影响 parse/装载期。）

## 1. 验收目标与“受支持源码”

目标是在固定 **Lua 5.1.5** 或 **Luau 0.735 默认编译配置**及相同宿主输入下，使受支持、可正常结束的确定性源码，经 `AST → IR → OBF v2 → Register VM` 后，运行输出逐字节相同，并保持返回值数量/内容、可观察写入和回调顺序。这里的“输出相同”不是混淆后的脚本文本与原文件相同。

验收条件包括：

- 原始与虚拟化后源码都通过对应的原生编译器，且执行成功；
- 使用相同 API、输入、模块路径和宿主配置；
- 程序在文档化的语法、宿主及资源范围内，不依赖下文的反射、分配布局或非确定性结果；
- 比较完整 stdout bytes；返回值、错误对象、nil holes、写入和回调通过源码断言/确定性日志及外部模块调用验证，而不只检查“VM 没崩”。

不能用 46/49 条 opcode 的执行覆盖推出“所有 Lua 程序完全等价”，也没有依据声称一个普遍的兼容率百分比。通过测试证明的是已覆盖路径，不是对任意源码的证明。

## 2. 本轮修复

### Luau 迭代器

- `__iter` 使用缓存的 `rawget` 查找，避免意外触发**元表自身的 `__index`**。
- 区分 nil 与 false：`__iter=false` 不能偷偷退回普通表遍历。
- 没有 `__iter` 时保留带 `__call` 的 table/userdata 迭代器，而不是当作普通容器。
- 检查 `__iter` 返回 nil；保留 generator/state/control、false 控制值、多返回值与尾部 nil。
- 仍不声称可访问被隐藏的元表。

### 闭包与运行时捕获

- Lua51 保持每次创建新闭包；Luau 对已判定可共享的闭包采用缓存和原始值比较，覆盖无捕获、递归、顶层只读捕获、函数初始化链和常用标量常量捕获。
- 按 BindingId 判断写入、循环/参数作用域和初始化器，不能按变量拼写猜测；已写入的捕获不能被当作只读。
- 独立标记递归 local-function 的 self capture，避免拿尚未初始化的 cell 与旧闭包比较。
- 从实际 IR 指令中收集 upvalue 使用，自底向上去掉只存在于类型或已删死分支中的捕获，并重编号所有传递捕获。公开 AST/resolver 的类型与命名检查不变。

### 常量、可达性与 repeat/continue

- 对 Luau 的只读标量、纯条件与短路表达式做有界分析；不执行用户调用、表访问、元方法或全局读取来推断常量。
- 基本块记录可达性，跳过已证明不可达的语句/分支。因此 `if false`、只读 false、纯常量比较等死分支里的 `continue` 不再让合法 repeat 被误拒绝。
- 活跃的 `continue` 跨过 until 所引用的 local 初始化时仍拒绝；这不是原生编译器全套数据流的实现。
- 整数值可以传播，但整数运算不能被当成 double：直接 `-Ni` 与 `local i=Ni; -i` 的不同原生行为保留。
- 字符串常量分析只处理不超过 64 KiB 的值；共享 `Rc` 防止声明别名复制字符串造成二次方存储。分析与 IR 扫描纳入 1M 工作预算，递归上限仍为 64。超出优化范围可以保留普通求值，不扩大安全门限。

## 3. 数据格式与兼容

公开文件仍为 **OBF v2、32-byte Header、24-byte prototype header、7-bit varint 指令流**；primitive opcode 仍是 Lua51 46 条、Luau 49 条，没有 native word，ISA 修订 1/2 兼容规则不变。区别只发生在脚本生成边界：已验证 `Program` 会被 seed-specific lowering 为私有 instruction encoding 1 / ISA15，而不是把 canonical linear stream 原样嵌入。直线 word 组合成 1~4 primitive recipe；每个真实 prototype 入口插入 2~4 个 neutral bundle，并按函数规模从 next/test edge 抽取至多 5 条、各插入 1 个。通常使用新增 scratch register 执行 `Nil/Not/Move/Clear` 后恢复无引用状态，满 256-register frame 则使用 `Move r0,r0`。这些中性节点有真实入边并实际 fetch，而非只存在于 dictionary。

ISA15 保留的每函数 semantic record 继承 ISA6/7 的四个 u16 槽位与 operands，但槽位语义归属按 per-prototype factorial 置换重排，不再固定为 `label,next_token,skip_token,recipe_token` 物理顺序：两个三层 edge token 分别绑定 source label、prototype、edge kind 与交叉项；五层 recipe token 绑定 ED 动态解出的真实后继。验证阶段和每次 `code[label]` fetch 都先经过 3-live/3-dead 的乱序 ED（每层 2~3 重谓词），再经过 5-live/5-dead 的 RD（每层 4~5 重谓词）；明文后继只在局部变量中短暂存在，不回写持久 record。ISA6 的 dictionary 存储 `descriptor_ops`，真实执行使用独立 `execute_ops`；live 替换仅发生在 encoding Form、目标 validator 与 control/terminal class 严格相同的 family 中，专项门要求至少 45% live primitive 被 camouflage。所有多-op recipe 都规划为 1~2-op fragment，`rid` 只映射随机入口 `sid`，全部 `sid` arms 跨 recipe 全局打乱；但 2-op 不再表示两个原 handler 文本串联。ISA12-C 只在首项为单语句 `R[a]` producer、次项至少含一个可安全 register read 且不属于 control/多写形态时，用最大非重叠配对生成 carried-value fragment；其余相邻项保持两个 single。records、兄弟 prototype/Closure 与 synthetic subtree 继续洗牌。覆盖测试在 ED/RD 与 fragment routing 后记录真实执行，既证明 neutral 入口链运行，也继续得到 46/46、49/49 primitive 覆盖。

ISA12-A 新增的 operand ABI 不改变用户可见求值顺序：canonical operand varint 仍按 recipe descriptor 顺序解码并先通过原 validator，之后才按 prototype profile 存入 record。profile 由 seed-derived 的模 4 family、模 3 rotation 与模 2,731 lane 三个可逆仿射分量计算；组合周期 32,772，覆盖 private image 32,767 prototype 上限且无碰撞。四个 family 分别为 packed-u24、反向 AC+B、转置三列和嵌套 tuple；runtime `OG` 恢复完全相同的 `a/b/c/k/j`，四种匹配返回/LHS 顺序只改变临时绑定表示。因而 Lua51/Luau 的 operand 值、操作顺序与错误边界不变，坏 record 仍在用户代码前出错；但静态分析不能再对所有函数套用一个 `I[6+3*i..]` 规则，fragment 也不再携带真实 opcode 数字赋值。

ISA12-B 的 register ABI 同样不改变逻辑寄存器语义或求值顺序。prototype id通过模4/127/257的family/stride/shift可逆仿射profile选择四个互斥257-key bank之一，并在bank内把逻辑`0..255`单射为物理table key；组合周期130,556，完整32,767-id私有域无重复。`SETUP`每frame只建立一次mapper，并用它初始化参数及Lua51 legacy args/varargs cell；生成器按lexer token span把所有双目标primitive模板的`R[index]`包裹为同一mapper访问，连续`Clear`、numeric-for的相邻寄存器、动态closure capture、cell/upvalue、callback与tailcall路径均包含在内。该过程不修改用户源码、不输出256项映射表、不复制每prototype handler。mapper公式随脚本交付且可逆，所以兼容性保持不代表primitive dataflow不可恢复。

ISA12-C 的 fusion 保留同样的求值与别名边界。首 primitive 先保存逻辑目标、先求 `RX(target)`（保持 Lua lvalue-before-RHS 次序），只求值一次 RHS，再照常写回 register；随后才解码第二项 operand。第二 primitive 的每个安全 read 改为 `RF(index,target,value)`，四个 frame family 分别用逻辑相等、反向条件、物理 key 相等与 shifted-mod-257 相等识别依赖，显式 value 参数完整承载 nil/false；不命中时才读当前 `R[RX(index)]`。首写回不会因 fusion 被删，故后续 liveness、register alias、cell/table引用仍保留。入口的 prelude/probes/segments+watermark/decrypt+decompress+parse/validator bind/execute 六阶段改为打乱 arm 的状态机，但严格依赖和 fail-closed 顺序不变。

ISA13 的 field-order 置换同样不改变求值顺序、操作数值或错误边界。record/segment 的槽位只是同一组 u16 token 的物理重排，parser/validator/fetch 按同一 id/slot 实时重算归属后再走原有 ED/RD/operand 校验；dictionary/metadata/tuple 的 per-image 置换也只改变生成 parser 的字面字段顺序。错序输入触发既有 label/token/operand/count 门而在用户代码前失败，不会静默执行错误语义；但置换规则随脚本交付且可逆，所以兼容性保持不代表字段布局不可恢复。ISA14 引入、ISA15 保留的 capture/constant 池同理只改变数据归属的物理位置，不改变闭包捕获关系、常量值与求值顺序；池掩码与置换规则同样随脚本交付且可逆。

ISA9 起把每个 prototype 的完整 semantic code 按 seed/owner context 切成两个非空节点，所有 prototype metadata 先连续写完并进入 header 区（capture/constant 不再跟随 prototype 连续存放，而进入两个全局随机池），segment 节点随后进入一个跨 prototype 的全记录随机池。metadata root 以及每条记录的 `id/owner/next` 分别用 full-width token-layer 参数与 owner/物理槽上下文掩码；三个 token 的物理槽位按 ISA13 引入、ISA15 保留的 per-segment factorial 置换重排，parser 先按 slot 重算归属再做原有验证；capture/constant 池记录同理先按 per-record profile 重算再验证掩码与类型。parser 精确读取 `2×np` 条记录，验证 ID 完整唯一、显式 owner、root 恰为 `2×owner+1` 且唯一、两节点 terminal-0 链、无环、总长，并在末尾逐 ID 核对全部节点已使用且无尾随，再恢复各函数 code。因而 semantic validator 运行前既不存在“metadata 后紧跟连续 code body”的物理函数边界，也不存在“header 后紧跟连续 capture/constant”的 per-prototype 数据邻接；拓扑错误在任何用户语义执行前失败。

ISA15 保留 ISA10 的 source-witness/control route：Luau 对 `debug.info(loadstring,"s")`、Lua 5.1 对 `debug.getinfo(loadstring,"S").source` 逐 byte fold并生成 `c1/c2/c3`；三 share 的和继续选择并掩码两套 fetch/dispatch 物理 state。新的 transport KDF 再将 shares、forms `pv`、domain/context 与 anti-hook attestation送入 ChaCha8 block，动态生成最终 key/nonce/counter。outer 与 inner 解密前分别重新执行 anti-hook，检查 source metadata、helper同源、primitive behavior与 zero-state ChaCha8 KAT；坏 transcript、hook 或错误 attestation都会派生错误 material并 fail closed。普通运行语义不依赖密文布局；但规范环境与 decoder均在客户端，精确模拟仍可恢复，不构成不可提取秘密。

ISA15 的 transport 顺序为 `bounded LZW → inner ChaCha8 → strict frame v2 → outer ChaCha8 → XXS:/混合 base86 三段`（逐段长度前缀、链式变宽组、随机高位填充；损坏仍在 parser 前无 stdout 失败）。LZW仍在每 8,192-byte解压输出边界重置，header固定原长、精确bit数和完整semantic Adler-32；只有严格小于原像才允许生成。inner只加密bitstream，全部header字段参与其context；frame v2再绑定descriptor/version、原长、cookie、tag与0..3 byte padding（tag 自 2026-09-10 起是 keyed dual-lane hash：两条模 65,521 lane 以运行时 key state + salt 起始，逐字节 `left×257+b` / `right×263+b+left`，再混入 cookie/descriptor；**不再是位置无关的 Adler 折叠**，但仍是损坏门而非 AEAD）；outer覆盖完整frame。目标端ChaCha实现拆为word ops、quarter round、block、KDF/stream与anti-hook五个随机sibling fields，Lua51用纯算术多态工具箱、Luau用隐藏名捕获的bit32+buffer多态工具箱（求值语义逐bit一致）。逆链依次校验outer anti-hook/stream、frame全部字段、inner anti-hook/stream、LZW长度/dictionary/reset/padding/完整消费/checksum，任一错误都在parser与用户代码前无stdout失败。`extract_embedded` 返回body仍由inner ChaCha8保护的LZW frame；`decrypt_embedded`严格解压后返回ISA15 image（含 K8 route 与 K9 affine operand key）。公开`.obf`仍逐字节round-trip且与seed无关。破坏性回归覆盖semantic/segment metadata、codec畸形、published ChaCha8 vector/domain差分、wrong witness/attestation、frame与每个outer ciphertext byte、native-byte wrapper hook、block helper tamper和双目标base86-valid篡改。完整LZW frame必须严格小于解压后的semantic bytecode；95,000/95,000 B 是历史独立整脚本预算，K10 起为 112,000 B/目标、K14 起为 120,000 B/目标（施工窗口可显式 `OBF_BENCH_SCRIPT_CAP=off` 暂停该门），两者均不与 ISA7 predecessor 比较。frame tag 是 fail-closed 损坏门，未宣称为正式 AEAD。

默认编译器输出 **ISA 修订 2**：

- prototype flags bit3 表示 Luau shared closure；只允许非入口 Luau prototype；
- capture kind2 表示递归初始化 local cell，仍为 `[kind:u8,index:u8]` 两字节，每 prototype 最多一个，只允许与 shared flag 同用；
- Rust reader/validator 与目标 decoder 都检查版本、目标、flags、capture 类型和范围；修复 checksum 不能绕过这些检查。

**ISA 修订 1 仍可读取、执行和逐字节 round-trip**，不会在 `serialize(decode(...))` 时静默升级，也不会给旧文件凭空添加共享语义。`Program.isa_version` 保存真实版本，`inspect-bytecode` 报告文件版本而非当前编译器版本。直接构造公开 IR/Program struct 的调用者需填写新增的 `shared_closure` / `isa_version` 字段；使用 compile/decode 的调用方式不变。

公开字节码仍不依赖 seed；private ISA15 image、per-prototype operand/register/dataflow/field-order profile与binding、capture/constant池洗牌/池序/掩码、field factorial参数、fusion配对、六状态entry graph、descriptor camouflage、semantic fragment/stage、global segment 切点/掩码/物理顺序、edge/recipe-token/label/neutral edge split、ChaCha salt/domain/context、runtime-witness share/attestation、fetch/dispatch mask、传输与最终布局依赖 seed 或实际目标 transcript。完整 VM 生成后，显式授权的 16 个私有 prototype/control 字段（含 K8 的 `routes`）按独立 seeded 双射缩为一/两字母，再执行 local 随机短名、仅限无歧义边界的可选分号省略、单行化与绑定校验。公开字段、导出、字符串、宿主 API/元方法和静态 NAMECALL 名称不改；没有 native fallback。

私有字段门继续检查 schema 冲突、未知/误用 marker 拒绝、marker-like 用户方法名、版本兼容和完整 token/bytecode 保持；ISA15 保留的 control 字段进入同一显式 schema，最终产物不得留下 marker。

在此之后，默认后端的整体输出改为 `local x={};return setmetatable({...},x):m()` 的分函数载荷形式：全部 VM 代码位于载荷表的 5 个随机数字键 section 函数与 1 个随机字母键入口函数中，section 之间用参数/返回值传递状态，互相递归的 Call/Make/H 保持同一函数作用域。`:m()` 直接命中载荷表自有键；入口 prototype 含 Varargs 指令时以 `:m(...)` 转发 chunk varargs，语义与未包装时一致。方法名与数字键由 seed 的独立随机流决定，bytecode、最终 local 与私有字段名不变；旧 native 后端输出不受影响。

## 4. 实际测试证据

新增 `tests/vm_parity.rs` 共 **13 项**：

- 121 个运算符/赋值/numeric-for 生成式案例，双目标、3 个 seed；
- 24 个 Luau compound-assignment 组合，3 个 seed；
- 11 个常量捕获/闭包身份案例，涵盖 NaN、Infinity、字符串、if expression、integer 与非折叠调用；
- 递归闭包、参数/循环捕获、类型擦除/传递 upvalue；
- raw `__iter`、callable table/userdata、false/nil、元方法与副作用；
- 多返回值、尾部 nil、1000 项 pack、pcall/xpcall、错误对象、外部模块返回值及冻结导出表；
- 原生可编译的 dead-continue 正例和仍应拒绝的初始化跳过反例；
- 修订 1 文件执行、CLI 版本报告及固定 seed 复现。

应用式语料：

- `tests/fixtures/parity_common.lua`：递归下降表达式求值器、单词统计、账户转账、事件订阅/取消、协程队列、nil-preserving callback pipeline；两端运行。
- `tests/fixtures/parity_luau.lua`：类型/泛型/方法类型实参、插值、continue、可调用迭代器、闭包共享与 integer。

ISA14 专项除保留 ISA13 的 witness/control、标准 ChaCha8 vector、双 domain/context/material 差分、stream round-trip、wrong attestation与frame-v2逐 ciphertext byte corruption外，还遍历完整32,767 prototype id域验证operand/register profile无碰撞，检查operand四种layout/rotation/binding、register四种bank family/256槽单射/物理界限/无完整映射表、旧fixed-slot/actual-opcode/规范化`R[a]/R[b]/R[c]/R[d[2]]`访问面消失；ISA12-C再验证producer/consumer eligibility、`fragments+fusions=operations`、每pair至少一个forwarded read、live fusion下界、四family RF、第九个整体状态机和旧线性entry导出锚点消失；ISA13验证record/segment factorial全范围置换合法与双端重算一致、固定seed parser字段序与跨seed变化，ISA14验证capture/constant池掩码/token合法、池洗牌coverage与池序翻转一致及池错序双目标无输出拒绝，并对Lua51/Luau多seed执行semantic round-trip及nil/false、alias、元方法、errors、cells/captures/callbacks/tailcalls/legacy-vararg运行差分；双目标集成测试继续覆盖行为等价 `string.byte` wrapper、loadstring/source hook、ChaCha helper/KAT tamper、attestation常量、密文损坏和五类字段序/池错序，均要求用户stdout前失败。`tools/test-matrix.sh` 还在编译前强制所有 `src/**/*.rs <= 80 KiB`；原124,624 B tests module已拆分，当前最大生成器源码仍低于门限。

运行语义批次的 `tools/test-matrix.sh` 在原 124 项上增加上述 13 项、2 项 codec 及 1 项目标 decoder，达到 PASS140；字段压缩批次加入 9 项回归达 PASS149；整体 `setmetatable` 方法调用包装再加 1 项形状/差分单元测试达 PASS150；7-bit varint 指令序列化批次更新布局断言并新增 1 项 codec 回归达 PASS151；嵌入 payload 加密批次新增密文熵值/解密等价与环境篡改 fail-closed 回归达 PASS153；密钥结构动态推导（零字面量）批次再加密钥不出现回归达 PASS154；常量池第二层加密批次加独立性与扫描器防御两项回归达 PASS156；base86 传输层批次加 codec 回归达 PASS157；水印批次加存在性/隐藏性单元与篡改静默集成回归达 PASS159；M7 结构随机化批次加变体族单元与体积预算测试达 PASS161；不透明真假分支批次加死臂/包装检测单元达 PASS162；全代码随机化批次（payload 字段序整体洗牌 + 加密乘子/混合常数/轮数 seed 派生，双端同步、逐 seed 布局断言）加 1 项单元测试达 PASS163；特征拆分隐藏批次（操作数验证器拆为形态表重建、varint 解码、边界验证三个独立洗牌字段，形态表改打包串）再加 1 项拆分结构单元测试达 PASS164；随机 opcode 重编号批次（64 槽单射置换、打包串重建、展开时重写操作码字节、死臂取像外值）再加 1 项置换/打包往返/运行等价单元测试达 PASS165；分派链二级拆分批次（G∈2..4 模数子链、选择器四拼写、每子链独立 fail-closed else）再加 1 项拓扑/复现/运行等价单元测试达 PASS166；全字段解除锚定批次（入口/解释器进洗牌池、逐字段 `,`/`;` 分隔符变体、prelude 语句/返回/解构三方洗牌）再加 1 项位置/分隔符/prelude 变体单元测试达 PASS167；全局名隐藏批次（字符函数池+双格式拼名+块级环境 getfenv(1) 索取、探针参数化 DB/GI/LS、审计改结构化探针识别、barrier 记账=3、体积门有意识上调）再加 1 项词法记号级零明文/池与格式多样性/运行等价单元测试达 PASS168；随机 section 批次（解码器单体扁平化为解密+2..4 随机 helper 簇+core、字段总数 20..22 逐 seed 变化、pos 访问器保 bp upvalue 语义）再加 1 项拆分数/接线/差分/运行等价单元测试达 PASS169；控制流扁平化批次（分段/解密/解析/解释器四类状态机+SETUP/PH/PU/PK 拆分，体积门有意识上调 27,500/28,500）再加 1 项状态机/拆分/复现/运行等价单元测试达 PASS170；g 槽位表批次（全部非递归阶段活局部与 SETUP 中间量改为 `local g={}` per-seed 随机数字 key 槽位读写、作用域尾 `g=nil` 清空、解释器帧寄存器 F/R/va 为递归实测例外，体积门 luau 28,500→29,800）再加 1 项槽位结构/复现/差分/运行等价单元测试达 PASS171；像内诱饵语义臂+假锚点批次（F5 为未用 opcode 发射"另一 opcode handler 文本"的假分派臂——键在置换像内静态不可过滤、形态表保证运行不可达；entry 死侧携带假 129 字节打包串/假 LCG 份额/假 Adler 门；体积门 27,500/29,800→29,500/31,800）再加 1 项臂扫描/毒化/真假串甄别/差分单元测试达 PASS172；密钥流原语族+跨阶段密钥派生批次（两层各自三族算术流：Lehmer/双 Lehmer 和/mod-2^32 LCG 高位输出；payload 种子混 forms 字段重建的 PT 三槽项、常量层种子混外层解密镜像的两框架字节，逐段黑盒被迫按依赖序模拟）再加 1 项族形态/耦合序/差分单元测试，最新 PASS173。ISA3 semantic virtualization 以新 graph/superoperator/decoy 结构测试替换旧单纯 opcode 重编号契约，测试总数不变；应用语料另通过 **debug/release 的 public binary 与 VM stdout 一致性、原生/VM stdout 一致性**。ISA 修订 1 的真实旧示例也经过该轮包装到 ISA3 后执行对照。2026-09-08 第一阶段再将私有格式升级为 ISA4 context-token 状态机；新增 token 五层双端往返/上下文差异、嵌套深度/死状态密度、损坏拒绝和双目标语法执行回归，完整门禁 PASS174（62 单元 + 112 集成）。同日第二阶段升级 ISA5：三层 edge token 与独立 ED、动态 successor fetch、入口/抽样边 reachable neutral bundle；新增 64-context edge 往返、neutral 实际执行、poison 不执行及未知 successor fail-closed，完整门禁 PASS175（63 单元 + 112 集成）。第三阶段升级 ISA6：live dictionary descriptor 与真实 execute sequence 分离，多-op recipe 拆成全局打乱的 `sid` fragment graph；新增严格 validator-class camouflage、≥45% 密度、split/fusion 与三维 dispatch 回归，完整门禁 PASS176（64 单元 + 112 集成）。第四阶段升级 ISA7 并加入 block transport v1；新增 roundtrip/schedule diversity/wrong-seed/key non-leak/逐 ciphertext byte corruption 单元门及双目标密文篡改静默失败集成门，golden 更新为 83,640/92,116 B。第五阶段升级 ISA8：新增 8 KiB bounded LZW、whole compressed-body inner stream、二/三字段随机 Lua inverse、严格codec malformed/incompressible及semantic→完整frame size gate；golden更新为82,572/92,022 B，完整矩阵 **PASS179（66 单元 + 113 集成）**。第六阶段升级 ISA9：新增 context-bound 两节点 prototype code graph、跨 prototype 全记录池、ID/owner/next/root/count/total/coverage 严格校验和双目标拓扑 corruption 门；crate-owned VM 可安全省略无歧义可选分号，公共 minify 不变；golden更新为82,729/91,722 B；相对ISA7整脚本仅作历史信息；完整矩阵 **PASS182（69 单元 + 113 集成）**，默认 15-run bench 为 68/88 ms。第七阶段升级 ISA10：key shares 混入实际 source transcript，share 和掩码 fetch/dispatch 表示；golden 为 82,514/91,344 B，完整矩阵 **PASS184（71 单元 + 113 集成）**，bench 为 67/88 ms。第八阶段升级 ISA11：两个domain-separated ChaCha8 pass替换旧custom streams，删除Feistel并升级frame v2，解密器拆为五字段且每次decrypt执行anti-hook；golden为82,201/90,866 B，完整矩阵 **PASS185（72 单元 + 113 集成）**，bench为94/113 ms。第九阶段 ISA12-A 首先加入全 prototype 异构 operand profile、四种物理 layout 与四种 fragment binding，并删除 fixed-slot/actual-opcode 静态桥；golden为72,198/80,636 B，完整矩阵 **PASS186（73 单元 + 113 集成）**，bench为95/114 ms。ISA12-B随后加入4×127×257的per-prototype register profile、frame-local mapper和全部primitive token-level register lowering；golden为74,540/83,040 B，完整矩阵 **PASS188（74单元 + 114集成）**，bench为96/115 ms。 ISA12-C再加入受控carried-value fusion、register-family `RF`与六状态entry graph；golden为78,706/87,167 B，SHA-256为`a9ef0ee9782615c9ca2eaa5c7b5a36aaa83b8e0900fc37ec0f47b1b9db26477b` / `d43482096f2bb4f9f89e69f0607f4c508a4d82b5498c5d65a1ff8b689967c68a`，完整矩阵 **PASS190（75单元 + 115集成）**，bench仍为96/117 ms。ISA13再加入record/segment factorial与dictionary/metadata/tuple字段序置换；golden为79,426/87,873 B，SHA-256为`f6ece30c4bbdea852a8fd365d8902a1285d03618c7e0d1636469443f6904bd92` / `d5980b569759fead1099e88802890373328528d2bb281c21162fdb89c2ea49c8`，完整矩阵 **PASS192（77单元 + 115集成）**，bench为98/116 ms。

压缩口径现已明确：比较对象是压缩前private semantic bytecode与压缩后完整LZW frame；Lua脚本、decoder及历史ISA golden均不参与该判定。历史整脚本数值只用于独立膨胀/性能观察。

## 5. 不应宣称已兼容的边界

- 原始 debug 栈、环境/加载反射、源码行号、dump、错误文本中的源文件/位置；明显操作会拒绝，动态或宿主别名不保证全部识别。用户直接 `error(message,0)` 的内容与错误对象有差分测试。
- 隐藏 metatable、未验证的 Roblox executor 扩展、不同 Luau 版本/编译选项。API 本身必须由宿主提供。
- GC 触发时机、弱引用在调用中的精确可回收时点、内存占用、普通递归栈预算、地址型 tostring、时间/随机输入等非确定性观察。
- **per-prototype 惰性解码与空闲回锁只缩短明文指令词的内存驻留窗口，不构成抗 dump 保证**：某个 prototype 正在执行时它的词表与路由仍是可读明文，常量池/捕获池目前全程常驻；能运行生成脚本的人都能按同一条 K12 链重放出任意时刻的解码结果。
- **含洞 table 的 `#`、底层数组/hash 容量与未规定的遍历顺序**。VM 的普通 table 分配不模拟原生所有容量提示；已知这些布局敏感源码可能输出不同。应使用显式长度/`pack.n`、连续数组或排序后的键，不把这些案例当作已经通过的兼容性证据。
- 原生编译器所有优化相关的函数身份。已匹配本轮测试的共享策略，但大常量、vector 常量等未模拟的优化路径仍需扩展，不能据此承诺所有 function equality 都相同。
- 资源超限及仍不能证明安全的 repeat/continue 控制流。已识别不支持项返回诊断，不偷偷换成原生编译结果。

下一步继续引入真实业务语料和永久最小复现；decrypt anti-hook、per-prototype operand/register ABI、carried-value fusion、有界 entry graph、parser field-order 置换、captures/constants 全局池（ISA14）、混合 base86 transport（ISA15/K9a）、K7 多态字运算、K8 indirect route 与 K9 per-lane affine operand key、K14 装载期校验器的种子化二叉搜索树分派（去掉“线性等值扫描”这一可正则形状，装载期每记录比较数降到 log2；仅改 VM 侧文本，双目标的语法/运行门与 corruption/拒绝门原样通过）、K13c 第一步（常量池记录改为 `{tg,off,len}` 字节坐标 + `PK` 区间/覆盖门，零行为差异，为“值只由保留字节推出”的加钥形态铺路）、K16（发射脚本里重复的轮常数收进包装表字段：写入是入口体第一条语句，读点 `t.k` 遍布入口与全部 section 字段；只认这个壳形、只在写入之后才执行的作用域里改写、遮蔽包装名的作用域保持原拼写、只改规范十进制字面量并以三条守恒自检兜底 ⇒ 壳的格式、payload 表键布局、镜像与公开 `.obf` 一字未动，`luac5.1 -l` 只有 max upvalues 16→17）、K17（十进制转义按后随字符取最短安全拼写：`\28` 而非 `\028`，只有后随数字时才补零；并把 `"`/`'`/`\` 按构造排除出 base86 字母池——仍是 86 个符号、span 99、分组余量与解码器形状不变 ⇒ 纯文本瘦身，镜像字节与公开 `.obf` 未动）已完成，**第二步（ISA17：payload 加钥 + 常量生命期）也已于同日落地**：`write_constant_pool` 逐记录移位，池循环只按 tag 跳过并记 `{tg,off,len,acc}`，`DC` 用新 prelude 原语 `U32/UK/NU` 逐记录重建，值随 `LVE` 与加载期 dry-run 释放。仍然成立的边界：密钥对烘在脚本里，同时持有镜像与脚本的人可以还原常量——本批消灭的是“镜像里躺着明文常量”与“未激活帧常驻常量”。**K12/ISA16 的链式 operand lane key 只把"离线整段解出"变成"必须按镜像顺序重放记录流"，不构成不可逆、也不是客户端秘密：一次完整执行后在 lane 解出处取快照仍可还原（这正是尚未实现的 T4 段级回锁要处理的场景，当前镜像在启动时一次性解出并常驻）。**frame-tag 批次的 3 个红灯已清掉：Luau probe transcript 改回隐藏名池运行时拼装（库名/反射名零明文恢复，K7 门同时扩到 18 个词并直接审计两份 golden），两套审计 pin 按观测重录。K10 已完成：`src/vm/custom/emit.rs` 按发射阶段拆出 `emit_decode.rs`/`emit_prelude.rs`（固定 seed golden 逐 byte 零差异，`emit.rs` 81,861 → 64,639 B），体积门按实测重钉为 112,000 B/目标；“不删步骤再压体积”经三项实测判定无空间（见 `项目交接总结.md` §0.1 第 4 条），进一步缩小需要 K9b/ISA16 级的分发形态改动；函数结构变换（inlining/outlining/reorder）另批独立评估。K18 生成器侧抽样与谓词形状（三族分工 + Hull–Dobell 全周期 LCG + `index()` 去取模改乘法-位移 + 入口头部 13 项等式检查逐项改形；镜像格式与公开 `.obf` 不变 ⇒ 不升 ISA；本批还实测出一个**检测缺口**——向私有图像尾部追加一字节并修补其自带长度/校验和，运行期无人发现，已用 `tests/runtime.rs` 的 `UNPINNED_TAIL_SLACK` tripwire 钉住，其中「段校验字回灌解码状态」已于 2026-09-12 的 K19 落地（回灌目标是段内数字表的旋转量而不是 ChaCha 密钥流，因此不需要动镜像格式 ⇒ 不占 ISA 位）；同日 K20 落地了 K6 打散的生成器侧：`setmetatable` 首实参里的一次性字面量改成入口状态机 11 个注入点的**运行期分段装载**（`VMS[k]=function...end;ck=ck+k;`，同键可先装 A 再装等价的 B，最后写入者生效），run 段在调用解释器之前核对 `ck` 总和 ⇒ 装载点承重，实测装载点 20–25 处跨 10–11 个区域、表字面量只剩 4–7 个数值字段（此前 25–27）、等价写法为白名单两对 + 一条机械守卫改写（`a~=b` ↔ `not(a==b)`），零新常数 ⇒ 求值语义/错误边界/镜像/公开 `.obf` 全不变，**同样不升 ISA**；壳格式（1 物理行、语句以 `;` 结尾）实测未变（分号 373→414 / 658→691，换行 0）。剩下的「声明式段长对账」连同 `UNPINNED_TAIL_SLACK` 一起排在下次私有 ISA 版本位（那一批才升 ISA18），同一批把等价写法扩到更多 handler。**通用 anti-debug 与多组假状态/假记录另行评估。不能把 runtime witness、anti-hook、ChaCha8、segment graph、LZW 或其他可逆 transport 宣称为客户端秘密，或声称已解决全部语义同构、静态恢复和兼容问题。
