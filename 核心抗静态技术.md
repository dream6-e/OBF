# 核心抗静态技术（含具体实现方法）
### —— 本次分析中真正阻止我还原源码的技术，以及怎么把它们做对/做全

配套：`OBF_TECHNIQUES.md`（45 项技术全清单）、`REPORT.md`（还原过程与证据链）。
本文**只保留真正造成损失的 9 项**，并给出可直接实现的代码。所有位运算/编码方案都已在 Python 中
逐条验证（表法 band、xor/or/not 恒等式、16-bit limb 乘法、Newton 模逆、仿射往返、LCG 满周期、
radix-85 均匀性自检）；并且下面 §T7 的整套 Lua 工具箱已用 **真 Lua（lupa / Lua 5.5）** 跑过交叉验证：
`band/bxor/bor/bnot/mul32/inv32/仿射往返/T1 的 CFB 反馈往返` 共 **699 项断言、0 失败**。
（这个 harness 还当场抓出了本文 T4 初版 `lock` 的一个真 bug，见 T4 内的红字说明。）

---

## 0. 威胁模型（先说清防谁）

| 攻击者能力 | 预期结果 |
|---|---|
| 只有文本（纯静态：可写分析器、可做代数折叠、不执行样本） | 目标：让"失败"成为稳定属性，而不是运气 |
| 能执行样本一次（哪怕只 hook 一个函数） | 目前**必然被破**；本文只讨论如何把"1 个 hook"提高到"必须按序跑完主路径" |
| 能 dump 运行内存 | 需要 T4 的回锁才不是白工 |

**全文唯一的总目标**：

> 把"离线解出明文"归约为"必须完整执行程序（含执行顺序与宿主交互）"。
> 只要解码不再是**纯函数**（输入 = 密文 + 静态可得常数），静态还原在理论上就不可行。

我这次能走到"14,937 个 u32 字"，是因为容器层解码仍是纯函数；我停在**这一步之后**，是因为
字流→指令/常量那一段不再是纯函数。T1 的作用就是把前者也变成后者。

---

## 1. 我这次失败的因果结构（是"环"不是"点"）

```
   ┌─────────────── 载荷解出的 header ────────┐
   │                                           ▼
[静态可见的 key 常数] ← 折噪声可得      [各段解密顺序] ← 只在执行期成立 (T1/T4)
   │                                           │
   ▼                                           ▼
字流 → 通道值 ──── 需要 lane key ────→ lane key 写在 handler 站点（可折 ← 这就是我的突破口）
   │
   └─► 常量位置：没有池，按尺寸分桶、逐条惰性解 (T3) ⇒ 无法"先定位再解"
调用图：b[z[4]]，下标来自未解载荷 (T2)          ⇒ 无法"先建图再解"
```
闭环的三条边，缺任意一条我今天就能出源码。下面 9 项就是维持这三条边的最小集合。

**优先级**：T1 > T2 > T3 > T4 > T5 > T6 > T7 > T8 > T9
（T7/T9 是"别留锚点"类，成本最低，先做；T1/T4 收益最大，最后做。）

---

## T1 · 顺序依赖解码（最关键：让静态没有不动点）

**机制**：第 i 个词的密钥不是 `f(i, 静态常数)`，而是 `f(i, 已解出的前缀)`。于是解码不可交换、
不可跳跃、不可"先取 key 再取数据" ⇒ **离线解码 ⟺ 按序重放**。

**编码期（发射器，Python）**：
```python
def encrypt(words, seed, GRP=64, PHI=0x9E3779B9, PHI2=0x45D9F3B):
    st = seed & 0xFFFFFFFF
    out = []
    for i, w in enumerate(words):
        if i % GRP == 0:
            st = (st * 213 + 225) & 0xFFFFFFFF          # 段密钥推进（LCG, 见 T3 的参数约束）
        k1 = (st ^ ((i * PHI) >> 12)) & 0xFFFFFFFF
        A  = ((k1 & 0xFFFF0000) | 1) & 0xFFFFFFFF       # lane 乘子：强制奇数 ⇒ 必可逆
        C  = (k1 + i * PHI2) & 0xFFFFFFFF                # lane 加子
        e  = ((w ^ k1) * A + C) & 0xFFFFFFFF             # XOR+仿射（别只用 XOR，见 T7）
        out.append(e)
        st = (st + e * 0x9E37 + i) & 0xFFFFFFFF          # ★反馈：后续 key 依赖本词密文
    return out
```

**运行期（Lua）**——一趟顺序扫描，不额外占内存：
```lua
local U32 = 4294967296
local function u32(x) return x % U32 end
-- mul32 / inv32 / band / bxor 见 T7 工具箱
local function decode_word(st, i, v, inv_tbl)
  if (i - 1) % 64 == 0 then st = u32(st * 213 + 225) end   -- mod 2^32；写成 %256 会与编码端不一致
  local k1 = bxor(st, (i * 0x45D9F3B) % U32)
  local A  = u32(band(k1, 4294901760) + 1)             -- 奇数
  local C  = u32(k1 + i * 0x45D9F3B)
  local inv = inv_tbl[A] or inv32(A)                    -- 表命中优先，miss 才现算
  return mul32(u32(v - C), inv), u32(st + v * 0x9E37 + i)
end
```

**必须避开的四个错法**
1. **反馈必须进 key**，不能只进"下一段的 salt"——后者可被整段并行解。
2. 运行期算模逆（5 次 Newton）太贵 ⇒ 发射器把用到的 `A` 的逆**预先塞进 24–64 项小表**，
   但保留 `inv32` 现算兜底（否则表 miss 就是硬崩）。
3. key 里**不要**掺 `os.clock`/`math.random`/`tick()` 这类跨运行不稳定的量 —— 那样运行期也会崩。
   只能用程序自身确定量：执行计数、已解前缀、常量个数、upvalue 数量。
4. **必须让 state 经过密文**（CFB 而非 CTR）。若密钥流只依赖位置，攻击者可以先解出整条密钥流
   再一次性异或 —— 我这次把 key 折成 `^K` 之后想走的就是这条路。

**成本**：每词 2×mul32 + 2 次加 ⇒ 解码期 +15%（解码只跑一次，稳态可忽略）。
**残留风险**：动态执行一次 + 在 `decode_word` 出口打快照 ⇒ 破（见 §5）。

---

## T2 · 数据驱动的间接分派（切断静态调用图）

**机制**：下一跳不是常量、也不是"小整数状态"，而是 `b[ticket]`，且 `ticket` 来自**已解码数据**。
Luraph 只做了 1 处（`b[z[4]]`）——太浪费；目标是每 5–10 块一处，并对下标再做一层仿射。

```lua
eL_step6 = function(b, u, z)
  local z   = b:L(u, b[94])
  local idx = u32(mul32(z[4], b.T1) + b.T2)      -- 下标本身也折一层（b.T1 取奇数）
  return 7, b[idx](b, z, b.s, nil)
end
```
```python
perm = rng.sample(range(N_HANDLERS), N_HANDLERS)      # handler 号 → 票号：per-build 随机置换
T1, T2 = odd_u32(), u32()
ticket = lambda h: (perm[h] * T1 + T2) & 0xFFFFFFFF   # 块尾发射这个数
# 块头解回： idx = mul32(u32(ticket - T2), inv32(T1))  —— 但 idx 的真正来源是数据，不是常数
```

**要点（这条为什么值 ★★★★★）**
- 成本为 0（本来就要一次表索引），却把"重建 handler→语义映射"变成"必须确定执行顺序"。
- 关键是**同一张表同时是 handler 表 + 常量池 + 可写内存**，并且**写入点要多**：
  Luraph 只有 3 个写点（`b[94]`/`b[2]`/`b.k`），我一次穷举就完事。目标 **≥20 个写点**，
  其中若干写在"永不成立的条件"分支里（诱饵写入 ⇒ 破坏"穷举写入点"这个手法本身）。
- 至少一个槽在运行期**被替换实现**（同名同 arity、语义等价、但内部状态号全变）：
  这样静态重建出的语义表在别的路径上是**错的**，而不是不全。

---

## T3 · 无池常量：分类型分桶 + 逐条惰性解密 + 立刻消费

**机制**：静态还原的最短路径是"定位常量区 → 整段解密 → 反序列化"。**取消"区"这个概念**即可。
Luraph 做对了 80%（尺寸分桶、惰性、类型化回读），漏在 ①同型模板重复 25 次 ②PRNG 参数全局唯一。

```python
def emit_const(c, kind, rng):
    size          = bucket_of(kind)                    # 1,2,4,8,12,16 / len(...)
    salt          = rng.randrange(256)
    a, b          = lcg_full_period_256(rng)           # ★每 build 且每桶不同（约束见下）
    order         = rng.choice(["key_last", "key_first", "interleave"])
    body          = f"""
      local buf = mkbuf({size})
      local k   = ({salt} + {rng.randrange(256)}) % 256
      for i = 0, {size-1} do
        local byte = rd(src, pos + i)
        {emit_write(buf, i, f"bxor3(byte, {salt}, k)")}
        {'k = (%d*k + %d) %% 256' % (a, b) if order!='interleave' else noise_stmt(rng)}
      end
      {gap_stmts(rng, n=rng.randrange(0, 4))}          # ★ 把 typed_read 与循环拆开
      return {typed_read(buf, kind, rng)}
    """
    if rng.random() < 0.35: body += dead_noise(rng)     # 等价噪声（+0 / *1 / 冗余双条件）
    return body
```

**LCG 参数的正确约束（Hull–Dobell, m = 2^k）**
```
c 与 m 互质                （m=256 ⇒ c 为奇数）
a−1 被 m 的每个素因子整除   （⇒ a 为奇数）
若 4 | m ⇒ a−1 被 4 整除
```
已验证：样本的 `(213, 225, 256)` → 213−1 = 212 = 4·53 ✓、225 奇 ✓ ⇒ 周期 256（满）；
反例 `(212, 225, 256)` 周期只有 **5**。**随机取参后必须断言满周期**，否则会得到退化密钥流。

另外：LCG 低位质量差 ⇒ 不要用 `k % 256` 的低几位当字节掩码。取高位（`k=(k*65536)%2^32; key=k/2^24`）
或加一次非线性（`key = bxor(k%256, (k/8192)%256)`）。

**要点**
- 尺寸类要**故意交叠**（同 size 走不同 handler），否则"同型 N 份"就是模板锚点。
- `typed_read` **不要紧跟**写循环：中间插 0–3 条无关语句。我认出这条流水线，靠的正是三段连在一起。
- PRNG **家族**也要换（LCG / xorshift / 表轮转 / 斐波那契混合）：只换参数不换结构，对我来说等于没换。

---

## T4 · 惰性回写 + **用尽回锁**（让 dump 只拿到当前段）

**机制**：明文只在"被执行到的瞬间、局部地"存在。Luraph 已有回写与 `pc-=1`，**没做回锁**，
所以内存快照仍有价值。补上回锁即闭环。

```lua
local LOCKED, UNLOCKED, BUDGET = 3, 2, 15

-- 解锁：掩码链依赖"本条密文"（最强，但见下方回锁难点）
local function unlock(s)                       -- s = {from,to,b,ikey,st,masks}
  s.masks = s.masks or {}
  for i = s.from, s.to do
    local mask = SBX[(s.ikey % #SBX) + 1][band(s.b, i) % 256]
    s.masks[i] = mask                           -- ★缓存，回锁要用
    local ct   = WORDS[i]
    WORDS[i]   = bxor(ct, mask)
    s.ikey     = u32(s.ikey * 19 + ct)           -- 链式：依赖密文 ⇒ 不可并行/不可猜起点
  end
  s.st = UNLOCKED
end
-- 回锁写法 (C)【已验证】：按缓存的 mask 反放
local function lock(s)
  for i = s.to, s.from, -1 do WORDS[i] = bxor(WORDS[i], s.masks[i]) end
  s.st = LOCKED
end

-- 回锁写法 (B)【已验证，更省内存】：链只依赖 (ikey, i)，unlock/lock 共用同一段循环体
--   （XOR 自逆 ⇒ 同一 key 序列跑两遍就回到原文；代价是链不再吃数据）
--   local function xorseg(s)
--     for i = s.from, s.to do
--       local mask = SBX[(s.ikey % #SBX) + 1][(i * 13 + 5) % 256]
--       WORDS[i] = bxor(WORDS[i], mask)
--       s.ikey   = u32(s.ikey * 19 + 13)
--     end
--   end
--   unlock = function(s) s.k0 = s.ikey; xorseg(s); s.st = UNLOCKED end
--   lock   = function(s) s.ikey = s.k0;  xorseg(s); s.st = LOCKED   end   -- ★必须从段起始 key 重放

local function word(i)
  local s = STATE[SEG_OF[i]]
  if s.st ~= UNLOCKED then unlock(s); s.left = BUDGET      -- ★解锁的同时把预算装上
  else s.left = s.left - 1; if s.left <= 0 then lock(s) end end
  return WORDS[i]
end
```
**这一条我原先写错了，是实测抓出来的**：最早的版本想用
`lock = 倒放 + s.ikey = u32(s.ikey * inv19)` 省掉 `masks` 缓存 —— 64 词里 **48 个回滚不上**。
原因：链式是 `ikey' = ikey*19 + ct`，反推要 `ikey = (ikey' − ct)*inv19`，而 `ct` 在倒放时已经不存在了。
⇒ 要么 **(C) 缓存 mask**（多 1 个数组，段长 ≤ 64，可接受），要么 **(B) 链只依赖 ikey+i**（零内存，但放弃"吃数据"的强度）。
上面两段都在真 Lua（lupa / Lua 5.5）里跑过完整往返才写进本文：B、C 均 0 失配。
harness 还顺手抓出第二个 bug —— 我最初 `unlock()` 里写 `s.st = UNLOCKED`（=2），
`BUDGET` 声明了却**没接上**，结果段只解锁 2 次访问就提前回锁（表现是"解锁后 mid=0/64"，
看起来像加密没生效）。⇒ 回锁类机制必须跑「解锁 N 次访问 → 自动回锁 → 数据与原文逐词相等 → 能再解锁」
这条完整时序，光看代码看不出来。

- 掩码不要用 `(x ^ i) & 127`：样本就是这个（`E(y[Q], q)` 再 `127`），线性 ⇒ 可解。用表驱动 S-box（256 项 per-build 随机），表本身也加密存放。

**成本**：每词 1 次表访问 + 段级循环；重复解锁有惩罚（可接受，热路径通常集中在少数段）。

---

## T5 · 位置相关密钥流（消灭"相同明文 ⇒ 相同密文"）

**机制**：我这次用一条统计钉死了整份分析 —— 密文重复值之间距离的 **gcd = 64 组 = 256 B**，
它同时证明了 (a) 我的分组边界正确、(b) 密钥与位置无关。**让 key 依赖位置，这个自检位就消失。**

```python
k = (seed + i * PHI + (i >> 8) * PHI2) & 0xFFFFFFFF   # 位置进 key
k = (k * k) >> 13                                     # 一次非线性，破掉线性差分
GRP = rng.choice([32, 64, 96, 0])                     # 0 = 每段长度从载荷随机读
```
**禁止**出现固定 64/256 这类"整段对齐"常数（统计上会留下台阶）。

**自检（必做）**：对编码结果计算 `gcd{ j−i : C[i]==C[j] }`；若得到任何 >1 的公因子
（尤其恰好等于段长）⇒ 密钥流没做到位置相关。

---

## T6 · 状态票：32-bit 仿射 + 故意二义（打散 CFF 的规模信息）

**机制**：样本的状态号是 1..236 的密集小整数 ⇒ 我一次统计就估出状态机规模、并确认了 CFF。
不必加密状态（拖慢），只需 **打散 + 让状态不唯一决定节点**。

```python
labels = sorted(rng.sample(range(1, 2**32), N_BLOCKS + pad))   # 32bit 随机、无密度结构
# 分派端仍是比较链： if st <= labels[i] then ...   —— 阈值不再是小整数密集区
for grp in pick_groups(labels, K=(2, 4)):          # ★把 K 个块塞进同一区间
    for blk in grp:
        blk.head = f"if st <= {grp.hi} and tag == {blk.tag} then"
```
```lua
if st <= 2718903577 then
  if tag ~= 61 then return 2043881190, ... end     -- 别的块也落在这个区间 ⇒ 反扁平化工具会错误合并节点
  -- ... block body ...
end
```
**效果**：① "阈值分布 ⇒ 状态数" 这条估算失效；② **基于状态号的 de-flattener 会产出错的图**
（正是我那个废弃草稿的失效方向）；③ 次级 `tag` 来自寄存器轮转后的槽，静态必须做数据流才能配对。
**成本**：0。

---

## T7 · 纯算术位域工具箱（**不依赖 bit32 / buffer**，且故意不用）

为什么不用：`buffer.readu8` 在 Roblox 上信噪比极高（正常脚本几乎不调），是完美的动态咽喉点；
`bit32.*` 是完美的静态关键词。换成算术实现，两头同时变钝。
另一个理由更重要：样本里 `iL` 被调 **330 次且实现唯一** ⇒ 免费送我一个"当黑盒整体符号折叠"的锚点。

```lua
local U32, MAXU = 4294967296, 4294967295
local function u32(x) return x % U32 end            -- Lua 的 % 对正模数恒 ≥0（负数也对）

-- 8×8 位表：构造期一次生成，只用 %2 与 /2（无任何位库）
local function mkbits(op)
  local T = {}
  for a = 0, 255 do
    local row = {}
    for b = 0, 255 do
      local x, y, r, p = a, b, 0, 1
      for i = 1, 8 do
        local xa, ya = x % 2, y % 2
        local v
        if op == 1 then v = xa * ya                    -- and
        elseif op == 2 then v = xa + ya - xa * ya      -- or
        else v = xa + ya - 2 * xa * ya end              -- xor
        r = r + v * p; x = (x - xa) / 2; y = (y - ya) / 2; p = p * 2
      end
      row[b] = r
    end
    T[a] = row
  end
  return T
end
local AND8, OR8, XOR8 = mkbits(1), mkbits(2), mkbits(3)
local function compose(T, a, b)                        -- 逐字节查表合成 32 位
  local r, s = 0, 1
  for i = 1, 4 do
    local x, y = a % 256, b % 256
    a = (a - x) / 256; b = (b - y) / 256
    r = r + T[x][y] * s; s = s * 256
  end
  return r
end
local band = function(a, b) return compose(AND8, a, b) end
local bor  = function(a, b) return compose(OR8,  a, b) end
local bxor = function(a, b) return compose(XOR8, a, b) end
-- 省一张表的恒等式（已验证；bnot 完全免费）
--   bxor(a,b) == u32(a + b - 2*band(a,b))
--   bor(a,b)  == u32(a + b -   band(a,b))
--   bnot(a)   == MAXU - a              -- 全 1 减 ⇒ 逐位取反，无借位
local function bnot(a) return MAXU - a end
local function shl(a, n) return u32(a * 2^n) end
local function shr(a, n) return (a - a % 2^n) / 2^n end
local function extract(a, from, width) return shr(a, from) % 2^width end

-- 乘法：16-bit limb，最大中间值 4,294,967,297 ≪ 2^53 ⇒ double 精确（已验证 2 万次）
local function mul32(a, b)
  a = a % U32; b = b % U32
  local a0 = a % 65536; local a1 = (a - a0) / 65536
  local b0 = b % 65536; local b1 = (b - b0) / 65536
  local lo, mid = a0 * b0, a0 * b1 + a1 * b0
  return u32(lo + 65536 * (mid % 65536))
end
-- 模逆：Newton×5（x = x(2−ax)），前提 a 为奇数。反例数 0（2 万次随机 + 边界值）
local function inv32(a)
  local x = 1
  for i = 1, 5 do x = mul32(x, u32(2 - mul32(a, x))) end
  return x
end
local enc32 = function(v, A, C) return u32(mul32(v, A) + C) end
local dec32 = function(v, A, C) return mul32(u32(v - C), inv32(A)) end
```

> **注意这条真正的承重约束**：`mul32` 拆 limb 不是为了"好看"，而是因为 **Lua 的 number 是 double**，
> `a*b`（a,b<2³²，积可达 2⁶⁴）会静默丢精度。所以"算术混淆"依赖的是 IEEE754，**不是 bit32** ——
> 删掉 bit32 整个方案照样成立。发射器必须带 lint：`assert 所有中间值 < 2^53`。

**多态化（关键，否则前面的努力白费）**
- 乘法 ≥3 实现：limb 表法 / `u32(a*b)` + 精度守卫 / 移位展开 `(a<<k)+(a<<j)+…`，1:1:1 混用；
- band ≥3 实现：表法 / 逐位累加 / 德摩根对偶 `a - bor(a, b') ...`；
- **每次调用随机选**。我这次的杀招是"发现某高频 handler 语义唯一 ⇒ 整体当黑盒折叠"；
  实现一多，"它是唯一实现"这个前提就不成立。

**噪声的模数多样化**（直接封掉我的恒等式识别法）
```
禁止：噪声常数对满足 c1 + c2 == 2^32      ← 样本的破点（`iL(·,4294967295)` 出现 111 次）
改为：c1 + c2 == 2^32 ± r，且 r 由同一函数另一分支吸收（噪声项参与计算 ⇒ 不可无条件抵消）
混用：mod 2^31 / 2^32 / 2^32−1 交替；用 band(x, 2147483647) 表达
```

---

## T8 · 去掉可 hook 的咽喉（动态侧唯一有用的改动，成本极低）

| 样本现状 | 问题 | 改法 |
|---|---|---|
| 所有字节读走 `b[66]` = buffer.readu8 | 1 个 hook 看尽全部解密，且几乎无噪声 | 4 条取字节路径混用：`s:sub(i,i)`+256 项 char→值表 / `string.gsub(".", f)` 批量 / 预算好的字节数组 + 表索引 / `readu8` 只留 20% |
| 载荷只经 `buffer.fromstring` 进入 | 1 个 hook 拿到整条明文流 | 容器解码本身分块多路（每 ~512 字符换一条路径）；或把字流以 `+n` 常数内联进 handler（省掉 fromstring，代价是体积） |
| 首屏解全量 | dump 时机无所谓 | 与 T4 回锁配合：只解当前段 |

```lua
local CHAR2BYTE = {}      -- 'string.char' 只在这一行出现（"API 名只允许出现一次"原则，即样本的 A2）
for i = 0, 255 do CHAR2BYTE[string.char(i)] = i end
local function at(s, i) return CHAR2BYTE[s:sub(i, i)] end
```
`string.byte/sub/char` 是高频合法调用 ⇒ hook 它噪声极大。这正是我们要的效果。

---

## T9 · 不留"漂亮常数"：radix / 位权 / 窗口 / 长度整除性

我确认 radix-85 只花了 3 步，全靠四个可 grep 的巧合：
1. 字符集是**连续的 85 个 ASCII**（`(`..`|`），频次平坦 ⇒ 直接判定 radix=85、offset=40；
2. 位权以**字面量**出现：`52200625 / 614125 / 7225` 各**全文只 1 次** ⇒ 直接点名解码函数；
3. `2^32` 邻域常数簇（`4294967295` ×111）；
4. **长度整除性 + 统计自洽**：74,690 → 窗口 74,685 = 5×14,937；且
   `1 − 2^32/85^5 = 3.2023%` vs 实测 `3.2737%`（0.5σ）⇒ 我由此**确认自己没解错**。
   （顺带：`radix 85` 不是随意选的——`ceil((2^32)^(1/5)) = 85`，是"5 字符覆盖 u32"的最小进制。）

**改法（每条都廉价）**
```python
alphabet = permuted(range(40, 40+85))            # ① 乱序：权重不变，字符不再连续
weight4  = "((r*r)*r)*r"                          # ② 位权运行期计算，r 由 2 个常数合成
grp_mode = "mixed-4-5-6"                          # ③ 组大小混合 ⇒ 长度不再整除
padding  = True                                   # ④ 高位塞随机 padding ⇒ 溢出比例偏离理论值
```
③/④ 的战术价值大于强度：**让攻击者无法用"整除性/统计自洽"确认自己解对了**，
他会在"我错了"与"它有额外料"之间反复消耗 —— 我这次就在这里烧了 ~4 步、并两次得出过早结论。

---

## 2. 发射器整合（顺序即正确性）

```python
def protect(ast, seed):
    R   = Det(seed); ctx = Ctx(R)
    ctx.radix, ctx.grp  = pick_radix_and_group(ast, R)              # T9
    words = pack_instrs(ast, soa=True)                               # OP/A/B/C 四通道（SoA）
    words = [enc32(w, A_l, C_l) for w, (A_l, C_l) in zip(words, ctx.lane_keys)]   # T7 仿射，非 XOR
    words = encrypt_stream(words, seed, R)                            # T1 + T5（CFB 反馈 + 位置相关）
    segs  = segment_and_lock_schedule(words, budget=R.int(9, 25))     # T4
    consts = emit_lazy_consts(ast.consts, R)                          # T3（每桶独立 PRNG 家族）
    blocks = flatten_with_32bit_tickets(ast.cfg, R)                   # T6（含二义分组）
    for i, blk in enumerate(blocks):
        if i % R.int(5, 10) == 0: blk.exit = indirect_ticket(blk, ctx)   # T2
        blk.code = obfuscate_arith(blk.code, R, mode=forbid_2pow32_sum(R))# T7 噪声多样化
    payload  = armour(words, consts, ctx, R)                           # T9
    hookfree = spread_byte_reads(payload, R)                            # T8
    audit_or_die(hookfree)                                              # §3
    return emit_single_expression(hookfree)
```
**唯一的硬约束**：`pack_instrs → enc32 → encrypt_stream` 的顺序不可换，因为 T1 的反馈依赖密文。
别把 T1 塞进 armour 层——那正是 Luraph 放的位置，也正是它容器层仍可被纯静态解开的原因。

---

## 3. 自检回归套件（每次 build 跑；把我这次用的手法反过来做成 lint）

> 可运行版本已落到 **`tools/audit.py`**（本文这段是它的精简镜像，第 9 项在脚本里换成了
> "均匀性自检"而不是长度整除性）。它**不需要**任何自定义格式知识，直接对产物文本跑。
> 在本样本上的实测输出：**71 项 FAIL**，分布 = 漂亮常数 ×6 / 字符集连续 ×1 / 噪声对=2^32 ×59 /
> 阈值过密 ×1 / 开场别名串 ×2 / 重复距离 gcd ×1 —— 这 6 类正好是本文 T9/T3/T7/T6/T7/T5 要消灭的东西。
> 换句话说：**我今天用的每一个锚点，都能变成一个 build 时的断言。**

```python
#!/usr/bin/env python3
"""audit.py —— 9 项静态泄漏自检；任一 FAIL 表示这个 build 能被静态推进一步。"""
import re, math, collections, sys
from functools import reduce
BODY = open(sys.argv[1], encoding="utf-8").read().split("\n")[-1]
gcd_all = lambda v: reduce(math.gcd, v) if v else 0
F = []
# 1) 漂亮常数：单值出现 >2 次即泄露（我靠它 30 秒定位解码函数）
nice = {85**k for k in range(1, 5)} | {2**32, 2**32-1, 2**32+1, 2**31, 2**16-1, 65536, 256}
for n in nice:
    c = len(re.findall(r"(?<![\d.])%d(?![\d.])" % n, BODY))
    if c > 2: F.append(f"[1] 漂亮常数 {n} 出现 {c} 次")
# 2) 字母表连续性 ⇒ radix 暴露
m = re.search(r"PAY=\[=\[(?:.{0,8})(.*?)\]\]=\]", BODY, re.S)
if m:
    cs = sorted(set(m.group(1))); span = ord(cs[-1]) - ord(cs[0]) + 1
    if span - len(cs) <= 12: F.append(f"[2] 字符集连续（{len(cs)} 个 / span {span}）")
# 3) 噪声对之和 == 2^32（我的折叠锚点）
for a, b in re.findall(r"MUL\((\d+),\s*4294967295\)[^\n]{0,200}?MUL\((\d+),", BODY):
    if (int(a) + int(b)) & (2**32 - 1) == 0: F.append(f"[3] 噪声对 {a}+{b} 可无条件抵消")
# 4) 状态阈值密度
ths = [int(x) for x in re.findall(r"(?:st|q)\s*<=\s*(\d+)", BODY)]
if ths and max(ths) < 3 * len(set(ths)): F.append(f"[4] 阈值过密（{len(set(ths))} 个, max={max(ths)}）⇒ 状态规模泄露")
# 5) 同型模板重复
sh = collections.Counter(re.sub(r"\w+", "X", x) for x in re.findall(r"local \w+=\w+-128[^\n]{0,60}", BODY))
for k, c in sh.items():
    if c > 4: F.append(f"[5] 模板重复 {c} 次: {k[:44]}")
# 6) 开场别名串（右边全是 b[数字] ⇒ 一次还原全表）
for m2 in re.finditer(r"local ([A-Za-z_,\s]+)=((?:b\[\d+\][,;]\s*){6,})", BODY):
    F.append("[6] 别名串暴露 ≥6 个槽")
# 7) 死表诱饵（零引用 ⇒ 负分）
for m3 in re.finditer(r"(\w+)={(?:\d{6,},){2,}\d{6,}}", BODY):
    n = m3.group(1)
    if len(re.findall(r"\b%s\b" % re.escape(n), BODY)) <= 1: F.append(f"[7] 死表 {n} 零引用")
# 8) 密文自相关（应喂真实字流，这里用字面量近似）
d, pos = [], collections.defaultdict(list)
for i, v in enumerate(map(int, re.findall(r"(?<![\d.])(\d{6,})(?![\d.])", BODY))): pos[v].append(i)
for p in pos.values(): d += [b - a for a, b in zip(p, p[1:])]
g = gcd_all(d)
if g > 1: F.append(f"[8] 重复距离 gcd={g} ⇒ 密钥流位置无关（见 T5）")
# 9) 长度整除性（T9③ 生效则应为"不整除"）
if m and len(m.group(1)) % 5 == 0: F.append("[9] 载荷长度被组大小整除 ⇒ 框架可自检（见 T9③④）")
print("\n".join(F) if F else "audit: clean"); sys.exit(1 if F else 0)
```
```
$ python3 tools/audit.py "Luraph15💀💀.lua"          # 对产物跑
$ python3 tools/audit.py out.lua --words /tmp/words.txt   # 第 8/9 项喂真实字流（更准）
```
第 8 项（重复距离 gcd）对**真实字流**跑最准；不喂 `--words` 时脚本会自己按 5 字符组粗估
（注意它是启发式：不复刻 T9 的窗口/头长度逻辑，所以组数可能与真实解码略有出入）。
把 §3 挂进 CI：**任一 FAIL = 那个 build 能被静态推进一步。**

---

## 4. 成本 / 效果

| 项 | 工时 | 体积 | 运行期 | 消灭了我在用的哪个手法 | 强度 |
|---|---|---|---|---|---|
| T1 顺序依赖解码 | 1–2 d | 0 | 解码期 +15% | "容器是纯函数"（我走到字流全靠它） | ★★★★★ |
| T2 间接分派 + 写入点分散 | 0.5 d | 0 | 0 | 穷举 `b[N]=` 写入点 | ★★★★★ |
| T3 无池常量 | 改 3 处 | +5% | +2 查表/常量 | 认同型模板 / grep LCG 常数 | ★★★★☆ |
| T4 回写 + 回锁 | 1 d | 0 | 段级重解 | dump 当前明文段 | ★★★★☆ |
| T5 位置相关密钥流 | 0.5 d | 0 | +1 mul/词 | 重复距离 gcd（我的定案证据） | ★★★★☆ |
| T6 32bit 状态票 + 二义 | 0.5 d | 0 | +1 比较/块 | 阈值密度估规模、de-flattener | ★★★☆☆ |
| T7 纯算术位域 + 多态 | 2 d | +8% | −（省 API） | 高频唯一 handler ⇒ 黑盒折叠 | ★★★★☆ |
| T8 去咽喉 | 1 d | 0 | 小 | hook 一个函数全开 | ★★★☆☆ |
| T9 无漂亮常数 + 去整除 | 0.5 d | 0 | 0 | radix 判定 3 步 + 统计自检位 | ★★★☆☆ |

**落地顺序**：T5/T6/T9（各半天，纯发射器改动，立刻让 §3 的自检有产出）→ T7/T2（结构性）→
T1/T4（需回归保证正确性，风险最高、收益最大）。

---

## 5. 诚实的残留弱点（这些技术挡不住什么）

1. **一个 hook 就够**：在 `decode_word` 出口或 `WORDS[]` 打快照即全开。T1/T4 只把"1 个 hook"
   变成"必须在正确顺序、且在回锁窗口内截获"。再往上只剩"把解码做到不可中断"（VM 内联 + 完整性联动），
   已超出静态范畴。
2. **可执行性风险落在 T1/T4**：反馈式解码 ⇒ 不能部分执行；段被跳过又回锁就必须重放。
   因此需要：`unlock` 幂等 + 每段 1 个校验字 + **校验失败走"语义错误分支"而不是报错**
   （让插桩者拿到"能跑但结果不对"的产物，比崩溃难查得多）。这条我认为是本文里第二值钱的设计。
3. **double 精度**：T7 全部依赖"中间值 < 2^53"。任何直写 `a*b` 都可能**静默错值**（不崩、只是错），
   发射器必须有 `assert max_intermediate < 2**53` 这条 lint。目标若是 Lua 5.3/5.4 整数模式，
   整套要改成 `//` 与原生 `& | ~`。
4. **"参数随机、结构不变"是不够的**：我认模板靠结构不靠常数（T3 要点）。同型实例数 >4 就会被统计抓住，
   §3 第 5 条就是给这条定的门限。

---

## 6. 一句话版

我这次输在三件事：**解密的 key 在时间之后（T1/T4）、调用图在数据里（T2）、常量根本没有池（T3）**。
`bit32` / `buffer` / 单行 / 短名 / CFF 只消耗了我的步数，没有消耗我的可能性。
所以预算应该全押在"**让解码不再是纯函数**"上，其余都是化妆。
