# OBF v2：AST → IR → Varint Register Bytecode

更新：2026-09-10。格式版本 **2**，默认 ISA 修订 **2**（仍兼容修订 **1**）；生成脚本内嵌的 private wire 现为 **instruction encoding 1 / ISA15**（`WIRE_ISA_VERSION = 15`，`src/vm/custom/semantic.rs`），并含 K8 per-prototype indirect route、K9 per-lane affine operand value key 与 2026-09-10 起的 keyed dual-lane frame tag。指令操作数以 **7-bit varint** 序列化：小数值 1 byte，较大值 2~N bytes；逻辑指令与 ISA 编号不变。本规范描述新自定义后端，**不是旧 OBF v1 的原生指令重新打包**。

## 1. 编译链路和边界

```text
UTF-8 source → 现有 owned AST / BindingId 解析
            → ir::Module（函数、寄存器、常量、捕获、基本块、符号后继）
            → 指令选择 / 基本块地址回填
            → canonical OBF v2 / ISA2（opcode + 7-bit varint 操作数）
            → Rust 校验
            → 生成脚本时：seed semantic lowering → private ISA15 recipe/edge/segment/pool graph
            → per-prototype operand ABI（4 family × 3 rotation × 2,731 sparse lane）
            → per-prototype register ABI（4 family × 127 stride × 257 shift）
            → per-id/slot field-order ABI（record 4! × segment 3! factorial 置换）
            → carried-value fusion / six-state entry graph
            → global cross-prototype code-segment pool
            → bounded LZW → inner ChaCha8 → strict frame v2 → outer ChaCha8/混合 base86
            → anti-hook + 逆序 frame/semantic 校验 → dynamic token fetch / 寄存器解释器
            → 完整生成后一次随机短名 + 单行化 + 绑定图复核
```

编译、编码、解码、VM 生成均只依赖 Rust `std`，不调用 `luac` 或 `luau-compile`。固定原生工具只用于差分/语法测试及显式旧后端。

- IR 是非 SSA 寄存器 IR，有独立的 typed instruction 和 terminator，不含 native word/AUX。逻辑指令保持 (opcode, A, B, C) 四字节 word；varint 只作用于文件序列化层。
- 常量池、prototype、寄存器和 upvalue 索引均从 **0** 开始；pack 内容从 **1** 开始，并有显式 `n`，不会用 `#table` 计算多返回值数量。
- 在公开 IR/ISA 语义中，参数 cell 占用逻辑 `R[0..parameters-1]`。其余局部绑定由 `NewCell` 创建；临时值是普通寄存器值。生成脚本的 ISA12-B runtime 会把这些逻辑编号按 prototype 映射到不同物理 table key，不改变公开 bytecode。
- 所有 local 都是 heap cell，闭包持有 cell 的引用。退出作用域 `Clear` 释放寄存器引用；复用寄存器/进入新循环轮次创建新 cell，不修改已逃逸闭包的旧 cell。无需原生 `CLOSE` 或可变宽捕获指令。
- `Call` 支持 VM 函数、宿主函数、元方法与回调；`TailCall` 对 VM 函数用 frame 循环替换，而非增加解释器调用栈。普通非尾递归仍使用宿主栈。
- 两端分别处理重复赋值、局部寄存器别名/冲突、方法查找时机、numeric-for 准备、表构造器列表刷新、Lua 5.1 隐式 `arg`、Luau generalized iteration/插值/整数/只读导出表。
- Lua 5.1 做必要的纯数值字面表达式折叠及其 `+0/-0` 常量去重规则；这不是对 `minify` 增加优化。Luau 类型、类型函数、泛型和 attribute 不成为运行时指令。

`.obf` 文件本身保持明文 canonical：**不含加密容器、压缩、随机 section、随机 opcode 或随机 dispatcher 布局**；同源同目标的文件字节与 seed 无关。这里描述的是可保存、可 inspect、可 round-trip 的公开 ISA2。生成脚本不再逐字节嵌入这段指令流，而会先从其已验证 `Program` 构造下面的 private ISA15，再叠加 bounded LZW、双 ChaCha8 与 frame v2 transport。Adler-32 是损坏检测，**不是认证、签名或加密**。

### 生成脚本 private ISA15 camouflaged-descriptor/fragment/segment/pool image

`virtualize` 和 `wrap-bytecode` 在 `src/vm/custom/semantic.rs` 中执行第二次、seed-specific 的语义 lowering；`compile` 输出格式与公开 API 不变：

1. 以入口、Jump 目标、控制后继和 Test 的 true/false 后继切 CFG 边；控制 primitive 只能位于 recipe 末尾，Test 后第一条强制 singleton。每个原始直线 bundle 含 1~4 个 primitive，全程序相同真实序列共享 recipe，并分配随机非零 `u16` ID。
2. ISA5 的 reachable neutral split 完整保留：每个真实 prototype 入口插入 2~4 个中性 bundle，并从真实 fall-through/Test 边抽取至多 5 条、各插入 1 个。通常为 frame 增加一个 scratch register，执行 `Nil/Not/Move/Clear` 后恢复无引用状态；256-register 满帧退化为 `Move r0,r0`。这些节点有真实入边且实际 fetch/执行，插入后与原 bundle 一起物理洗牌。
3. 每个 prototype 的 record 仍为四个 u16 槽位加 operands，但槽位的语义归属按 profile 置换（见第 17 条）：canonical 顺序仍是 `label,next_token,skip_token,recipe_token`，实际物理槽位是其 per-prototype factorial 置换。两个三层 edge token 绑定 source label、prototype、edge kind 与交叉项；五层 recipe token 绑定 label、ED 动态解出的 next/skip、prototype 与交叉项。wire 和持久 `code[label]` 均不保存明文 successor、recipe ID、ordinal 或 instruction offset。
4. **dictionary 不是语义真值表。** Rust `SemanticRecipe` 分离 `descriptor_ops` 与 `execute_ops`：wire 只写前者，真实 handler 只按后者生成。live descriptor 仅可在六个严格安全 family 内随机替换；每一项必须同时保持目标支持性、`encoding_form`、具体 operand validator 和 control/terminal graph class。固定回归要求超过三分之一 live recipe、至少 45% live primitive 的 descriptor 与实际执行语义不同。
5. 目标 decoder 按伪 descriptor 读取 canonical operand varint、运行对应边界 validator，并使用 descriptor 的最后一项验证 graph control class。损坏输入不能借 camouflage 绕过检查。每次 fetch 仍经 3-live/3-dead ED 与 5-live/5-dead RD 动态恢复后继和 `rid`。
6. **真实 recipe 不对应逐 primitive 原始 handler 串。** ISA12-C 在每个最长4项的 `execute_ops` 上枚举最大非重叠配对。相邻项只有同时满足“首 handler 是单语句 `R[a]=expr` producer；次 handler 为单 register 赋值或不直接写 register slot，且至少含一个 register read”时才形成 2-op fragment；control、多写、嵌套 `R[R[..]]` 与其他未证明形态保持各自 1-op fragment。每个 pair 先保存首项的逻辑目标、提前求出的物理 key 与只求值一次的结果，并保留原 register write；次项所有安全 read 改为 `RF(index,target,value)`，相等时直接消费 carry、不等时从当前 `RX` mapping 读取。fragment 仍获得随机 `sid`，`rid` 只路由入口，全部 arm 跨 recipe 洗牌。
7. prototype 按 parent-before-child 约束洗牌兄弟，parent 与 Closure Bx 同步 remap；另插入 2~4 个无 live Closure 入边的 synthetic subtree。每个 image 保留四个 graph-unreferenced poison recipe；它们与 graph-referenced neutral bundle、live descriptor camouflage 是三种不同机制。
8. **ISA15 保留的 global prototype code-segment graph**：先为每个 prototype 单独完成 recipe dictionary 与 record code image，再按 `token_layer[0].{add,multiplier}` 和 owner 推导 1..`code_len-1` 的非空切点，形成恰好两个 segment。prototype header 原 reserved u16 存 context-masked root；所有 prototype metadata/capture/constant 完成后，全部 segment 作为一个池做全记录 seed shuffle，metadata 后不再出现该函数的连续 code body。
9. 每个 pool record 写三个 u16 token再接 payload，但 token 的物理槽位按 per-segment factorial 置换（见第 17 条），不再固定为 `id,owner,next` 顺序。token 定义仍为 `id_token = id + add + physical_slot×multiplier`、`owner_token = owner + add + id×multiplier + physical_slot`、`next_token = next + add + id×multiplier + owner`（均模 65536），payload 边界仍由 owner/part/总长推导。每个 owner 的 root id 为 `2×owner+1`，terminal 为 `root+1` 且 next 必须为 0；wire 不保存明文 offset/length。
10. 目标 parser 固定读取 `2×prototype_count` 条记录，先验证 ID 位于完整域且不重复、显式 owner 等于 ID 推导 owner，再按 metadata root 遍历恰好两节点。root 必须等于 `2×owner+1` 且全局唯一，链不得 cycle/跨 owner/提前终止/越界，terminal next 必须为 0；parser 最后逐 ID 核对全局 used 集，每 prototype 总长、pool 末尾位置也必须同时精确一致。因此 duplicate、missing、错误 owner/root/next、截断和尾随均在 recipe parser 与用户代码前 fail closed。
11. **ISA12-A per-prototype operand ABI**：`src/vm/custom/lowering.rs` 从独立 seed stream 生成 family、rotation、lane 三个仿射分量。模数 4、3、2,731 两两互素，乘数在各自模环中可逆，组合周期为 32,772；因此 private image 允许的全部 32,767 个 prototype id 都得到不同 profile，而不是一张完整 profile 表。四种 family 分别存储旋转 packed-u24、反向 AC+B 双区、旋转转置三列、反向嵌套 tuple；lane 的稀疏基址为 `6+13×lane`，不会覆盖 record 固定头 `[1..5]`。
12. 目标 parser 在 operand varint 和 validator 通过后，按当前 prototype 的 profile 直接写入 record；graph validator 与 runtime fragment 共用 `OG(fid,I,qi,form)` 恢复 `a/b/c/k/j`。fragment 端不再出现固定 `I[6+3*i..]` 读取或 `o=<actual opcode>` 赋值；四种 `form` 改变五返回值顺序，调用处使用匹配 lvalue，按 operation 轮换并在每四项后重洗牌。getter/store 全局共享，不复制每函数整套 handler，故代码尺寸有界。
13. **ISA12-B per-prototype register ABI**：另一独立 seed stream 生成 family、stride、shift 三个可逆仿射分量，模数 4、127、257 两两互素且组合周期 130,556，因此完整 32,767-id 私有域无 profile 重复。family 选择四个互斥的 257-key bank，域内使用正向仿射、反向仿射、逻辑逆序仿射或 shift-before-multiply 形态；每种都把合法逻辑寄存器 `0..255` 单射为物理 key。目标端每 frame 只由 `fid` 计算一次 mapper，不保存 256 项映射表；`SETUP` 的参数/legacy vararg cell与全部 primitive handler共用它。生成器以 lexer token span 包裹模板中的每个 `R[index]`，因此 `Clear` 区间、numeric-for 相邻槽、`Closure` 的动态 `d[2]` capture、cells/upvalues/callbacks/tailcalls 都遵循同一 ABI，字符串和注释不做全局替换。
14. **ISA12-C register-family dataflow 与 entry graph**：`RK(fid,R)` 现在共同返回 `RX` 与 `RF`。四个 family 的 forwarding predicate 分别使用逻辑相等、反向不等分支、物理 key 相等和 `(index+shift) mod 257` 相等，命中时直接返回显式 value 参数，所以 `nil/false` 不经过有损的 `and/or` 选择；不命中才读取 `R[RX(index)]`。handler仍按recipe全局共享，不复制到每个prototype。顶层 entry 另将 prelude、三share probes、混合 base86 segments/watermark、decrypt+LZW+parser、validator/runtime bind、execute 六阶段放入 seeded state machine：状态值、文本 arm 顺序和比较拼写变化，依赖顺序与每阶段 fail-closed 不变。
15. **ISA15 保留的 runtime-witness + ChaCha8 material schedule**：三个 key probe 各自调用目标 debug source API；Luau 折叠 `debug.info(loadstring,"s")` 再追加 6 个库/反射名（`buffer|bit32|table|freeze|debug|info`），Lua 5.1 折叠 `debug.getinfo(loadstring,"S").source`，生成三个 share。追加串**不是字面量**：prelude 用隐藏名池的 6 个名字 local 拼成单个 `PTS` 并作为额外参数下发给三个 probe（probe 是 VMS 兄弟条目，看不见 prelude local）；Rust 侧 fold 与 Lua 表达式由同一份 `PROBE_TRANSCRIPT_NAMES` 推导，产物中不出现任何库名/反射名明文（K7 门同时审计探针 emit 与两份 golden）。随后把 shares、forms 重建的 `pv`、outer length/inner LZW context、domain 和 anti-hook attestation组成 base state并执行 ChaCha8 block，输出最终 256-bit key、96-bit nonce 与 counter。Rust 侧 transcript 为 `[C]`+名字串 / `=[C]`，attestation 取规范值；脚本不保存 fold、share、frame key 或两个 domain 的最终 material 数字字面量。
16. **ISA15 保留的 data-dependent fetch/dispatch 与 ISA11 transport**：入口在三个 share 都已重建后把 `(c1+c2+c3) mod 65520` 写入一个私有短字段。fetch/dispatch 各有两套互异 seeded 物理 state，运行时 `mask mod 2` 决定实际一套；初始化、短路条件、比较和转移再将所选 state 表示为 `(state+mask) mod 65521`。两个 domain-separated 标准 ChaCha8 pass、strict frame v2 和每次 decrypt 前 anti-hook 原样保留；旧 custom XOR streams 与 generalized Feistel 不再生成。旧私有 ISA 不是公开兼容格式。
17. **ISA15 保留的 parser field-order de-canonicalization**：record 的 4 个 u16 槽按 `(prototype_id×M+A) mod 24` 的 factorial 置换逐 prototype 重排，segment 的 3 个 token 按 `(physical_slot×M+A) mod 6` 逐 segment 重排；乘子 M 固定取自与阶乘互素的候选集（4 槽 8 个、3 token 2 个），保证全范围滑窗覆盖全部 24/6 种顺序。Rust 编码/`decode` 与生成 Lua parser/validator/fetch 用同一 id/slot 实时重算同一映射，不输出逐项对照表。dictionary entry header、prototype metadata 的 u32×4/u16×3/u8×2 宽度组与内存 record tuple 因无逐项 id，改用 per-image 置换直接 baked 进 parser 文本（`sont` keyed 查找）。固定 u16/u32/u8 slot 宽度、opcode/edge/recipe 语义与全部 validator 形状不变；错序落入既有 label/token/operand/count 门，在用户代码前 fail closed。

18. **ISA15 保留的 capture/constant pools（ISA14 引入）**：capture/constant不再跟随所属prototype连续存放，而进入两个全局随机池。每个 prototype 只留 **24 字节 metadata**（`u32×4` = parent / 常量数 / bundle 数 / code 字节数，`u16×3` = 寄存器数 / 捕获数 / masked segment root，`u8×2` = 参数数 / flags，槽位归属走 per-image 置换），全部 metadata 之后先 capture 池后 constant 池（`pools_flipped` 按 image 翻转）；镜像级 Header 为 **32 字节**，与公开格式同形（`OBF\x02` + target/`1`/encoding `1`/width `0` + header_size=32、image length、prototype count、entry=0、`WIRE_ISA_VERSION`=15、覆盖 `[32,len)` 的 Adler checksum）；每记录写3个匿名u16 token再接payload，token携带掩码owner/slot/index/tag并按per-record factorial profile重排（seed与掩码per-record独立）。目标parser先验证两池再按owner切片；池错序落入fail-closed门。

目标端只接受 instruction encoding `1` / ISA `15` 的内嵌 image，并先验证 capture/constant 两池总量/掩码/owner/slot/index/tag 与全局洗牌 coverage，再验证 segment ID/owner/next/root/count/total/coverage，再逐项验证 dictionary ID/长度/descriptor、operand 最小 varint、per-prototype operand/field-order profile、label 唯一性、ED 解出的 entry/next/skip/Jump 后继、控制位置、prototype parent/capture/Closure 关系与精确字节消耗，最后建立 token-only `code[label]` 图。公开 `.obf` 仍是 instruction encoding `0` / ISA `1|2`，两者不可混用。

这一层进一步切断或削弱了固定 operand slot、实际 opcode marker、全像统一逻辑 register key、逐 primitive 原始 handler 拼接边界、顶层稳定线性入口链、统一 record/segment/dictionary/metadata 字段顺序及 per-prototype capture/constant 邻接；但 primitive Lua 运算、pool token 掩码、register/dataflow/field-order mapper、fusion eligibility、factorial 置换规则、规范环境、ChaCha decoder、parser字段、profile/stage计算及segment inverse仍随脚本交付，故仍可通过执行或完整静态模拟恢复。runtime material不是客户端秘密，LZW也只压缩可逆表示，**不改变这一威胁结论**。captures/constants 全局池、K8 per-prototype indirect route 与 K9 per-lane affine operand value key 已完成；下一批（K10）应做体积 shrink 与静态表面收紧，而不是继续堆 transport/encoding。

### ISA15 嵌入 payload：bounded LZW、双 ChaCha8 与 anti-hook

生成顺序固定为：

```text
ISA15 semantic image（含全局 code-segment graph 与 capture/constant 池）
  → bounded LZW frame
  → inner ChaCha8（仅 bitstream，domain=2）
  → strict transport frame v2
  → outer ChaCha8（整帧，domain=1）
  → XXS: watermark / 混合 base86 segments
```

目标端严格逆序处理；outer/inner 每次 ChaCha8 调用前都先执行 anti-hook，任何一步失败均在 semantic parser 和用户代码之前调用 `E()`，不返回部分 bytes。

#### Bounded LZW frame

实现位于 `src/vm/custom/compress.rs`，只依赖 `std`。选择 LZW 而不是 ZSTD，是为了避免新增 Rust 依赖和目标端大型 ZSTD decoder。算法无损；Rust round-trip 必须逐 byte 等于输入，不做替换字符、部分输出或容错恢复。

| Offset | Bytes | 字段 | 规则 |
|---:|---:|---|---|
| 0 | 4 | magic/version | `LZW` + 数值 byte `1` |
| 4 | 4 | original_len | `1..16,777,216` |
| 8 | 4 | bit_len | 精确有效 bit 数，必须非零 |
| 12 | 4 | adler32 | 覆盖完整解压后 ISA15 image |
| 16 | `ceil(bit_len/8)` | code bits | LSB-first；末尾未使用高 bit 必须为零 |

Welch dictionary 在每个**解压输出** 8,192-byte 边界重置。每段最多 8,192 个 code，prefix/suffix dictionary、展开栈与遍历工作均有明确上界。Code canonical prefix 为：dictionary reference `00 + (code-256)`；低 literal `01 + 5 bit`；其余 literal `1 + 8 bit`。第一 code 禁止 reference，`code==next` 只允许标准 KwKwK；低值 literal 使用全宽形式必须拒绝。

Rust/Lua decoder 都要求 frame 总长、`bit_len`、reset 输出、dictionary range、prefix chain、phrase/output bound、最终 bit position、zero padding、输出总长和 Adler-32 全部一致。完整 frame 不严格小于原像时 encoder 返回 `Diagnostic`；信息论上不可能让所有输入无损且严格缩小，因此没有伪造的 raw fallback。当前golden seed按此边界实测为Lua51 `11,872→10,703 B`、Luau `15,203→14,653 B`，右侧均包含16-byte header。

#### 标准 ChaCha8 与运行时 KDF

实现位于独立 `src/vm/custom/chacha.rs`。`chacha8_block` 使用 IETF state：4 个常量 word、8 个 key word、32-bit counter、3 个 nonce word；执行 4 次 column+diagonal double round（共 8 rounds）后 feed-forward。quarter round 的 rotate 常数为 16/12/8/7。单元测试固定 zero-key/zero-counter/zero-nonce published vector，首 32 bytes 为 `3e00ef2f895f40d67f5bb8e81f09a5a12c840ec3ce9a7f3b181be188ef711a1e`。

Lua 5.1 没有可依赖的 `bit`/`bit32`，故用纯算术多态工具箱（`src/vm/custom/bitops.rs`）：XOR 与 rotate 各 3 种结构变体（字节合成/nibble 表/逐位循环、mul-div/嵌套/`2^n` 乘子），每 image 为 hot xor/rotate 各抽两种组成双闭包，quarter round 交替调用。Luau 则经隐藏名池捕获 `bit32`/`buffer` 方法（库名与方法名永不拼写），同样每操作 3 变体（直接调用/buffer 表组合/De Morgan 重组）加双闭包。双目标所有加法先模 `2^32`，模数拼写（字面/减法三明治 `((a+b)-c)`/双模字面外层）与求和项序逐 use-site 抽取；三明治任两数之和永不等于 `2^32`，除法只除 2 的幂，中间值恒低于 `2^53`。两条路径都与 Rust vector/round-trip 及真解释器向量 harness 对照。

每次 stream 的 base key 输入由 `s1/s2/s3`、`pv`、context、domain、attestation 与 8 个 seed salt 组成；base nonce 由 context、attestation、shares/`pv` 与该 domain 的 3 个 nonce salt 组成。先用 domain 对应 counter 执行一次 ChaCha8 block，输出 word 1..8 作为最终 key、word 9..11 作为 nonce，word 12 与 base counter/domain 组合为 stream counter。outer context 是完整 frame ciphertext length；inner context 覆盖 `original_len*31 + bit_len*17 + checksum*7 + reset_count*13 + body_len (mod 2^32)`。最终 key/nonce/counter 不序列化为 Lua literal。

两个 domain 使用同一标准 block primitive但 domain/context/nonce salt/counter 分离。inner 只保护 LZW body，header 保持 clear 以先做 allocation bound；header 全字段又参与 inner material，并被外层 frame/tag 与 outer ChaCha8 覆盖。stream counter 每 64 bytes 加一。

#### 五字段组合与 anti-hook

目标端不是一个可独立定位的单体 decryptor。wrapper key 向量扩为 29，新增五个 sibling numeric fields：

1. 32-bit XOR/rotate factory；
2. quarter-round factory；
3. ChaCha8 block factory；
4. domain-aware KDF/stream；
5. anti-hook/attestation。

五者与 parser、LZW、probe、segment、interpreter 等全部字段一起全局洗牌；entry 运行时才按 numeric key 取得局部引用并组合。LZW bit reader/dictionary 仍按 seed 选择二或三字段形态，因此物理位置和总 field count 都不固定。

anti-hook 是直接 numeric-keyed field，outer 与 inner 各调用一次。它通过隐藏捕获的 debug API 检查：`loadstring`/`string.byte` 为目标规范 native source；anti-hook、stream、ChaCha block 与 X8 为同一 generated chunk；`string.byte/char`、`table.concat`、`math.floor`、X8 行为正确；最后对 block helper执行 zero-state ChaCha8 KAT 并检查 word 1/8/16。规范 source transcript 与 KAT word 再折入 32-bit attestation，作为 KDF 必需输入。错误 metadata/hook/helper/KAT 立即失败；错误 attestation 即使被强行返回，也会派生错误 outer key并由 frame v2拒绝。

`scope::generated_vm_plan` 只审计 numeric field 的 direct statements，因此 gate 不藏在 nested closure。当前严格允许 1 个 `getfenv/_G` capture 和恰好 **12** 个 probe observations：3 share + 3 segment + anti-hook 内 6 source observations；部分缺失或额外集合均拒绝。

#### Strict transport frame v2

Feistel 已从当前生成链删除；ChaCha8 负责保密性，紧凑 frame v2负责 fail-closed framing/integrity：

| Offset | Bytes | 字段 | 规则 |
|---:|---:|---|---|
| 0 | 4 | descriptor | low byte version `2`、header width `16`、runtime-keyed high 16 bit |
| 4 | 4 | payload_len | ≤16 MiB，必须推出当前精确 padded length |
| 8 | 4 | cookie | 绑定 length、descriptor 与两个 runtime frame states |
| 12 | 4 | tag | 2026-09-10 起为 **keyed dual-lane integrity hash**：两条模 65,521 lane（`left=(left*257+b)`、`right=(right*263+b+left)`）以两个 frame key state + `tag_salt`/`cookie_salt` 起始逐字节推进，再混入 cookie(×17)、descriptor(×31) 压成 32 bit；**不再是 payload Adler**，仍是损坏门而非 AEAD |
| 16 | `payload_len` | payload | inner-ChaCha8-protected LZW frame |
| tail | 0..3 | padding | 每 byte 由 frame states、index 与 salt 推导并逐一校验 |

frame states 仍由三个 shares、`pv`、完整 frame length、两组非对称系数与 salts 在运行时生成。目标端依次检查总长/4-byte 对齐、descriptor、原长、exact padded length、cookie、tag 和每个 padding byte，全部通过后才返回 payload。该 tag/frame 用于有界损坏检测与 fail-closed，不宣称是经证明的 AEAD/Poly1305。

#### 混合 base86 段传输（ISA15）

`XXS:` 水印加 outer ciphertext 后按字节三等分（part 0 含水印头），每段独立混合 base86，各段自带长度前缀、链式宽度每段重置：4-char 前缀存段字节数（按 4-char 组同样加随机填充），随后按字节驱动的 4/5/6-char 组（载 3/4/4 字节，宽度取链式 `prev % 3`），1/2 字节余量用 2/3-char 尾组。所有组值叠加随机高位填充（`86^width` 冗余空间内均匀取），解码按固定字节宽截断丢弃。每 image 字母表为 28..=126 中逐 seed 的 86 子集（强制含 28/29/125/126，跨度恒 99，再逐 seed 丢 13 个并重排），以 8 个 sub-12 片段 baked 进脚本，解码器拼成 `ALPHA` 后建 `VAL[byte]=digit` 表，不再做算术 digit 映射。基数 86、`MM=256`、`M24=2^24` 全部以避开审计漂亮常数集的随机加法对隐藏；slot 键避开 85/86、状态号避开 256。每段解码是独立 4 状态机（prefix/main/tail/done，per-seed 状态号、分支洗牌），结尾做精确 end-of-string 检查；`~` 标记字节改为 `\127`（池外），forms/诱饵打包串永不混入段收集。验证侧逐段解码后尝试六种段序，仍要求水印、双 ChaCha8、frame v2、LZW 与 semantic Adler 唯一通过。

#### API、测试与边界

`extract_embedded` 去掉 outer ChaCha8 与 frame v2，返回 header clear、body 仍由 inner ChaCha8 保护的 LZW frame；`decrypt_embedded` 再解 inner并严格解压，结果必须等于同 Program/seed 的 deterministic ISA15 image，而故意不等于 canonical `.obf`。

专项门覆盖 ChaCha8 published vector、双 domain/context/material 差分、stream round-trip、wrong attestation/witness/seed、0..3 padding、每个 outer ciphertext byte corruption、compression 全截断/畸形/逐 frame byte损坏、行为等价 `string.byte` wrapper hook、generated block helper tamper及双目标 native syntax/runtime。压缩大小只比较**压缩前 private semantic bytecode**与**压缩后完整 LZW frame（含16-byte header）**，后者必须严格更小；生成Lua中的decoder、ChaCha8、anti-hook和包装不属于压缩数据，整脚本预算是另一条独立回归门。

ciphertext、decoder、salt、规范 source transcript 与全部 inverse 同时交付，所以攻击者仍可执行或精确模拟。ChaCha8/anti-hook提高静态恢复与 hook 成本，但不是服务端秘密、签名、不可逆保护，也不替代 semantic graph 的反同构工作。公共源码 finalizer继续分号优先；只有 crate-owned private VM在无歧义边界省略可选分号，最终仍重解析并核对绑定。

### 生成 VM 的私有字段短名

协议中的 `code/tags/parent/flags/shared/self/cached/control` 等是说明性名称，不要求运行时 Lua table 保留这些拼写。完整 VM 生成后，`vm::fields` 对显式标记的 **16** 字段 schema（`k tags u parent m p flags nu nk nc shared self code cached control routes`）分配无冲突 `[a-z]{1,2}`，当前均为单字母；constructor/decoder/operand validation/handler/closure cache/control mask/route table 统一改名，再执行原有最终 local 命名和分号策略。

字段映射只存在于 Rust 生成阶段，不写入 OBF 数据或运行时映射表。它不更改 Header、ISA、常量、指令或字节码 seed 独立性，也不改宿主 API、元方法、用户字段/导出/静态方法名。字段变换复核所有其他 token/字面量不变，不能当作针对任意用户表的通用压缩。

### 整体输出包装

最终脚本的 chunk 层只有两条语句：`local x={}` 与 `return setmetatable({...},x):m()`。全部 decoder/校验/runtime/handler/执行尾部以**多个函数**的形式存放在载荷表内：5 个随机数字键 section 函数（宿主捕获预导、bytecode decoder、操作数校验、CV/SV/Lookup 辅助、含 W/Call/Make/H 互相递归簇的解释器段），以及随机单字母字符串键 `m` 的入口函数 `(VMS,...)`。section 之间用参数/返回值传递状态；`:m()` 直接命中载荷表自有键，入口按顺序串联各 section 并返回程序结果。仅当入口 prototype 实际包含 Varargs 指令（chunk 读取 `...`）时调用写为 `:m(...)` 转发 chunk varargs，否则为 `:m()`。方法名与数字键来自独立 seeded 随机流；空表 `x` 与数字键不做字段压缩，bytecode 与其他命名流不受影响。旧 `--backend native` 不使用此包装。

## 2. 固定 Header：32 bytes

所有多字节数均为 little-endian。

| Offset | Bytes | 字段 | v2 值/规则 |
|---:|---:|---|---|
| 0 | 3 | magic | ASCII `OBF` |
| 3 | 1 | format_version | `2`（数值字节，不是字符 `'2'`） |
| 4 | 1 | target | Lua 5.1=`0x51`，Luau=`0x75` |
| 5 | 1 | endianness | `1` = little-endian |
| 6 | 1 | instruction_size | `0` = 7-bit varint 变长指令流（2~7 bytes/条）；旧值 `4` 已被取代 |
| 7 | 1 | flags | `0`；未知位拒绝 |
| 8 | 4 | header_size | `32` |
| 12 | 4 | file_size | 包括 Header 的完整文件长度 |
| 16 | 4 | prototype_count | `1..65536` |
| 20 | 4 | entry_prototype | v2 固定 `0` |
| 24 | 4 | isa_version | 新编译器写 `2`；reader/serializer 也支持并保留 `1` |
| 28 | 4 | payload_adler32 | 覆盖 `[32, file_size)` |

Header 后直接按 ID 顺序存放 prototype。父函数必须在子函数之前；没有 section 表、随机顺序或隐含 AUX 区。

### 每个 prototype

固定 **24-byte** record header，随后为捕获、常量、varint 指令流：

| Bytes | 字段 |
|---:|---|
| 4 | parent ID；入口为 `0xffffffff`，其他必须 `< 当前 ID` |
| 2 | register_count，`1..256` |
| 1 | parameter_count，`0..255` 且不超过 register_count |
| 1 | flags：bit0=variadic，bit1=Lua51 legacy arg slot，bit2=生成 legacy arg table，bit3=Luau shared closure（修订 2） |
| 2 | capture_count，`0..256` |
| 2 | reserved，必须为 `0` |
| 4 | constant_count，`0..65536` |
| 4 | instruction_count，必须大于 `0` |
| 4 | instruction_byte_count：varint 指令流字节数，必须位于 `2×n .. 7×n` 区间 |
| `capture_count × 2` | 每个捕获为 `[kind:u8, index:u8]`：kind0=父帧 local cell 寄存器，kind1=父帧 upvalue，kind2=递归初始化 local cell（修订 2） |
| 可变 | 按 tag 顺序编码 constant_count 个常量 |
| `instruction_byte_count` | 连续 varint 指令流；**没有 AUX 或 trailing data word**，解码必须恰好消耗全部字节并恢复出 instruction_count 条指令 |

legacy bit2 必须伴随 bit1；bit1 仅允许 Lua 5.1 的非入口变参函数，并必须预留 `R[parameter_count]`。Lua 5.1 函数自身语法中出现 `...` 时，不创建兼容 `arg` 表，slot 值为 nil；不受嵌套子函数的 `...` 影响。

shared bit3 只允许修订 2 的非入口 Luau prototype；每个 prototype 最多一个 kind2，且必须与 shared 同用，index 仍检查父寄存器范围。运行时优先复用首个闭包，捕获值使用 raw equality 比较，self capture 不拿未初始化 cell 参与比较。flag 未设置时保持逐次创建；修订 1 文件不能凭空获得新共享语义。未知位/版本/组合由 Rust 和目标 decoder 双侧拒绝。

`Program.isa_version` 保存读入版本，`serialize(decode(old))` 不静默升级。opcode 编号未改变；新增的是明确的闭包元数据，不是原生指令重打包。

### 常量 tags

| Tag | Payload |
|---:|---|
| 0 | nil，无 payload |
| 1 | `u8` boolean，只允许 0/1 |
| 2 | `u64` IEEE-754 binary64 bits；保留 signed zero、Infinity、NaN bits |
| 3 | `u32 byte_length` + 原始字符串字节；可含 NUL、非 UTF-8 |
| 4 | `i64` little-endian；仅 Luau，**不经过 f64** |
| 5 | `u32 byte_length` + ASCII method identifier；运行时仍是 string，用于安全生成静态 userdata NAMECALL 适配器 |

Lua 5.1 用普通数值运算恢复 binary64，不要求 bit32。Luau 的 integer 常量通过固定 0.735 `integer.fromstring(hex,16)` 精确重建，不在生成脚本中伪装为有损 double。

## 3. 指令编码（7-bit varint）

文件内每条指令为 `[opcode:u8][若干 varint 操作数]`，操作数个数与含义由该 opcode 的 Form 列决定；varint 为 LEB128 风格小端 7-bit 组（最高位为继续位），**必须是最小编码**（多字节值的末组非零，至多 4 组）：

```text
Form  文件字节序列                 恢复后的逻辑 word
Ax    [opcode][Ax-varint]          A=Ax&255, B=(Ax>>8)&255, C=Ax>>16
A     [opcode][A-varint]           B=C=0
AB    [opcode][A-varint][B-varint] C=0
ABx   [opcode][A-varint][Bx-varint] B=Bx&255, C=Bx>>8
ABC   [opcode][A-varint][B-varint][C-varint]
```

小数值占 1 byte（寄存器 0..127、小常量索引、小跳转目标），较大值 2~N bytes（寄存器 128..255 为 2，Bx 至 3，Ax 至 4）；每条指令合计 **2~7 bytes**。Rust reader 验证 varint 最小性/组数上限、字段值域（A/B/C ≤ 255、Bx ≤ 65535、Ax ≤ 0xFFFFFF）、指令数与字节数精确匹配及无尾随字节，然后恢复公开 API 使用的定长 `(opcode,A,B,C)` `Word`。生成脚本不会把该 linear word stream 原样交给目标 fetch-loop，而会按前述 ISA15 lowering 改写为带 per-prototype operand/field-order ABI 的 global-segment/pool-wrapped edge-token/recipe-token records（record 槽位与 segment token 均按 per-id/slot 置换重排，capture/constant 移入全局洗牌池），并在执行时把逻辑寄存器经当前 prototype 的 register ABI 映射为物理 key；`Test`/`Jump` 的 canonical 指令序号在 lowering 时解析并替换成 label 后继。

`ABC` 中有些操作只用 A/AB，编码时相应省略恒零字段；恢复后未使用逻辑字段必须为零。`Extract.C` 是 pack 的 1-based 常量索引，upvalue B 是索引，其余寄存器字段均必须 `< register_count`。

`Jump.Ax` 是该 prototype 内 **0-based 绝对指令编号**，不是 byte offset，也不是 native sBx。`Test` 在值为 nil/false 时跳过下一条指令。IR branch 固定降低为：

```text
Test condition
Jump then_block
Jump else_block
```

因此跨越 65535 的分支也不需要扩展 word；Ax ≤ 0xFFFFFF 的 24-bit 值域不变，varint 下大目标占 3~4 bytes。

### 完整 ISA（固定编号，不可重排）

`rA/rB/rC` 表示寄存器值，`K` 常量，`U` 捕获 cell。

| ID | Opcode | Form | 语义 |
|---:|---|---|---|
| 0 | Move | AB | `rA = rB` |
| 1 | Constant | ABx | `rA = K[Bx]` |
| 2 | Nil | A | `rA = nil` |
| 3 | NewCell | AB | `rA = 新 cell(rB)` |
| 4 | ReadCell | AB | `rA = value(rB)` |
| 5 | WriteCell | AB | `value(rA) = rB` |
| 6 | ReadUpvalue | AB | `rA = value(U[B])` |
| 7 | WriteUpvalue | AB | `value(U[B]) = rA` |
| 8 | ReadGlobal | ABx | `rA = env[K[Bx]]`，key 必须是字符串 |
| 9 | WriteGlobal | ABx | `env[K[Bx]] = rA` |
| 10 | NewTable | A | `rA = {}` |
| 11 | GetTable | ABC | `rA = rB[rC]` |
| 12 | SetTable | ABC | `rA[rB] = rC` |
| 13 | Method | ABC | receiver=rB，静态方法 key=rC，方法 callable 放入 rA |
| 14 | NewPack | A | `rA = {n=0}` |
| 15 | Push | AB | 向 pack rA 添加单值 rB（包括 nil） |
| 16 | Extend | AB | 把 pack rB 的全部 n 个值添加到 pack rA |
| 17 | Extract | ABC | `rA = rB[C]`，C=`1..255` |
| 18 | Varargs | A | 当前 frame 的 vararg pack 放入 rA |
| 19 | Call | ABC | `rA = pack(call(rB, unpack(rC)))` |
| 20 | Closure | ABx | 从直接子 prototype Bx 创建/按共享标记复用函数，按捕获表绑定 cell |
| 21 | Clear | AB | 释放 `R[A..B]` 的引用（inclusive） |
| 22 | Add | ABC | `rA = rB + rC` |
| 23 | Subtract | ABC | `rA = rB - rC` |
| 24 | Multiply | ABC | `rA = rB * rC` |
| 25 | Divide | ABC | `rA = rB / rC` |
| 26 | FloorDivide | ABC | `rA = rB // rC`，仅 Luau，保留 `__idiv` |
| 27 | Modulo | ABC | `rA = rB % rC` |
| 28 | Power | ABC | `rA = rB ^ rC` |
| 29 | Concat | ABC | `rA = rB .. rC` |
| 30 | Equal | ABC | `rA = (rB == rC)` |
| 31 | Less | ABC | `rA = (rB < rC)` |
| 32 | LessEqual | ABC | `rA = (rB <= rC)` |
| 33 | Not | AB | `rA = not rB` |
| 34 | Negate | AB | `rA = -rB` |
| 35 | Length | AB | `rA = #rB` |
| 36 | NumberPrepare | A | 检查/转换 R[A..A+2] 的 initial/limit/step；Lua51 额外执行 initial-step |
| 37 | NumberStep | A | `rA += R[A+2]` |
| 38 | NumberTest | AB | 检查 R[B..B+2] 的循环条件，boolean 写入 rA；step>0 才是递增分支 |
| 39 | IteratorPrepare | A | 归一化 generator/state/control；Luau raw `__iter`，无该字段时保留 `__call`，再处理普通 table |
| 40 | IteratorNext | AB | 调用 rB 的迭代器，把返回 pack 放入 rA，并更新 rB.control |
| 41 | SetList | ABC | 把 pack rB 写入 table rA，从数值索引 rC 开始；不把 n 当成数组元素 |
| 42 | ToString | AB | 使用捕获的原始 tostring，把 rB 转换到 rA |
| 43 | Export | ABC | 将 cell rA 链接到导出 table rB 的字段 rC；仅 Luau |
| 44 | Jump | Ax | 设置绝对 instruction PC |
| 45 | Test | A | nil/false 跳过下一条指令 |
| 46 | Return | A | 返回 pack rA；对宿主边界展开全部 n 个值 |
| 47 | TailCall | AB | function=rA、argument pack=rB；VM→VM 替换 frame |
| 48 | Freeze | A | 冻结 Luau 导出 table rA |

Lua 5.1 支持 **46** 条（不接受 26/43/48）；Luau 支持 **49** 条。没有把“不支持的指令”悄悄当 NOP 的实现。源码中的 `and/or`、`~=/>/>=`、if expression、复合赋值和循环控制在 IR 层展开；它们不是缺失的运行时 opcode。

指令文件位于 `src/vm/opcode/lua51/c*.rs` 和 `src/vm/opcode/luau/c*.rs`，各文件返回一份固定 handler；`mod.rs` 穷尽注册。原有根目录 `lua51_*.rs` / `luau_*.rs` 仅用于兼容旧 native backend，不挪动或删除。

## 4. 校验、资源门限和现阶段限制

- Rust reader 与目标 decoder 均验证 Header、checksum、长度、tag、保留位、计数、父子关系、capture 范围、opcode、寄存器、常量类型/索引、jump/test 目标及末指令不可 fall-through。
- 结构验证不是静态类型证明或沙箱；合法 bytecode 仍可有无限循环、类型错误或调用宿主能力。**不要运行不可信 bytecode**。
- v2 文件最多 16 MiB；所有 prototype 的指令+常量+捕获总数最多 1,000,000；函数最多 65536；每函数寄存器最多 256、常量最多 65536、捕获最多 256；每 prototype 的指令字节数必须在 `2×n .. 7×n` 区间且被解码精确消耗。私有 ISA15 label 空间为非零 u16，semantic lowering 最多接受每 prototype 65,531 个原始 bundle，以明确预留 4 个 neutral entry label；segment ID 使用 `2×owner+1/2`，所以私有 image 最多 32,767 个 prototype（公开 canonical v2 的 65,536 上限不变）；operand profile周期32,772，register profile周期130,556且物理register key小于1,028；record/segment field profile按乘加factorial逐id/slot覆盖全部24/6种顺序；超出返回 Diagnostic。
- AST lowering 使用 1,000,000 工作预算、64 层下降限制；超限返回 Diagnostic，不截断寄存器、不改用其他编码、不偷偷调用原生编译器。实际可编译程序受本后端资源限制，不等于原生 compiler 的资源范围。
- 混淆器实现源文件永久限制为单文件 **≤80 KiB（81,920 B）**；`tools/test-matrix.sh` 扫描全部 `src/**/*.rs`。超过门限必须拆分且以固定 seed golden 逐 byte 相同证明“拆分本身”不改生成代码。当前原 124,624 B 的 `src/vm/custom/tests.rs` 已拆为 `tests/{runtime,transport,semantic}.rs`，共享 module scope 由 `include!` 保持；K10 用同一手法把 81,861 B 的 `src/vm/custom/emit.rs` 按发射阶段拆出 `emit_decode.rs`（解码器 Lua 文本）与 `emit_prelude.rs`（宿主捕获 prelude，返回 `PreludeStage { text, header_end, ret_names, probe_transcript }`），由 `src/vm/custom/mod.rs` 以 `pub(crate) use` 重导出，拆分后 `emit.rs` 为 64,639 B。
- Luau 对纯条件/只读标量做有界可达性分析，已证明的 dead `continue` 不再误拒绝；活跃路径可能跨过 `until` 所用 local 初始化时仍保守拒绝，尚非原生 compiler 全套数据流。运行时捕获根据实际 IR 自底向上裁剪/重编号，类型/死代码的绑定检查仍留在公开 resolver。
- 不能模拟原始 debug 栈、环境反射、source location、dump 或精确错误位置。明显的 `debug/getfenv/setfenv/loadstring` 等在源码入口报诊断；任意动态/宿主别名不承诺完全识别。正常全局及 `_G` 数据访问仍支持。
- Luau generalized iteration 支持普通 table、可见 raw `__iter` 及 callable table/userdata；**被隐藏的 metatable 不属于已支持保证**：getmetatable 返回非 table 的锁定值时明确拒绝；table-valued 锁定值/任意宿主隐藏元表不能可靠识别或模拟，不能将其当作受支持的 visible `__iter`。不声称已完成 Roblox executor 实机验证。
- Luau integer 程序需要固定 0.735 integer library；Lua51/Luau 算术、元方法、内建库行为以相应宿主提供的实现为边界。GC 时机、内存占用及递归栈预算不承诺与原生实现完全相同。
- table 的数组/hash 容量提示不做全面模拟，含洞表的 `#`、未规定遍历顺序及地址型输出不保证相同；所有优化相关函数身份/NaN payload 观察也不能由当前测试推出完全等价。完整支持条件与明确边界见 [`虚拟机兼容性.md`](虚拟机兼容性.md)。

## 5. 测试证据与接口

当前 ISA15 在 ISA14 capture/constant 全局洗牌池基础上再把 transport 换成逐 image 混合 base86：三段独立长度前缀、链式 4/5/6 字组、`86^width` 冗余空间随机高位填充、逐 seed 86 字母表（28..=126 取 86，跨度恒 99）以 8 个 sub-12 片段 baked，Lua 端 `VAL` 表 + 独立 4 状态机解码；求值语义不变。K8 indirect route 与 K9 per-lane affine operand key 之后，2026-09-10 又把 frame tag 换成 keyed dual-lane hash；三者都只改 private wire/transport，不改公开 `.obf` 与求值语义。固定 seed golden 现为 **98,889/109,049 B**，SHA-256 为 `c01c17d47828852a484ff63354decdc53d5049003740b9d8ec6c505053c999b0` / `9cc4d6d42df5fef723b6d8825725fed59cd5d8df83701351c70b0fb593cc430f`（Lua51 那份在 09-10 的 probe-transcript 批次**逐字节未变**，Luau +111 B）；公开 `.obf` 仍是 5,525/6,575 B（SHA-256 `a33b5dcb…b81140` / `def67d59…45fa1d7`）。整脚本预算自 K10 起重钉为 **112,000 B/目标**并恢复 enforce（实测 8 种子最坏 100,049/109,825 B）。压缩硬门逐 seed 比较完整 LZW frame 与原始 semantic bytecode，不比较整份 Lua 或历史 ISA：golden seed 实测 Lua51 `14,073→13,475 B`、Luau `17,763→17,527 B`。当前矩阵为 **159 单元 + 116 集成 = 275 门，全绿**（frame-tag 批次留下的 3 个红灯已在 2026-09-10 清掉：两套审计 pin 重录 + Luau probe transcript 去明文，见 `项目交接总结.md` 第 0.1 节）；15-run bench 为 Lua51 **325 ms / 162.5x**、Luau **101 ms / 33.7x**。既有segment/semantic/ChaCha/corruption门全部保留（corruption 两门按 lexer+字母表 span 重写）；新增结构门验证字母表 span/池/跨度/重排、填充冗余界、编解码往返、六段序唯一解码、状态号/slot 规避、转义完备、诱饵零污染与段长上界。

**M7/ISA12 结构随机化**：独立 `structure` 随机流为 primitive operand validator、recipe entry router 与 semantic fragment dispatcher 注入等价变体。三者分别按 `o%G`、`rid%G`、`sid%G` 拆 2~4 个子链；arm 比较从 `x==K`/`K==x`/`not(x~=K)`/`x-K==0` 随机选择。varint 边界检查与元方法分支继续使用已有等价变体。单元测试在 token/结构级验证三维拓扑、跨 seed 多样性、同 seed 复现、live descriptor 等价 class、fragment split/fusion 及原生/VM 差分。descriptor camouflage 与双层 semantic routing 有意识提高体积；历史硬门为 85,000/94,000 B，随后统一为 95,000 B，K 系列施工期一度暂停（`OBF_BENCH_SCRIPT_CAP=off`）；**K10 按实测把预算重钉为 112,000 B/目标并恢复 enforce**（`tools/bench-vm.sh` 默认值与 `src/vm/custom/tests/semantic.rs` 同数，`off` 保留为施工窗口开关）。性能仍由 `tools/bench-vm.sh` 和 1500ms 灾难阈值看护。

**ISA12-A operand ABI**：profile 不是数字重编号，也不把所有函数布局列成运行时表；parser/store 与 `OG` 以 seed-derived 仿射参数计算 profile。四种 family 在 Lua table 形状、索引方向和 packed/tuple 表示上不同，且 4×3×2,731 的组合覆盖完整 private prototype 上限。四种 matching return/LHS order 去掉统一 handler prelude；共享 getter 避免 per-function handler 复制。

**ISA12-B register ABI**：`RegisterLayout` 以另一随机流计算 4×127×257 profile；目标 `RK(fid,R)` 选择四个 mapper closure 形态之一，`SETUP` 每 frame 得到 `RX` 后初始化物理 register table。生成器只对 crate-owned handler source进行 token-span `R[...]→R[RX(...)]` lowering，不修改约95个模板文件、不使用全局正则、不复制每函数handler。profile公式与primitive语义仍可逆。

**ISA12-C carried-value fusion / entry graph**：同一token分析器识别安全producer/consumer并生成一次求值的carry与`RF` consumer reads，最长4项recipe用最大非重叠配对，不能证明安全的形态绝不合并。`RK`同时返回与register family耦合的`RF`；entry新增第九个整体状态机（自身六个stage）。这削弱旧扫描器逐条“operand bind + 原handler”和顶层稳定接线，但`RF`及所有stage仍可被完整静态模拟。

**ISA15 保留的 parser field-order 与 capture/constant pools**：`FieldLayout`以独立随机流派生`(mult,add)`，乘子与阶乘互素；record/segment的Rust编码/`decode`与生成Lua parser/validator/fetch凭同一id/slot重算同一置换，parser fetch的`I[2]/I[3]/I[1]`静态下标随seed变化。dictionary/metadata/tuple的per-image置换以字面顺序baked进parser。固定slot宽度与opcode/edge/recipe语义不变，错序由既有门fail closed；置换规则仍可逆。ISA14 引入、ISA15 保留的 capture/constant 池每记录写 3 个匿名 u16 token（掩码 owner/slot/index/tag）再接 payload，token 物理槽位按 per-record factorial 置换（seed 与掩码 per-record 独立），两池全局洗牌且池顺序按 image 翻转；目标 parser 先验证两池再按 owner 切片，header 仅保留 24 字节 metadata。

**不透明真假分支与三类 recipe camouflage**：入口方法体继续使用恒真/恒假式在实际链旁放置不运行的真指令，primitive validator 也保留 200+ 不可能值死臂。四个 graph-unreferenced poison recipe 的 descriptor/execute 序列故意不同；ISA5 加入的 graph-referenced neutral recipe 仍由运行插桩证明实际执行；ISA6 则让 live recipe 本身也以 validation-equivalent descriptor 谎报。专项测试分别断言 poison 不执行、neutral 真执行、live camouflage 密度与 class 安全性、fragment stage 跨 recipe 打乱；损坏 recipe 长度/重复 ID/entry/record label、edge token、recipe token 及 prototype parent/flags/capture 的 image 均在用户输出前被目标端拒绝。

- `tests/custom_vm.rs`：八份既有语料、赋值/方法求值顺序、表刷新、多返回值/nil holes、循环闭包、continue/repeat、20,000 次尾调用、回调/coroutine/错误对象、Luau `__iter`/userdata NAMECALL/插值/精确 integer、冻结导出模块、GC、生成式程序，以及ISA12-C专门的nil/false carry、alias、元方法顺序、cell/capture与pcall error差分，和ISA13专门的record/segment/dictionary/metadata字段序错序fail-closed差分，以及ISA14专门的capture/constant池错序fail-closed差分。
- `tests/custom_bytecode.rs`：Header/varint 实际布局（含 `return 7` 的 13 字节指令流）、逐字节截断/损坏、修复 checksum 后的恶意结构、完整常量 round-trip、capture 图、目标扩展拒绝、70002 的大 jump、非法 IR/寄存器溢出、非最简 varint/字段越界/带外字节数/尾随字节拒绝。
- `tests/private_fields.rs` / `vm::fields::tests`：16 私有字段统一映射（含 K8 `routes`）、一/两字母池与 fail-closed、用户字段/模块导出/标准库/元方法/NAMECALL 保护、多 seed、compile/wrap/缺失 compiler/旧 ISA 执行；公开输入仍可逐字节 round-trip，解密并解压出的内嵌 payload 则必须标记为 ISA15 而非 canonical。
- `vm::custom::tests`：在真实 graph fetch 上、ED/RD 与 `rid→sid` 动态路由之后记录实际执行序列（finalizer **之前**），核对 **Lua51 46/46、Luau 49/49**；另检查 ISA12-A 全 prototype operand profile 唯一性、四种 family/rotation/binding、fixed-slot/actual-op marker 消失及 getter payload round-trip；ISA12-B 检查 register profile 全域唯一、每 profile 的逻辑槽单射/物理界限、seed变化、无256项映射表、双目标全部 primitive `R[...]` 已token lowering，并通过cells/upvalues/captures/callbacks/tailcalls/legacy vararg运行差分；ISA12-C检查producer/consumer token eligibility、carry与forward-read恒等式、完整fixture live fusion下界、第九个entry状态机及旧线性锚点消失；ISA13检查record/segment factorial全范围合法与双端重算一致、固定seed parser字段序与跨seed变化、双目标四类字段序错序无输出拒绝；ISA14检查capture/constant池掩码/token/profile合法、池洗牌coverage与池序翻转一致、双目标池错序无输出拒绝；三层 edge token 与五层 recipe token 往返/上下文差异、live descriptor 的 Form/validator/control 等价与 ≥45% 密度、真实dataflow pair/single fragment规划、三维 dispatch、可达 neutral 真执行、poison 不执行、record/prototype 洗牌、seed-specific wire、global segment id/owner/next/root/coverage、pool owner/slot/index/tag/coverage，以及目标 decoder 在用户代码执行前拒绝损坏 token/semantic graph/prototype metadata 和 segment duplicate/missing/越界/错误 owner/root/cycle/截断/尾随；compression 专项覆盖逐 byte 无损 round-trip、全部截断/逐 frame byte corruption、畸形 bitstream、dictionary/reset/output/padding/完整消费/checksum 与 incompressible rejection，ChaCha8/frame-v2 专项另覆盖 published vector、双 domain/context、runtime transcript→share→control/attestation 差分、最终 key/nonce/counter 零字面量、wrong witness/attestation/seed、严格 framing 和逐 ciphertext byte corruption。

Rust 入口：

```rust
let ast = obf::parse(source, target)?;
let ir = obf::ir::lower(&ast)?;
let bytes = obf::bytecode::custom::encode(&ir)?;
let program = obf::bytecode::custom::decode(&bytes, target)?;
assert_eq!(obf::bytecode::custom::serialize(&program)?, bytes);
let script = obf::vm::custom::emit(&bytes, target, seed)?;
```

便捷 API：`ir::compile(source,target)`、`vm::custom::compile(source,target)`、`vm::custom::virtualize(source,target,seed)`。CLI 默认也使用 AST/v2；旧后端需 `--backend native`。工作流：

```bash
target/debug/obf dump-ir --target lua51 -o target/script.ir script.lua
target/debug/obf compile --target lua51 -o target/script.obf script.lua
target/debug/obf inspect-bytecode --target lua51 target/script.obf
target/debug/obf wrap-bytecode --target lua51 --seed 123 -o script.vm.lua target/script.obf
```

默认接入时门禁 **PASS114**（32 单元 + 82 集成），其中核心 26 项、CLI 4 项为新增。分号增量当时为 **PASS124**；运行语义增量为 **PASS140**；私有字段压缩为 **PASS149**；整体方法调用包装为 **PASS150**；7-bit varint 指令序列化为 **PASS151**；嵌入 payload 加密与三探针密钥拆分为 **PASS153**，密钥结构动态推导（零字面量）后为 **PASS154**，常量池第二层加密后为 **PASS156**；base86 传输层与三段打乱后为 **PASS157**；固定水印与双函数检测后为 **PASS159**；M7 结构随机化后为 **PASS161**；不透明真假分支后为 **PASS162**；全代码随机化（字段序洗牌 + 加密参数 seed 派生）后为 **PASS163**；特征拆分隐藏（形态表打包重建/varint 解码/边界验证三独立字段）后为 **PASS164**；随机 opcode 重编号（分派/边界/终止检查全部跑 per-seed 置换编号，打包串重建、展开时重写字节）后为 **PASS165**；分派链二级拆分（2..4 条模数子链、四拼写选择器、链长逐 seed 变化）后为 **PASS166**；全字段解除锚定+分隔符/prelude 序微变体后为 **PASS167**；全局名隐藏（字符函数池拼名+块级环境索引，体积门有意识上调 25,500/27,000）后为 **PASS168**；随机 section 拆分（解码器扁平化为解密+2..4 随机 helper 簇+core 平级字段，字段总数 20..22 逐 seed 变化）后为 **PASS169**；全阶段控制流扁平化（四类逻辑状态机化+SETUP/PH/PU/PK 函数拆分，体积门有意识上调 27,500/28,500）后为 **PASS170**（58 单元 + 112 集成），默认 ISA 修订 2，兼容修订 1，binary 不因字段改名或包装而变（varint 批次按定义改变了指令字节布局）；g 槽位表（全部非递归阶段活局部与 SETUP 中间量改为 `local g={}` per-seed 随机数字 key 槽位读写、作用域尾 `g=nil` 清空；解释器帧寄存器 F/R/va 因递归嵌套帧为实测例外；体积门 luau 28,500→29,800）后为 **PASS171**（59 单元 + 112 集成）；像内诱饵语义臂+假锚点（F5 为未用 opcode 发射"另一 opcode handler 文本"的假分派臂，键=perm[未用槽] 像内不可过滤、形态表保证不可达；entry 死侧携带同形假 129 字节打包串/假 LCG 份额/假 Adler 门；体积门 27,500/29,800→29,500/31,800）后为 **PASS172**（60 单元 + 112 集成）；密钥流原语族+跨阶段密钥派生（两层各自三族算术流：Lehmer/双 Lehmer 和/mod-2^32 LCG 高位输出；payload 种子混 forms 字段重建的 PT 三槽项、常量层种子混外层解密镜像的两框架字节，逐段黑盒被迫按依赖序模拟）后为 **PASS173**（61 单元 + 112 集成）。针对静态破解报告的 ISA3 semantic virtualization 用新的 graph/superoperator/decoy 契约替换旧“一对一 opcode 重编号即充分”的测试，测试总数仍为 PASS173；golden 为 64,599/71,598 B，体积门改为 68,000/74,000 B。2026-09-08 第一阶段再升级 private ISA4：五层 graph-context recipe token、每 fetch 动态恢复、每层 4~5 重动态谓词及五个高密度死状态；新增 token 可逆性/上下文差分与双目标语法运行门，golden 更新为 68,344/75,615 B，预算调整为 72,000/80,000 B，完整门禁 PASS174（62 单元 + 112 集成）。同日第二阶段升级 private ISA5：三层 successor token + 3-live/3-dead ED、每次 fetch 动态后继、入口/抽样边 reachable neutral bundle；新增 neutral 实际执行/poison 不执行和 edge-token corruption 门，golden 为 71,084/78,081 B，完整门禁 PASS175（63 单元 + 112 集成）。第三阶段升级 private ISA6：live dictionary 改为严格 validation-equivalent descriptor camouflage，真实 recipe 拆为跨 recipe 全局打乱的 `sid` fragment graph；新增 camouflage class/密度与 split/fusion/三维 dispatch 回归，golden 为 80,369/89,149 B，完整门禁 PASS176（64 单元 + 112 集成）。第四阶段升级 private ISA7 并增加独立 block transport v1：16-byte frame、7~10 轮三 family 32-bit Feistel、动态双 key、ciphertext chaining、cookie/tag/padding 全校验；新增逐字节 corruption/schedule/key leak 单元和双目标密文篡改集成门，golden 为 83,640/92,116 B，完整门禁 **PASS178（65 单元 + 113 集成）**。第五阶段升级 private ISA8：semantic image 在 cipher 前进入 8 KiB bounded LZW，原 constant stream 扩展覆盖完整 compressed body，Lua inverse 按 seed 拆为二/三字段；golden为82,572/92,022 B；当时bench误加的ISA7整脚本门现已移除；完整矩阵 **PASS179（66 单元 + 113 集成）**。第六阶段升级 private ISA9：prototype code 改为 context-bound 两节点 `id/owner/next` graph 并跨 prototype 全局洗牌，新增完整拓扑/双目标 corruption 门和仅限生成 VM 的安全可选分号压缩；golden为82,729/91,722 B；相对ISA7的整脚本关系仅作历史信息；完整矩阵 **PASS182（69 单元 + 113 集成）**，默认 15-run bench 为 68/88 ms。第七阶段升级 private ISA10：三个 key share 折叠实际 debug source transcript，share 和掩码 fetch/dispatch 状态；新增两个单元门及前三次 source-only hook 的双目标 fail-closed 回归，golden 为 82,514/91,344 B，完整矩阵 **PASS184（71 单元 + 113 集成）**，bench 为 67/88 ms。第八阶段升级 private ISA11：双 domain ChaCha8 替换 custom streams，删除 Feistel、升级 frame v2，新增五个 shuffled crypto/anti-hook fields及完整负测；golden 为 82,201/90,866 B，完整矩阵 **PASS185（72 单元 + 113 集成）**，bench 为 94/113 ms。第九阶段 ISA12-A 首先落地 per-prototype operand ABI：4 family×3 rotation×2,731 lane 全域 profile、四种 fragment binding，并删除 fixed `6+3*i` bridge 与 actual-opcode marker；golden 为 72,198/80,636 B，完整矩阵 **PASS186（73 单元 + 113 集成）**，bench 为 95/114 ms。随后ISA12-B加入4 family×127 stride×257 shift的per-prototype register ABI，SETUP与全部primitive访问经同一frame mapper且不输出完整映射表；golden为74,540/83,040 B，完整矩阵 **PASS188（74单元 + 114集成）**，bench为96/115 ms。ISA12-C再以受控token结构分析生成carried-value pair、per-family `RF`，并把entry接线改为六状态图；golden为78,706/87,167 B，完整矩阵 **PASS190（75单元 + 115集成）**，bench仍为96/117 ms。同日第九阶段第四个结构子阶段升级 private ISA13：record 4槽/segment 3 token按per-id/slot factorial置换重排，dictionary/metadata/tuple用per-image置换baked进parser；新增parser字段序结构单元与双目标四类错序集成门，golden为79,426/87,873 B，完整矩阵 **PASS192（77单元 + 115集成）**，bench为98/116 ms。`tools/test-matrix.sh` 同时验证 80 KiB 源文件上限、debug/release、独立 compile/wrap、缺失外部 compiler 与旧显式后端。

**压缩口径纠正（2026-09-08）**：上述历史阶段曾记录ISA7整脚本比较，但当前验收已按用户明确口径修正。只有`semantic_image.bytes.len()`与`compress_bytecode(...)`返回的完整LZW frame长度构成压缩前/后比较；所有历史golden整文件大小只保留为体积趋势，不决定压缩是否成功。
